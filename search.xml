<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>网络安全概论</title>
    <url>/2020/07/17/introduction-to-cyberspace-security/</url>
    <content><![CDATA[<p>三小时女娲补天网安概论</p>
<a id="more"></a>
<h2 id="0x01网络信息安全发展历史"><a href="#0x01网络信息安全发展历史" class="headerlink" title="0x01网络信息安全发展历史"></a>0x01网络信息安全发展历史</h2><ol>
<li>网络信息安全历史：<ol>
<li>通信保密阶段：COMSEC，20世纪20-60年代，主要解决发方和收方的信息源编码（Source Coding）、信道编码和传输（Channel Coding）、通信协议和密码的问题。</li>
<li>信息安全阶段：INFOSEC，20世纪80-90年代，主要目标为保密性Confidential、完整性Integrity、可用性Availability。包括TCSEC橙皮书和ITSEC白皮书。</li>
<li>信息保障阶段：IA，20世纪90年代至今，主要为为从整体角度考虑其体系建设的信息保障(Information Assurance)阶段，代表是美国的IATF规范。</li>
<li>网络空间安全：2010年后</li>
</ol>
</li>
<li>信息安全的基本特征：<ol>
<li>相对性</li>
<li>时效性</li>
<li>安全攻击源和防范对象的不确定性</li>
<li>反传统的技术思维和复杂的人-机关系</li>
<li>相关性</li>
<li>信息结构的高度脆弱性和风险性</li>
<li>动态性</li>
</ol>
</li>
</ol>
<h2 id="0x02计算机网络"><a href="#0x02计算机网络" class="headerlink" title="0x02计算机网络"></a>0x02计算机网络</h2><ol>
<li><p>OSI七层模型</p>
<p>应用层Application Layer，表示层Presentation Layer，会话层Session Layer，传输层Transport Layer，网络层Network Layer，数据链路层DataLink Layer，物理层Physical Layer</p>
</li>
<li><p>TCP/IP协议栈</p>
<p>应用层，传输层，网络层，网络接口层</p>
</li>
<li><p>五层参考模型</p>
<p>应用层，传输层，网络层，数据链路层，物理层</p>
</li>
<li><p>相关概念</p>
<ol>
<li>MAC地址</li>
<li>IP地址、端口号、套接字Socket</li>
<li>TCP vs. UDP</li>
<li>域名解析</li>
</ol>
</li>
<li><p>网络工具</p>
<ol>
<li>ping使用ICMP检查目标IP是否可达</li>
<li>ipconfig/ifconfig查看网络配置信息</li>
<li>netstat查看网络连接状态</li>
<li>arp查看ARP缓存信息</li>
</ol>
</li>
</ol>
<h2 id="0x03安全基础"><a href="#0x03安全基础" class="headerlink" title="0x03安全基础"></a>0x03安全基础</h2><ol>
<li>信息安全的定义：信息安全是防止对知识、事实、数据或能力的非授权使用、误用、篡改或拒绝使用所采取的措施。</li>
<li>完整的定义：指在既定的安全密级条件下，信 息系统通过预警、保护、检测、响应、恢复和反击机制，抵 御意外事件或恶意行为攻击，确保信息系统避免非授权的访 问、破坏或者服务中断，实现信息和资源的保密性、完整性、 可用性、非否认性和可控性的能力。</li>
<li>三要素：<ol>
<li>机密性Confidential：不被截获和未授权使用</li>
<li>完整性Integrity：内容真实可信、不被冒充伪造和篡改</li>
<li>可用性Availability：信息和信息服务被授权人正常使用</li>
</ol>
</li>
<li>其他属性：<ol>
<li>不可否认性No-Repudiation：行为的不可否认</li>
<li>可控性Controllability：验证Authentication和审计Accountability</li>
<li>可存活性Survivability：在攻击和错误情况下继续提供服务</li>
</ol>
</li>
<li>安全攻击<ol>
<li>中断Interruption：破坏了可用性</li>
<li>修改Modification：破坏了完整性</li>
<li>伪造Fabrication：破坏了真实性</li>
<li>截取Interception：破坏机密性</li>
</ol>
</li>
<li>安全服务<ol>
<li>认证Authentication：提供实体的身份保证</li>
<li>访问控制Access Control：授权资源访问</li>
<li>机密性服务Confidential：信息不泄露和暴露</li>
<li>完整性服务Integrity：数据的价值和存在性没有改变</li>
<li>不可抵赖服务</li>
</ol>
</li>
<li>安全机制：安全服务的实现<ol>
<li>加密机制</li>
<li>数字签名</li>
<li>访问控制</li>
<li>数据完整性</li>
<li>通信业务流填充</li>
<li>路由控制</li>
<li>认证交换</li>
<li>公证机制</li>
</ol>
</li>
</ol>
<h2 id="0x04威胁和攻击技术"><a href="#0x04威胁和攻击技术" class="headerlink" title="0x04威胁和攻击技术"></a>0x04威胁和攻击技术</h2><ol>
<li>安全问题的根源：<ol>
<li>物理安全因素：设备本身的问题、环境的安全</li>
<li>方案设计因素：安全策略不合理、安全配置不当、设计需要牺牲安全机制</li>
<li>系统安全因素：操作系统和网络软件等的漏洞和后门</li>
<li>TCP/IP协议的安全因素：设计之初没有考虑安全问题</li>
<li>人的因素：无意的失误和错误、恶意攻击</li>
</ol>
</li>
<li>攻击技术：<ol>
<li>被动攻击：窃听和监视数据传输，不对数据进行修改，难以检测，重在预防<ol>
<li>嗅探sniff</li>
<li>分析</li>
</ol>
</li>
<li>主动攻击：数据流的篡改和错误数据的添加，能够检测，难以防止<ol>
<li>假冒fabrication</li>
<li>重放replay</li>
<li>篡改modification</li>
</ol>
</li>
</ol>
</li>
<li>攻击过程<ol>
<li>预攻击pre-attack：搜集信息，域名、IP、拓扑、OS、端口、服务</li>
<li>攻击attack：远程权限、接入、本地权限、提权、攻击</li>
<li>后攻击post-attack：植入木马、删除日志、进一步渗透</li>
</ol>
</li>
<li>攻击手段：<ol>
<li>网络监听sniff：监听网络状态和数据流</li>
<li>密码破解crack：字典攻击、暴力破解</li>
<li>会话劫持session hijack：”中间人攻击“</li>
<li>缓冲区溢出buffer overflow：输入数据规模超过了给定缓冲区的大小</li>
<li>拒绝服务攻击DoS：消耗目标资源使其无法为正常用户提供服务</li>
<li>病毒和蠕虫worm：自我复制和传播，利用漏洞传播</li>
<li>木马trojan：隐蔽运行，远程控制和信息窃取</li>
<li>SQL注入SQL insert：访问数据库的动态网页</li>
</ol>
</li>
<li>APT攻击：<ol>
<li>高级可持续性攻击：Advanced Persistent Threat</li>
<li>攻击路径：网络欺诈和0day攻击——找到漏洞——感染内部人员——获取数据</li>
</ol>
</li>
</ol>
<h2 id="0x05网络信息安全模型"><a href="#0x05网络信息安全模型" class="headerlink" title="0x05网络信息安全模型"></a>0x05网络信息安全模型</h2><ol>
<li>网络通信模型：<ol>
<li>包括：消息的安全转换、通信主体共享的秘密信息（密钥）、可信第三方trusted third party。</li>
<li>消息经过安全转换后经由信道传输。其中的安全转换通过该秘密信息指导。</li>
</ol>
</li>
<li>访问安全模型：<ol>
<li>外部安全：阻止非授权用户</li>
<li>内部安全：内部安全控制</li>
</ol>
</li>
<li>P2DR模型：<ol>
<li>安全策略Policy、防护Protection、检测Detection、响应Response</li>
<li>基于时间的安全理论，通过每一个行为消耗的时间衡量安全能力</li>
<li>暴露时间E = 检测时间 + 响应时间 - 攻击时间。攻击时间越短、响应时间越长，则暴露时间越长，系统越危险。当暴露时间小于0，可以认为系统是安全的。</li>
<li>系统安全 = 风险分析 + 安全策略 + 系统防护 + 实时监测 + 实时响应 + 灾难恢复，其中外围五个因素都收到了系统安全策略的影响。</li>
</ol>
</li>
<li>信息技术安全评估准则：<ol>
<li>TCSEC桔皮书：A、B3、B2、B1、C2、C1、D</li>
<li>INSEC桔皮书的进一步发展</li>
</ol>
</li>
</ol>
<h2 id="0x06密码学技术"><a href="#0x06密码学技术" class="headerlink" title="0x06密码学技术"></a>0x06密码学技术</h2><ol>
<li>密码学发展：手工阶段、机器阶段、现代密码学</li>
<li>基本概念：<ol>
<li>Cryptology、Cryptography、Cryptanalysis</li>
<li>加密Encryption、解密Decryption</li>
<li>无条件安全Unconditionally Security、计算安全Computationally Security</li>
</ol>
</li>
<li>编码原则：加密算法建立在算法的公开不影响铭文和密钥的安全</li>
<li>古典密码：古典替换（substitution，替换明文）、古典置换（permutation/transportation，改变明文字符的位置）</li>
<li>密码分析方式：<ol>
<li>唯密文攻击：只有密文串和加密算法，利用统计方式分析</li>
<li>已知明文攻击：具有明文密文和加密算法，推导密钥</li>
<li>自适应选择明文攻击：攻击者选择明文并得到对应的密文</li>
<li>选择密文攻击：攻击者选择密文兵构造对应的明文</li>
<li>选择文本攻击</li>
</ol>
</li>
<li>典型攻击方式：<ol>
<li>暴力攻击Brute Force：穷举法、字典攻击</li>
<li>数学方式：差分攻击（明文插值对密文插值的影响）、线性密码、插值攻击</li>
<li>密钥相关攻击</li>
</ol>
</li>
<li>对称密钥体系：<ol>
<li>分组密码：明文和密文分组，相同密钥和明文能得到相同密文</li>
<li>序列密码：流密码，按bit加密</li>
</ol>
</li>
<li>分组密码原理：<ol>
<li>明文消息编码得到的序列分组，在密钥控制下变换成等长的输出序列</li>
<li>混乱原则：Confusion，即使用替换法Substitution，S-box。防止利用明文和密文的依赖关系破解。</li>
<li>扩散原则：Diffusion，即使用置换法Permutation，P-box。密钥的每位数字影响密文的多个数字，明文的每位数字影像密文的多个数字。</li>
<li>Feistel加密结构：多轮迭代、使用动态子密钥</li>
</ol>
</li>
<li>数据加密标准DES：<ol>
<li>56bit的密钥加密64bit的明文分组</li>
<li>16轮迭代</li>
<li>double-DES、triple-DES</li>
</ol>
</li>
<li>高级加密标准AES：<ol>
<li>明文分组为16字节128bit</li>
<li>10轮迭代，但是不是Feistel结构</li>
</ol>
</li>
<li>随机数：<ol>
<li>特性：不可预料性、统计独立性</li>
<li>生成方法：自然随机数源，随机数生成函数，通常只能生成伪随机数序列</li>
</ol>
</li>
<li>流密码：<ol>
<li>原理：一次一密的密码体制绝对安全。</li>
<li>使用种子密钥生成密钥序列，利用密钥序列一次加密明文的一个或几个比特</li>
<li>硬件实现简单</li>
<li>公开算法包括RC4算法</li>
</ol>
</li>
<li>分组密码模式：<ol>
<li>ECB电子密码本：每一个分组使用相同的密钥</li>
<li>CBC分组链接：当前明文段与上一密文段异或处理后进行加密</li>
<li>CFB密码反馈：将分组密码转为流密码</li>
<li>CTR计数器：计数器加密后与明文异或后进行加密</li>
</ol>
</li>
</ol>
<h2 id="0x07公钥密码技术"><a href="#0x07公钥密码技术" class="headerlink" title="0x07公钥密码技术"></a>0x07公钥密码技术</h2><ol>
<li>消息认证：<ol>
<li>验证发送者的身份（真实性），验证信息完整性</li>
<li>常规方式：共享密钥、序列号、时间戳、错误检测码</li>
<li>非加密方式：报文鉴别码、单向散列函数</li>
</ol>
</li>
<li>消息认证码MAC：<ol>
<li>使用密钥，从消息中生成数据，成为MAC或密码校验和（cryptographic checksum）</li>
<li>通信双方使用相同的密钥计算MAC并验证，保证了完整性和真实性</li>
</ol>
</li>
<li>信息摘要/哈希/散列函数：<ol>
<li>变长输入压缩到定长的输出，输入改变后输出变化很大。</li>
<li>MAC计算速度慢，需要密钥，但是计算信息摘要不需要密钥，只能验证完整性</li>
<li>特点：单向性、抗碰撞性</li>
<li>安全问题：密码分析法（分析算法缺陷）、穷举攻击</li>
</ol>
</li>
<li>典型散列算法：<ol>
<li>MD5：明文补全为512bit的组，生成128bit的摘要，已经被证明了不安全可以破解。</li>
<li>SHA：明文补全为512bit的组，生成160bit的摘要，记录在5个32bit的数据中。</li>
<li>HMAC：使用散列函数实现的MAC，使用密钥参与哈希过程</li>
</ol>
</li>
<li>公钥密码思想：<ol>
<li>公钥公开，私钥保密，私钥可导出公钥，公钥难以计算私钥。</li>
<li>公私钥的生成利用了单向陷门函数，即容易计算但难以求逆，给定陷门后易于求逆。</li>
<li>常用数学难题：大整数因子分解，有限域上乘法群的离散对数问题，椭圆曲线离散对数问题。</li>
</ol>
</li>
<li>RSA机制：<ol>
<li>利用大整数因子分解难题</li>
<li>给定素数p、q，公开其乘积n，选择e与$\varphi(n)$互素，计算$d=e^{-1}mod\ \varphi(n)$。</li>
<li>给定私钥p和q和d，计算公钥e和n比较简单，但是逆运算非常难。</li>
</ol>
</li>
<li>Diffie-Hellman密钥交换：<ol>
<li>用户事先共享a和p</li>
<li>用户选择随机数$X_A$和$X_B$</li>
<li>计算$Y_A = a^{X_A}mod\ p$和$Y_B = a^{X_B} mod\ p$并交换</li>
<li>计算$K = Y_B^{X_A} mod\ p= Y_A^{X_A}mod\ p$。</li>
<li>容易被中间人攻击</li>
</ol>
</li>
<li>椭圆曲线机制ECC，相比RSA在较短的密钥提供了更高的安全性。</li>
<li>对称和非对称加密<ol>
<li>对称加密：速度快，密钥短但是管理困难</li>
<li>非对称加密：密钥管理简单，可以数字签名，速度慢</li>
</ol>
</li>
<li>数字签名技术：<ol>
<li>同时保护信息完整性和信息发送者的身份确认</li>
<li>原理：数字信息经过散列函数计算摘要，使用私钥加密信息摘要并作为信息的一部分。使用公钥解密得到摘要并检查信息的完整性。</li>
</ol>
</li>
</ol>
<h2 id="0x08密钥分发和用户认证"><a href="#0x08密钥分发和用户认证" class="headerlink" title="0x08密钥分发和用户认证"></a>0x08密钥分发和用户认证</h2><ol>
<li>密码管理处理密码产生到销毁过程中的各个问题，包括初始化、产生、存储、分配、停用、更新、销毁。</li>
<li>密钥生产形式：密钥管理中心集中生产（有边界生产），个人生成（无边界生产）</li>
<li>密钥分配：自动分配机制减轻负担，同时尽量减小密钥量</li>
<li>对称密码体制：<ol>
<li>一方选择密钥，通过安全方式传递给另一方</li>
<li>可信第三方选定，安全方式传递给双方</li>
<li>使用旧密钥加密新密钥传输</li>
<li>可信第三方的加密分发：设立KDC(Key Distribution Center)使用永久密钥加密传输会话密钥，会话密钥用于加密会话内容，一次一密。</li>
</ol>
</li>
<li>分布式环境认证：<ol>
<li>一组工作站和一组分布式服务器组成</li>
<li>工作站可以保证用户认证，客户端向服务器验证，客户端和服务器的双向验证</li>
<li>Kerberos：提供两个密钥TGT和ST，用户向DC验证身份得到TGT，访问服务时通过TGT得到ST，使用ST与服务器加密传输并验证身份。</li>
<li>kerberos保存有用户的ID和密钥散列，同时与每一个应用服务器共享一个保密密钥。</li>
<li>一个kerberos环境包括kerberos服务器、应用服务器和工作站。实际上存在跨环境的访问，此时需要不同环境的Kerberos服务器之间共享一个密钥实现相互注册。</li>
</ol>
</li>
<li>公钥证书：<ol>
<li>证书管理机构CA（Certificate Authority），证书Certificate</li>
<li>证书包括用户的相关信息，用户的公钥，时间戳等</li>
<li>CA使用公开公钥，使用自己的私钥为证书签名</li>
</ol>
</li>
<li>对称+非对称的密钥分配：<ol>
<li>使用对称密钥加密信息，使用非对称加密方式分发加密密钥</li>
<li>分配步骤：使用对方公钥加密交互信息（验证对方身份）+使用自己私钥和对方公钥加密信息密钥+使用信息密钥加密传输</li>
</ol>
</li>
<li>X.509标准：<ol>
<li>CA为用户颁发证书，证书包括用户公钥，使用CA公钥签名</li>
<li>CA保证了用户信息的正确性，证书内容无法伪造或篡改</li>
<li>根CA向下级CA签名，构建树状结构、证书链</li>
<li>根证书可以是自签名的</li>
</ol>
</li>
<li>PKI公钥基础设施：<ol>
<li>PKI是生成、管理、存储、分发和吊销基于公钥密码学的公钥证书所需要的硬件、软件、人员、策略和规程的总和</li>
<li>包括证书库、CA、RA、证书注销列表、密钥备份恢复、应用接口</li>
<li>用户的证书请求经由RA发送到CA</li>
<li>不同的CA之间可以交叉认证，交换密钥信息，验证对方的可信任性</li>
</ol>
</li>
<li>信息隐藏：利用载体中随机性的荣誉部分隐藏秘密信息。</li>
<li>数字水印</li>
</ol>
<h2 id="0x09网络访问控制和云安全"><a href="#0x09网络访问控制和云安全" class="headerlink" title="0x09网络访问控制和云安全"></a>0x09网络访问控制和云安全</h2><ol>
<li>访问控制：允许或限制访问能力和范围。包括阻止对资源的未授权访问和拒绝向非授权主体提供服务</li>
<li>访问控制实体：<ol>
<li>客体：被保护的资源对象</li>
<li>主体：访问行为发起者</li>
<li>授权：安全访问策略，规定主体对客体执行的动作</li>
</ol>
</li>
<li>访问控制策略<ol>
<li>最小特权策略，</li>
<li>最小泄露策略</li>
<li>多级安全策略</li>
</ol>
</li>
<li>EAP协议（Extensible Authentication Protocol）：运行在链路层的安全认证协议</li>
<li>IEEE 802.1X协议：基于端口的认证策略</li>
<li>云计算：并行计算、分布式计算、网格计算的融合和发展<ol>
<li>一种弹性计算资源，通过网络获取的共享资源</li>
<li>网络获取、可伸缩性、资源共享</li>
<li>服务模式：SaaS软件即服务、PaaS平台即服务、IaaS基础设施即服务</li>
<li>部署模型：公有云、私有云、社区云、混合云</li>
</ol>
</li>
<li>云计算安全风险：滥用和恶意使用、不安全的API、数据泄露、账户或服务劫持</li>
</ol>
<h2 id="0x0A-WEB安全和传输层安全"><a href="#0x0A-WEB安全和传输层安全" class="headerlink" title="0x0A WEB安全和传输层安全"></a>0x0A WEB安全和传输层安全</h2><ol>
<li>架构：Browser/Server结构，应用层使用HTTP协议</li>
<li>Web内容<ol>
<li>静态内容：固定网页的访问</li>
<li>动态内容：利用数据库维护，动态生成网页内容</li>
</ol>
</li>
<li>Web不安全的根源：<ol>
<li>Internet的开放性，任何人都可以建立web站点</li>
<li>DNS域名可以自由注册，按时间顺序</li>
</ol>
</li>
<li>浏览器安全问题<ol>
<li>辅助应用程序：web数据是浏览器无法解析的类型时，会自动调用辅助应用程序</li>
<li>插件：浏览特定信息的软件模块，可以发送有害数据攻击</li>
<li>Cookie：存储传递的状态信息，可能会泄露用户的信息</li>
<li>脚本：运行在服务器/浏览器上的程序，可以嵌入恶意代码</li>
</ol>
</li>
<li>服务器安全问题：<ol>
<li>口令被破解</li>
<li>文件被未授权访问</li>
<li>DoS攻击</li>
<li>命令注入攻击</li>
</ol>
</li>
<li>web安全：<ol>
<li>浏览器安全：限制权限、限制数据类型</li>
<li>服务器安全：访问控制、认证、防火墙、日志、用户账户管理、服务端口管理</li>
<li>流量安全</li>
</ol>
</li>
<li>流量安全：IPsec、SSL/TLS、HTTPS等</li>
<li>SSL/TLS：传输层安全协议<ol>
<li>建立在TCP之上的安全套接层，包括握手协议、压缩、加解密、计算校验</li>
<li>传输流程：分段，压缩，增加MAC，加密，增加SSL记录（头部），得到SSL报文</li>
<li>握手流程：交换基本信息、传输证书并确认、密钥交换获得会话密钥</li>
</ol>
</li>
<li>HTTPS：应用层安全协议，即HTTP+SSL/TLS</li>
<li>SSH：安全的远程登录和交互式会话，可以替代telnet，也可以建立传输隧道。</li>
</ol>
<h2 id="0x0B-无线网络安全"><a href="#0x0B-无线网络安全" class="headerlink" title="0x0B 无线网络安全"></a>0x0B 无线网络安全</h2><ol>
<li>无线网络组网：基站，Wi-Fi热点、无线AP</li>
<li>无线网络特点：<ol>
<li>开放性：容易接入，无硬件防护</li>
<li>移动性：设备移动范围较大</li>
<li>动态性：动态拓扑</li>
<li>不稳定性：信道传输不稳定，易监听和干扰</li>
</ol>
</li>
<li>无线网络安全威胁：<ol>
<li>窃听、监听、截取：入侵者接入系统</li>
<li>主动攻击：IP/MAC欺骗、DoS攻击、恶意代码、网络接管</li>
<li>法律问题</li>
</ol>
</li>
<li>移动设备安全问题：缺乏物理控制、不可信网络、未知来源程序等</li>
<li>传输安全：数据加密、认证机制、防火墙、入侵检测等</li>
<li>WLAN标准：802.11协议、Bluetooth协议等</li>
<li>802.11协议框架<ol>
<li>只涉及物理层和数据链路层的MAC子层，上层与其他保持一致。</li>
<li>最小组成块BSS包括一个接入点AP和若干个移动站STA。</li>
<li>通过固定设施将若干BSS组成一个整体ESS</li>
</ol>
</li>
<li>WLAN安全：<ol>
<li>考虑：组织未授权用户进入网络，防止数据窃听</li>
<li>安全服务：AP的SSID匹配、有线等效保密WEP、物理地址MAC过滤</li>
<li>WEP机制：提供身份验证、加密和密钥管理、完整性检查。认证太简单被破解、单向认证、重放攻击、密钥管理机制不完整</li>
<li>WPA机制：WEP机制的进一步发展，每一个分组使用不同的分组密钥</li>
</ol>
</li>
<li>安全无线网络802.11i标准<ol>
<li>包括801.1x协议、EAP、RADIUS协议</li>
<li>包括TKIP密钥协议和CCMP安全协议</li>
</ol>
</li>
</ol>
<h2 id="0x0C-电子邮件安全"><a href="#0x0C-电子邮件安全" class="headerlink" title="0x0C 电子邮件安全"></a>0x0C 电子邮件安全</h2><ol>
<li>电子邮件发送流程：<ol>
<li>用户与用户代理MUA交互，使用POP或者IMAP协议</li>
<li>用户代理借助报文传输代理交互，使用SMTP协议</li>
</ol>
</li>
<li>电子邮件安全问题：<ol>
<li>协议安全问题，例如SMTP和MIME的安全问题</li>
<li>内容安全：邮件窃听、邮件欺骗、利用邮件传播病毒</li>
<li>垃圾邮件</li>
</ol>
</li>
<li>电子邮件安全机制<ol>
<li>端到端安全机制：PGP，S/MIME等</li>
<li>传输层安全机制：SSL SMTP，SSL POP等</li>
<li>应用层网关</li>
</ol>
</li>
<li>PGP安全邮件标准<ol>
<li>提供服务：认证（使用数字签名）、保密（使用对称加密）、压缩（zip压缩）、兼容性（base64编码）、分段重组</li>
<li>密钥：会话密钥、公钥、私钥、口令</li>
<li>定义了密钥ID，即公钥的低64位作为身份的标识符</li>
<li>用户的私钥使用口令进行CAST-128/IDEA/3DES加密</li>
<li>基于信任的关系模型，每个公钥被签名，用户对公钥的信任程度取决于对签名人的信任程度</li>
</ol>
</li>
<li>SMTP：<ol>
<li>只能传输ASCII字符</li>
<li>邮件内容大小的限制</li>
</ol>
</li>
<li>S/MIME：<ol>
<li>MIME的安全性扩展</li>
<li>结合PKI，使用X.509证书</li>
<li>提供数据加密、数据签名、加密和签名的嵌套</li>
</ol>
</li>
<li>DKIM电子邮件验证标准：<ol>
<li>邮件的加密签名和域名/企业关联起来，防止电子邮件欺诈</li>
<li>公钥位于DNS，私钥位于邮件服务器，在邮件中签名</li>
</ol>
</li>
</ol>
<h2 id="0x0D-IP安全"><a href="#0x0D-IP安全" class="headerlink" title="0x0D IP安全"></a>0x0D IP安全</h2><ol>
<li>当前的互联网使用TCP/IP协议栈，基本完全建立在IP之上，但是IP协议设计时并没有考虑很多安全问题，从本质上来说就是不安全的，修改IP报文非常简单。攻击包括：窃听、篡改、IP欺骗、重防攻击</li>
<li>IPsec：<ol>
<li>IP协议中集成的安全机制</li>
<li>在IP层上创建安全的隧道层VPN</li>
<li>包括三个协议：AH协议、ESP协议、IKE协议</li>
<li>两种操作模式：传输模式和隧道模式</li>
<li>两个数据库：安全策略数据库SPD，安全关联数据库SAD</li>
</ol>
</li>
<li>IPsec的体系：DOI即解释域，使用IKE协商SA。</li>
<li>工作模式：<ol>
<li>传输模式：保护IP载荷，用于主机的端到端通信</li>
<li>隧道模式：保护IP包，将旧包封装在新包中传输，用于网关模式</li>
</ol>
</li>
<li>SA安全关联<ol>
<li>IPsec的基础，两个实体协商建立的协定</li>
<li>内容包括IPsec协议，工作模式、各种算法、密钥、窗口、计数器等信息</li>
<li>单向的逻辑连接，因此常成对出现</li>
<li>三个参数惟一标示：安全参数索引、目的IP地址、安全协议标识符</li>
</ol>
</li>
<li>SAD：每个SA的参数值</li>
<li>SPD：人机的安全接口</li>
</ol>
<p>【后面还有很多内容 暂时跳过】</p>
<h2 id="0x0E-恶意软件"><a href="#0x0E-恶意软件" class="headerlink" title="0x0E 恶意软件"></a>0x0E 恶意软件</h2><ol>
<li>恶意破坏作用的代码和程序，非授权性和破坏性</li>
<li>恶意代码：<ol>
<li>病毒：自我复制和破坏</li>
<li>蠕虫：自我复制和传播，消耗系统资源</li>
<li>木马：窃取信息，远程控制，隐藏</li>
<li>僵尸：远程控制发动攻击</li>
</ol>
</li>
<li>病毒特征：传染性、隐蔽性、潜伏性、破坏性、寄生性</li>
<li>病毒生命周期：睡眠、传播、触发、执行</li>
<li>宏病毒：将宏代码写入模版文件，再写入所有的文件中</li>
<li>蠕虫：利用漏洞自主传播消耗资源</li>
<li>木马：隐蔽窃取信息，自启动，信息传播</li>
<li>僵尸网络：CC服务器控制大量僵尸主机</li>
<li>钓鱼和鱼叉攻击：伪造信息获得信任</li>
<li>病毒检测：<ol>
<li>特征码检测</li>
<li>文件比较</li>
<li>启发式扫描、文件散列</li>
<li>行为检测、沙箱</li>
<li>系统边界、流量检测</li>
</ol>
</li>
<li>DDoS：消耗目标服务器计算资源/缓冲区，利用协议设计的漏洞</li>
</ol>
<h2 id="0x0F-入侵检测"><a href="#0x0F-入侵检测" class="headerlink" title="0x0F 入侵检测"></a>0x0F 入侵检测</h2><ol>
<li>三种手段：<ol>
<li>统计分析：统计正常使用的属性</li>
<li>模式匹配：匹配误用数据库</li>
<li>完整性分析：检查文件和对象是否被更改</li>
</ol>
</li>
<li>两种模型：<ol>
<li>异常检测Anomaly Detection：基于用户行为，包括各类阈值</li>
<li>误用检测Misuse Detection：基于规则和模式匹配，记录非正常操作的规则和特征</li>
</ol>
</li>
<li>信息响应：<ol>
<li>主动响应：根据检测到的入侵行为采取措施</li>
<li>被动响应：记录和报告问题</li>
</ol>
</li>
<li>主动响应：<ol>
<li>搜集信息：使用蜜罐等技术</li>
<li>入侵追踪：基于主机或者给予网络</li>
<li>联动响应：通知防火墙和其他控制设备加以响应和处理</li>
</ol>
</li>
<li>审计记录<ol>
<li>对于用户的行为做记录</li>
<li>包括：主体、客体、时间戳、异常条件、行为</li>
</ol>
</li>
<li>IDS：<ol>
<li>从系统关键点搜集信息，检测是否有攻击发生并发出警告</li>
<li>基本不具有访问控制能力，只能用于侦察和预警</li>
<li>是防火墙的补充</li>
</ol>
</li>
<li>IDS分类<ol>
<li>基于主机：从主机中获得数据</li>
<li>基于网络：获得的信息为网络中的数据包</li>
<li>混合型，完备的IDS是网络和主机混合的分布式系统</li>
</ol>
</li>
<li>口令的脆弱性：字典攻击、穷举攻击、Hash表攻击等</li>
<li>口令的防护：安全策略（时间、长度、复杂性）、限制尝试次数、加盐</li>
</ol>
<h2 id="0x10-防火墙"><a href="#0x10-防火墙" class="headerlink" title="0x10 防火墙"></a>0x10 防火墙</h2><ol>
<li>防火墙：内外网边界的过滤机制，保护本地系统不受网络安全威胁</li>
<li>划分：<ol>
<li>内部网络：安全可信赖</li>
<li>外部网络：不安全，不可信赖</li>
<li>DMZ非军事区：内网对外提供服务的主机位于非军事区</li>
</ol>
</li>
<li>防火墙提供了服务控制、方向控制、用户控制和行为控制</li>
<li>分类<ol>
<li>包过滤：在网络层，根据数据包的报头信息加以处理</li>
<li>网关/代理服务器：在应用层，处理用户的应用层请求</li>
<li>状态监测：在传输层和应用层，检测传输层连接的跟踪</li>
</ol>
</li>
<li>SOCKS防火墙安全会话转移协议：<ol>
<li>一种传输层的网络代理协议</li>
<li>利用防火墙简单的转发传输数据包</li>
</ol>
</li>
<li>防火墙部署：<ol>
<li>双宿主机Dual-homed，位于不同网络上的多个网卡</li>
<li>屏蔽主机：增加路由器，数据先经过包过滤路由器和堡垒主机</li>
<li>屏蔽子网：两个路由器，形成DMZ隔离带</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Trojan</tag>
        <tag>Hexo</tag>
        <tag>OpenSSL</tag>
        <tag>HTTPS</tag>
        <tag>SSH</tag>
        <tag>Socket</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows安全管理工具总结</title>
    <url>/2020/06/25/windows-security-tools/</url>
    <content><![CDATA[<p>简单整理Windows安全课堂上讲解和提及的各种安全工具。</p>
<a id="more"></a>
<h2 id="CMD-命令"><a href="#CMD-命令" class="headerlink" title="CMD 命令"></a>CMD 命令</h2><ul>
<li><p>ver：查看内核版本。</p>
</li>
<li><p>systeminfo：查看系统信息，包括版本，补丁等大量信息。</p>
</li>
<li><p>set：查看所有的环境变量。</p>
</li>
<li><p>sc：查询服务信息。</p>
<ul>
<li>sc query [service name] 查询状态</li>
<li>sc qc [service name] 查询配置</li>
<li>sc start [service name] 启动需要手动启动的服务/驱动</li>
<li>sc stop [service name] 停止服务</li>
<li>sc showsid [service name] 查看服务的SID</li>
</ul>
</li>
<li><p>attrib：修改文件属性</p>
<ul>
<li><p>例如隐藏文件：添加hidden和system属性</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">&gt; attrib +h +s file-name</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>whoami：显示域名和用户名（没有加入域时，显示主机名）。</p>
<ul>
<li>whoami /user显示用户名和SID</li>
<li>whoami /all显示详细信息（特权、权限、所属组等）</li>
<li>whoami /all 显示详细的用户访问控制令牌信息</li>
</ul>
</li>
<li><p>hostname：查看主机名称</p>
</li>
<li><p>net：用户和用户组操作</p>
<ul>
<li>net user [username] 查看用户信息</li>
<li>net user [username] /add 添加新的用户</li>
<li>net user [username] [password] /add添加用户</li>
<li>net user [username] /del</li>
<li>net localgroup [groupname] [username] /add 用户加入本地组</li>
</ul>
</li>
<li><p>makecab：压缩工具</p>
</li>
<li><p>wusa：补丁安装命令</p>
</li>
<li><p>netstat：查看网络连接信息，各种参数参见help信息</p>
</li>
<li><p>reg：注册表管理工具</p>
<ul>
<li>reg save [hkey] [filename] 将注册表导出到文件中</li>
</ul>
</li>
<li><p>cacls：查看客体的安全访问控制项ACE</p>
</li>
</ul>
<h2 id="系统工具（cmd-R）"><a href="#系统工具（cmd-R）" class="headerlink" title="系统工具（cmd+R）"></a>系统工具（cmd+R）</h2><ul>
<li>regedit：（Registry Editer）注册表查看和编辑。</li>
<li>services.msc：服务控制面板（位于控制面板-系统和安全-管理工具-服务），查看和管理系统服务（只能看到用户态服务）。</li>
<li>secpol.msc：（Security Policy）本地安全策略配置。</li>
<li>syskey：SAM锁定工具，可以将密码存入本地或软盘。</li>
<li>msinfo32.exe：查看所有加载的驱动程序。</li>
<li>control：控制面板</li>
<li>eventvwr：（Event Viewer）审计日志查看器</li>
<li>certmgr.msc：（Certificate Manager）证书管理工具</li>
</ul>
<h2 id="其他工具"><a href="#其他工具" class="headerlink" title="其他工具"></a>其他工具</h2><ul>
<li>PE文件查看器：<ul>
<li>PE Explorer。</li>
<li>Stud_PE。</li>
<li>PEiD：可以看到加壳工具和编译工具。</li>
</ul>
</li>
<li>DarkRemoteRAT：一个木马软件</li>
<li><p>Agony：一个RootKit代码演示工具</p>
<ul>
<li>agony -p [进程名] 隐藏进程</li>
<li>agony -f [文件名] 隐藏文件/文件夹</li>
</ul>
</li>
<li><p>SystInternalsSuite工具集：</p>
<ul>
<li>autoruns：查看自启动项（注册表，自启动目录，服务，计划程序等）</li>
<li>procexp（process explorer）：查看进程信息（父子进程关系，进程加载的DLL和句柄列表，访问控制令牌，DEP，ASLR，完整性级别，虚拟化等，包括svchost内部的具体服务，进程的网络连接信息）</li>
<li>tcpview：查看网络连接、端口、数据等</li>
<li>procmon（process monitor）：检查和记录进程的各种操作</li>
<li>PsGetSid：查看用户的SID（默认显示主机SID）</li>
<li>psexec：运行程序<ul>
<li>使用-s参数可以使用SYSTEM权限运行程序</li>
<li>使用-l参数可以使用Low完整性级别运行程序</li>
</ul>
</li>
<li>procdump：进程内存导出工具</li>
<li>AccessChk：查看主客体对象的完整性级别</li>
<li>Strings：二进制代码的明文字符串查找</li>
</ul>
</li>
<li>RootKit/BootKit检测工具<ul>
<li>XueTr（XT）：一个RootKit检测查看工具</li>
<li>PowerTool（PT）：可以检测到Bootkit和Rootkit</li>
<li>IceSword：不再更新，经典</li>
<li>RootkitUnhooker（RKU）</li>
<li>Kaspersky TDSSKiller：可以检测到Bootkit和Rootkit</li>
</ul>
</li>
<li>OSR Driver Loader：一个向系统加载驱动的工具。</li>
<li>nc：一个常见的网络监控和通信工具。“网络的瑞士军刀”</li>
<li>wireshark：一个开源的网络数据包分析软件。</li>
<li>沙箱工具（用于动态分析）：<ul>
<li>SSM沙箱：System Safety Monitor，给系统函数加hook</li>
<li>Cuckoo：开源的沙箱，使用最多</li>
<li>Anubis沙箱</li>
<li>Norman沙箱</li>
<li>GFI沙箱</li>
</ul>
</li>
<li>OllyDbg：一个常见的动态分析调试工具。</li>
<li>静态分析工具：<ul>
<li>IDA Pro：高级分析工具</li>
<li>Depends：查看链接库，导入导出表</li>
<li>Dependancy Walker</li>
</ul>
</li>
<li>明文字符串查找：<ul>
<li>BinText：查看明文字符串，包括函数名等</li>
<li>Strings</li>
</ul>
</li>
<li>加壳识别和脱壳：<ul>
<li>VMUnpacker</li>
<li>UPX：可以加壳和脱壳</li>
<li>UnPEPack</li>
<li>ASPack unpacker</li>
</ul>
</li>
<li>文件格式识别：<ul>
<li>PEid：检查是否加壳，检测编译工具</li>
<li>FileAnalyzer</li>
</ul>
</li>
<li>反病毒引擎扫描：<ul>
<li>VirusScan</li>
<li>VirusTool</li>
</ul>
</li>
<li>mimikatz：一个获得登陆用户明文密码的工具</li>
<li>winhex：二进制读写工具，直接操纵硬盘而不是使用WinAPI</li>
<li>SAMINside：SAM文件解析和读取工具</li>
<li>本地密码破解工具：<ul>
<li>L0phtCrack</li>
<li>SAMInside</li>
<li>Ophcrack</li>
</ul>
</li>
<li>Windows本地密码散列导出工具：<ul>
<li>Pwdump</li>
<li>wce：Windows密码凭证编辑器</li>
<li>gsecdump</li>
<li>copypwd</li>
<li>QuarksPwDump</li>
</ul>
</li>
<li>提取登录用户明文密码：<ul>
<li>wce：Windows密码凭证编辑器</li>
<li>mimikatz</li>
</ul>
</li>
<li>数据恢复工具：<ul>
<li>EasyRecovery</li>
<li>FinalData</li>
</ul>
</li>
</ul>
<h2 id="参考Github项目"><a href="#参考Github项目" class="headerlink" title="参考Github项目"></a>参考Github项目</h2><ul>
<li>UACME</li>
<li>LOLBAS</li>
<li>BypassAntiVirus</li>
</ul>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】A Discriminative Feature Learning Approach for Deep Face Recognition</title>
    <url>/2020/06/13/center-loss/</url>
    <content><![CDATA[<p>论文题目：A Discriminative Feature Learning Approach for Deep Face Recognition</p>
<p>作者：Yandong Wen, Kaipeng Zhang, Zhifeng Li, and Yu Qiao</p>
<p>会议/时间：ECCV2016</p>
<p>链接：<a href="https://ydwen.github.io/papers/WenECCV16.pdf" target="_blank" rel="noopener">原文链接</a></p>
<a id="more"></a>
<h2 id="论文目标"><a href="#论文目标" class="headerlink" title="论文目标"></a>论文目标</h2><p>之前的工作中仅仅使用Softmax loss作为模型的监督信号，学到的模型具有一定判别能力(Separable)，本文通过介绍一种新的Centor Loss作为监督信号进行学习，使模型学到更具有判别能力(Discriminative)的特征。Centor Loss可以学习到一类特征的分类中心并减小类内距离，从而更具有更强的判别能力。</p>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><p>传统的度量学习(Metric Learning)中，由于识别样本在训练集中出现过，使用Softmax可以起到比较好的学习效果。但是在面识别任务中，很难预先搜集到所有可能的实体的数据用于学习，因此对于泛化能力的要求更高。这就要求学习到的特征具有更小的类内距离和更大的类间距离。</p>
<p>使用contrastive loss或者triplet loss对于pair/triplet的取样要求比较高，使用精心设计的取样方法可以在一定程度上避免，但是引入了更高的计算复杂度。</p>
<h2 id="本文思路-解决方案"><a href="#本文思路-解决方案" class="headerlink" title="本文思路/解决方案"></a>本文思路/解决方案</h2><p>首先定义了centor loss函数<script type="math/tex">\mathcal{L}_c = \frac{1}{2}\sum\limits_{i=1}^{m}||\mathbb{x}_i-\mathbb{c}_{y_i}||^2_2</script>，其中$\mathbb{c}$表示对应的类的中心。</p>
<p>直观的想法是在每一轮学习结束后将所有同类别数据的特征求均值，但是在大规模数据库中难以实现，因此采用每一批中同类别的数据的特征求均值用于类中心的更新。即令<script type="math/tex">\Delta\mathbb{c}_j = \frac{\sum\limits_{i=1}^{m}\delta(y_i=j)(\mathbb{c}_j - \mathbb{x}_i)}{1+\sum\limits_{i=1}^{m}\delta(y_j = j)}</script>。其中$\delta(condition) = condition\ is\ true ? 1: 0$，同时引入一个超参数$\alpha$作为类的中心更新时的“学习率”。</p>
<p>完整的loss function为<script type="math/tex">\mathcal{L} = \mathcal{L}_s + \lambda\mathcal{L}_c</script>，其中$\lambda$为权重系数。</p>
<p>网络结构如下：可以看到引入了跨层连接和Joint Supervision Signal。</p>
<p><img src="//YangLeiSX.github.io/2020/06/13/center-loss/center_loss_structure.png" alt="模型结构"></p>
<p>训练结果如下：可以看到在引入了Center Loss之后，类间距离显著减小，判别能力增强。</p>
<p><img src="//YangLeiSX.github.io/2020/06/13/center-loss/center_loss_1.png" alt="学习结果"></p>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>可以发现$\lambda=0$时的学习效果较差，当<script type="math/tex">\lambda</script>太大时学习效果也会下降，测试得到的参数为<script type="math/tex">\lambda=0.03</script>。</p>
<p>可以发现<script type="math/tex">\alpha</script>的取值对结果影响不大（不为零时），测试得到的参数为<script type="math/tex">\alpha=0.5</script>。</p>
<p>最终实现的模型在小数据库训练，LFW达到了99.28%泛化准确率，YTF达到了94.9%的泛化准确率。</p>
<p>在MegaFace数据库上测试的结果中，本文的模型均达到了更好的性能。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>提出了Center Loss，使用Center Loss结合Softmax Loss实现具有判别力的模型学习。</li>
<li>使用了跨层连接的模型结构。</li>
<li>使用Joint Supervision Signal，合理选择权重超参数。</li>
</ol>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>WinAPI的数据类型(字符串)</title>
    <url>/2020/06/11/win-api-type/</url>
    <content><![CDATA[<p>刚开始使用Windows提供的各种API函数时，对于函数参数的类型感到非常的迷惑，实际上Windows中对于各种C++数据类型做了封装和处理，得到了一组Windows的变量类型，其中的部分字符和字符串的定义如下。</p>
<p>其他的类型定义可以在具体使用时搜索查看头文件（例如<code>#include &lt;Windows.h&gt;</code>）或者MSDN。</p>
<a id="more"></a>
<h2 id="字符和字符串类型"><a href="#字符和字符串类型" class="headerlink" title="字符和字符串类型"></a>字符和字符串类型</h2><div class="table-container">
<table>
<thead>
<tr>
<th>变量类型</th>
<th>解释</th>
<th>定义</th>
</tr>
</thead>
<tbody>
<tr>
<td>CHAR</td>
<td>ANSI型字符</td>
<td>char</td>
</tr>
<tr>
<td>WCHAT</td>
<td>Unicode型字符</td>
<td>wchar_t</td>
</tr>
<tr>
<td>TCHAR</td>
<td>自适应字符（ANSI or Unicode）</td>
<td>可变</td>
</tr>
<tr>
<td>LPSTR/PSTR</td>
<td>ANSI型字符串</td>
<td>char*</td>
</tr>
<tr>
<td>LPWSTR/PWSTR</td>
<td>Unicode型字符串</td>
<td>wchar_t*</td>
</tr>
<tr>
<td>LPTSTR/PTSTR</td>
<td>自适应字符串（ANSI or Unicode）</td>
<td>可变</td>
</tr>
<tr>
<td>LPCSTR/PCSTR</td>
<td>常量ANSI型字符串</td>
<td>const char*</td>
</tr>
<tr>
<td>LPCWSTR/PCWSTR</td>
<td>常量Unicode型字符串</td>
<td>const wchar_t*</td>
</tr>
<tr>
<td>LPCTSTR/PCTSTR</td>
<td>常量自适应字符串</td>
<td>可变</td>
</tr>
</tbody>
</table>
</div>
<p>其中的前缀LP/P，可以理解为表示指针Pointer（字符串变量为字符数组的首地址指针）。</p>
<h2 id="字符串函数"><a href="#字符串函数" class="headerlink" title="字符串函数"></a>字符串函数</h2><p>绝大多数操作字符串的函数都提供了A和W两种，例如RegOpenExA()和RegOpenExW()，同时提供了自适应的版本RegOpenEx()，A为后缀的函数参数通常为ANSI型，W为后缀的函数参数通常为Unicode型，无后缀的函数使用自适应定义。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> UNICODE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TCHAR Wchar_t</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TCHAR char</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> UNICODE</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RegOpenEx RegOpenExW</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RegOpenEx RegOpenExA</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p><a href="https://docs.microsoft.com/en-us/windows/win32/learnwin32/working-with-strings" target="_blank" rel="noopener">参考MSDN</a></p>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
        <tag>Windows</tag>
        <tag>WinAPI</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Deep Learning Face Representation by Joint Identification-Verification</title>
    <url>/2020/06/06/deepid2/</url>
    <content><![CDATA[<p>论文题目：Deep Learning Face Representation by Joint Identification-Verification</p>
<p>作者：Yi Sun、 Xiaogang Wang、 Xiaoou Tang</p>
<p>时间：2014</p>
<p>链接：<a href="https://arxiv.org/pdf/1406.4773.pdf" target="_blank" rel="noopener">原文链接</a></p>
<a id="more"></a>
<h2 id="论文目标"><a href="#论文目标" class="headerlink" title="论文目标"></a>论文目标</h2><p>在面部识别中，关键任务是提取面部特征，并增大类间距离、减小类内距离。</p>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><p>在之前的工作中，本文作者提出的DeepID使用softmax作为输出用于身份验证，达到了比较好的效果。其他诸如LDA、metric learning等方法使用线性模型或使用浅层网络，具有一定的局限性。</p>
<h2 id="本文思路-解决方案"><a href="#本文思路-解决方案" class="headerlink" title="本文思路/解决方案"></a>本文思路/解决方案</h2><p>同时使用两种supervisory signal来指导模型的学习，即同时使用identification（给定输入，判断属于哪一个类别，多分类）和verification（给定两个输入，判断是否属于同一类别，二分类），前者可以增大类间距离，后者减小类内距离。</p>
<p>深度神经网络的结构与本文作者<a href="http://www.ee.cuhk.edu.hk/~xgwang/papers/sunWTcvpr14.pdf" target="_blank" rel="noopener">之前的工作</a>基本相同，即包括四层卷积和三层最大池化，经过跨层连接得到长度为160的特征向量。</p>
<p>使用特征网络的输出向量分别用于identification和verification，反向传播时使用超参数$\lambda$对两个损失函数的梯度加权。在前者使用softmax得到输出并优化cross-entropy loss。在后者使用基于L1/L2正则和cosine相似的损失函数，并对比较其效果。</p>
<p>基于cosine相似:<script type="math/tex">Verif(f_i,f_j, y_{ij}, \theta_{ve}) = \frac{1}{2}(y_{ij}-\sigma(wd+b))^2</script>，其中$\sigma$为sigmoid函数，<script type="math/tex">d = \frac{f_i · f_j}{||f_i||_2·||f_j||_2}</script>。</p>
<p>基于L2的:<script type="math/tex">Verif(f_i,f_j, y_{ij}, \theta_{ij}) = \begin{cases} \frac{1}{2}||f_i - f_j||^2 &if\ y_{ij}=1\\\frac{1}{2}\max(0, m-||f_i - f_j||^2)&if\ y_{ij}=-1\end{cases}</script>，其中<script type="math/tex">y_{ij}</script>表示输入是否为同一类别，$ m $为指定的边界，在训练过程中手动调整。</p>
<p>训练过程中，每一张图片选择了400个patch，包括不同位置、大小、通道（RGB or 灰度），训练得到200个网络。</p>
<p>测试Verification时，通过前后向贪婪算法选择其中效果最好的25个得到$25*160=4000$维度的特征向量，并通过PCA压缩，便于识别和预测。在预测时分别使用L2 Norm模型和联合贝叶斯模型来得到结果。</p>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>对于超参数$\lambda$的选择，通过测试可以发现在0.05处达到最高。当$\lambda=0$或者$\lambda=+\infty$时，据无法得到比较好的效果，说明identification和verification对于特征提取都具有重要作用。</p>
<p>与DeepID中的情况相同，训练时使用的类别越多，学习效果越好。</p>
<p>使用联合贝叶斯模型实现预测可以得到更高的准确率。同时在训练过程中，使用L2正则的损失函数效果更好。</p>
<p>多次选择效果比较好的patch组合，使用得到的结果结合SVM进行预测，整体系统的性能达到了99.15%的识别准确率。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>使用identification和verification结合的方式，合理选择权重超参数，使得学习效果进一步提升。</li>
<li>在verification中测试多种损失函数，最终选择了效果最好的基于L2的模型。</li>
</ol>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】FaceNet：A Unified Embedding for Face Recognition and Clustering</title>
    <url>/2020/06/06/facenet/</url>
    <content><![CDATA[<p>论文题目：FaceNet: A Unified Embedding for Face Recognition and Clustering</p>
<p>作者：Florian Schroff、Dmitry Kalenichenko、James Philbin</p>
<p>会议/时间：2015</p>
<p>链接：<a href="https://arxiv.org/pdf/1503.03832.pdf" target="_blank" rel="noopener">原文链接</a></p>
<a id="more"></a>
<h2 id="论文目标"><a href="#论文目标" class="headerlink" title="论文目标"></a>论文目标</h2><p>学习一种直接的从面部图片到欧氏空间向量的映射，用于人脸识别、分类和聚类等工作。欧氏空间向量之间的距离表示图片内人脸的相似度。</p>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><p>过去的人脸识别网络中往往通过人脸识别/分类等任务训练，最终将其中的某一层提取出来用作特征向量的生成。缺点在于泛化能力可能不够强，而且往往生成的特征向量长度比较大，效率较低。多数研究中对于较长的特征向量使用PCA简化。</p>
<h2 id="本文思路-解决方案"><a href="#本文思路-解决方案" class="headerlink" title="本文思路/解决方案"></a>本文思路/解决方案</h2><p>通过引入triplet loss和online triplet mining method，直接学习得到特征向量，具体是对于深度神经网络的输出，经过L2Norm后得到向量，并使用Triplet loss优化。本文作者使用的两种深度网络分别来自Zeiler&amp;Fergus Model和Szegedy’s Inception Model，将其视作黑箱并训练。</p>
<p>文中假设相同的人输入的数据分布在欧氏空间内的一个超平面，当给定一组triplet（包括锚anchor、正例positive、反例negative，其中anchor和positive是同一类）时，则有<script type="math/tex">||f(x_i^a)-f(x_i^p)||^2_2+\alpha < ||f(x_i^a)-f(x_i^n)||^2_2</script>，即正例的距离与反例距离的差值不小于给定的边界值$\alpha$。从而得到优化目标为<script type="math/tex">triplet\ loss = \sum\limits_{i}^{N}\left[||f(x_i^a)-f(x_i^p)||^2_2+\alpha - ||f(x_i^a)-f(x_i^n)||^2_2 \right]_+</script>。</p>
<p>在训练过程中，需要选择合适的数据计算损失函数，如果选择简单的triplet会导致收敛缓慢，训练效果较差。因此在训练过程中，对于每一个给定的数据<script type="math/tex">x_i^a</script>，选择距离最大的正例(<script type="math/tex">\arg\max_{x_i^p}||f(x_i^a)-f(x_i^p)||^2_2</script>)和距离最小的反例(<script type="math/tex">\arg\min||f(x_i^a)-f(x_i^n)||^2</script>)，称为hard取样。</p>
<p>在实际的训练过程中，获得满足要求的triplet是不可能的，两种可行的方法包括：offline方式（每次训练N步后计算距离并选择triplet）、online方法（在每一个batch中选择满足要求的数据）。选择后者时需要注意保证每一个batch中都必须含有正例和反例。</p>
<p>当在训练开始时就选择hard采样时，会导致模型进入局部最优，因此提出了semi-hard取样，即选择那些满足<script type="math/tex">||f(x_i^a)-f(x_i^p)||^2_2 < ||f(x_i^a)-f(x_i^n)||^2_2</script>的反例数据组成triplet进行训练。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>提出了triplet loss，便于直接训练一个图像到向量的映射。</li>
<li>分析了选择triplet的方式，使用semi-hard策略解决hard策略的缺点。</li>
</ol>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Deep Learning Face Representation from Predicting 10000 Classes</title>
    <url>/2020/06/06/deepid/</url>
    <content><![CDATA[<p>论文题目：Deep Learning Face Representation from Predicting 10,000 Classes</p>
<p>会议：CVPR2014</p>
<p>作者：Yi Sun、Xiaogang Wang、 Xiaoou Tang</p>
<p>链接：<a href="http://www.ee.cuhk.edu.hk/~xgwang/papers/sunWTcvpr14.pdf" target="_blank" rel="noopener">原文链接</a></p>
<a id="more"></a>
<h2 id="论文目标"><a href="#论文目标" class="headerlink" title="论文目标"></a>论文目标</h2><p>通过深度神经网络学习高层次的图像特征并用于身份验证。</p>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><p>无限制条件下的面部识别和验证是近年来的研究热点，大多面部识别算法是通过浅层模型学习到的超完备的底层特征实现。</p>
<h2 id="本文思路-解决方案"><a href="#本文思路-解决方案" class="headerlink" title="本文思路/解决方案"></a>本文思路/解决方案</h2><p>通过深度卷积神经网络学习得到特征表示并用于面部识别。其中的特征表示通过深度神经网络的最后一层得到，称为DeepID（Deep Hidden Identity Features）。这一特征表示被用于最后的多分类识别任务，保证了卷积神经网络可以充分的学习到每个人的特征，具有更好的泛化能力。</p>
<p>网络结构如下</p>
<p><img src="//YangLeiSX.github.io/2020/06/06/deepid/deepid1_model.png" alt="网络结构"></p>
<p>通过跨层连接，保证DeepID可以获取到更多的信息，学习到更高级的特征（Conv4之后的特征更加高级/抽象）。跨层连接使用公式<script type="math/tex">y_i = \max(0, \sum\limits_{i}x_i^1\omega_{i,j}^1+\sum\limits_{i}x_i^2\omega_{i,j}^2+b_j)</script>实现，其中的$\omega$为权重。最终使用softmax层作为预测输出。</p>
<p>特征提取部分的具体工作方式：每一张照片，划分为10个位置，每个位置选取三个不同的输入规模，每个照片得到RGB和灰度图，即每一条数据得到$10\times3\times2=60$条数据（patches）作为输入，训练60个网络。对于每一个卷积网络，给定数据，将其翻转后，得到两个向量作为输出，总的输出数据量为$160<em>2</em>60$。</p>
<p><img src="//YangLeiSX.github.io/2020/06/06/deepid/deepid1_structure.png" alt="系统结构"></p>
<p>在面部识别的预测中，使用联合贝叶斯方法。同时也测试了使用深度神经网络进行预测。</p>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>在CelebFaces上训练，在LFW上测试，达到了SOTA的效果。</p>
<p>测试中可以发现添加了跨层连接的模型具有更低的验证错误率和更高的预测正确率。</p>
<p><img src="//YangLeiSX.github.io/2020/06/06/deepid/deepid1_test.png" alt="测试结果"></p>
<p>使用更多的patch与仅使用一张图片作为输入相比也具有更高的识别准确率。</p>
<p>与现有的识别算法比较，具有更高的识别准确率（97.45%）。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>提出了跨层连接（multi-scale），可以显著提高识别准确率。</li>
<li>使用同一张照片的多个patch作为输入，包括不同的位置，不同的大小，不同的通道（RGB、灰度），并将60个网络的输出合并作为预测的依据，可以提高识别准确率。</li>
<li>通过增加识别的人数（多分类的输出）可以使得特征网络学习到关键的信息。</li>
</ol>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>Winsock编程记录</title>
    <url>/2020/05/13/winsock/</url>
    <content><![CDATA[<p>Winsock就是windows下的socket编程的简称，函数用法与BSD的socket（即unix中的socket）编程基本相同。</p>
<p>所有的代码实现位于<a href="https://github.com/YangLeiSX/DataComSJTU" target="_blank" rel="noopener">我的Github</a>。</p>
<p><strong>重要：</strong>所有的函数使用、讲解和实例请参考<a href="https://docs.microsoft.com/en-us/windows/win32/winsock/using-winsock" target="_blank" rel="noopener">Winsock文档 via Microsoft</a>。</p>
<a id="more"></a>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><p>使用VS开发，需要使用的头文件和库文件如下</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;ws2tcpip.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 静态库</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> comment (lib, <span class="meta-string">"Ws2_32.lib"</span>)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> comment (lib, <span class="meta-string">"Mswsock.lib"</span>)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> comment (lib, <span class="meta-string">"AdvApi32.lib"</span>)</span></span><br></pre></td></tr></table></figure>
<h2 id="基本数据结构"><a href="#基本数据结构" class="headerlink" title="基本数据结构"></a>基本数据结构</h2><p>使用比较多的数据结构包括sockaddr，addrinfo等，定义如下。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> &#123;</span></span><br><span class="line">    ushort  sa_family;</span><br><span class="line">    <span class="keyword">char</span>    sa_data[<span class="number">14</span>];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> &#123;</span></span><br><span class="line">    <span class="keyword">short</span>   sin_family;</span><br><span class="line">    u_short sin_port;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span>  <span class="title">in_addr</span> <span class="title">sin_addr</span>;</span></span><br><span class="line">    <span class="keyword">char</span>    sin_zero[<span class="number">8</span>];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span>             ai_flags;</span><br><span class="line">    <span class="keyword">int</span>             ai_family;</span><br><span class="line">    <span class="keyword">int</span>             ai_socktype;</span><br><span class="line">    <span class="keyword">int</span>             ai_protocol;</span><br><span class="line">    <span class="keyword">size_t</span>          ai_addrlen;</span><br><span class="line">    <span class="keyword">char</span>            *ai_canonname;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span> *<span class="title">ai_addr</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">ai_next</span>;</span></span><br><span class="line">&#125; ADDRINFOA, *PADDRINFOA;</span><br></pre></td></tr></table></figure>
<p>这里的sockaddr_in为IP协议的地址结构体，sockaddr为所有网络层协议的地址结构体，可以保存所有网络层协议的地址数据。当使用IP协议栈时使用sockaddr_in结构体，winsock的函数大多使用sockaddr作为函数参数，从而实现更好的兼容性。（winsock还实现了sockaddr_storage，地址空间更大，兼容性更好）。</p>
<h2 id="基本函数模块"><a href="#基本函数模块" class="headerlink" title="基本函数模块"></a>基本函数模块</h2><p>由于Winsock有完备的错误代码提示，因此养成在每一步操作之后都要检查错误代码的习惯。这里添加一个全局的函数返回值（错误代码）变量用于检测操作是否完成。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> iResult = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<h3 id="Winsock初始化：WSAStartup"><a href="#Winsock初始化：WSAStartup" class="headerlink" title="Winsock初始化：WSAStartup()"></a>Winsock初始化：WSAStartup()</h3><p>Winsock在socket各种函数的基础上实现了一套WSA函数（WinSockApplication），需要在程序前部初始化。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">WORD wVersion = MAKEWORD(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line">WSADATA wsaDATA;</span><br><span class="line"><span class="comment">// initialize winsock</span></span><br><span class="line">iResult = WSAStartup(wVersion, &amp;wsaDATA);</span><br><span class="line"><span class="keyword">if</span> (<span class="number">0</span> != iResult) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"WSAStartup failed: %d\n"</span>, iResult);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Winsock清理：-WSACleanup"><a href="#Winsock清理：-WSACleanup" class="headerlink" title="Winsock清理： WSACleanup()"></a>Winsock清理： WSACleanup()</h3><p>Winsock在程序推出之前需要使用WSACleanup()函数完成收尾工作，通常位于main函数的return之前。</p>
<h3 id="获得本地主机名：gethostname"><a href="#获得本地主机名：gethostname" class="headerlink" title="获得本地主机名：gethostname()"></a>获得本地主机名：gethostname()</h3><p>可以获得本地的主机名</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> hostname[NI_MAXHOST];</span><br><span class="line"><span class="keyword">int</span> hostlen = NI_MAXHOST;</span><br><span class="line"><span class="comment">// 获得本地主机名</span></span><br><span class="line">gethostname(hostname, hostlen);</span><br></pre></td></tr></table></figure>
<h3 id="获得地址信息：getaddrinfo"><a href="#获得地址信息：getaddrinfo" class="headerlink" title="获得地址信息：getaddrinfo()"></a>获得地址信息：getaddrinfo()</h3><p>给定主机名和端口号，可以解析得到目标主机的网络地址（例如IP地址），类似ARP协议的作用。需要使用hints传入参数。</p>
<p>其中第一个参数为NULL时为获得本地主机信息。</p>
<p>当目标主机存在多个网卡/IP地址时，得到的result为一个链表，使用<code>result-&gt;ai_next</code>连接。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> hostname[NI_MAXHOST] = “localhost”;</span><br><span class="line"><span class="keyword">char</span> servname[NI_MAXSERV] = “<span class="number">8080</span>”;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">result</span> = <span class="title">NULL</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">hints</span>;</span></span><br><span class="line"><span class="comment">// 初始化</span></span><br><span class="line">ZeroMemory(&amp;hints, <span class="keyword">sizeof</span>(hints));</span><br><span class="line">hints.ai_family = AF_UNSPEC;</span><br><span class="line">hints.ai_socktype = SOCK_STREAM;</span><br><span class="line">hints.ai_protocol = IPPROTO_TCP;</span><br><span class="line"><span class="comment">// 解析地址</span></span><br><span class="line">iResult = getaddrinfo(hostname, servname, &amp;hints, &amp;result);</span><br><span class="line"><span class="keyword">if</span> (<span class="number">0</span> != iResult) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"getaddrinfo failed: %d\n"</span>, iResult);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="获得主机名信息-getnameinfo"><a href="#获得主机名信息-getnameinfo" class="headerlink" title="获得主机名信息: getnameinfo()"></a>获得主机名信息: getnameinfo()</h3><p>给定目标主机的网络地址，可以解析得到主机名和端口号，类似RARP协议的作用。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> hostname[NI_MAXHOST];</span><br><span class="line"><span class="keyword">char</span> servname[NI_MAXSERV];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">sa</span>;</span></span><br><span class="line"><span class="keyword">int</span> addrlen = <span class="keyword">sizeof</span>(struct sockaddr);</span><br><span class="line">u_short port = <span class="number">8080</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* localhost = <span class="string">"127.0.0.1"</span>;</span><br><span class="line"><span class="comment">// 初始化</span></span><br><span class="line">inet_pton(AF_INET, localhost, &amp;sa.sin_addr.s_addr);</span><br><span class="line">sa.sin_family = AF_INET;</span><br><span class="line">sa.sin_port = htons(port);</span><br><span class="line"><span class="comment">// 获得主机名信息</span></span><br><span class="line">iResult = getnameinfo(</span><br><span class="line">    (struct sockaddr*) &amp;sa, addrlen,</span><br><span class="line">    hostname, NI_MAXHOST,</span><br><span class="line">    servname, NI_MAXSERV,</span><br><span class="line">    <span class="number">0</span>);</span><br><span class="line"><span class="keyword">if</span> (<span class="number">0</span> != iResult) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"getnameinfo failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="清空地址信息-freeaddrinfo"><a href="#清空地址信息-freeaddrinfo" class="headerlink" title="清空地址信息: freeaddrinfo()"></a>清空地址信息: freeaddrinfo()</h3><p>使用getaddrinfo得到的地址消息不再使用时，建议显式清空。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">result</span>;</span> <span class="comment">// 非空的地址信息指针</span></span><br><span class="line">freeaddrinfo(result);</span><br></pre></td></tr></table></figure>
<h3 id="地址信息可视化"><a href="#地址信息可视化" class="headerlink" title="地址信息可视化"></a>地址信息可视化</h3><p>想要以可读方式显示地址信息，涉及到大端/小端的转换、二进制和点分十进制的转换等处理。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> *<span class="title">result</span>;</span> <span class="comment">// 非空的地址信息指针</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">server_addr</span>;</span></span><br><span class="line"><span class="keyword">char</span> ipstringbuffer[<span class="number">46</span>];</span><br><span class="line"><span class="keyword">short</span> port;</span><br><span class="line"><span class="comment">// 转换地址格式</span></span><br><span class="line"><span class="built_in">memcpy</span>(&amp;server_addr, result-&gt;ai_addr, <span class="keyword">sizeof</span>(server_addr));</span><br><span class="line">inet_ntop(AF_INET, &amp;server_addr.sin_addr, ipstringbuffer, <span class="keyword">sizeof</span>(ipstringbuffer));</span><br><span class="line">port = ntohs(server_addr.sin_port);</span><br><span class="line"><span class="comment">// 显示地址信息</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Server Address: %s\n"</span>, ipstringbuffer);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Server Port Number: %d\n"</span>, port);</span><br></pre></td></tr></table></figure>
<h3 id="创建套接字-socket"><a href="#创建套接字-socket" class="headerlink" title="创建套接字: socket()"></a>创建套接字: socket()</h3><p>socket编程中核心的部分为套接字socket，创建socket的进程才可以实现不同主机上的进程间通信。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s = INVALID_SOCKET;</span><br><span class="line"><span class="comment">// 创建套接字</span></span><br><span class="line">s = socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);</span><br><span class="line"><span class="keyword">if</span> (ConnectSocket == INVALID_SOCKET) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"socket failed : %d\n"</span>, WSAGetLastError());</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="关闭套接字-closesocket"><a href="#关闭套接字-closesocket" class="headerlink" title="关闭套接字: closesocket()"></a>关闭套接字: closesocket()</h3><p>套接字使用完毕后，建议手动关闭套接字。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s;  <span class="comment">// 合法套接字</span></span><br><span class="line"><span class="comment">// 关闭套接字</span></span><br><span class="line">closesocket(s);</span><br></pre></td></tr></table></figure>
<h3 id="绑定套接字-bind"><a href="#绑定套接字-bind" class="headerlink" title="绑定套接字: bind()"></a>绑定套接字: bind()</h3><p>在服务器端创建的套接字需要绑定，即建立socket和地址之间的联系。相当于内部地址（进程描述符、套接字描述符）和外部地址（网络地址、端口号）之间建立连接。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s;  <span class="comment">// 合法的套接字</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">local</span>;</span></span><br><span class="line"><span class="comment">// 绑定套接字</span></span><br><span class="line">iResult = bind(s, local-&gt;ai_addr, (<span class="keyword">int</span>)local-&gt;ai_addrlen);</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"bind failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    freeaddrinfo(local);</span><br><span class="line">    closesocket(s);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="监听端口-listen"><a href="#监听端口-listen" class="headerlink" title="监听端口: listen()"></a>监听端口: listen()</h3><p>在服务器端绑定后的套接字可以监听端口，用于连接型的数据传输。类似TCP连接中等待SYN报文。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s;  <span class="comment">// 合法的套接字</span></span><br><span class="line"><span class="comment">// 监听端口（套接字）</span></span><br><span class="line">iResult = listen(s, SOMAXCONN);</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"listen failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    closesocket(s);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="接受连接请求-accpet"><a href="#接受连接请求-accpet" class="headerlink" title="接受连接请求: accpet()"></a>接受连接请求: accpet()</h3><p>当服务器端接收到连接请求时（请求队列不为空），接受连接请求并创建连接。类似TCP连接中发送SYN ACK报文。此时创建了用于传输的套接字，与监听套接字分离。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET ListenSocket;  <span class="comment">// 合法的套接字</span></span><br><span class="line">SOCKET ComSocket = INVALID_SOCKET;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span>  <span class="title">client_addr</span>;</span></span><br><span class="line"><span class="keyword">int</span> addr_len = <span class="keyword">sizeof</span>(struct sockaddr_in);</span><br><span class="line"><span class="comment">// 接受连接请求</span></span><br><span class="line">ComSocket = accept(ListenSocket, (struct sockaddr*)&amp;client_addr, &amp;addr_len);</span><br><span class="line"><span class="keyword">if</span> (ComSocket == INVALID_SOCKET) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"accept failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    closesocket(ListenSocket);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="请求连接-connect"><a href="#请求连接-connect" class="headerlink" title="请求连接: connect()"></a>请求连接: connect()</h3><p>客户端需要向服务器发送链接请求时，使用connect函数。类似TCP连接中发送SYN报文。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s;  <span class="comment">// 合法套接字</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">addrinfo</span> <span class="title">remote</span>;</span></span><br><span class="line"><span class="comment">// 发送连接请求</span></span><br><span class="line">iResult = connect(s, remote-&gt;ai_addr, (<span class="keyword">int</span>)remote-&gt;ai_addrlen);</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"connect failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    closesocket(s);</span><br><span class="line">    s = INVALID_SOCKET;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="关闭连接-shutdown"><a href="#关闭连接-shutdown" class="headerlink" title="关闭连接: shutdown()"></a>关闭连接: shutdown()</h3><p>通信结束时，不可以直接关闭socket，应该首先关闭连接（关闭发送方向的连接，表示不再发送消息）。类似TCP连接中的FIN报文。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">SOCKET s;</span><br><span class="line"><span class="comment">// 关闭发送方向请求</span></span><br><span class="line">iResult = shutdown(s, SD_SEND);</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"shutdown failed: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    closesocket(s);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="接受数据-recv"><a href="#接受数据-recv" class="headerlink" title="接受数据: recv()"></a>接受数据: recv()</h3><p>用于建立连接之后的数据传输。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RECV_BUFLEN 512</span></span><br><span class="line"><span class="keyword">char</span> recvbuf[RECV_BUFLEN];</span><br><span class="line"><span class="keyword">int</span> recvbuflen = RECV_BUFLEN;</span><br><span class="line">SOCKET s; <span class="comment">// 建立连接的套接字</span></span><br><span class="line"><span class="built_in">memset</span>(recvbuf, <span class="number">0</span>, recvbuflen);</span><br><span class="line"><span class="comment">// 接受数据</span></span><br><span class="line">iResult = recv(s, recvbuf, recvbuflen, <span class="number">0</span>);</span><br><span class="line"><span class="keyword">if</span> (iResult &gt; <span class="number">0</span>)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Bytes received: %d\n"</span>, iResult);</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (iResult == <span class="number">0</span>)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Connection closed\n"</span>);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"recv failed: %d\n"</span>, WSAGetLastError());</span><br></pre></td></tr></table></figure>
<h3 id="发送数据-send"><a href="#发送数据-send" class="headerlink" title="发送数据: send()"></a>发送数据: send()</h3><p>用于建立连接之后的数据传输</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SEND_BUFLEN 512</span></span><br><span class="line"><span class="keyword">char</span> sendbuf[SEND_BUFLEN];</span><br><span class="line"><span class="built_in">snprintf</span>(sendbuf, SEND_BUFLEN, <span class="string">"hello"</span>);</span><br><span class="line"><span class="comment">// 发送数据</span></span><br><span class="line">iResult = send(ConnectSocket, sendbuf, <span class="keyword">int</span>(<span class="built_in">strlen</span>(sendbuf) + <span class="number">1</span>), <span class="number">0</span>);</span><br><span class="line"><span class="keyword">if</span> (iResult &gt; <span class="number">0</span>)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Bytes Sent: %d\n"</span>, iResult);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"send failed: %d\n"</span>, WSAGetLastError());</span><br></pre></td></tr></table></figure>
<h3 id="接受数据-recvfrom"><a href="#接受数据-recvfrom" class="headerlink" title="接受数据: recvfrom()"></a>接受数据: recvfrom()</h3><p>用于无连接的数据传输，由于是无连接的，需要在函数中传递地址指针保存数据发送方的网络地址信息。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DEFAULT_BUFLEN 512</span></span><br><span class="line">SOCKET udp_s; <span class="comment">// 合法的socket</span></span><br><span class="line"><span class="keyword">char</span> msgbuf[DEFAULT_BUFLEN];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">other_addr</span>;</span></span><br><span class="line"><span class="keyword">int</span> addrlen = <span class="keyword">sizeof</span>(other_addr);</span><br><span class="line"><span class="keyword">char</span> ipstringbuffer[<span class="number">46</span>];</span><br><span class="line"><span class="keyword">short</span> port;</span><br><span class="line"><span class="comment">// 接受数据</span></span><br><span class="line">iResult = recvfrom(udp_s, msgbuf, DEFAULT_BUFLEN, <span class="number">0</span>, (struct sockaddr*) &amp;other_addr, &amp;addrlen);</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"recvfrom() fail: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">&#125;</span><br><span class="line">inet_ntop(AF_INET, &amp;si_other.sin_addr, ipstringbuffer, <span class="number">46</span>);</span><br><span class="line">port = ntohs(si_other.sin_port);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Receive %d bytes from %s:%d.\n"</span>, iResult, ipstringbuffer, port);</span><br></pre></td></tr></table></figure>
<h3 id="发送数据-sendto"><a href="#发送数据-sendto" class="headerlink" title="发送数据: sendto()"></a>发送数据: sendto()</h3><p>用于无连接的数据传输。其中的sockaddr数据是合法的地址数据。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DEFAULT_BUFLEN 512</span></span><br><span class="line">SOCKET udp_s; <span class="comment">// 合法的socket</span></span><br><span class="line"><span class="keyword">char</span> msgbuf[DEFAULT_BUFLEN];</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">other_addr</span>;</span></span><br><span class="line"><span class="keyword">int</span> addrlen = <span class="keyword">sizeof</span>(other_addr);</span><br><span class="line"><span class="built_in">snprintf</span>(msgbuf, DEFAULT_BUFLEN, <span class="string">"hello"</span>);</span><br><span class="line"><span class="comment">// 发送数据</span></span><br><span class="line">iResult = sendto(udp_s, msgbuf, <span class="keyword">int</span>(<span class="built_in">strlen</span>(msgbuf) + <span class="number">1</span>), <span class="number">0</span>, (struct sockaddr*) &amp;other_addr, addrlen)</span><br><span class="line"><span class="keyword">if</span> (iResult == SOCKET_ERROR) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sendto() fail with error code: %d\n"</span>, WSAGetLastError());</span><br><span class="line">    <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="大-小端相关处理"><a href="#大-小端相关处理" class="headerlink" title="大/小端相关处理"></a>大/小端相关处理</h3><p>winsock中数据结构sockaddr和addrinfo结构体中都是网络序保存。</p>
<p>大小端转换常用的函数包括ntohs、htons、ntohl、htonl。</p>
<p>函数名中的 h 表示 HOST 主机 序，n 表示 NETWORK 网络序，s 表示 short(16bit)，l 表示 long(32bit)。</p>
<p>常用的函数inet_ntop 和 inet_pton 可以实现 IP 地址的</p>
<p>点分十进制和二进制格式之间的转换。通过第一个参数</p>
<p>AF_INET 或 AF_INET6 可以兼容 ipv4 和 ipv6。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">server_addr</span>;</span></span><br><span class="line"><span class="keyword">char</span> ipstringbuffer[<span class="number">46</span>];</span><br><span class="line"><span class="keyword">short</span> port;</span><br><span class="line"><span class="comment">// IP地址</span></span><br><span class="line">inet_ntop(AF_INET, &amp;server_addr.sin_addr, ipstringbuffer, <span class="keyword">sizeof</span>(ipstringbuffer));</span><br><span class="line"><span class="comment">// 端口号</span></span><br><span class="line">port = ntohs(server_addr.sin_port);</span><br></pre></td></tr></table></figure>
<h2 id="常见socket实体的实现"><a href="#常见socket实体的实现" class="headerlink" title="常见socket实体的实现"></a>常见socket实体的实现</h2><p>具体的函数模块的使用请参考上述代码。</p>
<h3 id="TCP-C-S结构"><a href="#TCP-C-S结构" class="headerlink" title="TCP C/S结构"></a>TCP C/S结构</h3><p>客户端：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">client</span><span class="params">()</span></span>&#123;</span><br><span class="line">    WSAStartup();</span><br><span class="line">    getaddrinfo(remote);</span><br><span class="line">    <span class="keyword">for</span> addrinfo in linkList:</span><br><span class="line">        socket();</span><br><span class="line">        connect();</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        send() <span class="function"><span class="keyword">or</span> <span class="title">recv</span><span class="params">()</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">    shutdown();</span><br><span class="line">    <span class="keyword">do</span>&#123;</span><br><span class="line">        recvlen = recv();</span><br><span class="line">    &#125;<span class="keyword">while</span>(recvlen&gt;<span class="number">0</span>);</span><br><span class="line">    closesocket();</span><br><span class="line">    WSACleanup();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>服务器：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">server</span><span class="params">()</span></span>&#123;</span><br><span class="line">    WSAStartup();</span><br><span class="line">    getaddrinfo(local);</span><br><span class="line">    listenSocket = socket();</span><br><span class="line">    bind();</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        listen(listenSocket);</span><br><span class="line">        tranSocket = accept();</span><br><span class="line">        createThread(tranSocket);</span><br><span class="line">    &#125;</span><br><span class="line">    closesocket(listenSocket);</span><br><span class="line">    WSACleanup();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">serverThread</span><span class="params">()</span></span>&#123;</span><br><span class="line">    send() <span class="function"><span class="keyword">or</span> <span class="title">recv</span><span class="params">()</span></span>;</span><br><span class="line">    shutdown();</span><br><span class="line">    <span class="keyword">do</span>&#123;</span><br><span class="line">        recvlen = recv();</span><br><span class="line">    &#125;<span class="keyword">while</span>(recvlen&gt;<span class="number">0</span>);</span><br><span class="line">    closesocket(tranSocket);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="UDP-通信"><a href="#UDP-通信" class="headerlink" title="UDP 通信"></a>UDP 通信</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">udpEntity</span><span class="params">()</span></span>&#123;</span><br><span class="line">    WSAStartup();</span><br><span class="line">    getaddrinfo(remote);</span><br><span class="line">    getaddrinfo(local);</span><br><span class="line">    socket();</span><br><span class="line">    bind();</span><br><span class="line">    createThread();</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        sendto();</span><br><span class="line">    &#125;</span><br><span class="line">    terminateThread();</span><br><span class="line">    closesocket();</span><br><span class="line">    WSACleanup();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">udpRecvThread</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        recvfrom();</span><br><span class="line">    &#125;</span><br><span class="line">    endthread();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="ping命令（ICMP协议）"><a href="#ping命令（ICMP协议）" class="headerlink" title="ping命令（ICMP协议）"></a>ping命令（ICMP协议）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">ping</span><span class="params">()</span></span>&#123;</span><br><span class="line">    WSAStartup();</span><br><span class="line">    getaddrinfo(remote);</span><br><span class="line">    getaddrinfo(local);</span><br><span class="line">    socket();</span><br><span class="line">    bind();</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        buildICMP();</span><br><span class="line">        sendto();</span><br><span class="line">        recvfrom();</span><br><span class="line">    &#125;</span><br><span class="line">    WSACleanup();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Socket</tag>
        <tag>Winsock</tag>
      </tags>
  </entry>
  <entry>
    <title>SSH学习记录</title>
    <url>/2020/05/08/ssh-usage/</url>
    <content><![CDATA[<p>简单记录SSH的用法</p>
<a id="more"></a>
<h2 id="SSH登陆"><a href="#SSH登陆" class="headerlink" title="SSH登陆"></a>SSH登陆</h2><ol>
<li><p>首次登录时发送服务器公钥的fingerprint，要求用户确认。</p>
<p> 用户确认后公钥保存到known_hosts，默认可信。</p>
</li>
<li><p>密钥登陆：ssh -p [port] -i [id_file] [user]@[ip_address]</p>
</li>
<li><p>公钥登陆：客户端ssh-keygen生成密钥对，ssh-copy-id传输公钥到服务器(自动写入authorized_keys)。</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ssh-keygen -t rsa</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成SSH公私钥对</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> -t 使用rsa算法 [dsa|ecdsa|ed25519|rsa]</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ssh-copy-id -i ~/.ssh/id_rsa.pub auth@192.168.1.10</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 将公钥传输到SSH服务器</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> -i 指定公钥文件</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> user@address 指定SSH服务器的IP地址和公钥对应的用户</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 需要使用口令方式通过认证</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>私钥登陆：服务器ssh-keygen生成密钥对，将公钥追加到服务器authorized_keys，通过scp或其他安全方式传输私钥到客户端。</p>
</li>
<li><p>ssh-copy-id的替代方式:</p>
<p> Linux机器：</p>
 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh &lt;user&gt;@&lt;ip_address&gt; <span class="string">"cat &gt;&gt; authorized_keys"</span> &lt; id.pub</span><br></pre></td></tr></table></figure>
<p> Windows机器：</p>
 <figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">&gt; <span class="built_in">Get-Content</span> new.pub | ssh &lt;user&gt;@&lt;ip_address&gt; <span class="string">"cat &gt;&gt; authorized_keys"</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>权限：通常公钥权限644，私钥权限600。authorized_keys权限600，known_hosts权限644。</p>
<p> 服务器端私钥权限为640、644之后，SSH服务器会自动忽略对应密钥对（认为该私钥是不安全的）。</p>
<p> authorized_keys权限改为620、622之后，SSH服务器会拒绝读取（认为公钥不安全），只能口令登陆。</p>
</li>
<li><p>文件：</p>
<p> authorized_keys在服务器端保存客户端的公钥。用于密钥登陆时加密随机挑战。防止冒用。</p>
<p> known_hosts在客户端保存服务器的公钥。用于公钥的比较确认，口令登录时用公钥加密口令。</p>
</li>
<li><p>scp：基于SSH的加密文件传输，使用单行命令传输文件。</p>
</li>
<li><p>sftp：基于SSH的加密文件传输，类似FTP的操作和使用方式。</p>
</li>
</ol>
<h2 id="SSH连接"><a href="#SSH连接" class="headerlink" title="SSH连接"></a>SSH连接</h2><h3 id="信息交换"><a href="#信息交换" class="headerlink" title="信息交换"></a>信息交换</h3><ol>
<li><p>TCP连接：三次握手建立TCP连接。发送SYN，接收SYN ACK，发送ACK。</p>
</li>
<li><p>版本协商：双方交换SSH版本。</p>
</li>
<li><p>算法协商：包括公钥算法，加密算法，MAC（消息验证码）算法，压缩算法列表</p>
</li>
<li><p>密钥协商：Elliptic Curve Diffie-Hellman Key Exchange</p>
<p> 客户端发送ECDH Key Exchange Init报文。包括客户端交换密钥Q_C。</p>
<p> 服务器发送ECDH Key Exchange Replay报文。包括ECDSA服务器公钥Q和ECDH交换密钥Q_S。</p>
<p> 接下来通过发送New Keys报文表示交换密钥已经建立。</p>
</li>
<li><p>登陆认证：（传输过程使用交换密钥对称加密）</p>
<p> 口令认证过程：服务器发送公钥。客户端使用服务器公钥加密口令。服务器使用私钥解密验证。</p>
<p> 密钥认证过程：服务器使用客户公钥加密发送随机字符串。客户端使用客户私钥加密。</p>
</li>
</ol>
<h3 id="防御中间人攻击"><a href="#防御中间人攻击" class="headerlink" title="防御中间人攻击"></a>防御中间人攻击</h3><p>中间人拦截客户端的连接请求并发送自己的公钥，从而假冒服务器窃取用户信息。</p>
<ol>
<li>客户端首次连接时保存服务器的公钥。中间人攻击时，中间人发送的服务器公钥与保存的公钥不同。</li>
<li>SSH引入了公钥认证机制，通过安全的方式发送公钥保存在服务器的authorized_keys中。</li>
</ol>
<h2 id="端口转发"><a href="#端口转发" class="headerlink" title="端口转发"></a>端口转发</h2><h3 id="socks代理"><a href="#socks代理" class="headerlink" title="socks代理"></a>socks代理</h3><p>要求机器A配置socks代理、机器B做代理服务器、访问web服务。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 机器A</span></span><br><span class="line">ssh -f -N -D 127.0.0.1:1080 &lt;user&gt;@&lt;addressB&gt;</span><br></pre></td></tr></table></figure>
<p>机器A浏览器设置代理为127.0.0.1:1080即可通过机器B访问网页。</p>
<h3 id="远程转发"><a href="#远程转发" class="headerlink" title="远程转发"></a>远程转发</h3><p>要求机器A访问本地端口、机器B做代理服务器、访问目标网站。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 机器A</span></span><br><span class="line">ssh -f -N -R 3456:bbs.fudan.edu.cn:23 &lt;user&gt;@&lt;addressB&gt;</span><br></pre></td></tr></table></figure>
<p>机器A访问localhost:3456会经由机器B访问复旦BBS。</p>
<h3 id="本地转发"><a href="#本地转发" class="headerlink" title="本地转发"></a>本地转发</h3><p>要求机器A访问本地端口、机器B开启web服务并做代理。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 机器A</span></span><br><span class="line">ssh -f -N -L 3456:lohalhost:80 &lt;user&gt;@&lt;addressB&gt;</span><br></pre></td></tr></table></figure>
<p>机器A访问localhost:3456实际访问到addressB:80。</p>
<h2 id="SSH设置"><a href="#SSH设置" class="headerlink" title="SSH设置"></a>SSH设置</h2><ol>
<li><p>设置规则之后重启服务</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">systemctl restart ssh</span><br><span class="line"><span class="meta">#</span><span class="bash"> 或者使用 service</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>禁止使用密码登录</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> /etc/ssh/ssgd_config</span></span><br><span class="line">PasswordAuthentication no</span><br></pre></td></tr></table></figure>
</li>
<li><p>拒绝用户登录</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> /etc/ssh/sshd_config</span></span><br><span class="line">DenyUsers user1</span><br></pre></td></tr></table></figure>
</li>
<li><p>设置用户白名单</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> /etc/ssh/sshd_config</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 按优先级从高到低</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 指定禁止某用户登录</span></span><br><span class="line">DenyUsers &lt;user&gt;</span><br><span class="line"><span class="meta">#</span><span class="bash"> 指定仅限某用户登录</span></span><br><span class="line">AllowUsers &lt;user&gt;</span><br><span class="line"><span class="meta">#</span><span class="bash"> 指定禁止某用户组登录</span></span><br><span class="line">DenyGroup &lt;group&gt;</span><br><span class="line"><span class="meta">#</span><span class="bash"> 指定仅限某用户组登录</span></span><br><span class="line">AllowGroup &lt;group&gt;</span><br></pre></td></tr></table></figure>
</li>
<li><p>设置IP白名单</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 按优先级从高到低</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> /etc/hosts.allow</span></span><br><span class="line">sshd:192.168.198.1:allow</span><br><span class="line"><span class="meta">#</span><span class="bash"> /etc/hosts.deny</span></span><br><span class="line">sshd:ALL</span><br></pre></td></tr></table></figure>
</li>
<li><p>禁止登陆root用户，如果使用弱口令的话可能被爆破</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> /etc/ssh/sshd_config</span></span><br><span class="line">PermitRootLogin no</span><br></pre></td></tr></table></figure>
</li>
<li><p>禁止无密码的登录</p>
 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> /etc/ssh/sshd_config</span></span><br><span class="line">PermitEmptyPasswords no</span><br></pre></td></tr></table></figure>
</li>
<li><p>更改端口</p>
 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># /etc/ssh/sshd_config</span></span><br><span class="line">Port 40121</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>SSH</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt开发OpenSSL程序踩坑</title>
    <url>/2020/04/21/log-dev-qt-ssl/</url>
    <content><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>分别由两个人开发的代码需要被整合进入一个完整的项目并运行。</p>
<a id="more"></a>
<p>其中第一位同学使用Visual Studio开发基于C++和openSSL的加解密的程序，使用vcpkg安装和编译openSSL:x86-windows并且集成到Visual Studio中。另一位同学使用Qt Creator开发基于C++和Qt的GUI程序。</p>
<h2 id="IDE选择"><a href="#IDE选择" class="headerlink" title="IDE选择"></a>IDE选择</h2><p>由于需要将两份代码合成一个完整的项目运行，我首先选择IDE为Visual Studio，但是由于并没有在VS中写过Qt程序，添加的QT VS TOOL插件总是无法正常编译。因此放弃使用Visual Studio作为项目IDE。选择Qt Creator作为开发环境。</p>
<h2 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h2><p>遇到的第一个问题是将openSSL集成到Qt Creator，从网上找到的资料都是从OpenSSL官网下载，并在Qt项目文件中添加库和头文件路径。如下代码所示</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LIBS += \</span><br><span class="line">    -Lpath/lib -lssl \</span><br><span class="line">    -Lpath/lib -lcrypto</span><br><span class="line">INCLUDEPATH += path/include</span><br></pre></td></tr></table></figure>
<p>我遇到的问题是，添加头文件正常，但是在编译的时候会出现<code>undefined reference to xxx</code>的报错，仔细一看是openssl库中的函数，在代码中<code>follow symbols</code>可以跳转到头文件中对应的定义，因此猜测是头文件正常但库文件没有正常加载。</p>
<p>这里涉及到的我尝试过的库文件包括</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">libssl.lib</span><br><span class="line">libcrypto.lib</span><br><span class="line">libssl.a</span><br><span class="line">libcrypto.a</span><br><span class="line">libssl.dll.a</span><br><span class="line">libcrypto.dll.a</span><br><span class="line">libssl-1_1.dll</span><br><span class="line">libcrypto-1_1.dll</span><br><span class="line">libeay32.dll</span><br><span class="line">ssleay32.dll</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>于是开始各种下载和添加库文件。包括但不限于</p>
<ol>
<li>使用vcpkg编译x64-windows版本的openssl库并添加（考虑到32位和64位可能产生的冲突）</li>
<li>官网下载openssl安装包（由于下载速度太慢，我选择了64位light版本的安装包，但是light版本安装包没有lib文件，完整版本的安装包就不曾下载成功过）</li>
<li>将lib和include文件copy到项目目录下，再使用上述方法添加路径。</li>
</ol>
<p>在这个时候，我查到一篇文章说qt本身是包括openssl的但是在编译的时候默认不安装，因此qt的环境下实际上是包括openssl对应的库文件和头文件的。经过一番查找我找到这个路径<code>C:\Qt\Qt5.12.0\Tools\mingw730_64\opt</code>。</p>
<p>这个路径的lib目录下包括了libssl.a、libcrypto.a、libssl.dll.a、libcrypto.dll.a，bin目录下包括libeay32.dll、ssleay32.dll，include目录下包括openssl头文件目录。因此我修改.pro文件如下。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 这里的$$quote我也没搞清啥意思，大概是双引号</span><br><span class="line"></span><br><span class="line">LIBS += \</span><br><span class="line">    -LC:\Qt\Qt5.12.0\Tools\mingw730_64\opt\lib -lssl \</span><br><span class="line">    -LC:\Qt\Qt5.12.0\Tools\mingw730_64\opt\lib -lcrypto</span><br><span class="line"></span><br><span class="line">INCLUDEPATH += \</span><br><span class="line">    $$quote(C:\Qt\Qt5.12.0\Tools\mingw730_64\opt\include)</span><br></pre></td></tr></table></figure>
<p>经过多次编译，并注释掉一些无用的函数，终于实现了<code>0 warnigns, 0 errors</code>，呜呼。</p>
<h2 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h2><p>接下来遇到的问题是，程序无法运行。在Qt Creator中直接运行，程序闪退并显示<code>程序异常结束</code>。在Qt Creator中开启Debugger，同样也是闪退，添加的断点直接被无视了。</p>
<p>刚开始考虑到可能是被杀毒软件杀掉了，因此经过一番操作关闭了Windows Defender，但是仍然无法运行，陷入停滞。</p>
<p>这个时候查到一篇文章，详细介绍了Qt Creator中的闪退的原因并给出了一个检查的方法，就是找到编译生成的exe文件，直接双击运行。程序自然无法运行并提示缺少许多dll文件，其中包括qt的依赖库例如QtCore.dll等等，因此使用windeployqt.exe(位于<code>C:\Qt\Qt5.12.0\5.12.0\mingw73_64\bin</code>)将Qt所有的依赖dll复制到exe对应的路径下，这个时候再次点击exe文件，显示<code>缺少libeay32.dll</code>于是将上述的libeay32.dll和ssleay32.dll（位于<code>C:\Qt\Qt5.12.0\Tools\mingw730_64\opt\bin</code>）复制到这个路径下。</p>
<p>双击exe，正常运行！！！经过测试，一切正常。每次使用qt编译后，将得到的exe文件拷贝到一个新的路径下，调用windeployqt和复制dll就可以正常运行。</p>
<h2 id="总结反思"><a href="#总结反思" class="headerlink" title="总结反思"></a>总结反思</h2><ol>
<li><p>我使用Qt官网下载的安装包安装Qt时，IDE选择了QtCreator，编译环境我选择了MinGW和MSVC，因此在我的安装目录下（<code>C:\Qt\Qt5.12.0\5.12.0</code>）包括了如下四个编译环境，每一个环境中都包含了windeployqt等一系列exe文件，和完整的Qt编译环境（bin + lib + include）。</p>
<blockquote>
<p>mingw73_64</p>
<p>msvc2015_64</p>
<p>msvc2017</p>
<p>msvc2017_64</p>
</blockquote>
<p> 但是非常疑惑的一点是，在编译时我检查Qt Cretor的编译输出窗口，发现我编译使用的是Tools目录下的mingw730_64。进一步检查发现Tools目录下（<code>C:\Qt\Qt5.12.0\Tools</code>）包括如下目录。</p>
<blockquote>
<p>mingw730_64</p>
<p>QtCreator</p>
</blockquote>
<p> 前者不是一个完整的Qt编译环境，不包括qt的各种dll，而是包括了ar、ld、gcc、make、objdump等一系列工具，更像是一个编译环境。其opt目录下包括了openssl的编译环境（前文使用到的）。</p>
<p> 那么这两个不同的mingw环境究竟有什么区别呢？后者可不可以换成我之前自己手动安装的MinGW环境呢？</p>
</li>
<li><p>使用vcpkg编译得到的环境和使用官方安装包以及从GitHub直接下载手动编译的环境有什么区别。</p>
</li>
<li><p>对于库文件来说存不存在32位和64位的差异，如果不存在的话，那么vcpkg编译时为什么提供了x86-windows和x64-windows的不同的triplets（还包括了arm、linux等其他选择）。</p>
</li>
<li><p>函数静态库和动态库在使用时一定要同时存在么，即编译时指定lib文件，执行时exe文件路径中要包含对应的dll文件。</p>
</li>
<li><p>vcpkg编译得到的libssl.lib和mingw730_64的opt路径下的libssl.a文件有什么差异，能不能简单通过改变后缀名来实现适配。同理还有.dll文件和.so文件之间的关系。</p>
</li>
</ol>
<p>最后的最后，还是感觉任重而道远。上述问题大多都是操作系统和C语言编译的基础知识，还是要努力打好基础。</p>
]]></content>
      <categories>
        <category>开发经历</category>
      </categories>
      <tags>
        <tag>OpenSSL</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>使用STL线程库模拟生产者消费者问题</title>
    <url>/2020/04/20/cpp-thread-sim/</url>
    <content><![CDATA[<p>在C++11的STL线程库没有实现现成的信号量，可以使用互斥量和条件变量实现信号量机制。</p>
<p>建议使用自己定义的namespace以防止与现成的函数/类产生冲突。</p>
<p>由于使用了lambda函数和C++11的STL线程库，因此编译时需要指定<strong>—std=c++11</strong>。</p>
<a id="more"></a>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @file: main.cpp</span></span><br><span class="line"><span class="comment"> * @brief: Simulate Productor/Consumer Problem</span></span><br><span class="line"><span class="comment"> * @author: YangLei</span></span><br><span class="line"><span class="comment"> * @date: 2020-04-20</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;list&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BUFFER_SIZE 5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> userdef &#123;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">semaphore</span> &#123;</span></span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//  construction function</span></span><br><span class="line">    explicit semaphore(int value = 1): count(value), wakeups(0) &#123;&#125;</span><br><span class="line">    <span class="comment">//  semWait（or operation P）</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">wait</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; lock(mutex);</span><br><span class="line">        <span class="keyword">if</span> (--count &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            condition.wait(lock, [&amp;]()-&gt;<span class="keyword">bool</span>&#123;<span class="keyword">return</span> wakeups &gt; <span class="number">0</span>;&#125;);</span><br><span class="line">            --wakeups;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//  semSignal（or operation V）</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">signal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; lock(mutex);</span><br><span class="line">        <span class="keyword">if</span> (++count &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            ++wakeups;</span><br><span class="line">            condition.notify_one();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">int</span> wakeups;</span><br><span class="line">    <span class="built_in">std</span>::mutex mutex;</span><br><span class="line">    <span class="built_in">std</span>::condition_variable condition;</span><br><span class="line">&#125;;  <span class="comment">//  class semaphore</span></span><br><span class="line">&#125;;  <span class="comment">//  namespace userdef</span></span><br><span class="line"></span><br><span class="line">userdef::<span class="function">semaphore <span class="title">product</span><span class="params">(BUFFER_SIZE)</span></span>;    <span class="comment">//  space for new product  </span></span><br><span class="line">userdef::<span class="function">semaphore <span class="title">consume</span><span class="params">(<span class="number">0</span>)</span></span>;              <span class="comment">//  products in buffer</span></span><br><span class="line">userdef::<span class="function">semaphore <span class="title">mutex</span><span class="params">(<span class="number">1</span>)</span></span>;                <span class="comment">//  mutex for buffer operation</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt; products;                    <span class="comment">//  products buffer</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> seed = (<span class="keyword">unsigned</span> <span class="keyword">int</span>)time(<span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief: Thread for Productors</span></span><br><span class="line"><span class="comment"> * @params: n(int) -- total umber of products</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">productor</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        product.wait();                     <span class="comment">//  wait for buffer space</span></span><br><span class="line">        <span class="keyword">int</span> size = rand_r(&amp;seed) % <span class="number">10</span>;</span><br><span class="line">        <span class="built_in">std</span>::this_thread::sleep_for(</span><br><span class="line">            <span class="built_in">std</span>::chrono::milliseconds(<span class="number">200</span> * (rand_r(&amp;seed) % <span class="number">10</span>)));</span><br><span class="line">        mutex.wait();                       <span class="comment">//  mutex lock</span></span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Product: "</span>  &lt;&lt; size &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        products.push_back(size);</span><br><span class="line">        mutex.signal();                     <span class="comment">//  mutex unlock</span></span><br><span class="line">        consume.signal();                   <span class="comment">//  signal to consumer</span></span><br><span class="line">    &#125;</span><br><span class="line">    quit.signal();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief: Thread for Consumers</span></span><br><span class="line"><span class="comment"> * @params: None</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">consumer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        consume.wait();                     <span class="comment">//  wait for products</span></span><br><span class="line">        mutex.wait();                       <span class="comment">//  mutex lock</span></span><br><span class="line">        <span class="keyword">int</span> size = products.front();</span><br><span class="line">        products.pop_front();</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Consume: "</span> &lt;&lt; size &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        mutex.signal();                     <span class="comment">//  mutex unlock</span></span><br><span class="line">        product.signal();                   <span class="comment">//  signal to productor</span></span><br><span class="line">        <span class="built_in">std</span>::this_thread::sleep_for(<span class="built_in">std</span>::chrono::milliseconds(<span class="number">200</span> * size));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief: main function for simulation</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> number = <span class="number">10</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">prod</span><span class="params">(productor, number)</span></span>;    <span class="comment">//  productor thread</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">cons</span><span class="params">(consumer)</span></span>;             <span class="comment">//  consumer thread</span></span><br><span class="line">    prod.join();</span><br><span class="line">    cons.join();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>QT程序打包部署</title>
    <url>/2020/04/16/qt-deploy/</url>
    <content><![CDATA[<p>使用qt编写的GUI程序部署和发布流程。</p>
<a id="more"></a>
<h2 id="Python-amp-PyQt程序部署"><a href="#Python-amp-PyQt程序部署" class="headerlink" title="Python&amp;PyQt程序部署"></a>Python&amp;PyQt程序部署</h2><p>使用pyinstaller工具打包部署使用pyqt编写的GUI程序。（适用于Windows和macOS）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用pip安装pyinstaller工具</span></span><br><span class="line">$ pip install pyinstaller</span><br><span class="line">$ pyinstaller -F -w --noconfirm --icon myicno.ico mainwindow.py</span><br><span class="line"><span class="comment"># 其中：</span></span><br><span class="line"><span class="comment"># -F            表示打包为一个单独的exe文件</span></span><br><span class="line"><span class="comment"># -w            表示exe文件运行时隐藏cmd窗口(黑框)</span></span><br><span class="line"><span class="comment"># --icon        表示指定图标</span></span><br><span class="line"><span class="comment"># --noconfirm   表示覆盖原有文件时不询问</span></span><br></pre></td></tr></table></figure>
<p>运行结束后，在当前目录下会生成两个文件夹build和dist。前者存放编译生成的一些中间文件，后者为distribution的简称，存放打包结束的文件。</p>
<p>使用-F生成的是单个exe文件，但是文件非常大。不添加-F选项得到的文件夹中包括程序主文件和大量依赖的dll文件。</p>
<p>不添加-w选项时，上述生成的文件运行时会出现一个cmd窗口，显示程序中的qDebug()信息。因此在最后的发布版本中建议加上-w选项，并注释掉qDebug信息。</p>
<p>当build和dist文件夹存在时，pyinstaller写入文件时会询问时候覆盖原有的文件，使用—noconfirm表示直接覆盖，不询问用户。</p>
<h2 id="C-amp-QT程序部署"><a href="#C-amp-QT程序部署" class="headerlink" title="C++&amp;QT程序部署"></a>C++&amp;QT程序部署</h2><p>使用QT自带的windeployqt.exe工具。通常位于QT的安装路径的/bin下。</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 将Qt Creator编译生成的release文件夹下的内容复制到一个新的文件夹</span></span><br><span class="line"><span class="comment"># 此时直接点击mainwindow.exe运行失败，因为缺少一些依赖的dll文件</span></span><br><span class="line">$ windeployqt.exe mainwindow.exe</span><br></pre></td></tr></table></figure>
<p>运行结束后，这个文件夹下包含程序主文件和依赖的dll文件。</p>
<h2 id="程序发布"><a href="#程序发布" class="headerlink" title="程序发布"></a>程序发布</h2><p>可以使用Inno Setup工具将上述部署得到的包含dll和exe的文件夹打包成一个安装包发布。</p>
<p>可以填写程序名称、版本号、网址、LICENSE文件、README文件、安装包图标，并选择程序主exe文件、选择程序依赖文件夹，Inno Setup工具就会将所有需要的文件打包为一个setup.exe安装包。</p>
<p>用户可以点击该安装包将文件安装到系统目录(例如C:\ProgramFiles)中并创建桌面和开始菜单图标。</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>C++并发编程:线程创建和运行</title>
    <url>/2020/04/16/cpp-thread-01/</url>
    <content><![CDATA[<p>使用C++11实现的标准线程库进行并发编程</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br></pre></td></tr></table></figure>
<a id="more"></a>
<h2 id="创建和结束线程"><a href="#创建和结束线程" class="headerlink" title="创建和结束线程"></a>创建和结束线程</h2><h3 id="创建线程"><a href="#创建线程" class="headerlink" title="创建线程"></a>创建线程</h3><p>使用thread类的构造函数创建线程</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// initFunc 是线程中要执行的函数名</span></span><br><span class="line"><span class="built_in">std</span>::<span class="function">thread <span class="title">t</span><span class="params">(initFunc)</span></span>;</span><br></pre></td></tr></table></figure>
<p>实际上，任何可执行的对象都可以被传入线程中</p>
<p>包括函数、可执行类/结构体等</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 可执行类的定义</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">callable_class</span>&#123;</span></span><br><span class="line">  <span class="keyword">int</span> &amp;i;</span><br><span class="line">  callable_class(<span class="keyword">int</span> &amp;i_):i(i_)&#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"The value i have is "</span> &lt;&lt; i &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="函数传参"><a href="#函数传参" class="headerlink" title="函数传参"></a>函数传参</h4><p>线程的iniFunc可以有多个参数</p>
<p>在构造函数中的函数指针后跟参数列表</p>
<p>注意，thread的构造函数默认按值传参，即<strong>复制</strong>到线程变量内部</p>
<p><strong><em>如果initFunc需要引用传参 则要使用 std::ref()</em></strong></p>
<p><strong><em>如果形参和实参类型不同 则要显式使用类型转换</em></strong></p>
<h3 id="结束线程"><a href="#结束线程" class="headerlink" title="结束线程"></a>结束线程</h3><p>选择线程分离或者结合</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="function">thread <span class="title">t</span><span class="params">(initFunc)</span></span>;</span><br><span class="line"><span class="comment">// 线程分离</span></span><br><span class="line"><span class="comment">// 显式决定不等待线程执行结束</span></span><br><span class="line">t.detach();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程结合</span></span><br><span class="line"><span class="comment">// 等待线程执行结束</span></span><br><span class="line">t.join();</span><br></pre></td></tr></table></figure>
<h4 id="线程分离"><a href="#线程分离" class="headerlink" title="线程分离"></a>线程分离</h4><p>分离后的线程无法通信无法直接控制</p>
<p>守护线程通常被分离，运行在后台</p>
<h4 id="线程结合"><a href="#线程结合" class="headerlink" title="线程结合"></a>线程结合</h4><p>通常需要保证所有的线程都被结合</p>
<p>一种方法是RAII（资源获取即初始化），使用一个保护类包裹</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">thread_guard</span>&#123;</span></span><br><span class="line">  <span class="built_in">std</span>::thread t;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="comment">// 只允许显式类型转换</span></span><br><span class="line">  explicit thread_guard(std::thread t_):t(std::move(t_))&#123;</span><br><span class="line">    <span class="keyword">if</span>(!t.joinable)</span><br><span class="line">      <span class="keyword">throw</span> <span class="built_in">std</span>::logit_error(<span class="string">"No thread"</span>);</span><br><span class="line">  &#125;  </span><br><span class="line">  <span class="comment">// 保证程序/函数结束时线程被合并</span></span><br><span class="line">  ~thread_guard()&#123;t.join();&#125;</span><br><span class="line">  <span class="comment">// 不生成默认构造函数和赋值函数</span></span><br><span class="line">  thread_guard(thread_guard <span class="keyword">const</span> &amp;) = <span class="keyword">delete</span>;</span><br><span class="line">  thread_guard&amp; <span class="keyword">operator</span>=(thread_guard <span class="keyword">const</span> &amp;) = <span class="keyword">delete</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTPS初步理解</title>
    <url>/2020/04/14/websec-https/</url>
    <content><![CDATA[<p>相关概念：SSL、TLS、证书。</p>
<a id="more"></a>
<p>SSL或TLS运行在传输层和应用层之间。（早期称为SSL，后期称为TLS）。用户网络加密通讯。</p>
<p>HTTPS可以看作HTTP+TLS，包括两个部分：握手协议+记录协议。</p>
<p>链接流程（握手协议）主要流程：</p>
<ol>
<li>客户端HELLO（发送支持的TLS版本）</li>
<li>服务器HELLO（选择TLS版本，发送服务器证书）</li>
<li>客户端检查证书，生成主密钥（随机数）并使用服务器证书加密</li>
<li>服务器解密后对主密钥加密并发回（类似Diffie Hellman交换）</li>
<li>客户端验证主密钥，使用主密钥得到交换密钥用于非对称加密</li>
<li>服务器验证客户端（可选）</li>
</ol>
<p>主要概念：使用服务器的公钥加密一个信息，服务器使用公钥解密后将该信息以客户端公钥加密后发回。用户客户端和服务器之间的验证。</p>
<p>记录协议中将应用层数据分段、压缩、添加下层首部（HTTP）、对称加密、添加TLS头部并发送。</p>
<p>证书包括公钥内容和私钥拥有者的信息。属于可信第三方（公钥基础设施PKI），用于证明公钥内容正确性。</p>
<p>通常由CA机构使用自己的私钥为服务器的证书请求添加数字签名。拿到证书以后，客户端通过使用CA的公钥解密得到证书内部的服务器公钥。CA使用层次签名，顶级CA的证书使用自签名，其公钥在全网公开。</p>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>HTTPS</tag>
      </tags>
  </entry>
  <entry>
    <title>openSSL使用记录</title>
    <url>/2020/04/14/websec-openssl/</url>
    <content><![CDATA[<p>openssl的功能主要包括对称加解密、计算哈希（信息摘要）和发布证书。</p>
<a id="more"></a>
<h3 id="加解密"><a href="#加解密" class="headerlink" title="加解密"></a>加解密</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl enc -e -aes-128-cbc -<span class="keyword">in</span> main.c -out main.enc</span><br><span class="line">openssl enc -d -aes-128-cbc -<span class="keyword">in</span> main.enc -out main_out.c</span><br></pre></td></tr></table></figure>
<h3 id="计算摘要"><a href="#计算摘要" class="headerlink" title="计算摘要"></a>计算摘要</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ openssl dgst -sha1 main.c</span><br><span class="line">SHA1(main.c)= cc9bf223848c972e66968fb3846fc7f85721796e</span><br><span class="line">$ <span class="built_in">echo</span> <span class="string">"\n"</span> &gt;&gt; main.c</span><br><span class="line">$ openssl dgst -sha1 main.c</span><br><span class="line">SHA1(main.c)= 52019cdc4aea16ee68f4d881465f6b8f03cf1836</span><br></pre></td></tr></table></figure>
<h3 id="自建CA并签发-amp-撤销证书"><a href="#自建CA并签发-amp-撤销证书" class="headerlink" title="自建CA并签发&amp;撤销证书"></a>自建CA并签发&amp;撤销证书</h3><h4 id="更改文件目录设置"><a href="#更改文件目录设置" class="headerlink" title="更改文件目录设置"></a>更改文件目录设置</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># CA目录</span></span><br><span class="line">dir=/home/yanglei/Workspace/ssl</span><br><span class="line"></span><br><span class="line"><span class="comment"># 证书目录</span></span><br><span class="line">certs=<span class="variable">$dir</span>/certs</span><br><span class="line"><span class="comment"># 证书备份目录</span></span><br><span class="line">new_certs_dir=<span class="variable">$dir</span>/newcerts</span><br><span class="line"><span class="comment"># CA证书</span></span><br><span class="line">certificate=<span class="variable">$dir</span>/cacert.pem</span><br><span class="line"></span><br><span class="line"><span class="comment"># CA数据库</span></span><br><span class="line">database=<span class="variable">$dir</span>/CA/index.txt</span><br><span class="line"><span class="comment"># CA序列号</span></span><br><span class="line">serial=<span class="variable">$dir</span>/CA/serial</span><br><span class="line"></span><br><span class="line"><span class="comment"># 吊销证书目录</span></span><br><span class="line">crl_dir=<span class="variable">$dir</span>/crl</span><br><span class="line"><span class="comment"># 吊销证书编号</span></span><br><span class="line">crlnumber=<span class="variable">$dir</span>/crlnumber</span><br><span class="line">crl=<span class="variable">$dir</span>/crl.pem</span><br><span class="line"></span><br><span class="line"><span class="comment"># CA私钥目录</span></span><br><span class="line">privatekey=<span class="variable">$dir</span>/private/cakey.pem</span><br></pre></td></tr></table></figure>
<p>创建文件夹certs、newcerts、CA、private、crl</p>
<p>创建文件crlnumber（注意写入echo 01 &gt; crlnumber）</p>
<p>创建文件CA/index.txt、CA/serial（注意写入echo 01 &gt; serial）</p>
<p>创建随机数private/.rand</p>
<h4 id="证书字段"><a href="#证书字段" class="headerlink" title="证书字段"></a>证书字段</h4><p>Country Name（C）国家</p>
<p>State or Province Name（S）省</p>
<p>Locatily Name（L）市</p>
<p>Organization Name（O）组织名</p>
<p>Organization Unit Name（OU）补充</p>
<p>Common Name（CN）通常为域名/网址</p>
<p>Email Address</p>
<h4 id="生成自签名证书"><a href="#生成自签名证书" class="headerlink" title="生成自签名证书"></a>生成自签名证书</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># /home/yanglei/Workspace/ssl</span></span><br><span class="line">$ openssl genrsa -out private/cakey.pem 1024</span><br><span class="line">$ openssl req -new -x509 -key private/cakey.pem -out cacert.pem -days 3650</span><br><span class="line"><span class="comment"># 或者先生成请求再签名</span></span><br><span class="line">$ openssl req -new -key xxx.pem -out xxx.csr</span><br><span class="line">$ openssl x509 -req -<span class="keyword">in</span> xxx.csr -signkey cakey.pem -out xxx.crt</span><br></pre></td></tr></table></figure>
<h4 id="签发证书"><a href="#签发证书" class="headerlink" title="签发证书"></a>签发证书</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># /home/yanglei/Workspace/testssl</span></span><br><span class="line">$ openssl genrsa -out user1.pem 1024</span><br><span class="line">$ openssl rsa -<span class="keyword">in</span> user1.pem -out user1.pub -pubout</span><br><span class="line">$ openssl req -new -key user1.pem -out user1.csr -days 365</span><br><span class="line"><span class="comment"># /home/yanglei/Workspace/ssl</span></span><br><span class="line">$ openssl ca -<span class="keyword">in</span> ../testssl/user1.csr -out certs/user1.crt -days 365</span><br><span class="line">$ openssl x509 -<span class="keyword">in</span> certs/user1.crt -noout -text</span><br></pre></td></tr></table></figure>
<h4 id="撤销证书"><a href="#撤销证书" class="headerlink" title="撤销证书"></a>撤销证书</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 吊销证书</span></span><br><span class="line">$ openssl ca -revoke newcerts/01.pem</span><br><span class="line"><span class="comment"># 发布吊销列表</span></span><br><span class="line">$ openssl ca -gencrl -out crl/ca.crl</span><br><span class="line"><span class="comment"># 查看吊销列表</span></span><br><span class="line">$ openssl crl -<span class="keyword">in</span> crl/ca.crl -noout -text</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>OpenSSL</tag>
      </tags>
  </entry>
  <entry>
    <title>Make进阶使用</title>
    <url>/2020/04/09/makefile-plus/</url>
    <content><![CDATA[<p>由于课程需要，进一步学习了一下make的使用，在之前的基础上添加一些高级知识，<br>同时提供一个makefile的模版共以后使用。</p>
<a id="more"></a>
<h2 id="Make的内部规则"><a href="#Make的内部规则" class="headerlink" title="Make的内部规则"></a>Make的内部规则</h2><div class="table-container">
<table>
<thead>
<tr>
<th>目标</th>
<th>依赖</th>
<th>规则</th>
</tr>
</thead>
<tbody>
<tr>
<td>x.o</td>
<td>x.c</td>
<td>cc -c x.c</td>
</tr>
<tr>
<td>x.o</td>
<td>x.s</td>
<td>as -o x.s</td>
</tr>
<tr>
<td>x.o</td>
<td>x.y</td>
<td>yacc x.y<br>cc -c y.tab.c<br>rm y.tab.c<br>mv y.tab.o x.o</td>
</tr>
<tr>
<td>x.o</td>
<td>x.l</td>
<td>lex x.l<br>cc -c lex.yy.c<br>rm -f lex.yy.c<br>mv les.yy.o x.o</td>
</tr>
<tr>
<td>x.c</td>
<td>x.y</td>
<td>yacc x.y<br>mv y.tab.c x.c</td>
</tr>
<tr>
<td>x.c</td>
<td>x.l</td>
<td>lex x.l<br>mv les.yy.c x.c</td>
</tr>
<tr>
<td>x.a</td>
<td>x.c</td>
<td>cc -c x.c<br>ar rv x.a x.c<br>rm -f x.o</td>
</tr>
</tbody>
</table>
</div>
<p>利用上述规则可以简写makefile。</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">main: main.o libylmath.a ylmathp.o</span></span><br><span class="line">    cc -o main -L . -l ylmath main.o ylmathp.o</span><br><span class="line"></span><br><span class="line"><span class="section">libylmath.a: libylmath.a(ylmath.o)</span></span><br><span class="line"></span><br><span class="line"><span class="section">ylmathp.o: ylmathp.h</span></span><br></pre></td></tr></table></figure>
<h2 id="Make的内部宏"><a href="#Make的内部宏" class="headerlink" title="Make的内部宏"></a>Make的内部宏</h2><div class="table-container">
<table>
<thead>
<tr>
<th>内部宏</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>$&lt;</td>
<td>使目标过时的依赖文件（即已更新的文件）</td>
</tr>
<tr>
<td>$*</td>
<td>不带后缀的依赖文件(常用写法$*.c)</td>
</tr>
<tr>
<td>$@</td>
<td>目标文件名，用于显式说明行</td>
</tr>
<tr>
<td>$?</td>
<td>类似$&lt; 用于显式说明行</td>
</tr>
<tr>
<td>$%</td>
<td>用于处理库文件的依赖文件</td>
</tr>
</tbody>
</table>
</div>
<p>这些宏大多用于修改上述隐含规则或创建自己的隐含规则。</p>
<h2 id="实用技巧"><a href="#实用技巧" class="headerlink" title="实用技巧"></a>实用技巧</h2><p>使用一个例子展示如下</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用宏定义增加可移植性</span></span><br><span class="line">INSTDIR= bin</span><br><span class="line">CFLAGS= -O -g</span><br><span class="line">LDFLAGS=</span><br><span class="line">CC= cc</span><br><span class="line"><span class="comment"># 文件声明</span></span><br><span class="line">HEADERS= ylmath.h ylmathp.h</span><br><span class="line">SOURCE= main.c ylmathp.c</span><br><span class="line">OBJECT= $(SOURCE: .c=.o)</span><br><span class="line"><span class="comment"># 库声明</span></span><br><span class="line">LIBSRC= ylmath.c</span><br><span class="line">LIBOBJ= ylmath.o</span><br><span class="line">LIBDIR= lib</span><br><span class="line"></span><br><span class="line"><span class="comment"># 覆盖默认的.c.o规则，添加-O(优化) -g(调试)</span></span><br><span class="line"><span class="section">.c.o:</span></span><br><span class="line">    <span class="variable">$(CC)</span> <span class="variable">$(CFLAGS)</span> -c <span class="variable">$*</span>.c</span><br><span class="line"></span><br><span class="line"><span class="comment"># 连接得到可执行文件</span></span><br><span class="line"><span class="section">demo: <span class="variable">$(OBJECT)</span> libylmath.a</span></span><br><span class="line">    <span class="variable">$(CC)</span> -o <span class="variable">$@</span> <span class="variable">$(OBJECT)</span> -L<span class="variable">$(LIBDIR)</span> -lylmath</span><br><span class="line"></span><br><span class="line"><span class="section">all: demo install</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 声明头文件，使用.c.o规则生成</span></span><br><span class="line"><span class="variable">$(OBJECT)</span>: <span class="variable">$(HEADERS)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用.c.a规则生成库并移到lib文件夹</span></span><br><span class="line"><span class="comment"># -开头的命令忽略运行错误和命令返回码，继续执行</span></span><br><span class="line"><span class="section">libylmath.a: libylmath.a(<span class="variable">$(LIBOBJ)</span>)</span></span><br><span class="line">    -mv <span class="variable">$(LIBDIR)</span>/libylmath.a libylmath.a.old</span><br><span class="line">    -mv libylmath.a <span class="variable">$(LIBDIR)</span>/libylmath.a</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新bin文件夹</span></span><br><span class="line"><span class="section">install: demo libylmath.a</span></span><br><span class="line">    -mv <span class="variable">$(INSTDIR)</span>/demo demo.old</span><br><span class="line">    -mv demo <span class="variable">$(INSTDIR)</span>/demo</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">    -rm *.old demo <span class="variable">$(LIBDIR)</span>/libylmath.a</span><br><span class="line"></span><br><span class="line"><span class="comment"># 记录更新信息</span></span><br><span class="line"><span class="comment"># @开头的命令在运行时不显示</span></span><br><span class="line"><span class="section">print: <span class="variable">$(SOURCE)</span> <span class="variable">$(OBJECT)</span></span></span><br><span class="line">    @ echo printing modified files</span><br><span class="line">    @ echo <span class="variable">$?</span> &gt; <span class="variable">$@</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
        <tag>Make</tag>
      </tags>
  </entry>
  <entry>
    <title>[HAL]STM32与ESP8266的交互</title>
    <url>/2020/04/08/stm32-esp8266-hal/</url>
    <content><![CDATA[<p>使用Clion+CubeMX开发，开发板为野火指南者，芯片为STM32F103ZET6.</p>
<p>简单实现使用串口经过STM32与ESP8266通信。</p>
<a id="more"></a>
<h2 id="Cube-MX设置"><a href="#Cube-MX设置" class="headerlink" title="Cube MX设置"></a>Cube MX设置</h2><h3 id="串口设置"><a href="#串口设置" class="headerlink" title="串口设置"></a>串口设置</h3><p>开发板使用USART1与PC通信，因此设置USART1，使用PA9和PA10端口通信，参数为115200-8-N-1。同时添加RX和TX的DMA设置,NVIC开启串口的global中断。</p>
<p>板载的ESP8266与STM芯片使用USART3通信,使用PB10和PB11端口,参数为115200-8-N-1.同理也需要添加RX和TX的DMA设置,NVIC开启串口global中断.</p>
<h3 id="控制引脚设置"><a href="#控制引脚设置" class="headerlink" title="控制引脚设置"></a>控制引脚设置</h3><p>ESP8266需要添加片选引脚和复位引脚.板载ESP8266使用PB8作为片选引脚(高电平为使能),PB9作为复位引脚(低电平复位).</p>
<h3 id="其他设置"><a href="#其他设置" class="headerlink" title="其他设置"></a>其他设置</h3><p>SYS中设置调试器模式.</p>
<p>RCC中设置HSE使用外部晶振,在Clock Configuration中设置时钟.</p>
<p>生成代码时选择根据外设生成对应的.C/.H文件.</p>
<h2 id="Clion代码"><a href="#Clion代码" class="headerlink" title="Clion代码"></a>Clion代码</h2><p>使用DMA接收数据,使用空闲中断处理.将PC发送的数据转发给ESP8266并将ESP8266的回复信息转发到PC.需要注意添加的代码位于指定的user code区间防止被覆盖.</p>
<h3 id="usart设置"><a href="#usart设置" class="headerlink" title="usart设置"></a>usart设置</h3><p>在usart.c中添加必要的变量和缓冲区.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// BUFFER_SIZE定义在main.h中,不大于200</span></span><br><span class="line"><span class="comment">/* USER CODE BEGIN 0 */</span></span><br><span class="line"><span class="keyword">uint8_t</span> usart1_rx_buffer[BUFFER_SIZE];</span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart1_rx_len = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart1_recv_end_flag = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">uint8_t</span> usart3_rx_buffer[BUFFER_SIZE];</span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart3_rx_len = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart3_recv_end_flag = <span class="number">0</span>;</span><br><span class="line"><span class="comment">/* USER CODE END 0 */</span></span><br></pre></td></tr></table></figure>
<p>在usart.h中添加变量声明</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN Private defines */</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">uint8_t</span> usart1_rx_buffer[BUFFER_SIZE];</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart1_rx_len;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart1_recv_end_flag;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">uint8_t</span> usart3_rx_buffer[BUFFER_SIZE];</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart3_rx_len;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">volatile</span> <span class="keyword">uint8_t</span> usart3_recv_end_flag;</span><br><span class="line"><span class="comment">/* USER CODE END Private defines */</span></span><br></pre></td></tr></table></figure>
<h3 id="GPIO设置"><a href="#GPIO设置" class="headerlink" title="GPIO设置"></a>GPIO设置</h3><p>在main.c的while(1)循环之前设置ESP8266使能和复位</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN 2 */</span></span><br><span class="line"><span class="comment">// 设置ESP8266选择</span></span><br><span class="line">HAL_GPIO_WritePin(CH_GPIO_Port, CH_Pin, GPIO_PIN_SET);</span><br><span class="line">HAL_GPIO_WritePin(RST_GPIO_Port, RST_Pin, GPIO_PIN_RESET);</span><br><span class="line">HAL_Delay(<span class="number">500</span>);</span><br><span class="line">HAL_GPIO_WritePin(RST_GPIO_Port, RST_Pin, GPIO_PIN_SET);</span><br><span class="line"><span class="comment">/* USER CODE END 2 */</span></span><br></pre></td></tr></table></figure>
<h3 id="中断响应"><a href="#中断响应" class="headerlink" title="中断响应"></a>中断响应</h3><p>在main.c的while(1)循环之前开启接收DMA和空闲中断</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN 2 */</span></span><br><span class="line"><span class="comment">// 开启USART的接收DMA传输中断</span></span><br><span class="line">HAL_UART_Receive_DMA(&amp;huart1, usart1_rx_buffer, BUFFER_SIZE);</span><br><span class="line">__HAL_UART_ENABLE_IT(&amp;huart1, UART_IT_IDLE);</span><br><span class="line">HAL_UART_Receive_DMA(&amp;huart3, usart3_rx_buffer, BUFFER_SIZE);</span><br><span class="line">__HAL_UART_ENABLE_IT(&amp;huart3, UART_IT_IDLE);</span><br><span class="line"><span class="comment">/* USER CODE END 2 */</span></span><br></pre></td></tr></table></figure>
<p>在stm32f1xx_it.c中修改中断响应,分别修改USART1_IRQHandler和USART3_IRQHandler.当接收DMA接收完成时,依次触发DMA接收中断和USART全局中断.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN USART1_IRQn 0 */</span></span><br><span class="line"><span class="comment">// 局部变量</span></span><br><span class="line"><span class="keyword">uint32_t</span> tmp_flag = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">uint32_t</span> temp;</span><br><span class="line"><span class="comment">// 空闲状态</span></span><br><span class="line">tmp_flag = __HAL_UART_GET_FLAG(&amp;huart1, UART_FLAG_IDLE);</span><br><span class="line"><span class="keyword">if</span>((tmp_flag != RESET))</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 关闭空闲状态</span></span><br><span class="line">    __HAL_UART_CLEAR_IDLEFLAG(&amp;huart1);</span><br><span class="line">    <span class="comment">// 清空寄存器</span></span><br><span class="line">    temp = huart1.Instance-&gt;SR;</span><br><span class="line">    temp = huart1.Instance-&gt;DR;</span><br><span class="line">    HAL_UART_DMAStop(&amp;huart1);</span><br><span class="line">    <span class="comment">// 获取未传输比特数 用于计算已传输比特数</span></span><br><span class="line">    temp = hdma_usart1_rx.Instance-&gt;CNDTR;</span><br><span class="line">    usart1_rx_len = BUFFER_SIZE - temp;</span><br><span class="line">    <span class="comment">// 开始标志位</span></span><br><span class="line">    usart1_recv_end_flag = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* USER CODE END USART1_IRQn 0 */</span></span><br></pre></td></tr></table></figure>
<p>在main.c的循环部分添加中断处理部分</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PC传输中断</span></span><br><span class="line"><span class="keyword">if</span>(usart1_recv_end_flag == <span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 发送到ESP8266</span></span><br><span class="line">    HAL_UART_Transmit(&amp;huart3, usart1_rx_buffer, usart1_rx_len, <span class="number">0xFF</span>);</span><br><span class="line">    <span class="comment">// 清空缓冲和标志位</span></span><br><span class="line">    <span class="built_in">memset</span>(usart3_rx_buffer, <span class="number">0</span>, BUFFER_SIZE);</span><br><span class="line">    usart1_rx_len = <span class="number">0</span>;</span><br><span class="line">    usart1_recv_end_flag = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 开启接收中断</span></span><br><span class="line">    HAL_UART_Receive_DMA(&amp;huart1, usart1_rx_buffer, BUFFER_SIZE);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// ESP8266传输中断</span></span><br><span class="line"><span class="keyword">if</span>(usart3_recv_end_flag == <span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 发送到PC显示</span></span><br><span class="line">    HAL_UART_Transmit(&amp;huart1, usart3_rx_buffer, usart3_rx_len, <span class="number">0xFF</span>);</span><br><span class="line">    <span class="comment">// 清空缓存和标志位</span></span><br><span class="line">    <span class="built_in">memset</span>(usart3_rx_buffer, <span class="number">0</span>, BUFFER_SIZE);</span><br><span class="line">    usart3_rx_len = <span class="number">0</span>;</span><br><span class="line">    usart3_recv_end_flag = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 开启接收中断</span></span><br><span class="line">    HAL_UART_Receive_DMA(&amp;huart3, usart3_rx_buffer, BUFFER_SIZE);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* USER CODE END WHILE */</span></span><br></pre></td></tr></table></figure>
<p>从而实现STM32转发PC和ESP8266的串口通信,接下来就可以通过ESP8266的AT指令进行设置和操作.同时在PC的串口调试工具上检测到回复的信息.</p>
<h2 id="我遇到的坑"><a href="#我遇到的坑" class="headerlink" title="我遇到的坑"></a>我遇到的坑</h2><ol>
<li>ESP8266的使能引脚和复位引脚配置错误,导致ESP8266的通信出现一些问题.</li>
<li>代码中的发送指令使用HAL_UART_Transmit_DMA,导致发送/接收到的指令不全,因此改用HAL_UART_Transmit函数.</li>
<li>网上找到的代码和野火提供的代码大多都是基于STM32标准库和Keil开发环境,使用HAL库和CubeMX开发的代码不多,而且最开始学习STM32也是基于STM32标准库,因此会有一些不适应.</li>
</ol>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>[HAL]STM32定时器和按键中断的使用</title>
    <url>/2020/04/08/stm32-interrupt-hal/</url>
    <content><![CDATA[<p>使用Clion+CubeMX开发，开发板为野火指南者，芯片为STM32F103ZET6.</p>
<p>简单实现使用定时器和按键中断点亮LED。</p>
<a id="more"></a>
<h2 id="Cube-MX设置"><a href="#Cube-MX设置" class="headerlink" title="Cube MX设置"></a>Cube MX设置</h2><h3 id="引脚设置"><a href="#引脚设置" class="headerlink" title="引脚设置"></a>引脚设置</h3><p>板载RGB-LED使用的引脚PB0，PB1，PB5设置为output push pull（推挽输出）。</p>
<p>板载微动开关使用的引脚PA0，PC13设置为外部中断且下降沿触发。同时NVIC设置EXTI0和EXTI15-10的使能。</p>
<h3 id="时钟设置"><a href="#时钟设置" class="headerlink" title="时钟设置"></a>时钟设置</h3><p>选择TIM1，时钟源选择内部时钟（Internal Clock）。设置参数为预分频36000，计数为1000，此时频率为72MHz/36000/1000=2Hz。</p>
<p>NVIC开启TIM1 update interrupt中断。</p>
<h3 id="其他设置"><a href="#其他设置" class="headerlink" title="其他设置"></a>其他设置</h3><p>SYS中设置调试器模式.</p>
<p>RCC中设置HSE使用外部晶振,在Clock Configuration中设置时钟.</p>
<p>生成代码时选择根据外设生成对应的.C/.H文件.</p>
<h2 id="Clion代码"><a href="#Clion代码" class="headerlink" title="Clion代码"></a>Clion代码</h2><p>在CubeMX设置时为GPIO指定user label。在生成的代码中就会自动定义对应的GPIO端口和引脚。</p>
<h3 id="LED宏定义"><a href="#LED宏定义" class="headerlink" title="LED宏定义"></a>LED宏定义</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN Private defines */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ON  GPIO_PIN_RESET</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> OFF GPIO_PIN_SET</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED1(a) HAL_GPIO_WritePin(LED1_GPIO_Port, LED1_Pin, a)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED2(a) HAL_GPIO_WritePin(LED2_GPIO_Port, LED2_Pin, a)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED3(a) HAL_GPIO_WritePin(LED3_GPIO_Port, LED3_Pin, a)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED1_ON         LED1(ON)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED1_OFF        LED1(OFF)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED1_TOGGLE     HAL_GPIO_TogglePin(LED1_GPIO_Port, LED1_Pin)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED2_ON         LED2(ON)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED2_OFF        LED2(OFF)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED2_TOGGLE     HAL_GPIO_TogglePin(LED2_GPIO_Port, LED2_Pin)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED3_ON         LED3(ON)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED3_OFF        LED3(OFF)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED3_TOGGLE     HAL_GPIO_TogglePin(LED3_GPIO_Port, LED3_Pin)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_RED &#123;LED1_ON;LED2_OFF;LED3_OFF;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_GREEN &#123;LED1_OFF;LED2_ON;LED3_OFF;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_BLUE &#123;LED1_OFF;LED2_OFF;LED3_ON;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_YELLOW &#123;LED1_ON;LED2_ON;LED3_OFF;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_PURPLE &#123;LED1_ON;LED2_OFF;LED3_ON;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_CYAN &#123;LED1_OFF;LED2_ON;LED3_ON;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_WHITE &#123;LED1_ON;LED2_ON;LED3_ON;&#125;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LED_BLACK &#123;LED1_OFF;LED2_OFF;LED3_OFF;&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* USER CODE END Private defines */</span></span><br></pre></td></tr></table></figure>
<h3 id="按键中断处理"><a href="#按键中断处理" class="headerlink" title="按键中断处理"></a>按键中断处理</h3><p>在stm32f1xx_it.c文件中发现EXTI中断调用HAL_GPIO_EXTI_IRQHandler(void)函数，网上找到的资料说该函数调用弱定义的HAL_GPIO_EXTI_Callback()函数。（虽然我没找到在哪）</p>
<p>因此在main.c函数中重复定义该函数用于中断处理.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN 4 */</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * @brief EXTI Handler Callback: KEY Response</span></span><br><span class="line"><span class="comment">  * @retval None</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HAL_GPIO_EXTI_Callback</span><span class="params">(<span class="keyword">uint16_t</span> GPIO_Pin)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// select target GPIO</span></span><br><span class="line">    <span class="keyword">switch</span> (GPIO_Pin)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">case</span> KEY1_Pin:LED1_TOGGLE;<span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> KEY2_Pin:LED2_TOGGLE;<span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* USER CODE END 4 */</span></span><br></pre></td></tr></table></figure>
<h3 id="时钟中断处理"><a href="#时钟中断处理" class="headerlink" title="时钟中断处理"></a>时钟中断处理</h3><p>在stm32f1xx_it.c文件中发现TIM1中断调用HAL_TIM_IRQHandler(void)函数，网上找到的资料说该函数调用弱定义的HAL_TIM_PeriodElapsedCallback()函数。</p>
<p>因此在main.c函数中重复定义该函数用于中断处理.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* USER CODE BEGIN 4 */</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * @brief TIM Handler Callback: LED Blink</span></span><br><span class="line"><span class="comment">  * @retval None</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HAL_TIM_PeriodElapsedCallback</span><span class="params">(TIM_HandleTypeDef *htim)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// TIM1: LED3 blink</span></span><br><span class="line">    <span class="keyword">if</span> (htim-&gt;Instance == htim1.Instance)</span><br><span class="line">    &#123;</span><br><span class="line">        LED3_TOGGLE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* USER CODE END 4 */</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>STM32的中断操作</title>
    <url>/2020/04/04/stm32-int/</url>
    <content><![CDATA[<p>中断的概念是嵌入式开发和计算机系统中非常重要的部分。</p>
<p>可以参考学堂在线的《ARM微控制器与嵌入式系统》的相关章节。</p>
<a id="more"></a>
<p>两个概念：</p>
<ul>
<li>NVIC（嵌套向量中断控制器）：用于设置中断优先级和中断使能。位于芯片内部。</li>
<li>EXTI（外部中断/时间控制器）：用于设置外部中断和事件。位于APB2总线上。</li>
</ul>
<p>注意：异常和中断清单在stm32f10x.h中的IRQn_Type中。</p>
<p>NVIC相关的处理位于misc.h中，EXTI相关的处理位于stm32f10x_exti.h中。</p>
<p>全流程：1. 初始化GPIO 2. 初始化EXTI 3. 配置NVIC使能中断 4. 编写中断服务函数</p>
<h2 id="NVIC的设置"><a href="#NVIC的设置" class="headerlink" title="NVIC的设置"></a>NVIC的设置</h2><p>STM32使用4bit表示中断优先级。通常首先设置优先级分组，共有五种优先级分组，分别使用0-4bit表示主优先级，剩下的表示子优先级。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">NVIC_PriorityGroupConfig(NVIC_PriorityGroup_1);</span><br></pre></td></tr></table></figure>
<p>具体的设置方式使用对应的初始化结构体</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">NVIC_InitTypeDef NVIC_InitStructure;</span><br><span class="line"></span><br><span class="line">NVIC_InitStructure.NVIC_IRQChannel = EXTI0_IRQn;<span class="comment">// 中断编号 EXTI0_IRQn</span></span><br><span class="line">NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = <span class="number">1</span>;</span><br><span class="line">NVIC_InitStructure.NVIC_IRQChannelSubPriority = <span class="number">1</span>;</span><br><span class="line">NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;<span class="comment">// 中断使能</span></span><br><span class="line"></span><br><span class="line">NVIC_Init(&amp;NVIC_InitStructure);</span><br></pre></td></tr></table></figure>
<p>同时还需要设置中断服务函数，其函数声明写在stm32f10x_it.h中,函数定义写在stm32f10x_it.c中。中断服务函数名在汇编启动文件中定义，与中断名相对应（例如EXTI0_IRQn和EXTI0_IRQHandler）。</p>
<p>（考虑到相关的宏定义的有效性，也可以写在对应模块的C文件中）</p>
<h2 id="EXTI的设置"><a href="#EXTI的设置" class="headerlink" title="EXTI的设置"></a>EXTI的设置</h2><p>EXTI包括了20个外部中断/事件线。包括中断屏蔽寄存器、边沿选择寄存器、请求挂起寄存器、软件中断事件寄存器。可以从外设事件或者GPIO输入产生中断和事件。其中的中断线路信号传入NVIC内，事件线路信号得到一个脉冲（用于其他的外设使用）。</p>
<p>GPIO线可以挂载到EXTI的0-15上（注意编号一一对应，例如GPIOA-3挂在EXTI3上），剩下的四根线用于特定的任务。注意这里的EXTI线的具体定义分别为EXTI0、EXTI1、EXTI2、EXTI3、EXTI4、EXTI9_5、 EXTI15_10。位于“stm32f10x_exti.h”中。</p>
<p>具体的设置方式使用初始化结构体:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先设置GPIO 注意打开AFIO时钟（用于EXTI设置）</span></span><br><span class="line">GPIO_InitTypeDef GPIO_InitStructure;</span><br><span class="line"></span><br><span class="line">RCC_APB2PeriphClockCmd((RCC_APB2Periph_GPIOA|RCC_APB2Periph_AFIO),ENABLE);</span><br><span class="line">GPIO_InitStructure.GPIO_Pin = GPIO_PIN_0</span><br><span class="line">GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IN_FLOATING;</span><br><span class="line"></span><br><span class="line">GPIO_Init(GPIOA, &amp;GPIO_InitStructure);</span><br><span class="line"><span class="comment">// 将GPIO挂载到EXTI上</span></span><br><span class="line">GPIO_EXTILineConfig(GPIO_PortSourceGPIOA,\</span><br><span class="line">                    GPIO_PinSource0);</span><br><span class="line"><span class="comment">// 再设置EXTI线</span></span><br><span class="line">EXTI_InitTypeDef EXTI_InitStructure;</span><br><span class="line"></span><br><span class="line">EXTI_InitStructure.EXTI_Line = EXTI_LINE0;<span class="comment">// 与GPIOpin对应</span></span><br><span class="line">EXTI_InitStructure.EXTI_Mode = EXTI_Mode_Interrupt;<span class="comment">// Interrupt或者Event</span></span><br><span class="line">EXTI_InitStructure.EXTI_Trigger = EXTI_Trigger_Rising;<span class="comment">// Rising或Falling或RIsing_Falling</span></span><br><span class="line">EXTI_InitStructure.EXTI_LineCmd = ENABLE;</span><br><span class="line"></span><br><span class="line">EXTI_Init(&amp;EXTI_InitStructure);</span><br></pre></td></tr></table></figure>
<h2 id="中断服务函数"><a href="#中断服务函数" class="headerlink" title="中断服务函数"></a>中断服务函数</h2><p>中断号和终端服务函数在NVIC中设置，分别参见stm32f10x.h和汇编启动文件的定义。</p>
<p>中断服务函数需要在stm32f10x_it.h中定义，在stm32f10x_it.c中实现，或者在对应的模块C文件中实现（考虑到宏定义的有效性）。</p>
<p>中断服务函数中通过EXTI_GetITStatus()获得中断标志位为SET/RESET。处理结束后通过EXTI_ClearITPendingBit()清除中断标志位。</p>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>STM32的时钟处理</title>
    <url>/2020/04/04/stm32-clock/</url>
    <content><![CDATA[<p>STM32的时钟主要包括四个部分：系统时钟SYSCLK，AHB总线时钟HCLK，APB2总线时钟PCLK2，APB1总线时钟PCLK1。主要的时钟处理为设置各个时钟的频率和不同总线时钟和外设时钟的开启。</p>
<a id="more"></a>
<p>通常的设置为PCLK2=HCLK=SYSCLK=PLLCLK=72MHz，PCLK1=HCLK/2=36MHz。</p>
<p>库中自带了SetSysClockTo72()通过寄存器操作实现上述设置,也可以通过RCC库的函数进行具体的设置.</p>
<h2 id="系统时钟设置"><a href="#系统时钟设置" class="headerlink" title="系统时钟设置"></a>系统时钟设置</h2><p>时钟信号有三个来源：高速外部时钟HSE、高速内部时钟HSI(实际使用一半的频率HSI/2)、锁相环倍频输出时钟PLL。其中的PLL时钟的来源可以是HSE或HSI，但是HSI存在漂移，通常不使用。</p>
<p>通常情况下HSE设置为8MHz, 因此PLL的倍频因子设置为9,从而得到72MHz的系统时钟.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"># 设置HSE或HSI的开启</span><br><span class="line">RCC_HSEConfig(RCC_HSE_ON);</span><br><span class="line">RCC_HSICmd(ENABLE);</span><br><span class="line"># 设置PLL的输入为HSE或HSI/<span class="number">2</span> pllmul为倍频因子</span><br><span class="line">RCC_PLLConfig(RCC_PLLSource_HSE_Div1, pllmul);</span><br><span class="line">RCC_PLLConfig(RCC_PLLSource_HSI_Div2, pllmul);</span><br><span class="line">RCC_PLLCmd(ENABLE);</span><br><span class="line"># 设置系统时钟为PLL的输出</span><br><span class="line">RCC_SYSCLKConfig(RCC_SYSCLKSource_PLLCLK);</span><br></pre></td></tr></table></figure>
<h2 id="总线时钟设置"><a href="#总线时钟设置" class="headerlink" title="总线时钟设置"></a>总线时钟设置</h2><p>总线时钟通常是在系统时钟的基础上做分频.</p>
<p>AHB总线时钟为HCLK,是系统时钟在AHB分频器按１分频得到,因此为72MHz.</p>
<p>APB2总线时钟为PCLK2 ,是AHB总线时钟在APB2分频器按1分频得到,因此为72MHz.</p>
<p>APB1总线时钟为PCLK1,是AHB总线时钟在APB1分频器二分频得到,因此为36MHz.</p>
<h3 id="外设时钟设置"><a href="#外设时钟设置" class="headerlink" title="外设时钟设置"></a>外设时钟设置</h3><p>具体使用外设时的时钟频率要根据需要具体设置.</p>
<ul>
<li>USB时钟:通常令PLL时钟为72MHz,USB时钟为48MHz,通过USB分频器的１.５倍分频实现．</li>
<li>Cortex时钟:通常为9MHz,通过HCLK经过8分频得到.用于驱动内核定时器SysTick.</li>
<li>ADC时钟：最高为14MHz,具体根据使用情况选择.通过PCLK2分频得到.</li>
<li>RTC时钟:可以通过HSE/128或LSE或HSI提供.</li>
<li>独立看门狗时钟:通过LSI提供.</li>
<li>MCO信号输出:对外提供时钟信号,通过复用GPIO实现.时钟来源可以是PLLCLK/2,HSI,HSE,SYSCLK</li>
<li>GPIO信号输出:使用RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOx, ENABLE);开启</li>
</ul>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>Clion开发STM32环境搭建</title>
    <url>/2020/04/04/stm32-clion/</url>
    <content><![CDATA[<p>Windows下使用CLion和CubeMX构建STM32开发环境。</p>
<p>使用野火指南者开发版，芯片为STM32F103VET6。使用配套CMSIS-DAP调试器。</p>
<a id="more"></a>
<ul>
<li><p>下载安装</p>
<ul>
<li>下载<a href="https://www.st.com/content/st_com/en/products/development-tools/software-development-tools/stm32-software-development-tools/stm32-configurators-and-code-generators/stm32cubemx.html" target="_blank" rel="noopener">STM32CubeMx</a></li>
<li>下载Clion</li>
<li>下载交叉编译工具链<a href="https://developer.arm.com/tools-and-software/open-source-software/developer-tools/gnu-toolchain/gnu-rm/downloads" target="_blank" rel="noopener">GNU Tools Arm Embedded</a>，注意添加系统路径并在cmd测试“arm-none-eabi-gcc -v”</li>
<li>下载<a href="https://sourceforge.net/projects/mingw/files/" target="_blank" rel="noopener">MinGW</a>，注意至少安装gcc，g++，cmake</li>
<li>下载<a href="https://gnutoolchains.com/arm-eabi/openocd/" target="_blank" rel="noopener">OpenOCD</a></li>
</ul>
</li>
<li><p>设置Clion</p>
<ul>
<li>File——Settings——Build，Excution，Deplotment——Toolchains，设置好mingw</li>
<li>File——Settings——Plugin，搜索安装插件openocd</li>
<li>File——Settings——Build，Excution，Deplotment——Embedded Deployment，设置路径</li>
<li>File——Settings——Build，Excution，Deplotment——OpenOCD support，设置路径</li>
</ul>
</li>
<li><p>创建CubeMX工程</p>
<ul>
<li>选择芯片</li>
<li>选择功能及其对应的引脚</li>
<li>选择sys功能，选择使用串口下载或者使用调试器</li>
<li>时钟配置，使用HSE，9倍PLL，APB1选择2分频</li>
<li>选择保存路径，选择工具为SW4STM32</li>
<li>生成配置代码</li>
</ul>
</li>
<li><p>Clion导入工程</p>
<ul>
<li>选择Cube MX生成的代码，默认选项导入</li>
<li>Tool——Update CMake to STM32 Projects（可以设为自动导入）</li>
<li>Build——编译Project或者编译“OCD+你创建的工程名“</li>
<li>Run——Edit Configuration——OpenOCD——Board config file——选择一个stm32f10系列的板子（我选择stm32f103c8_blue_pill，之后修改）——点击Cope to Project&amp;Use</li>
<li><p>修改cfg文件，添加野火的调试器。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 选择对应调试器</span><br><span class="line">source [find interface/cmsis-dap.cfg]</span><br><span class="line"># 选择模式，这里的swd对应Cube MX设置”5线JTAG“ 对应野火调试器直接插指南者的”SWD“</span><br><span class="line">transport select swd</span><br><span class="line"># 选择片上FLASH大小</span><br><span class="line">set FLASH_SiZE 0x80000</span><br><span class="line"># 选择芯片</span><br><span class="line">source [find target/stm32f1x.cfg]</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>运行</p>
<ul>
<li>在main.c的对应位置添加代码，注意需要添加在指定的user code部分，否则更新后可能被覆盖</li>
<li>Run——点击Run ”OCD+你创建的工程名“</li>
</ul>
</li>
</ul>
<p>参考链接：<a href="https://blog.csdn.net/PoJiaA123/article/details/81628952?depth_1-utm_source=distribute.pc_relevant_right.none-task-blog-OPENSEARCH-2&amp;utm_source=distribute.pc_relevant_right.none-task-blog-OPENSEARCH-2" target="_blank" rel="noopener">紫色能量的博客</a></p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】ABS: Scanning Neural Networks for Back-doors by Artificial Brain Stimulation</title>
    <url>/2020/03/30/abs-nn-backdoor/</url>
    <content><![CDATA[<p>论文题目：ABS: Scanning Neural Networks for Back-doors by Artificial Brain Stimulation</p>
<p>会议：CCS2019</p>
<p>作者：Y. Liu .etc from Purdue University</p>
<p>链接：<a href="https://doi.org/10.1145/3319535.3363216" target="_blank" rel="noopener">原文链接</a></p>
<a id="more"></a>
<h2 id="论文目标-现存问题"><a href="#论文目标-现存问题" class="headerlink" title="论文目标/现存问题"></a>论文目标/现存问题</h2><p>预训练的人工智能模型可能存在一些通过训练或更改模型参数而插入的后门（AI木马）。这些模型在处理普通的输入时会得到正确的结果，但是在遇到特殊的输入数据时会出现错误的分类结果，这里的特殊输入数据通常含有称为trojan trigger的特殊模式/特征。</p>
<p>因此文章实现了一种技术，通过为神经元引入不同级别的激励来检测其输出的变化而逆向构建出trojan trigger，从而证明AI模型有没有收到AI木马攻击。</p>
<h2 id="相关工作-现状"><a href="#相关工作-现状" class="headerlink" title="相关工作/现状"></a>相关工作/现状</h2><p>由于训练模型在搜集数据和计算能力方面的要求，越来越多的预训练模型开始在网络上传播，这些模型往往经由大的服务商训练，但是也存在一些经过个人训练或者再训练(retrain)的模型。就像传统软件的木马或者后门一样，预训练的模型也可能被AI木马攻击或者插入后门。</p>
<p>Gu et al.等<sup><a href="#fn_1" id="reffn_1">1</a></sup>在2017年的研究表明通过污染训练数据可以得到藏有后门的模型，即“Data Poisoning”。此时需要攻击者可以参与训练的过程，接触到数据集并对数据进行相关操作，包括添加碎片（patch）或者扰动（perturbation）。</p>
<p>Liu et al.等<sup><a href="#fn_2" id="reffn_2">2</a></sup>的研究证明劫持部分神经元并使用精心构造的数据训练也可以藏入后门，即“Neuron HiJacking”。此时攻击者得到预训练的模型，从中选择一部分关键神经元并构造可以使这些神经元产生错误输出的触发器并再次训练部分模型。</p>
<p>通常被藏入后门的模型在处理普通的输入数据时可以得到正常的结果（甚至更高的准确率），但是在处理添加了trojan trigger的数据时会得到错误的结果。</p>
<p>现存的一些检查AI木马的算法具有一些局限性，例如需要提供trojan trigger，造成模型性能的下降，需要大量输入数据样本，或者无法解决特征域的攻击行为。其中比较好的是Nerual Cleanse<sup><a href="#fn_3" id="reffn_3">3</a></sup>，但是仍然无法解决需要样本数量大、需要对trojan trigger的先验知识、无法处理尺寸较大的trojan trigger等缺点。</p>
<p>这里的trojan trigger可以是输入域（像素级别）或者是特征域。目前的trojan trigger包括两类，分别为基于碎片（patch based trigger）和基于扰动（perturbations based trigger）的触发器。前者为插入图片或者覆盖原图片的一小块图像，后者为在原始数据上添加特定的数据扰动。这两种都属于输入域的攻击。常见的特征域的攻击包括为图片添加滤镜等方式。</p>
<h2 id="本文思路-解决方案"><a href="#本文思路-解决方案" class="headerlink" title="本文思路/解决方案"></a>本文思路/解决方案</h2><p>AI木马的实质是通过改变个别神经元的权重，使得某些特别的数据或行为出现时会激活并导致错误的分类结果。</p>
<p>本文的算法主要从EBS（Electrical Brain Stimulation）技术中得到启发，该技术向神经元提供不同强度的电刺激并观察结果，从而研究特定神经元的功能。</p>
<p>本文实现的算法中，通过为神经元提供不同程度的激励，被攻击的神经元就会在个别标签下产生错误的结果，通过这些选定的神经元可以逆向得到trojan trigger，如果该特征/输入可以使得正常的数据得到错误的结果，可以认为该网络存在后门或已被攻击。</p>
<p>模型要点：1. 成功的AI木马攻击必定存在受损的神经元（错误地将trojan trigger看作是目标标签的特征之一）。2. 受损神经元在特征空间中的表示是目标标签的子空间。</p>
<p>因此在具体的实现中，给定输入数据得到每一层神经元的激活信息，当选定神经元的激活信息改变时，检查输出信息的变化情况，可以判断该神经元的状态（当其他神经元激活信息改变时输出稳定于错误的标签时即可判断）。同时通过逆向工程的思想推测trojan trigger并检查消除假阳性的神经元。对每一个标签的检查中仅需要一张正常的照片即可用于神经元状态的分析。</p>
<p>整体的流程包括三个部分：1. 激励测试并选择候选神经元。2. 对于候选神经元检测并推断trojan trigger。3. 使用trojan trigger对其他数据进行测试。</p>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>本文的主要结果为两个算法，分别用于选择候选神经元，逆向生成trojan trigger。经过测试，本文实现的技术可以检测到绝大多数的AI木马攻击，达到了90%甚至更高的正确率。同时所采用的逆向工程技术可以近似推断得到trojan trigger。</p>
<p>本文的技术具有同时应对输入域和特征域的攻击行为、需要较少的输入数据、对trigger尺寸不敏感、高效的的特点。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本文实现的方法存在如下的改进方向：</p>
<ol>
<li>测试过程中可能误将目标标签的特征识别为trojan trigger。应当加以区分并避免这样的情况。</li>
<li>对于复杂的基于特征域的攻击难以有效地识别和分析。</li>
<li>对于标签特定的攻击方式难以有效识别和分析，只能识别将所有其他的数据转移为特定标签的攻击方式。</li>
<li>算法效率仍需提升。</li>
<li>仅针对单个神经元进行分析，不能处理多个神经元受更改的情况。</li>
<li>攻击模型的调整。</li>
</ol>
<blockquote id="fn_1">
<sup>1</sup>. TianyuGu,BrendanDolan-Gavitt,andSiddharthGarg.2017.Badnets:Identifying vulnerabilities in the machine learning model supply chain.<a href="#reffn_1" title="Jump back to footnote [1] in the text."> &#8617;</a>
</blockquote>
<blockquote id="fn_2">
<sup>2</sup>. Yingqi Liu, Shiqing Ma, Yousra Aafer, Wen-Chuan Lee, Juan Zhai, Weihang Wang, and Xiangyu Zhang. 2018. Trojaning Attack on Neural Networks.<a href="#reffn_2" title="Jump back to footnote [2] in the text."> &#8617;</a>
</blockquote>
<blockquote id="fn_3">
<sup>3</sup>. BolunWang,YuanshunYao,ShawnShan,HuiyingLi,BimalViswanath,Haitao Zheng, and Ben Y Zhao. 2019. Neural cleanse: Identifying and mitigating backdoor attacks in neural networks.<a href="#reffn_3" title="Jump back to footnote [3] in the text."> &#8617;</a>
</blockquote>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Neural Network</tag>
        <tag>Trojan</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】CloudLeak: Large-Scale Deep Learning Models Stealing Through Adversarial Examples</title>
    <url>/2020/03/17/cloudleak/</url>
    <content><![CDATA[<p>论文标题：CloudLeak：Large-Scale Deep Learning Models Stealing Through Adversarial Examples</p>
<p>会议：Network and Distributed Systems Security (NDSS) Symposium 2020</p>
<a id="more"></a>
<h2 id="论文背景和目标"><a href="#论文背景和目标" class="headerlink" title="论文背景和目标"></a>论文背景和目标</h2><p>基于云的机器学习服务（Cloud-Based MLaaS）是指将训练好的深度神经网络部署在云上并通过应用程序接口（API）提供分类和预测任务服务。尽管MLaaS被构建成基于云的黑盒服务，攻击者仍然可以通过精心构建的对抗样本来窃取网络参数。</p>
<p>论文中实现了一种使用对抗样本窃取网络参数的方法“FeatureFool”，并且证明其相对于其他攻击模型具有更少的请求次数。论文中通过结合几种比较新颖的算法可以达到非常好的效果。</p>
<p>相关的概念包括：</p>
<ul>
<li><p>迁移学习(Transfre Learning)：识别并将在一个任务上学到的能力应用于另一个不同的任务。文章中通过结合VGG19和DeepID的迁移学习，在具有较好的性能的同时加快了模型窃取的速度。详见Y. Sun等的<em>Deep Learning Face Represatation from Predictiing 1000 CLasses</em>。</p>
</li>
<li><p>对抗攻击(Adversarial Attack)：在初始输入上增加一个小的扰动使得模型得到错误的预测结果。包括白盒攻击和黑盒攻击。文章中使用DNN内部特征来生成对抗样本的方法，主要通过恶意的特征构建对抗样本并求解可以减小置信度的网络参数。</p>
</li>
<li><p>模型提取攻击(Model Extraction Attack)：通过目标模型的输入和对应的输出标签/置信度来提取参数并得到一个等效的模型。文章使用迁移学习和对抗样本结合的方法提取网络模型。</p>
</li>
<li><p>主动学习(Active Learning)：模型选择并推送关键的样本（难以分类）要求用户打上标签，从而提高训练呢和预测效率。文章通过对抗样本的构建，解决了主动学习中边界样本趋于相同的问题，</p>
</li>
<li><p>基于云的机器学习服务平台(Cloud-based MLaaS Platform)：指为终端用户提供提供机器学习解决方案的平台，包括模型训练、预测分析等。文章重点介绍了五个MLaaS Platform即Microsoft、Face++、IBM、Google、Clarifai。这些服务通常要求用户提供已标注的数据，由平台训练后提供API用于预测。整个过程是黑盒服务，用户不能接触到训练好模型内部的细节。</p>
</li>
</ul>
<h2 id="相关工作"><a href="#相关工作" class="headerlink" title="相关工作"></a>相关工作</h2><h3 id="其他人的成果"><a href="#其他人的成果" class="headerlink" title="其他人的成果"></a>其他人的成果</h3><p>一方面通过攻击可以盗取网络内部参数，称为模型提取攻击（Model Extraction Attack）。</p>
<p>例如2016年，F. Tramèr等实现了第一个模型提取攻击方法<em>Stealing Machine Learning Models vis Prediction APIs</em>，通过模型预测结果和输入数据来学习模型内部参数。</p>
<p>其他的工作还有B. Wang等的<em>Stealing Hyperparameters in Machine Learning</em>、A. Dmitrenko等的<em>Dnn Model Extraction Attacks Using Prediction Interfaces</em>、Y. Shi等的<em>How to Steal a Machine Learning Classifier with Deep Learning</em>。</p>
<p>另一方面通过攻击可以盗取网络的训练集，称为成员推理攻击（Membership Inference Attack）。</p>
<p>2017年，R. Shokri等实现了一种成员推理攻击方法<em>Membership Inference Attacks against Machine Learning Models</em>，可以判断目标模型的训练集是否包含某一条特定的数据。</p>
<p>其他的工作还有Y. Long等的<em>Understanding Membership Inference on Well-generalized Learning Models</em>。</p>
<p>存在一些应对模型提取攻击的防御方案，大多都是在预测请求过程(query process)中加以处理，但是难以同时满足性能和效率的要求。例如M. Juuti等的<em>Prada: Protecting against DNN Model Stealing Attacks</em>。</p>
<h3 id="局限性"><a href="#局限性" class="headerlink" title="局限性"></a>局限性</h3><ul>
<li>目前大多数模型攻击方法只能应用于比较简单的小规模机器学习模型中。</li>
<li>目前的攻击方法的请求规模与模型参数成比例。在攻击含有百万量级参数的大型网络时效率低下。</li>
</ul>
<h2 id="本文解决方案"><a href="#本文解决方案" class="headerlink" title="本文解决方案"></a>本文解决方案</h2><h3 id="模型假设"><a href="#模型假设" class="headerlink" title="模型假设"></a>模型假设</h3><p>攻击者对网络模型的结构、参数、训练集没有先验的信息。但是可以使用任意输入并得到对应的预测结果。</p>
<h3 id="模型思路"><a href="#模型思路" class="headerlink" title="模型思路"></a>模型思路</h3><p>通过使用对抗样本请求模型预测并得到结果，使用输入输出对（input-output pair）训练从候选库中选择出来的替代模型，从而实现对目标模型参数的窃取。实际上，通过使用预测结果可以大量减少标记成本并得到训练替代模型的数据集。基本流程为：1.生成对抗样本得到合成数据集。2.请求目标模型得到输出结果。3.根据模型输出标记对抗样本。4.使用合成数据集训练替代模型。</p>
<h3 id="符号定义"><a href="#符号定义" class="headerlink" title="符号定义"></a>符号定义</h3><p>目标网络$f_v(x)$的输入为$x$，输出$[f_v^1(x),f_v^2(x)…]$，学习目标为在构造的数据集$T={(x, f_v(x))}$上训练一个替代模型（网络）$f_s(x)$使得两者具由基本相同的功能。攻击者对于目标网络的结构$A$、参数$W$、训练集$D$没有任何先验的信息。</p>
<h3 id="模型实现"><a href="#模型实现" class="headerlink" title="模型实现"></a>模型实现</h3><h4 id="数据集生成"><a href="#数据集生成" class="headerlink" title="数据集生成"></a>数据集生成</h4><p>基于边界的对抗性主动学习（Margin-based Adversarial AL）：从未标记的样本中选择一组“有用的”样本。即从生成的未标记样本中选择预测置信度最低、预测不确定性最大的样本<script type="math/tex">\mathit{Q}_{multiclass}^{LC}:\mathbb{x}_s^* \in arg\min\limits_{x'\in D_u(x)}k(x',y,w)</script>。注意这里得到的样本太多，预测时需要大量的请求次数，因此采用进一步采样得到更小的数据集。</p>
<p>这里的采样方式包括：随机采样（Random Sample，RS）、投影梯度下降（Projected Gradient Descent）、Carlini &amp; Wagner 攻击（Carlini and Wagner Attack，CW）、Feature Adversary（FA）、FeatureFool（FF， 本文提出的算法）。</p>
<p>在使用上述FF算法求解时使用L-BFGS算法进行优化求解并使用平均测量误差（Average Test Error，ATE）方法衡量数据集质量。其优化流程主要为：1.确定原图像和目标图像。2.原图像加扰动后加入特征提取网络。3.选择网络中指定特征层的输出得到显著图。4.比较显著图与目标图像的显著图并加以优化。</p>
<h4 id="替代模型训练"><a href="#替代模型训练" class="headerlink" title="替代模型训练"></a>替代模型训练</h4><ol>
<li>随机选择一组初始化数据集，并使用上述各种算法得到一组小规模的对抗样本。这些样本可以很容易地使本地的替代模型得到错误的预测结果。</li>
<li>使用对抗样本请求预测并得到构造数据集。使用几种训练好的模型（AlexNet、VGG19、VGGFace、ResNet50）构建迁移学习框架，使用构造数据集训练并更新上述框架的最后几层全联接的参数。</li>
<li>重复使用本地模型构建对抗样本并请求预测，使用预测结果构建数据集进行训练的过程。最终得到性能足够逼近目标模型的替代模型。</li>
</ol>
<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>作者选择了三个平台的四种模型进行了攻击演示。经测试，使用文章设计实现的FF算法在多数目标平台上可以实现比较好的效果。而且与现存的三种模型提取攻击相比，具有更高的准确度和相同准确率下更少的请求次数，从而具有更少的学习成本。</p>
<p>使用现有的PRADA检测机制可以发现，文章实现的算法可以很好的逃避PRADA机制的检测，即相比其他的攻击方式，要想检测到本文实现的攻击需要更多的请求次数。文章推荐的检测方式为通过提取网络每一层的特征数据来区分恶意数据和合法数据的特征分布。</p>
<h2 id="总结分析-局限性"><a href="#总结分析-局限性" class="headerlink" title="总结分析-局限性"></a>总结分析-局限性</h2><p><strong>攻击请求方式的改进</strong>：文章实现的方法中，通过对正常数据添加扰动构建攻击样本，造成了数据污染，从而降低了替代模型的预测准确率。</p>
<p><strong>多标签任务的扩展</strong>：文章实现的方法仅能用于单标签的多分类任务，因此需要在数据生成和替代样本结构选择上更加注意。</p>
<p><strong>其他学习领域的扩展</strong>：文章实现的方法仅能用于图像分类任务，因此在处理音频或文字时需要对模型做相应的调整。</p>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>PyQt 工作环境搭建</title>
    <url>/2020/03/15/pyqt-env/</url>
    <content><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>之前使用过C++和QT开发具有GUI的小工具。考虑到人生苦短，决定转到使用PyQt，因此在mac上搭建PyQt工作环境。由于电脑上已经安装了QtCreator和Qt环境，网上也有很多教程因此不再赘述。</p>
<a id="more"></a>
<h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><p>为了干净我选择使用virtualenv构建用于PyQt的虚拟环境。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ virtualenv pyqtenv</span><br><span class="line">$ <span class="built_in">source</span> pyquenv/bin/activate</span><br><span class="line">(pyqtenv)$ pip install PyQt5</span><br></pre></td></tr></table></figure>
<p>似乎Qt Creator的python项目默认使用PySide2，但是在新建项目或者文件的时候有一个”Qt Module”的选项可以选择PyQt5或者PySide2。我选择PyQt5。</p>
<p>需要注意执行前在Qt Creator的项目选项中选择Python解释器，即选择所安装的虚拟环境目录。通常情况下QtCreator会发现并自动生成“pyqtenv virtual environment”，如果没有自动生成则需要在manage选项中手动添加并选择执行路径”/path_to_your_env/pyqtenv/bin/python”。</p>
<h2 id="测试代码"><a href="#测试代码" class="headerlink" title="测试代码"></a>测试代码</h2><p>由于人生苦短，我选择使用Qt Designer进行UI设计，如果是喜欢手撸代码的大佬请跳过以下内容。</p>
<p>在Qt Creator中选择新建Qt Designer Form得到ui文件，并通过Qt Designer设计界面。得到mainwindow.ui界面文件。此时需要注意要将界面文件导入py项目需要使用PyQt5自带的一个工具pyuic5编译得到UI类。（如果有更好的方法欢迎告诉我，我也不想这么麻烦）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span> pyquenv/bin/activate</span><br><span class="line">(pyqtenv)$ pyuic5 -o ui_mainwindow.py mainwindow.ui</span><br></pre></td></tr></table></figure>
<p>之后就可以在Qt Creator中添加ui编译得到的py文件，一种方法是使用UI类作为基类之一并使用self.setupUI(self)。还有一种方法是使用UI类作为成员，即self.ui = Ui_MainWindow()。我选择前者，代码如下。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">from</span> PyQt5.QtWidgets <span class="keyword">import</span> QApplication, QMainWindow</span><br><span class="line"><span class="keyword">from</span> ui_mainwindow <span class="keyword">import</span> Ui_MainWindow</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MainWindow</span><span class="params">(QMainWindow, Ui_MainWindow)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        super(MainWindow, self).__init__()</span><br><span class="line">        self.setupUi(self)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    app = QApplication([])</span><br><span class="line">    window = MainWindow()</span><br><span class="line">    window.show()</span><br><span class="line">    sys.exit(app.exec_())</span><br></pre></td></tr></table></figure>
<p>可以正常实现自己设计的UI界面就表明没啥问题了。</p>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>使用dlib提取图像中人的嘴部</title>
    <url>/2020/03/15/python-dlib/</url>
    <content><![CDATA[<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>dlib可以检测图像中的人脸，并且可以检测出人脸上的68个关键点，其中后20个点表示了唇部的关键点，因此可以使用dlib检测人脸并通过嘴部关键点得到嘴部图像。</p>
<a id="more"></a>
<h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> dlib</span><br><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line"><span class="comment"># 加载面部检测模型</span></span><br><span class="line">predictor_path = <span class="string">'.\\shape_predictor_68_face_landmarks.dat'</span></span><br><span class="line">detector = dlib.get_frontal_face_detector()</span><br><span class="line">predictor = dlib.shape_predictor(predictor_path)</span><br><span class="line"><span class="comment"># 检测面部并选择</span></span><br><span class="line">faces = detector(img, <span class="number">1</span>)</span><br><span class="line">face = faces[<span class="number">0</span>]</span><br><span class="line"><span class="comment"># 检测关键点并选择唇部</span></span><br><span class="line">points = predictor(img, face)</span><br><span class="line">mouth_points = [(point.x, point.y) <span class="keyword">for</span> point <span class="keyword">in</span> points.parts()[<span class="number">48</span>:]]</span><br><span class="line"><span class="comment"># 截取唇部图像</span></span><br><span class="line">center = np.mean(mouth_points, axis=<span class="number">0</span>).astype(int)</span><br><span class="line">rect = cv2.boundingRect(np.array(mouth_points))</span><br><span class="line">ratio = rect[<span class="number">2</span>] / rect[<span class="number">3</span>]</span><br><span class="line"><span class="comment"># 给定输出规模shape=[width, height]</span></span><br><span class="line"><span class="keyword">if</span> shape[<span class="number">0</span>] / shape[<span class="number">1</span>] &gt; ratio:</span><br><span class="line">  shape[<span class="number">0</span>], shape[<span class="number">1</span>] = shape[<span class="number">0</span>] / shape[<span class="number">1</span>] * rect[<span class="number">3</span>], rect[<span class="number">3</span>]</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">  shape[<span class="number">0</span>], shape[<span class="number">1</span>] = rect[<span class="number">2</span>], shape[<span class="number">1</span>] / shape[<span class="number">0</span>] * rect[<span class="number">2</span>]</span><br><span class="line">mouth_img = img[int(center[<span class="number">1</span>] - shape[<span class="number">1</span>] / <span class="number">2</span>):int(center[<span class="number">1</span>] + shape[<span class="number">1</span>] / <span class="number">2</span>),</span><br><span class="line">             int(center[<span class="number">0</span>] - shape[<span class="number">0</span>] / <span class="number">2</span>):int(center[<span class="number">0</span>] + shape[<span class="number">0</span>] / <span class="number">2</span>)]</span><br><span class="line"><span class="comment"># 保存图像</span></span><br><span class="line">Image.fromarray(mouth_img).save(<span class="string">"img_path"</span>)</span><br></pre></td></tr></table></figure>
<p>参考：<a href="https://zhuanlan.zhihu.com/p/44169338" target="_blank" rel="noopener">张宏伦的知乎专栏</a></p>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Computer Vision</tag>
      </tags>
  </entry>
  <entry>
    <title>STM32的GPIO处理</title>
    <url>/2020/03/05/stm32-gpio/</url>
    <content><![CDATA[<p>以STM32F103VET6点亮LED为例简单记录一下STM32的GPIO如何控制。</p>
<a id="more"></a>
<h2 id="GPIO控制原理"><a href="#GPIO控制原理" class="headerlink" title="GPIO控制原理"></a>GPIO控制原理</h2><p>片上外设挂载有三条总线，APB1、APB2、AHB。GPIO挂载在APB2总线上。共有七组GPIO（端口），每个GPIO有16个引脚（位）。</p>
<p>GPIO的行为通过7个寄存器来控制，每个寄存器为32bit。</p>
<p>七个寄存器分别为CRL（低八位的控制寄存器）、CRH（高八位的控制寄存器）、IDR（输入数据寄存器）、ODR（输出数据寄存器）、BSRR（端口位设置/清除寄存器）、BRR、LCKR。</p>
<p>CRH和CRL：每四位为一组控制一位的IO。其中低两位控制运行模式（输入、2MHz输出、10MHz输出、50MHz输出），高两位控制IO配置（输入：模拟、浮空、上/下拉。输出：推挽、开漏）。使用宏定义的工作模式时，每一个宏使用8bit数字表示，实际写入的时候写入的是bit[3:2]。</p>
<p>BSRR（端口位设置/清除寄存器）：低16位触发时表示ODR对应位置1，高16位触发时表示ODR对应位置0。</p>
<p>BRR（端口位清除寄存器）：低16位触发时表示ODR对应的位置0。</p>
<p>ODR（输出数据寄存器）：对应的位置0或置1表示对应引脚高低电平。</p>
<p>IDR（输入数据寄存器）：使用上下拉输入的时候保存输入引脚的值0或1。</p>
<p>需要注意的是，GPIO使用前要进行时钟的使能，此时使用RCC_APB2ENR寄存器激活APB２总线上的对应外设。其中ｂｉｔｓ［８：２］表示七个GPIO。</p>
<h2 id="使用寄存器操作"><a href="#使用寄存器操作" class="headerlink" title="使用寄存器操作"></a>使用寄存器操作</h2><p>开启GPIOB的0号引脚</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 开启GPIOB的时钟</span></span><br><span class="line">RCC_APB2ENR |= (<span class="number">1</span> &lt;&lt; <span class="number">3</span>);</span><br><span class="line"><span class="comment">// GPIOB的0号引脚控制位全部置0</span></span><br><span class="line">GPIOB_CRL &amp;= ~(<span class="number">0x0F</span> &lt;&lt; (<span class="number">4</span>*<span class="number">0</span>));</span><br><span class="line"><span class="comment">// GPIOB的0号引脚设置为10MHz推挽输出</span></span><br><span class="line">GPIOB_CRL |= (<span class="number">1</span> &lt;&lt; (<span class="number">4</span>*<span class="number">0</span>));</span><br><span class="line"><span class="comment">// GPIOB的0号引脚拉低</span></span><br><span class="line">GPIOB_ODR |= (<span class="number">0</span> &lt;&lt; <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<h2 id="使用标准库操作"><a href="#使用标准库操作" class="headerlink" title="使用标准库操作"></a>使用标准库操作</h2><p>使用标准库开启时钟并设置引脚</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化结构体</span></span><br><span class="line">GPIO_InitTypeDef GPIO_InitStructure;</span><br><span class="line"><span class="comment">// 开启时钟</span></span><br><span class="line">RCC_ARB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);</span><br><span class="line"><span class="comment">// 设置引脚和工作模式</span></span><br><span class="line">GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0;</span><br><span class="line">GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;</span><br><span class="line">GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;</span><br><span class="line"><span class="comment">// 写入工作模式</span></span><br><span class="line">GPIO_Init(GPIOB, &amp;GPIO_InitStructure);</span><br><span class="line"><span class="comment">// 引脚置位</span></span><br><span class="line">GPIO_SetBits(GPIOB, GPIO_Pin_0);</span><br><span class="line">GPIO_ResetBits(GPIOB, GPIO_Pin_0);</span><br><span class="line"><span class="comment">// 或者使用BSRR、BRR、ODR设置</span></span><br><span class="line">GPIOB-&gt;BSRR |= <span class="number">0x01</span>;</span><br><span class="line">GPIOB-&gt;BRR |= <span class="number">0x01</span>;</span><br><span class="line">GPIOB-&gt;ODR |= <span class="number">0x01</span>;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>STM32</tag>
      </tags>
  </entry>
  <entry>
    <title>花书第三章:概率与信息论</title>
    <url>/2020/02/20/flowerC3/</url>
    <content><![CDATA[<p>概率论能够提出不确定的声明以及在不确定性存在的情况下进行推理,信息论可以量化概率分布中的不确定性总量。</p>
<a id="more"></a>
<h2 id="为什么使用概率"><a href="#为什么使用概率" class="headerlink" title="为什么使用概率"></a>为什么使用概率</h2><p>机器学习经常需要处理不确定量和随机量。</p>
<p>不确定性来源：被建模系统内在的随机性、不完全观测、不完全建模。</p>
<p>使用简单而不确定的规则要比复杂而确定的规则更加实用。</p>
<p>与事件发生频率相联系的成为频率派概率，与事件发生的确定性水平有关的（概率表示信任度）为贝叶斯概率。</p>
<h2 id="随机变量"><a href="#随机变量" class="headerlink" title="随机变量"></a>随机变量</h2><p>可以随机取值的变量。</p>
<p>离散的或者连续的。</p>
<h2 id="概率分布"><a href="#概率分布" class="headerlink" title="概率分布"></a>概率分布</h2><p>描述随机变量在可能取到的状态的可能性大小。</p>
<h3 id="离散随机变量和概率质量函数"><a href="#离散随机变量和概率质量函数" class="headerlink" title="离散随机变量和概率质量函数"></a>离散随机变量和概率质量函数</h3><p>概率质量函数PMF或者称为概率分布函数，将随机变量的状态映射到概率。</p>
<p>作用于多个变量的是联合概率分布函数。</p>
<p>注意归一化条件$\sum\limits_{x \in x} P(x) = 1$。</p>
<h3 id="连续随机变量和概率密度函数"><a href="#连续随机变量和概率密度函数" class="headerlink" title="连续随机变量和概率密度函数"></a>连续随机变量和概率密度函数</h3><p>概率密度函数PDF描述连续型随机变量。</p>
<h2 id="边缘概率"><a href="#边缘概率" class="headerlink" title="边缘概率"></a>边缘概率</h2><p>从联合概率计算某一个变量子集的概率分布</p>
<p>使用求和$P(x) = \sum\limits_{y}P(x,y)$或者积分$p(x) = \int p(x,y)dy$。</p>
<h2 id="条件概率"><a href="#条件概率" class="headerlink" title="条件概率"></a>条件概率</h2><p>$P(y={y}|x=x) = \frac{P(x=x,y=y)}{P(x=x)}$</p>
<h2 id="条件概率的链式法则"><a href="#条件概率的链式法则" class="headerlink" title="条件概率的链式法则"></a>条件概率的链式法则</h2><p>或者称为乘法法则</p>
<p>$P(abc) = P(a|bc)P(b|c)P(c)$</p>
<h2 id="独立性和条件独立性"><a href="#独立性和条件独立性" class="headerlink" title="独立性和条件独立性"></a>独立性和条件独立性</h2><p>有$P(xy) = P(x)P(y)$称为概率独立，即$x \bot y$。</p>
<p>有$P(xy|z) = P(x|x)P(y|z)$称为条件独立，即$x\bot y\ |\ z$。</p>
<h2 id="期望、方差和协方差"><a href="#期望、方差和协方差" class="headerlink" title="期望、方差和协方差"></a>期望、方差和协方差</h2><p>期望是$\mathbb{E}[f(x)] = \int p(x)f(x)dx$或者$\mathbb{E}[f(x) = \sum \limits_{x}P(x)f(x)]$。</p>
<p>方差为$Var(f(x)) = \mathbb{E}[(f(x) - \mathbb{E}[f(x)])^2]$，根式为标准差。</p>
<p>协方差衡量多个变量相关性$Cov(f(x), g(y)) = \mathbb{E}[(f(x) - \mathbb{E}[f(x)])\times(g(y) - \mathbb{E}[g(y)])]$。</p>
<p>随机向量的协方差矩阵有$Cov(\mathbf{x})_{i,j} = Cov(x_i, x_j)$。对角线为方差。</p>
<h2 id="常用的概率分布"><a href="#常用的概率分布" class="headerlink" title="常用的概率分布"></a>常用的概率分布</h2><h3 id="Bernoulli分布"><a href="#Bernoulli分布" class="headerlink" title="Bernoulli分布"></a>Bernoulli分布</h3><p>二值分布$P(x=1)=\phi$、$P(x=0)=1-\phi$</p>
<h3 id="Multinoulli分布"><a href="#Multinoulli分布" class="headerlink" title="Multinoulli分布"></a>Multinoulli分布</h3><p>称为范畴分布categorical distribution，其实是多项式分布（N个元素取n次）的n=1的情况。</p>
<p>表示有限个不同状态的离散随机变量的分布</p>
<h3 id="高斯分布"><a href="#高斯分布" class="headerlink" title="高斯分布"></a>高斯分布</h3><p>称为正态分布normal distribution。</p>
<p>$\mathcal{N}(x;\mu,\sigma^2) = \sqrt{\frac{1}{2\pi\sigma^2}}exp(-\frac{1}{2\sigma^2}(x-\mu)^2)$</p>
<p>通常使用$\beta = \frac{1}{\sigma^2}$作为高斯分布的精度precision</p>
<p>重要性：独立随机变量的和近似服从正态分布；正态分布是引入先验知识量最少的分布。</p>
<p>可以推广到多维正态分布$\mathcal{N}(x;\mu,\Sigma)=\sqrt{\frac{1}{(2\pi)^ndet(\Sigma)}}exp(-\frac{1}{2}(x-\mu)^T\Sigma^{-1}(x-\mu))$。</p>
<p>其中的$\mu$表示均值，$\Sigma$表示协方差矩阵。</p>
<h3 id="指数分布和Laplace分布"><a href="#指数分布和Laplace分布" class="headerlink" title="指数分布和Laplace分布"></a>指数分布和Laplace分布</h3><p>指数分布<script type="math/tex">p(x;\lambda) = \lambda 1_{x\geq0}exp(-\lambda x)</script>，其中的指示函数$ 1_{x\geq0} $将自变量范围限制在不为负数。</p>
<p>Laplace分布$Laplace(x;\mu, \gamma) = \frac{1}{2\gamma}exp(-\frac{|x-\mu|}{\gamma})$。</p>
<h3 id="Dirac分布和经验分布"><a href="#Dirac分布和经验分布" class="headerlink" title="Dirac分布和经验分布"></a>Dirac分布和经验分布</h3><p>当概率质量集中在一点时得到Dirac分布$p(x) = \delta(x-\mu)$。可以表示一次观察的结果。</p>
<p>经验分布$\hat{p}(\mathbf{x}) = \frac{1}{m}\sum \limits_{i=1}^{m} \delta(\mathbf{x} - \mathbf{x}^{(i)})$。可以表示对样本的m次观察，或者规模为m的训练集。</p>
<p>训练模型时，看作用训练集的经验分布指明采样来源的分布。</p>
<p>经验分布可以看作训练数据似然最大的概率密度函数。</p>
<h3 id="分布的混合"><a href="#分布的混合" class="headerlink" title="分布的混合"></a>分布的混合</h3><p>混合分布mixture dixtribution由多个分布成分component组成，首先每个成分来自Multinoulli分布。</p>
<p>$P(x) = \sum \limits_{i} P(c=i)P(x|c=i)$其中$P(c)$是各个组成成分的一个Multinoulli分布。</p>
<p>引入了潜变量的概念，即不能直接观测到的变量，但是观测变量受到潜变量的影响，两者的分布共同影响最终分布。</p>
<p>常见高斯混合模型，每一个成分都是均值和协方差矩阵各不相同的高斯分布。其中给定了$P(c=i)$是每一个高斯分布的成分的先验概率，而$P(c=i｜x)$称为后验概率。</p>
<p>高斯混合模型是万能近似器，任何平滑的概率密度都可以用足够多成分的高斯混合模型以任意精度来逼近。</p>
<h2 id="常见函数的有用性质"><a href="#常见函数的有用性质" class="headerlink" title="常见函数的有用性质"></a>常见函数的有用性质</h2><p>Logistic Sigmoid函数：$\sigma(x) = \frac{1}{1+exp(-x)}$。</p>
<p>Softplus函数：$\zeta(x) = log(1+exp(x))$。</p>
<p>其中Softplus函数可以看作是正部函数的平滑版本，正部函数$x^+ = max{0, x}$就是常用的ReLU激活函数。</p>
<h2 id="贝叶斯规则"><a href="#贝叶斯规则" class="headerlink" title="贝叶斯规则"></a>贝叶斯规则</h2><p>贝叶斯规则：$P(x|y) = \frac{P(x)P(y|x)}{P(y)}$。</p>
<p>其中可以计算$P(y) = \sum\limits_{x}P(y|x)P(x)$。</p>
<h2 id="连续型变量的技术细节"><a href="#连续型变量的技术细节" class="headerlink" title="连续型变量的技术细节"></a>连续型变量的技术细节</h2><p>连续型随机变量和概率密度函数的简单定义可能会引起悖论，因此使用测度论的一些知识加以处理。</p>
<p>提出“零测度”的集合、“几乎处处”成立的概念。</p>
<p>同时对于具有函数关系的随机变量的分布的处理不能简单的使用微分的概念处理。</p>
<h2 id="信息论"><a href="#信息论" class="headerlink" title="信息论"></a>信息论</h2><p>定义自信息量$I(x) = -logP(x)$。</p>
<p>定义信息熵（香农熵）<script type="math/tex">H(x) = \mathbb{E}_{X \backsim P}[I(x)] = -\mathbb{E}_{X\backsim P}[log(P(x)]</script>。</p>
<p>定义KL测度，表示两个概率分布的差异<script type="math/tex">D_{KL}(P||Q) = \mathbb{E}_{X\backsim P}[log\frac{P(x)}{Q(x)}] = \mathbb{E}_{X\backsim P}[logP(x)-logQ(x)]</script>。注意由于X服从的分布不同，KL测度是不对称的。</p>
<p>定义交叉熵（cross entropy）<script type="math/tex">H(P,Q) = H(P) + D_{KL}(P||Q) = -\mathbb{E}_{X\backsim P}logQ(x)</script>。</p>
<h2 id="结构化概率模型"><a href="#结构化概率模型" class="headerlink" title="结构化概率模型"></a>结构化概率模型</h2><p>涉及在很多个相互影响的随机变量上的概率分布时，可以按照依赖和独立关系将其分解成多个概率分布的乘积。</p>
<p>通常使用图模型表示多个变量之间的关系，称为结构化概率模型或者图模型。</p>
<p>有向图模型中，使用有向边表示条件概率分布。$p(x) = \prod\limits_{i}p(x_i|Pa\mathcal{G}(x_i))$其中$Pa\mathcal{G}(x_i)$表示$x_i$的父节点。</p>
<p>无向图模型中，将图分为团（团内部任意两节点之间存在无向边）并带有因子，整个概率分布表示为各个团对应的函数按因子加权和并且归一化$p(x) = \frac{1}{Z}\prod\limits_{i}\phi^{(i)}(\mathcal{C}^{(i)})$。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>花书第二章:线性代数</title>
    <url>/2020/02/17/flowerC2/</url>
    <content><![CDATA[<p>深度学习算法需要了解一些基本的线性代数知识。</p>
<a id="more"></a>
<h2 id="标量、向量、矩阵、张量"><a href="#标量、向量、矩阵、张量" class="headerlink" title="标量、向量、矩阵、张量"></a>标量、向量、矩阵、张量</h2><p>张量可以理解为维度比较高的数组，元素分布在若干维坐标的规则网络中。</p>
<p>深度学习允许矩阵和向量的相加：<script type="math/tex">C = A + b := C_{i,j} = A_{i,j}+b_j</script>，即向量和矩阵的每一行相加。这种将向量复制到很多位置的方法称为广播。</p>
<h2 id="矩阵和向量相乘"><a href="#矩阵和向量相乘" class="headerlink" title="矩阵和向量相乘"></a>矩阵和向量相乘</h2><p>矩阵乘法要求满足形状匹配。前者列数等于后者行数。</p>
<p>矩阵乘法不可交换，向量和向量的点积可以交换。</p>
<h2 id="单位矩阵和逆矩阵"><a href="#单位矩阵和逆矩阵" class="headerlink" title="单位矩阵和逆矩阵"></a>单位矩阵和逆矩阵</h2><p>单位矩阵：对角线元素为1</p>
<p>形如$Ax=b$的方程有解$x=A^{-1}b$。前提为$A^{-1}$存在。</p>
<h2 id="线性相关和生成空间"><a href="#线性相关和生成空间" class="headerlink" title="线性相关和生成空间"></a>线性相关和生成空间</h2><p>矩阵A的列向量的线性组合b称为列空间或者值域。对于列空间内的向量b存在向量x使得$Ax=b$。</p>
<p>要使方程有解，要求列向量组成的列空间为整个$\mathbb{R}^m$。即列向量线性无关。</p>
<h2 id="范数"><a href="#范数" class="headerlink" title="范数"></a>范数</h2><p>定义$L^p$范数为$||x||_p = (\sum_i|x_i|^p)^{\frac{1}{p}}$。</p>
<p>范数是将向量映射到非负值的函数。满足$f(x)=0 -&gt; x=0$、$f(x+y) \leq f(x)+f(y)$、$f(\alpha x)=|\alpha|f(x)$。</p>
<p>最常用的为$L^2$范数、平方$L^2$范数和$L^1$范数。</p>
<p>最大范数$||x||_{\infty} = max|x_i|$。</p>
<p>衡量矩阵使用Frobenius范数<script type="math/tex">||A||_F = \sqrt{\sum_{i,j}A_{i,j}^2}</script>。</p>
<h2 id="特殊矩阵和向量"><a href="#特殊矩阵和向量" class="headerlink" title="特殊矩阵和向量"></a>特殊矩阵和向量</h2><p>对角矩阵：只有主对角线有非零元素。乘法计算非常高效。</p>
<p>对称矩阵：转置和原矩阵相同。</p>
<p>单位向量具有单位范数。</p>
<p>正交矩阵表示行向量和列向量分别是标准正交。$AA^T=I=A^TA$。</p>
<h2 id="特征分解"><a href="#特征分解" class="headerlink" title="特征分解"></a>特征分解</h2><p>将矩阵分为特征值和特征向量。即$Av=\lambda v$。</p>
<p>将特征向量作为列向量拼接得到V，将特征值得到对角矩阵，可以有$A = Vdiag(\lambda)V^{-1}$。</p>
<p>对于实对称矩阵A，有$A = Q \Lambda Q^T$。</p>
<p>所有特征值为正数的矩阵为正定矩阵。</p>
<h2 id="奇异值分解SVD"><a href="#奇异值分解SVD" class="headerlink" title="奇异值分解SVD"></a>奇异值分解SVD</h2><p>将矩阵分解为奇异值和奇异向量。$A = UDV^T$</p>
<p>如果A为$m\times n$，则U为$m\times m$、D为$m \times n$对角矩阵、V为$n \times n$。</p>
<p>U的列向量叫做左奇异向量，是$AA^T$的特征向量。V的列向量为右奇异向量，是$A^TA$的特征向量。D的对角线元素叫做奇异值，是$AA^T$和$A^TA$特征值的平方根。</p>
<p>SVD将矩阵求逆拓展到非方矩阵。</p>
<h2 id="Moore-Penrose伪逆"><a href="#Moore-Penrose伪逆" class="headerlink" title="Moore-Penrose伪逆"></a>Moore-Penrose伪逆</h2><p>伪逆是指非方矩阵的逆。通过奇异值分解来计算。</p>
<p>由$A=UDV^T$得$A^+ = VD^+U^T$。其中$D^+$是D的非零元素取倒数。</p>
<p>通过伪逆求$Ax=y$的解$x=A^+y$。A列数大于行数时结果$||x||_2$最小，行数大于列数时$||Ax-y||_2$最小。</p>
<h2 id="迹运算"><a href="#迹运算" class="headerlink" title="迹运算"></a>迹运算</h2><p>迹运算是对角元素之和$Tr(A)=\sum A_{i,i}$。</p>
<p>具有很多性质。例如Frobenius范数$||A||_F = \sqrt{Tr(AA^T)}$、循环置换后迹不变$Tr(ABC)=Tr(BCA)=Tr(CAB)$。</p>
<h2 id="行列式"><a href="#行列式" class="headerlink" title="行列式"></a>行列式</h2><p>将矩阵映射到实数的函数。</p>
<p>数值上是矩阵特征值的乘积。</p>
<h2 id="主成分分析PCA"><a href="#主成分分析PCA" class="headerlink" title="主成分分析PCA"></a>主成分分析PCA</h2><p>关键思想：高维空间的数据的有损压缩。</p>
<p>对于$\mathbb{R}^n$中的数据$x^{(i)}$使用$c^{(i)} \in \mathbb{R}^l,l \lt n$表示，即使用低维数据表示。使用$f(x)=c$表示编码，$g(c) = Dc$表示解码过程。要求$||x-g(c)||_2$达到最小，表示信息损失越小。</p>
<p>问题变为最小化问题<script type="math/tex">c^*=\arg\min \limits_{c}||x-g(c)||_2^2</script>（使用平方$L^2$范数简化运算和表示）。进一步展开表达式并带入$g(c) = Dc$得到最终的优化目标函数<script type="math/tex">c^* = \arg\min\limits_{c}(-2x^TDc+c^Tc)</script>。通过微分可以解得$c=D^Tx$。因此要求得到一个合适的编码矩阵。</p>
<p>由于对所有的点使用相同的编码矩阵，因此寻找编码矩阵时要求原数据和重构数据的误差矩阵的Frobenius范数最小，即<script type="math/tex">D^* = \arg\min\limits_{D}\sqrt{\sum\limits_{i,j}(x_j^{(i)} - r(x^{(i)})_j)^2} \ subject\ to \ D^TD=I_l</script>。</p>
<p>当$l=1$的时候，上述方程简化为<script type="math/tex">d^* = \arg\max\limits_{d}Tr(d^TX^TXd)\ subject\ to\ d^Td=1</script>。此时d为$X^TX$最大的特征值对应的特征向量。</p>
<p>最终得到，矩阵D的列向量为前$l$个最大的特征值对应的特征向量。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>花书第一章:引言</title>
    <url>/2020/02/16/flowerC1/</url>
    <content><![CDATA[<p>人工智能的真正挑战在于解决那些对人来说很容易执行，但是很难形式化描述的任务。</p>
<a id="more"></a>
<p>机器学习算法的性能会受到数据表示方法的影响，因此解决方法是使用机器学习来发掘表示本身，而不是由人来选择表示然后使用机器学习将表示映射到输出。称为表示学习。</p>
<p>深度学习通过使用简单的概念构造复杂概念来进行表示学习。代表是多层感知机。</p>
<p>模型深度有两种衡量方式：需要执行的顺序质量的数量，或者使用描述概念关联的图的深度。</p>
<p>整体来说深度学习是机器学习的一种，构建嵌套的层次概念体系。由简单概念构造复杂概念，由一般抽象定义高级抽象。</p>
<h2 id="内容的高层组织"><a href="#内容的高层组织" class="headerlink" title="内容的高层组织"></a>内容的高层组织</h2><p>PASS</p>
<h2 id="历史趋势"><a href="#历史趋势" class="headerlink" title="历史趋势"></a>历史趋势</h2><p>控制论、人工神经网络、线性模型、梯度下降算法、学习XOR函数的局限性 -&gt; 联结主义、分布式表示、反向传播算法、长短期记忆、计算能力的局限性 -&gt; 无监督学习、小数据集的泛化、贪婪逐层预训练思想。</p>
<p>训练数据量增加简化了模型构建。重点在于通过无监督学习和半监督学习充分利用未标注样本。</p>
<p>模型中神经元的连接数基本不变，但是神经网络规模逐渐增大。</p>
<p>强化学习领域的发展。在没有人类操作者指导的前提下失错并学习。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>brew使用记录</title>
    <url>/2020/02/13/deal-with-brew/</url>
    <content><![CDATA[<p>在MacOS上使用的包管理软件主要有<a href="https://www.macports.org" target="_blank" rel="noopener">MacPorts</a>和<a href="https://brew.sh/index_zh-cn" target="_blank" rel="noopener">Homebrew</a>。由于朋友推荐我一直使用Homebrew作为自己的mac的包管理工具。</p>
<a id="more"></a>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>在mac的terminal里面直接运行官网提供的安装命令就可以安装。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 安装homebrew</span></span><br><span class="line">/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"</span><br><span class="line"><span class="meta">#</span><span class="bash"> 安装homebrew cask</span></span><br><span class="line">brew tap caskroom/cask</span><br></pre></td></tr></table></figure>
<p>安装homebrew的时候有时候会非常慢，可以根据<a href="https://www.jianshu.com/p/85958956c75a" target="_blank" rel="noopener">sipeiyouyang的教程</a>更换下载源。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 得到安装脚本</span></span><br><span class="line">curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install &gt;&gt; brew_install</span><br><span class="line"><span class="meta">#</span><span class="bash"> 更改安装脚本中的下载源地址如下：</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> BREW_REPO = <span class="string">"https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/brew.git"</span>.freeze</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 运行</span></span><br><span class="line">/usr/bin/ruby brew_install</span><br></pre></td></tr></table></figure>
<h2 id="换源"><a href="#换源" class="headerlink" title="换源"></a>换源</h2><p>由于官方的brew源网站在外面，由于众所周知的问题，homebrew经常会遇到速度非常慢或者443 timeout。</p>
<p>其实homebrew比较不好的一点是，brew update命令的可视化比较差，在运行update的时候经常需要把terminal挂在后台跑很久，因此比较推荐使用-v参数。可以看到具体的update过程，这样出现问题也可以容易的发现问题出在哪里。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 可视化更新</span></span><br><span class="line">brew update -v</span><br></pre></td></tr></table></figure>
<p>更换国内中科大的源，根据<a href="https://zhuanlan.zhihu.com/p/59805070" target="_blank" rel="noopener">知乎专栏</a>。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 更换源</span></span><br><span class="line">cd "$(brew --repo)"</span><br><span class="line">git remote set-url origin https://mirrors.ustc.edu.cn/brew.git</span><br><span class="line"><span class="meta">#</span><span class="bash"> 更换核心程序源</span></span><br><span class="line">cd "$(brew --repo)/Library/Taps/homebrew/homebrew-core"</span><br><span class="line">git remote set-url origin https://mirrors.ustc.edu.cn/homebrew-core.git</span><br><span class="line"><span class="meta">#</span><span class="bash"> 更换应用软件源</span></span><br><span class="line">cd "$(brew --repo)"/Library/Taps/caskroom/homebrew-cask</span><br><span class="line">git remote set-url origin https://mirrors.ustc.edu.cn/homebrew-cask.git</span><br><span class="line"><span class="meta">#</span><span class="bash"> 更换bottles源</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 使用bash</span></span><br><span class="line">echo 'export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles' &gt;&gt; ~/.bash_profile</span><br><span class="line">source ~/.bash_profile</span><br><span class="line"><span class="meta">#</span><span class="bash"> 使用zsh</span></span><br><span class="line">echo 'export HOMEBREW_BOTTLE_DOMAIN=https://mirrors.ustc.edu.cn/homebrew-bottles' &gt;&gt; ~/.zshrc</span><br><span class="line">source ~/.zshrc</span><br></pre></td></tr></table></figure>
<h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><p>其实基本的用法还是比较简单的，就和绝大多数包管理工具都差不多。包括update,upgrade,install,uninstall,search,list这些基本的操作都是比较简单的，基本上看看brew —help就可以了。</p>
<p>新掌握到的两个工具是</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 清空brew仓库缓存</span></span><br><span class="line">brew cleanup</span><br><span class="line"><span class="meta">#</span><span class="bash"> 检查brew运行环境，大多数情况下可以不用管</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 可以根据里面的指示换会官方源</span></span><br><span class="line">brew doctor</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>MacOS</tag>
        <tag>Homebrew</tag>
      </tags>
  </entry>
  <entry>
    <title>pip的一些操作</title>
    <url>/2020/02/13/deal-with-pip/</url>
    <content><![CDATA[<p>闲来无事简单整理了一下电脑上的pip环境。</p>
<p>由于最近需要学习PyTorch、Tensorflow和Keras的相关知识。需要使用到这几种框架的Python环境。为了更好的管理电脑上的系统环境，对Mac上的pip环境进行了整理。</p>
<p>在Windows主机上我主要使用anaconda管理虚拟环境。而在macOS上使用virtualenv管理虚拟环境。主要原因在于之前一直是在mac上使用virtualenv，后来发现anaconda配置环境更加简洁，但是又懒得在mac上换了，于是造成了现在这样的一个情况。</p>
<a id="more"></a>
<h2 id="清空pip包"><a href="#清空pip包" class="headerlink" title="清空pip包"></a>清空pip包</h2><p>由于将所有的python环境都使用virtualenv在一个专门的目录中进行管理，因此mac上初始python的各种环境就没有必要再留着了，但是删除的时候不太确定到底那些可以删掉。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 查看pip已经安装的各种包</span></span><br><span class="line">pip list</span><br><span class="line"><span class="meta">#</span><span class="bash"> 使用pip删除对应的包</span></span><br><span class="line">pip uninstall module_name</span><br></pre></td></tr></table></figure>
<p>查阅相关资料找到了知乎上<a href="https://www.zhihu.com/question/42673932" target="_blank" rel="noopener">more wang的回答</a>找到了可以清空pip所有包的方法。可以使当前环境中的pip包全部删除，只剩下pip、setuptools、wheel。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 删除pip的所有的包，其实就是列出所有的包然后逐个uninstall</span></span><br><span class="line">pip freeze | grep -v "^-e" | xargs pip uninstall -y</span><br></pre></td></tr></table></figure>
<h2 id="pip换源"><a href="#pip换源" class="headerlink" title="pip换源"></a>pip换源</h2><p>老生常谈的话题了，晚上能找到很多教程，比如<a href="https://yq.aliyun.com/articles/652884?spm=a2c4e.11155472.0.0.57662878hemZW1" target="_blank" rel="noopener">这个</a>。</p>
<h2 id="pip删除缓存"><a href="#pip删除缓存" class="headerlink" title="pip删除缓存"></a>pip删除缓存</h2><p>默认情况下使用pip安装包的时候，会将包缓存在本地的某个目录中，再次安装时会检查缓存，如果缓存存在的话使用缓存安装包，可以节约时间和流量。</p>
<p>可以在安装的时候指定不使用缓存。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">pip install --no-cache-dir nodule_name</span><br></pre></td></tr></table></figure>
<p>搜索发现CSDN上的<a href="https://blog.csdn.net/kangkanglou/article/details/78955298" target="_blank" rel="noopener">一个博客</a>，删除缓存时可以直接删除对应的文件夹。</p>
<blockquote>
<p>Linux and Unix</p>
<p>~/.cache/pip</p>
<p>OS X</p>
<p>~/Library/Caches/pip</p>
<p>Windows</p>
<p>%LocalAppData%\pip\Cache</p>
</blockquote>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>pip</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch学习笔记</title>
    <url>/2020/02/12/python-pytorch/</url>
    <content><![CDATA[<p>PyTorch学习笔记。基于《Deep Learning with PyTorch》，主要为相关语法的笔记。<br>用来自己写代码的时候参考。Dataset部分还需要进一步完善。</p>
<a id="more"></a>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.optim <span class="keyword">as</span> optim</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line"><span class="keyword">import</span> torch.utils.data <span class="keyword">as</span> data</span><br><span class="line"><span class="keyword">from</span> torchvision <span class="keyword">import</span> transforms</span><br></pre></td></tr></table></figure>
<p>torch.nn提供常见的神经网络结构</p>
<p>torch.util.data用于加载数据（使用Dataset和DataLoader）</p>
<p>torch.optim用于优化</p>
<p>torch.nn.DataParallel和torch.distributed用于特定平台的加速计算</p>
<p>torchvision.transforms提供常见图形格式的转换</p>
<h2 id="Tensor"><a href="#Tensor" class="headerlink" title="Tensor"></a>Tensor</h2><h3 id="Basic"><a href="#Basic" class="headerlink" title="Basic"></a>Basic</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 生成tensor</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = torch.Tensor([[<span class="number">1.</span>,<span class="number">2.</span>],[<span class="number">2.</span>,<span class="number">3.</span>],[<span class="number">3.</span>,<span class="number">4.</span>]])</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = torch.FloatTensor([<span class="number">1.</span>,<span class="number">2.</span>,<span class="number">3.</span>])</span><br><span class="line"><span class="comment"># 查看相关信息</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(a.shape)</span><br><span class="line">torch.Size([<span class="number">3</span>,<span class="number">2</span>])</span><br><span class="line"><span class="comment"># tensor是按维度一次顺序储存（先行后列）</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(a.storage())</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(a[<span class="number">0</span>].stride)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(a[<span class="number">0</span>].storage_offset())</span><br><span class="line"><span class="comment"># 通常情况下 obj[i][j] = offset+stride[0]*i+stride[1]*j</span></span><br></pre></td></tr></table></figure>
<p><strong><em>使用下标得到的subtensor指向原数据，如果不希望更改原本的数据，需要使用.clone()得到数据的拷贝。</em></strong></p>
<p>二维向量使用.t()可以转置。实际上储存空间没有变，只更改了stride()高维向量使用.transpose(d_1,d_2)可以交换指定的两个维度值。</p>
<p>使用.contiguous()重新排布张量的内部存储使其成为contiguous tensor可以提高运算效率。</p>
<p>使用.dtype查看元素类型。包括int(8、16、32、64)，uint8，float(16、32、64)，即不同大小的整数和浮点数。默认的Tensor生成的是FloatTensor(float32)。可以在torch.tensor(…, dtype=float)直接指明tensor内部类型。或者使用.float()、.to(float)、.to(dtype=float)转换。</p>
<p>索引方法和python的list差不多。</p>
<p>pytorch的tensor高度兼容numpy。可以使用.numpy()直接转换为numpy的array对象。或者使用torch.from_numpy(array)从numpy的array转化为tensor。</p>
<p>使用torch.save(p,f)和p=torch.load(f)可以存取tensor。通常后缀名为.t</p>
<p>部分pytorch函数支持in-place版本，即a = torch.sqrt(a)可以换成a.sqrt_()，减少空间成本。</p>
<h3 id="Device"><a href="#Device" class="headerlink" title="Device"></a>Device</h3><p>使用CUDA的环境中，可以将tensor保存在GPU中。建议在程序开始是检测系统环境。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">dev = <span class="string">'cuda'</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">'cpu'</span></span><br></pre></td></tr></table></figure>
<p>用于GPU的tensor可以使用torch.tensor(…, device=’cuda’)生成，或者.to(device=’cuda’)将其转换为CUDA的格式。便于使用CUDA计算。</p>
<p>在多GPU的环境中，使用’cuda:0’等来表示所使用的GPU。</p>
<p>（或者os.enrivon[‘CUDA_VISIBLE_DEVICES’] = ‘0’ ? ）</p>
<h2 id="Data"><a href="#Data" class="headerlink" title="Data"></a>Data</h2><h3 id="表格数据"><a href="#表格数据" class="headerlink" title="表格数据"></a>表格数据</h3><p>可以使用csv.reader或者np.loadtxt处理</p>
<h4 id="csv数据"><a href="#csv数据" class="headerlink" title="csv数据"></a>csv数据</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">path = <span class="string">"something.csv"</span></span><br><span class="line">data_numpy = np.loadtxt(path, dtype=np.float32, delimiter=<span class="string">','</span>, skiprows=<span class="number">1</span>)</span><br><span class="line"><span class="comment"># 根据文件情况确定delimiter(, or ;)</span></span><br><span class="line"><span class="comment"># skiprows跳过表头</span></span><br><span class="line">col_list = next(csv.reader(open(path), delimiter=<span class="string">','</span>))</span><br><span class="line"><span class="comment"># 读取到表头</span></span><br><span class="line">data_tensor = torch.from_numpy(data_numpy)</span><br></pre></td></tr></table></figure>
<p><strong><em>数值型target可以看作是一个值，也可以转化为独热码。前者存在大小比较的涵义，后者更多用于分类，没有顺序关系。</em></strong></p>
<p>需要注意原本数据取值范围是基于1（1～max）还是基于0（0～max-1），前者需要减1（scatter是基于0的）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 独热码生成示例</span></span><br><span class="line"><span class="comment"># target是一个long类型的一维tensor,使用unsqueeze增一个维度</span></span><br><span class="line">target_oneshot = torch.zeros(target.shape[<span class="number">0</span>],<span class="number">10</span>)</span><br><span class="line">target_oneshot.scatter_(<span class="number">1</span>, target.unsqueeze(<span class="number">1</span>), <span class="number">1.0</span>)</span><br><span class="line"><span class="comment"># 第一个参数表示独热码维数</span></span><br><span class="line"><span class="comment"># 将第三个参数移动到第二个参数表示的位置上</span></span><br></pre></td></tr></table></figure>
<p>使用zip()可以将多个可迭代数据（list等）打包成元组的list用于迭代。</p>
<h4 id="时间序列数据"><a href="#时间序列数据" class="headerlink" title="时间序列数据"></a>时间序列数据</h4><p>在单个数据的基础上增加了时间维度，具有顺序特性。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">path = <span class="string">"something.csv"</span></span><br><span class="line">data_numpy = np.loadtxt(path, dtype=nnp.float32, delimiter=<span class="string">','</span>, skiprows=<span class="number">1</span>,converters=&#123;<span class="number">1</span>: <span class="keyword">lambda</span> x: float(x[<span class="number">8</span>:<span class="number">10</span>])&#125;)</span><br><span class="line"><span class="comment"># 这里converters表示对第1列用lambda处理</span></span><br><span class="line">data_tensor = torch.from_numpy(data_numpy)</span><br></pre></td></tr></table></figure>
<p>使用.view()可以改变数据维度，使用-1参数推测维度值</p>
<p>使用cat((tensor_1, tensor_2), dim=1)可以将多个tensor按照目标维度连在一起(目标维度上长度为两者之和，其他维度长度不变)。</p>
<p><strong><em>数值型数据可以映射到0～1（(data-min)/(max-min)）或者-1～1或者转变为标准正态分布(data - mean)/std</em></strong></p>
<h3 id="文本数据"><a href="#文本数据" class="headerlink" title="文本数据"></a>文本数据</h3><p>两种方式，针对character的处理和针对word的处理。通常将其转换为独热码。</p>
<p>对于读入的字符串使用.split(‘\n’)切成行。或者.replace(‘\n’,’ ‘).split()直接得到所有的字符。</p>
<p>使用.lower()可以变为小写，便于分析。使用.strip()删去对应的字符，没有参数时删除首尾空格。</p>
<p>针对character的处理可以按照其ASCII码的数值变为独热码。使用ord()可以得到ASCII数字（0～127）。</p>
<p>针对word的处理可以按照字典文件转变为独热码。或者使用embedding。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建字典示例</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'path'</span>) <span class="keyword">as</span> f:</span><br><span class="line">  text = f.read()</span><br><span class="line">punctuation = <span class="string">'.,;:"!?”“_-'</span><span class="string">'</span></span><br><span class="line"><span class="string"># 切分单词</span></span><br><span class="line"><span class="string">word_list = text.lower().replace('</span>\n<span class="string">', '</span> <span class="string">').split()</span></span><br><span class="line"><span class="string"># 除去标点</span></span><br><span class="line"><span class="string">word_list = [word.strip(punctuation) for word in word_list]</span></span><br><span class="line"><span class="string"># 除去重复单词并排序</span></span><br><span class="line"><span class="string">word_list = sorted(set(word_list))</span></span><br><span class="line"><span class="string"># 得到字典索引</span></span><br><span class="line"><span class="string">word2index_dict = &#123;word: i for (i, word) in enumerate(word_list)&#125;</span></span><br></pre></td></tr></table></figure>
<p>使用embedding可以将字典转化为固定长度的浮点数向量。比较理想的方式是将相近含义或距离比较小的单词映射到距离比较近的向量。通常情况理想的embedding是使用神经网络学习生成的。</p>
<p>（torch.nn.Embedding(num, dim))</p>
<h3 id="图像数据"><a href="#图像数据" class="headerlink" title="图像数据"></a>图像数据</h3><p>导入图像的方法很多，包括imageio.imread()，PIL.Image.open()，cv2.imread()等。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> imageio</span><br><span class="line">img_arr = imageio.imread(<span class="string">'path'</span>)</span><br><span class="line">image_tensor = torch.tensor(img_arr)</span><br><span class="line"><span class="comment"># image_tensor = torch.from_numpy(img_arr)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line">image = Image.open(<span class="string">'path'</span>)</span><br><span class="line">iamge_tensor = torch.tensor(np.array(image))</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line">image = cv2.imread(<span class="string">'path'</span>)</span><br><span class="line">image_tensor = torch.tensor(image)</span><br></pre></td></tr></table></figure>
<p>PyTorch处理的图像要求是C * H * W结构，默认图像为H * W * C因此需要torch.transpose()转换一下。如果是视频，应该得到N * C * H * W。</p>
<blockquote>
<p>Tensorflow的图像要求是H * W * C</p>
</blockquote>
<p>读取多张图片时可以创建N*C*H*W的tensor再依次读取并存入。或者使用stack()创建。</p>
<p>图像可以根据网络需求进行缩放，旋转和剪切。</p>
<p>通常要进行适当的正规化。</p>
<h3 id="三维数据"><a href="#三维数据" class="headerlink" title="三维数据"></a>三维数据</h3><p>例如CT扫描数据，具有三个空间维度。</p>
<p>通常使用5D的tensor表示 N*C*D*H*W。D、H、W表示三个空间维度。C表示通道（通常为一维通道，类似灰度图像）</p>
<h2 id="Model"><a href="#Model" class="headerlink" title="Model"></a>Model</h2><p>构建模型</p>
<h3 id="Model-Basic"><a href="#Model-Basic" class="headerlink" title="Model Basic"></a>Model Basic</h3><p>主要成分包括Model(前向传播)、Loss Function、Gradient(反向传播)。</p>
<p>pytorch的tensor提供了requires_grad=True可以自动求导/梯度。指定了自动求导的张量参与的计算会被记录（计算图中的叶子结点），便于求梯度反向传播。拥有.grad成员，默认为None。</p>
<p>指定自动求导的张量参与计算得到的张量（计算图上层节点）拥有.backward()成员，之后原张量（叶子结点）的.grad成员为该张量（上层节点）对应的梯度。</p>
<p><strong><em>注意这里的.grad梯度值为累计得到，使用完毕需要使用.zero_()归零，防止过分累积。同时新的一次计算时使用.detach()将其从计算图中分离</em></strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 自动梯度举例</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">training_loop</span><span class="params">(n_epochs, learning_rate, params, source, target)</span>:</span></span><br><span class="line">  <span class="keyword">for</span> epoch <span class="keyword">in</span> range(<span class="number">1</span>, n_epochs+<span class="number">1</span>):</span><br><span class="line">    <span class="keyword">if</span> params.grad <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">      params.grad.zero_()</span><br><span class="line">    predict = model(source, *params)</span><br><span class="line">    loss = loss_fn(predict, target)</span><br><span class="line">    loss.backward()</span><br><span class="line"></span><br><span class="line">    params = (params - learning_rate * params.grad) \</span><br><span class="line">            .detach().requires_grad_()</span><br><span class="line">  <span class="keyword">return</span> params</span><br></pre></td></tr></table></figure>
<p>同样，显示输出结果等对结果进行操作时必须使用detach()从计算图中分离。</p>
<h3 id="Optim"><a href="#Optim" class="headerlink" title="Optim"></a>Optim</h3><p>模型的参数被传入优化器，每当给定输入后计算反向传播和梯度并按照优化器策略自动更新。</p>
<p>优化器包括zero_grad和step成员，前者清空梯度，后者更新参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.optim <span class="keyword">as</span> optim</span><br><span class="line"><span class="comment"># 创建优化器</span></span><br><span class="line">params = torch.tensor([<span class="number">1.0</span>,<span class="number">0.0</span>], requires_grad=<span class="literal">True</span>)</span><br><span class="line">learning_rate = <span class="number">1e-5</span></span><br><span class="line">optimezer = optim.SGD([params], lr=learning_rate)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用优化器</span></span><br><span class="line">predict = model(source, *params)</span><br><span class="line">loss = loss_fn(predict, target)</span><br><span class="line">optimizer.zero_grad()</span><br><span class="line">loss.backward()</span><br><span class="line">optimizer.step()</span><br></pre></td></tr></table></figure>
<h3 id="Training、Validation、Overfitting"><a href="#Training、Validation、Overfitting" class="headerlink" title="Training、Validation、Overfitting"></a>Training、Validation、Overfitting</h3><ul>
<li>training loss停止下降，可能是网络结构不合适。</li>
<li>training loss和validation loss变化趋势相反，可能是过拟合。</li>
</ul>
<p>使用torch.randperm()可以得到随机排布的参数值用来选择验证集。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">n_samples = origin_data.shape[<span class="number">0</span>]</span><br><span class="line">n_val = int(<span class="number">0.2</span> * n_samples)</span><br><span class="line">shuffled_indices = torch.randperm(n_samples)</span><br><span class="line">train_data = origin_data[shuffled_indices[:-n_val]]</span><br><span class="line">val_data = origin_data[shuffled_indices[-n_val:]]</span><br></pre></td></tr></table></figure>
<p>在计算验证集的损失函数时可以关闭自动梯度功能减轻系统负担</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train_loop</span><span class="params">(n_epochs, optimizer, params, train_s, train_t, val_s, val_t)</span>:</span></span><br><span class="line">  <span class="keyword">for</span> epoch <span class="keyword">in</span> range(<span class="number">1</span>, n_epochs_1):</span><br><span class="line">    train_p = model(train_s, *params)</span><br><span class="line">    train_loss = loss_fn(train_p, train_t)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">      val_p = model(val_s, *params)</span><br><span class="line">      val_loss = loss_fn(val_p, val_t)</span><br><span class="line">      <span class="keyword">assert</span> val.loss.requires_grad == <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    ytain_loss.backward()</span><br><span class="line">    optimizer.step()</span><br></pre></td></tr></table></figure>
<p><strong><em>建议的做法是使用bool参数控制计算图是否开启反向传播和自动梯度</em></strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calc_forward</span><span class="params">(source, target, is_train)</span>:</span></span><br><span class="line">  <span class="keyword">with</span> torch.set_grad_enabled(is_train):</span><br><span class="line">    predict = model(source, *params)</span><br><span class="line">    loss = loss_fn(predict, target)</span><br><span class="line">  <span class="keyword">return</span> loss</span><br></pre></td></tr></table></figure>
<h3 id="调参思路"><a href="#调参思路" class="headerlink" title="调参思路"></a>调参思路</h3><ol>
<li>learning_rate过大，在极值点附近震荡或者发散。=&gt; 调节学习率。</li>
<li>梯度的不同分量差距太大，相同的学习率对于梯度的不同分量学习效果不同。=&gt; 尽量保证数量级相同。=&gt; 例如正规化到标准正态分布。</li>
<li>epoch数量不够，尚未达到稳定点。=&gt; 增大epoch次数。</li>
<li>优化器不合适。=&gt; 换用不同优化器。 =&gt; 对应更改学习率。</li>
</ol>
<h2 id="NN-Module"><a href="#NN-Module" class="headerlink" title="NN Module"></a>NN Module</h2><p>构建神经网络</p>
<h3 id="NN-Basic"><a href="#NN-Basic" class="headerlink" title="NN Basic"></a>NN Basic</h3><p>最简单的Neuron := Linear Transformation + Non-Linear Function(Activation) 简单来说$o = f(w * x + b)$。</p>
<p>网络必须是一个继承自nn.Module的类类型，而且不能是List或者Dict。如果需要的话使用nn.ModuleList或nn.ModuleDict。</p>
<p>数据通常为 N(umber)*C(hannel)*…结构。对应的nn中的模型参数通常为in_channels和out_channels。</p>
<p>单独使用nn中定义的module时使用optim的参数为model的parameters()成员。named_paramster()成员可以得到参数名。</p>
<p>nn中定义了很多Loss Function。例如nn.MSELoss()。</p>
<p>使用nn.Sequential()可以将多层捆绑为一个整体。使用OrderedDict()可以为每一层指定名称，默认使用0-based数字。使用.引导的层次命名访问每一层的数据成员。</p>
<h3 id="框架"><a href="#框架" class="headerlink" title="框架"></a>框架</h3><p>使用nn.Module派生的子类，定义.forward()函数给定输入计算得到输出，动态建立计算图即可用于自动求导和优化。</p>
<p>nn包括的没有参数的网络结构，比如这里的Tanh()、ReLU()等可以使用nn.functional实现，减轻网络结构的复杂度。</p>
<p>实际上大部分的网络都可以在functional中找到对应的版本，计算输出时使用函数参数表示网络参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 代码中省略参数</span></span><br><span class="line"><span class="keyword">from</span> torch.nn <span class="keyword">import</span> Module, Conv2d, Dropout2d, Linear</span><br><span class="line"><span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">model_name</span><span class="params">(Module)</span>:</span></span><br><span class="line">  <span class="comment"># 使用pytorch声明网络结构</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">    <span class="comment"># 使用torch.nn中的常见结构构建网络</span></span><br><span class="line">    <span class="comment"># 声明网络的成分</span></span><br><span class="line">    super().__init__()</span><br><span class="line">    self.conv = Conv2d(...)</span><br><span class="line">    self.drop = Dropout2d(...)</span><br><span class="line">    self.fc1 = Linear(...)</span><br><span class="line">    self.fc2 = Linear(...)</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self,x)</span>:</span></span><br><span class="line">    <span class="comment"># 执行前向传播 指定了网络连接结构</span></span><br><span class="line">    x = F.relu(F.max_pool2d(self.drop(self.conv(x)), ...))</span><br><span class="line">    x = x.view(...)</span><br><span class="line">    x = F.relu(self.fc1(x))</span><br><span class="line">    x = F.dropout(x,...)</span><br><span class="line">    x = F.log_softmax(self.fc2(x))</span><br><span class="line">    <span class="keyword">return</span> x</span><br></pre></td></tr></table></figure>
<p>可以直接声明比较复杂的结构，前向传播的时候比较简单</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 例如将卷积激活池化合在一起</span></span><br><span class="line">self.conv = torch.nn.Sequential(</span><br><span class="line">  torch.nn.Conv2d(...),</span><br><span class="line">  torch.nn.ReLU(...),</span><br><span class="line">  roech.nn.MaxPool2d(...))</span><br><span class="line"><span class="comment"># 前向传播可以是</span></span><br><span class="line">conv_out = self.conv(x)</span><br></pre></td></tr></table></figure>
<h2 id="Dataset"><a href="#Dataset" class="headerlink" title="Dataset"></a>Dataset</h2><h3 id="Dataset-Basic"><a href="#Dataset-Basic" class="headerlink" title="Dataset Basic"></a>Dataset Basic</h3><p>使用torch.utils.data的Dataset和DataLoader抽象类可以多线程数据预读取和批量加载。</p>
<p>常见的数据集和训练好的模型大多已经封装在torchvision.datasets之中。</p>
<p>数据正规化可以方便处理，根据数据的特性确定正规化参数。</p>
<p>使用softmax输出的结果选择最大值作为分类输出。</p>
<p>分类可以使用NLLLoss()作为损失函数。</p>
<h3 id="Dataset-amp-DataLoader"><a href="#Dataset-amp-DataLoader" class="headerlink" title="Dataset &amp; DataLoader"></a>Dataset &amp; DataLoader</h3><p>Dataset主要的两个成员函数__getitem__()和__len__()。</p>
<p>DataLoader提供对于Dataset的处理，包括batch_size、shuffle、num_workers（子线程数）。</p>
<p>DataLoader得到的是可迭代对象，可以使用next(loader)来获取下一批数据，或者使用for循环（for i, data in enumerate(loader)）的方法读取数据用于训练。</p>
<p>通常需要使用transforms来进行预处理，包括ToTensor()、Normalize()、RandomHorizontalFlip()、RandomRotationn()、Resize()等处理方式。</p>
<h3 id="Hand-on"><a href="#Hand-on" class="headerlink" title="Hand-on"></a>Hand-on</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.utils.data <span class="keyword">as</span> data</span><br><span class="line"><span class="keyword">import</span> torchvision.transfroms <span class="keyword">as</span> transforms</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">dataset_name</span><span class="params">(data.Dataset)</span>:</span></span><br><span class="line">  <span class="comment"># 创建数据集</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,...)</span>:</span></span><br><span class="line">    self.length = ...</span><br><span class="line">    self.data = [...]</span><br><span class="line">    self.transforms = transforms.Compose([</span><br><span class="line">      transforms.ToTensor(),</span><br><span class="line">      transforms.Normalize(mean=[...], std=[...]),</span><br><span class="line">      transforms.Resize([,,,])</span><br><span class="line">    ])</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__getitem__</span><span class="params">(self, item)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> self.transforms(self.data[item])</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__len__</span><span class="params">(self)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> self.length</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用数据集</span></span><br><span class="line">data_set = dataset_name(...)</span><br><span class="line">data_loader = data.Dataloader(data_set,...)</span><br></pre></td></tr></table></figure>
<h2 id="Visualize"><a href="#Visualize" class="headerlink" title="Visualize"></a>Visualize</h2><p>可以使用visdom工具进行pytorch的可视化。</p>
<p>也有使用tensorboardx的可视化方案，功能比较强大， 和tensorflow中的基本相同，<a href="https://github.com/zergtant/pytorch-handbook/blob/master/chapter4/4.2.2-tensorboardx.ipynb" target="_blank" rel="noopener">使用方法参见pytorch-handbook</a>。</p>
<h3 id="Visual-Hand-on"><a href="#Visual-Hand-on" class="headerlink" title="Visual Hand-on"></a>Visual Hand-on</h3><p>使用visdom监控loss或者accuracy的时候</p>
<p>可以使用visdom的追加值绘制曲线</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> visdom <span class="keyword">import</span> Visdom</span><br><span class="line"><span class="comment"># 创建一个Visdom的对象</span></span><br><span class="line">env = Visdom()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始点</span></span><br><span class="line">x,y=<span class="number">0</span>,<span class="number">0</span></span><br><span class="line">pane= env.line(</span><br><span class="line">    X=np.array([x]),</span><br><span class="line">    Y=np.array([y]),</span><br><span class="line">    opts=dict(title=<span class="string">'dynamic data'</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 追加新的数据点</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">    time.sleep(<span class="number">1</span>) <span class="comment"># 每隔一秒钟打印一次数据</span></span><br><span class="line">    x+=i</span><br><span class="line">    y=(y+i)*<span class="number">1.5</span></span><br><span class="line">    print(x,y)</span><br><span class="line">    env.line(</span><br><span class="line">        X=np.array([x]),</span><br><span class="line">        Y=np.array([y]),</span><br><span class="line">        win=pane, <span class="comment"># win参数确认使用哪一个pane</span></span><br><span class="line">        update=<span class="string">'append'</span>) <span class="comment"># 增加一个数据点</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
        <tag>PyTorch</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Attention is All You Need</title>
    <url>/2020/02/04/transformer/</url>
    <content><![CDATA[<p>论文题目: Attention is All You Need</p>
<p>开源项目地址<a href="https://github.com/tensorflow/tensor2tensor" target="_blank" rel="noopener">transformer</a></p>
<a id="more"></a>
<h2 id="主要内容"><a href="#主要内容" class="headerlink" title="主要内容"></a>主要内容</h2><p>文章实现了一种仅使用attention机制，完全去除卷积网络和循环网络的结构，称为Transformer网络。</p>
<p>在实现比较高的准确性的基础上，具有较低的计算成本。</p>
<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>目前常用的序列处理模型通常使用一个编码器和一个解码器，在两者的连接中使用attention机制，而且编码器和解码器的实现使用复杂的CNN和RNN。</p>
<p>由于RNN结构引入的时序逻辑不利于并行计算，引入比较高的计算成本。因此Transformer结构中除去了RNN而仅使用attention结构来学习序列前后的相关性。</p>
<p>在transformer中仅使用self-attention机制来学习序列中不同元素之间的关系。</p>
<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>使用self-attention和full connect实现。</p>
<p>编码器的实现中，使用若干个块，每个块包括一个multi-head attention层和一个全联接的feed foward层，其中每一层都引入了残差连接和正规化，构成$LayerNorm(x + Sublayer(x))$的结构。</p>
<p>解码器的实现中，同样使用若干个块，每个块为编码器的基本块之前加上一个masked multi-head attention结构。同时将上一时刻的输出作为下一个时刻的输入，使得输出仅由之前的输入决定。</p>
<p>Scaled Dot-Product Attention的结构为将Q(uery)、K(ey)、V(alue)映射得到输出。输入为$d_k$维的Key向量和$d_v$维的Value向量，计算方法为矩阵相乘的结果缩放后再Softmax得到Value元素对应的权重，即$Attention(Q,K,V) = Softmax(\frac{QK^T}{\sqrt{d_k}})V$。使用矩阵乘法的实现计算更快。</p>
<p>Multi-head Attnetion为多个Attention层组合得到。通过将V、K、Q线性投影到h个attention网络，将其结果通过concat组合之后再线性投影得到结果，即$MultiHead(Q,K,V) = Concat(head_1,hed_2…)W^O$，其中$head_i = Attention(QW^Q_i, KW^K_i, VW^V_i)$。</p>
<p>注意解码器的Q来自解码器上一输出经过Masked Multi-Head Attention的结果，K、V来自编码器的输出。而编码器的Q、K、V来自上一层的输出。</p>
<p>在Masked Multi-Head Attention中，SoftMax的输出被屏蔽（设置为$-\infty$）。</p>
<p>在Attention之后的Feed Forward网络中，使用两次ReLU得到$FFN(x) = max(0, xW_1+b_1)W_2+b_2$。</p>
<p>对于输入，还需要进行embedding和positional encoding操作。使用learned embeddings将输入转化为向量。后者引入顺序和位置信息，使用$PE<em>{(pos,2i)} = sin(pos/10000^{2i/d</em>{model}})$和$PE<em>{(pos,2i+1)} = cos(pos/10000^{2i/d</em>{model}})$，其中pos为位置，i为维数。</p>
<p>对于输出，使用线性转换（learned linear transformation）和softmax将输出转变为概率。这里的线性转换和输入的embeddings使用相同的权重（embedding中乘$\sqrt{d_{model}}$,即向量规模的平方根）。</p>
<h2 id="评价"><a href="#评价" class="headerlink" title="评价"></a>评价</h2><p>使用attention相比直接使用卷积或循环，计算复杂度比较低，同时更有利于并行计算。</p>
<p>同时attention结构更有助于学习长句子，能学习到距离较远的信息。</p>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
        <tag>Machine Translate</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Deep Residual Learning for Image Recognition</title>
    <url>/2020/02/03/resnet/</url>
    <content><![CDATA[<p>Deep Residual Learning for Image Recognition</p>
<a id="more"></a>
<h2 id="主要内容"><a href="#主要内容" class="headerlink" title="主要内容"></a>主要内容</h2><p>构建了一种残差网络结构，有利于深度神经网络的训练，同时证明了该残差网络结构更容易优化并且在深度增加时仍能实现较高的准确率。同时训练了一个152层的网络用语图像识别。</p>
<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>传统的用于图像识别的DCNN通过增加深度可以提高识别性能。但是会出现梯度爆炸/消失的现象（vanishing/exploding gradient）。</p>
<p>通过引入正则化等方法可以使深度网络实现收敛（SGD）。尽管可以收敛，但深度增加时会出现性能下降（degradation）。</p>
<p>因此文中实现了一种残差网络deep residual learning，用来解决degradation问题。</p>
<p>主要方式为：要学习得到H(x)，构造F(x)=H(x)-x并学习，最终得到F(x)+x即为所学习的目标。这里的+x可以使用短接（shortcut connection：跳过若干层的连接）来实现。这样的操作相当于恒等映射，没有引入新的参数或计算复杂度。</p>
<img src="/2020/02/03/resnet/block.png" title="残差网络模块">
<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>通过使用短接可以使得非线性的网络更好的拟合恒等映射，即令网络参数为0。</p>
<p>因此构造的网络形如$y = F(x,{W_i})+x$。使用两层网络和ReLU构造网络块得到$F = W_2\sigma(W_1x)$，其中$\sigma$表示ReLU，最终的输出为$\sigma{y}$。可以添加一个矩阵$W_s$用于将F的输出和x的规模对齐。</p>
<p>这里F的结构可以使用两层或三层网络，但是不能只有一层（实际上构成一个线性单元）。同时每一层的结构可以是全联接层也可以是卷积层。</p>
<p>类比VGG-19网络，构造一个34层的深度卷积神经网络，以及其对应的深度残差网络。其中后河通过在前者的网络结构中每隔两层添加一个短路连接得到。</p>
<h2 id="评价"><a href="#评价" class="headerlink" title="评价"></a>评价</h2><p>相对于普通的深度网络，深度残差网络更容易训练。</p>
<p>相对于相同深度的网络，深度残差网络可以得到更高的准确度。</p>
<p>当网络深度增加时，使用深度残差网络可以缓减错误率上升的情况。</p>
<p>短路连接使用不同的方式（直接映射/规模不同时使用矩阵/全部使用矩阵）可以带来轻微的性能提升，但直接映射的复杂度比较低。</p>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
        <tag>Computer Vision</tag>
      </tags>
  </entry>
  <entry>
    <title>TMUX 使用</title>
    <url>/2020/02/03/tmux/</url>
    <content><![CDATA[<p>TMUX 使用</p>
<p>参考网上一些博客的教程<sup><a href="#fn_1" id="reffn_1">1</a></sup>。同时参考了一些自主定制tmux功能的教程<sup><a href="#fn_2" id="reffn_2">2</a></sup>。</p>
<p>仅用于使用参考。</p>
<a id="more"></a>
<h2 id="关键概念"><a href="#关键概念" class="headerlink" title="关键概念"></a>关键概念</h2><ul>
<li><p>会话 session：用户和终端的交互。允许创建多个会话，关闭时内部进程不会终止，允许再次进入，直至被终止。</p>
</li>
<li><p>窗口 window：每个会话可以创建多个窗口，可以相互切换。</p>
</li>
<li><p>窗格 pane：每个窗口可以创建多个窗格，用于同时处理多个命令行界面。</p>
</li>
<li>前缀：所有操作的快捷键要求以ctrl+b启动。</li>
</ul>
<h2 id="安装和启动"><a href="#安装和启动" class="headerlink" title="安装和启动"></a>安装和启动</h2><p>使用对应平台的包管理工具直接安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动第一个会话</span></span><br><span class="line">$ tmux</span><br><span class="line"><span class="comment"># 查看快捷键</span></span><br><span class="line">$ tmux list-keys</span><br><span class="line"><span class="comment"># 查看命令</span></span><br><span class="line">$ tmux list-commands</span><br><span class="line"><span class="comment"># 查看会话信息</span></span><br><span class="line">$ tmux info</span><br></pre></td></tr></table></figure>
<h2 id="会话管理"><a href="#会话管理" class="headerlink" title="会话管理"></a>会话管理</h2><div class="table-container">
<table>
<thead>
<tr>
<th>功能</th>
<th>命令</th>
<th>快捷键</th>
<th>注意</th>
</tr>
</thead>
<tbody>
<tr>
<td>创建会话</td>
<td>tmux new -s \<name\></name\></td>
<td></td>
<td>默认会话名为基于0的数字</td>
</tr>
<tr>
<td>分离会话</td>
<td>tmux detach</td>
<td>ctrl+b d</td>
<td>会话和进程在后台运行</td>
</tr>
<tr>
<td>查看会话</td>
<td>tmux ls</td>
<td>ctrl+b s</td>
<td>查看被分离的会话</td>
</tr>
<tr>
<td>接入会话</td>
<td>tmux attach -t \<name\></name\></td>
<td></td>
<td></td>
</tr>
<tr>
<td>杀死会话</td>
<td>tmux kill-session -t \<name\></name\></td>
<td></td>
<td>建议在会话内部使用exit</td>
</tr>
<tr>
<td>切换会话</td>
<td>tmux switch -t \<name\></name\></td>
<td></td>
<td></td>
</tr>
<tr>
<td>重命名会话</td>
<td>tmux rename-session -t \<name> \<new name></new></name></td>
<td>ctrl+b $</td>
</tr>
</tbody>
</table>
</div>
<h2 id="窗格操作"><a href="#窗格操作" class="headerlink" title="窗格操作"></a>窗格操作</h2><div class="table-container">
<table>
<thead>
<tr>
<th>功能</th>
<th>命令</th>
<th>快捷键</th>
<th>注意</th>
</tr>
</thead>
<tbody>
<tr>
<td>上下划分</td>
<td>tmux split_window</td>
<td>ctrl+b “</td>
<td></td>
</tr>
<tr>
<td>左右划分</td>
<td>tmux split_window -h</td>
<td>ctrl+b %</td>
<td></td>
</tr>
<tr>
<td>移动光标</td>
<td>tmux select-pane -[UDLR]</td>
<td>ctrl+b 方向键</td>
<td>参数表示上下左右移动</td>
</tr>
<tr>
<td>交换窗格位置</td>
<td>tmux swap-pane -[UDLR]</td>
<td>ctrl+b [{ } ctrl+o alt+o]</td>
<td>参数表示上下左右移动交换</td>
</tr>
<tr>
<td>切换窗格</td>
<td></td>
<td>ctrl+b [o;]</td>
<td>参数表示按编号上下移动</td>
</tr>
<tr>
<td>关闭窗格</td>
<td></td>
<td>ctrl+b x</td>
<td>也可以在窗格内使用exit</td>
</tr>
<tr>
<td>窗格拆分为窗口</td>
<td></td>
<td>ctrl+b ！</td>
<td></td>
</tr>
<tr>
<td>调整窗格大小</td>
<td></td>
<td>ctrl+b ctrl+方向键</td>
<td></td>
</tr>
<tr>
<td>查看窗格序号</td>
<td></td>
<td>ctrl+b q</td>
</tr>
</tbody>
</table>
</div>
<h2 id="窗口操作"><a href="#窗口操作" class="headerlink" title="窗口操作"></a>窗口操作</h2><div class="table-container">
<table>
<thead>
<tr>
<th>功能</th>
<th>命令</th>
<th>快捷键</th>
<th>注意</th>
</tr>
</thead>
<tbody>
<tr>
<td>创建窗口</td>
<td>tmux new-window -n \<name\></name\></td>
<td>ctrl+b c</td>
<td></td>
</tr>
<tr>
<td>切换窗口</td>
<td>tmux select-window -t \<name\></name\></td>
<td></td>
<td></td>
</tr>
<tr>
<td>重命名窗口</td>
<td>tmux rename-window \<name\></name\></td>
<td>ctrl+b ,</td>
<td>为当前窗口重命名</td>
</tr>
<tr>
<td>切换窗口</td>
<td></td>
<td>ctrl+b [pn]</td>
<td>表示按编号上下切换</td>
</tr>
<tr>
<td>切换窗口</td>
<td></td>
<td>ctrl+b \<number></number></td>
<td>切换到指定窗口</td>
</tr>
<tr>
<td>选择窗口</td>
<td></td>
<td>ctrl+b w</td>
</tr>
</tbody>
</table>
</div>
<blockquote id="fn_1">
<sup>1</sup>. <a href="http://www.ruanyifeng.com/blog/2019/10/tmux.html#comment-text" target="_blank" rel="noopener">阮一峰的日志</a><a href="#reffn_1" title="Jump back to footnote [1] in the text."> &#8617;</a>
</blockquote>
<blockquote id="fn_2">
<sup>2</sup>. <a href="https://blog.csdn.net/williamyuyuyu/article/details/79283374#1更改前缀" target="_blank" rel="noopener">Tmux教程——打造完美的Linux终端</a><a href="#reffn_2" title="Jump back to footnote [2] in the text."> &#8617;</a>
</blockquote>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>tmux</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Effective Approaches to Attention-based Neural Machine Translation</title>
    <url>/2020/02/02/attention/</url>
    <content><![CDATA[<p>论文笔记:Effective Approaches to Attention-based Neural Machine Translation</p>
<a id="more"></a>
<p>开源项目地址：<a href="http://nlp.stanford.edu/projects/nmt" target="_blank" rel="noopener">stanford.edu</a>或者<a href="https://github.com/tensorflow/nmt" target="_blank" rel="noopener">github</a></p>
<h2 id="主要内容"><a href="#主要内容" class="headerlink" title="主要内容"></a>主要内容</h2><p>NMT使用RNN的架构来实现序列到序列的学习。Attention机制被广泛用于训练神经网络并提升其性能。</p>
<p>因此可以使用attention机制通过聚焦于输入序列的某一部分来提高NMT的性能。</p>
<p>文中构建了两种使用attention的NMT模型：global模型和locol模型。前者注意输入序列全体，后者每次针对输入序列的一个子集。两种模型的结构都很简单，而且后者的计算成本更低。</p>
<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>传统NMT模型包括一个编码器和一个解码器，前者将输入映射到固定长度的向量$\mathbf{S}$，后者将向量映射到输出序列。</p>
<p>其具体实现有CNN-RNN结构，也有RNN-RNN结构（包括LSTM-LSTM，GRU-GRU等）。</p>
<p>其他的实现中使用$\mathbf{S}$作为初始化解码器的工具，而使用attention机制的模型在整个翻译过程中都将$\mathbf{S}$用做参考。</p>
<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>使用深度LSTM作为编码器和解码器。</p>
<p>目标函数为$J = \sum _{(x,y)\in \mathbb{D}}-logp(y|x)$其中$\mathbb{D}$为训练数据。</p>
<p>globel和locol两种不同的模型区别在于内容向量$c_t$的生成方式不同。当内容向量$c_t$生成后，即可计算输出。</p>
<p>即首先计算attentional hidden state:$\tilde{h}_t = tanh(W_c [c_t;h_t])$。</p>
<p>然后得到预测向量 $p(y<em>t|y</em>{&lt; t}, x) = softmax(W_s \tilde{h}_t)$ 。</p>
<h3 id="global-attention"><a href="#global-attention" class="headerlink" title="global attention"></a>global attention</h3><p>考虑到编码器的所有隐含状态，通过编码器状态$\bar{h}_s$和解码器状态$h_t$通过score()函数得到global align weights即$a_t$，这里的向量$a_t$为可变长度的。最终使用$a_t$对$\bar{h}_s$加权得到$c_t$从而得到$\hat{h}_t$.</p>
<p>具体score函数见文献原文。</p>
<p>global attention考虑到所有的输入符号，计算成本比较高，而且无法处理较长的序列。</p>
<h3 id="locol-attention"><a href="#locol-attention" class="headerlink" title="locol attention"></a>locol attention</h3><p>借鉴soft和hard attention的思想<sup><a href="#fn_2" id="reffn_2">2</a></sup>，使用加窗的方式，考虑一部分的隐状态。</p>
<p>首先确定位置$p_t$，然后根据位于$[p_t-D,p_t+D]$的编码器状态使用相同的score()函数得到$a_t$。不同的是这里的$a_t$为固定长度($2D+1$)的向量。</p>
<p>关于位置$p_t$的确定有不同的方法。</p>
<p>locol-m方法：令$p_t = t$,使用前述方法计算$a_t$。</p>
<p>locol-p方法：通过学习到的参数预测，即令$p_t = S·sigmoid(v_p^Ttanh(W_p h_t))$，其中 $v_p$ 和 $W_p$为学习得到的参数，S为输入序列长度。同时计算$a_t = align(h_t,\bar{h}_s) exp(- \frac{(s-p_t)^2}{2\sigma^2})$，其中$\sigma = \frac{D}{2}$。</p>
<h3 id="input-feeding"><a href="#input-feeding" class="headerlink" title="input-feeding"></a>input-feeding</h3><p>将上一输出作为输入传入网络用于产生下一输出。</p>
<h3 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h3><p>使用WMT的英语-德语双向翻译任务，共4.5M句子，包括116M英语单词110M德语单词。</p>
<p>选择其中的50k单词作为训练的单词表。每个LSTM具有四层，每层1000单元，每个单元为1000维张量。</p>
<h2 id="评价"><a href="#评价" class="headerlink" title="评价"></a>评价</h2><p>global attention和local attention相比其他人的实现<sup><a href="#fn_1" id="reffn_1">1</a></sup>更加简单</p>
<p>通过input-feeding使得之前学习得到的结果得到充分利用，这一方法也可以用于非attention的其他RNN架构的网络中。</p>
<p>最终训练结果在WMT上得分23.0超过了最好的系统。</p>
<p>论文实现的系统具有更低的test cost。可以更好的处理长句子。</p>
<blockquote id="fn_1">
<sup>1</sup>. <a href="https://arxiv.org/pdf/1409.0473" target="_blank" rel="noopener">Neural Machine Translation by Jointly Learning to Align and Translate</a><a href="#reffn_1" title="Jump back to footnote [1] in the text."> &#8617;</a>
</blockquote>
<blockquote id="fn_2">
<sup>2</sup>. <a href="http://proceedings.mlr.press/v37/xuc15.pdf" target="_blank" rel="noopener">Show,Attend and Tell:Neural Image Caption Generation with Visual Attention</a><a href="#reffn_2" title="Jump back to footnote [2] in the text."> &#8617;</a>
</blockquote>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
        <tag>Machine Translate</tag>
      </tags>
  </entry>
  <entry>
    <title>【论文】Sequence to Sequence Learning with Neural Networks</title>
    <url>/2020/02/02/seq2seq/</url>
    <content><![CDATA[<p>论文笔记:Sequence to Sequence Learning with Neural Networks</p>
<a id="more"></a>
<h2 id="主要内容"><a href="#主要内容" class="headerlink" title="主要内容"></a>主要内容</h2><p>使用一个深度LSTM网络将输入序列映射到固定长度的向量，再使用另一个深度LSTM网络将该向量映射（decode）到目标序列。</p>
<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>DNN在语音识别和视觉识别领域有非常好的效果，但是仅能用于输入和输出可以化为（encode）相同的固定规模（known and fixed）的向量。很多实际问题中输入和输出需要用未知长度的向量表示。</p>
<p>LSTM可以在具有长期时间相关性（long range temporal dependencies）的数据中具有比较好的学习效果，因此用于在输入序列和相应的输出序列之间进行学习。</p>
<p>同时通过在训练中反转输入序列引入短期依赖便于训练和优化。</p>
<h2 id="实现方式"><a href="#实现方式" class="headerlink" title="实现方式"></a>实现方式</h2><h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h3><p>使用单个RNN由给定输入序列$(x_1,x_2,…,x_t)$计算输出序列$(y_1,y_2,…,y_t)$的方法如下：</p>
<p>$h<em>t = sigm(W^{hx}x_t + W^{hh}h</em>{t-1})$、$y_t = W^{yh}h_t$。但是并不能用于不同长度的输入和输出序列。</p>
<p>因此使用两个LSTM取代两个RNN来实现长期时间相关性的学习。</p>
<p>在LSTM中使用条件概率的计算方法如下：</p>
<p>$p(y<em>1,…,y</em>{T’}|x<em>1,…,x_T) = \prod</em>{t=1}^{T’}p(y<em>t|v,y_1,…,y</em>{t-1})$，其中v是输入序列的一种表示。</p>
<p>具体实现中，==使用两个深度LSTM网络，每个网络具有4层，用于训练的输入序列反转。==</p>
<p>每个LSTM通过最大化对数几率进行训练，目标函数为$\frac{1}{|S|}\sum_{(T,S)\in \mathbb{S}} logp(T|S)$，其中$\mathbb{S}$为训练集。</p>
<p>预测时依据$\hat{T} = arg max_{\mathbf{T}} p(T|S)$，使用自左向右的Beam Search算法<sup><a href="#fn_1" id="reffn_1">1</a></sup>得到翻译结果。</p>
<p>输入序列反转后，训练的性能提高（原文使用“最小时滞 minimal time lag”加以解释：输入序列和输出序列的开头几个单词的距离减小）。可以认为输出序列的前半部分正确率较高而后半部分表现比较差。</p>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>使用WMT’14 英语-法语数据库进行训练。</p>
<p>每个LSTM网络有4层，每层1000个单元，每个数据使用1000维张量表示。</p>
<p>输入词汇量160000，输出词汇量80000。输出时使用8000输入的softmax。共有384M参数。</p>
<p>为了防止梯度爆炸，每一个batch训练结束后对梯度正规化。</p>
<h2 id="评价"><a href="#评价" class="headerlink" title="评价"></a>评价</h2><h3 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h3><p>训练结果使用BLEU打分，在WMT‘14数据库上得到37.0分</p>
<h3 id="结果评价"><a href="#结果评价" class="headerlink" title="结果评价"></a>结果评价</h3><p>使用深度LSTM网络实现的sequence到sequence模型在机器翻译问题上取得了不错的效果。</p>
<p>其在较长的句子上性能比较好。并且可以处理主动语态和被动语态。</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><blockquote id="fn_1">
<sup>1</sup>. <a href="https://www.jianshu.com/p/c7aab93b944d" target="_blank" rel="noopener">Beam Search</a><a href="#reffn_1" title="Jump back to footnote [1] in the text."> &#8617;</a>
</blockquote>
]]></content>
      <categories>
        <category>论文阅读</category>
      </categories>
      <tags>
        <tag>Deep Learning</tag>
        <tag>Machine Translate</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第五章:神经网络</title>
    <url>/2020/01/31/melonC5/</url>
    <content><![CDATA[<p>西瓜书第五章</p>
<a id="more"></a>
<h2 id="神经元模型"><a href="#神经元模型" class="headerlink" title="神经元模型"></a>神经元模型</h2><p>M-P神经元模型：n个其他单元的输入信号通过带权重的连接，总的输入值与阈值比较，通过激活函数处理产生输出。</p>
<p>常见的激活函数有阶跃函数和Sigmoid函数$\frac{1}{1+e^{-x}}$。</p>
<h2 id="感知机与多层网络"><a href="#感知机与多层网络" class="headerlink" title="感知机与多层网络"></a>感知机与多层网络</h2><p>感知机为两层网络，一层输入一层输出。学习目标为权重和阈值，其中阈值的学习也可以转化为权重。</p>
<p>学习过程为$w_i \leftarrow w_i + \Delta w_i$，$\Delta w_i = \eta(y-\hat{y})x_i$，其中$\eta$为学习率。</p>
<p>可以证明线性可分的问题（可以使用线性超平面划分）使用感知机学习可以收敛。</p>
<p>解决线性不可分的问题要使用多层神经元，在输入和输出之间添加隐含层。</p>
<p>得到多层前馈神经网络，即不存在同层连接和跨层连接。</p>
<h2 id="误差逆传播算法"><a href="#误差逆传播算法" class="headerlink" title="误差逆传播算法"></a>误差逆传播算法</h2><p>backpropagation算法，用于训练多层前馈神经网络。</p>
<p>通常情况下，训练集中的输入数据和输出数据为多维向量（离散属性的处理）。</p>
<p>BP是迭代学习算法，每一轮中使用广义感知机的学习规则$v \leftarrow v+\Delta v$更新参数。</p>
<p>使用梯度下降学习策略，以目标的负梯度方向调整参数，使用学习率调节步长。</p>
<p>输出值相对权值的梯度的计算根据链式法则由激活函数和每一层的输入值决定。</p>
<p>上述标准BP算法每次针对一个样例更新参数。</p>
<p>累计BP算法读取一遍训练集（epoch）并得到累计误差，从而进行参数更新，更新频率比较低。</p>
<p>为了防止过拟合，可以采用两种策略</p>
<p>早停：训练集分出一部分验证集，训练集误差降低而验证集误差升高时停止训练。</p>
<p>正则化：目标函数中增加一个表示网络复杂度的成分，例如权值平方和得到$E = \lambda \frac{1}{m}\sum E_k+(1-\lambda)\sum w_i^2$。</p>
<h2 id="全局最小与局部极小"><a href="#全局最小与局部极小" class="headerlink" title="全局最小与局部极小"></a>全局最小与局部极小</h2><p>误差目标函数可以看作参数值的函数。神经网络的训练过程为寻找最优的一组参数值。</p>
<p>存在全局最小值和局部极小值，后者表示邻域内的误差函数都大于该点。</p>
<p>梯度为0的点可以是局部极小值点。基于梯度的搜索策略可能落入局部极小值点。</p>
<p>常见跳出局部最优值的方法有：</p>
<p>多组不同的参数初始化：从不同的初始值开始搜索。</p>
<p>模拟退火：参数更新时以一定的概率接受次优解（此概率逐渐减小）</p>
<p>随即梯度下降法</p>
<h2 id="其他常见神经网络"><a href="#其他常见神经网络" class="headerlink" title="其他常见神经网络"></a>其他常见神经网络</h2><h3 id="RBF网络"><a href="#RBF网络" class="headerlink" title="RBF网络"></a>RBF网络</h3><p>径向基函数网络，通常为单隐层网络，使用径向基函数作为激活函数。</p>
<p>高斯径向基函数$\rho(x,c_i) = e^{-\beta_i||x-c_i||^2}$。</p>
<h3 id="ART网络"><a href="#ART网络" class="headerlink" title="ART网络"></a>ART网络</h3><p>自适应谐振理论网络。使用竞争学习的思想进行无监督学习。</p>
<p>竞争学习中每一时刻仅有一个竞争获胜的神经元被激活，其他的被抑制。</p>
<p>识别层每个神经元记录一个模式类。计算输入向量与每个神经元的距离值，如果距离小于设定的阈值则激活某一个神经元并抑制其他的，同时更新参数。如果距离都大于阈值则将输入向量设置为新的一类。</p>
<p>ART网络缓解了可塑性-稳定性窘境。可以增量学习和在线学习。</p>
<h3 id="SOM网络"><a href="#SOM网络" class="headerlink" title="SOM网络"></a>SOM网络</h3><p>自组织(特征)映射网络。将高维数据映射到低维空间并保持数据点之间的拓扑关系。</p>
<p>输出层神经元按矩阵形式排列。</p>
<p>训练时根据输入选择激活的神经元，同时更新权值使临近神经元与当前样本距离减小。</p>
<h3 id="级联相关网络"><a href="#级联相关网络" class="headerlink" title="级联相关网络"></a>级联相关网络</h3><p>属于一种结构自适应网络，网络结构也是学习目标之一。</p>
<p>初始情况只有输入和输出，训练过程中逐渐增加隐层单元，通过最大化新单元的输出和误差函数的相关性来训练参数。</p>
<h3 id="Elman网络"><a href="#Elman网络" class="headerlink" title="Elman网络"></a>Elman网络</h3><p>是最常用的递归神经网络之一，网络的输出不仅取决于当前输入还取决于上一时间步的输出。</p>
<p>隐层神经元的输出参与下一时刻的输入。</p>
<h3 id="Boltzmann机"><a href="#Boltzmann机" class="headerlink" title="Boltzmann机"></a>Boltzmann机</h3><p>基于能量的模型。</p>
<p>使用$s<em>i$表示单元的状态，$w</em>{ij}$表示权值，$\theta_i$表示阈值。</p>
<p>定义能量$E(s) = -\sum\sum w_{ij}s_is_j-\sum\theta_is_i$。概率$P(s) = \frac{e^{-E(s)}}{\sum e^{-E(s)}}$。</p>
<p>训练过程为将训练样本看作状态向量并使其概率尽可能大。</p>
<p>具体使用中常用受限Boltzmann机，并使用CD算法进行训练。</p>
<h2 id="深度学习"><a href="#深度学习" class="headerlink" title="深度学习"></a>深度学习</h2><p>典型深度学习模型就是深层的神经网络，通过增加隐层的数量来增加模型复杂度。</p>
<p>多隐层的神经网络难以直接使用BP算法训练，会出现不收敛的情况。主要有两种解决方法。</p>
<p>使用无监督逐层训练的方法，每次训练一层隐节点，称为预训练，全部完成后进行微调。</p>
<p>使用权共享的思路，让一组神经元使用相同的权。在卷积神经网络中有非常重要的作用。</p>
<p>（在CNN中每个卷积层包含多个特征映射，每个池化层基于局部相关性原理进行压采样，其中每一组神经元都使用相同的连接权，减少参数数量。）</p>
<p>深度学习可以理解为特征学习或者表示学习。传统机器学习中通过人工设计样本特征，而特征学习通过机器学习技术自适应产生特征。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Machine Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第四章:决策树</title>
    <url>/2020/01/31/melonC4/</url>
    <content><![CDATA[<p>西瓜书第四章</p>
<a id="more"></a>
<h2 id="基本流程"><a href="#基本流程" class="headerlink" title="基本流程"></a>基本流程</h2><p>决策树的每一个叶子结点表示一个决策结果，每个中间节点表示一个属性测试。</p>
<p>学习目的为生成一颗泛化能力强的决策树（处理未见示例能力强）</p>
<p>决策树学习的基本过程是递归下降的划分过程，节点对给定的数据集和属性集学习并得到一个划分或者将自己标记为叶子结点。</p>
<h2 id="划分选择"><a href="#划分选择" class="headerlink" title="划分选择"></a>划分选择</h2><p>决策树学习的关键步骤在于选择合适的最优划分属性。</p>
<h3 id="信息增益"><a href="#信息增益" class="headerlink" title="信息增益"></a>信息增益</h3><p>使用信息熵（频率负对数的期望）来度量样本集合纯度。</p>
<p>$Ent(D) = -\sum_{k=1}^{|y|}p_k log_2 p_k$ 值越小表示样本集合D的纯度越高。</p>
<p>定义信息增益，度量使用属性a对样本进行划分后的性能提升。其中划分后的值为各个子节点的信息熵的加权和。</p>
<p>$Gain(D,a)=Ent(D)-\sum_{v=1}^{V}\frac{|D^v|}{|D|}Ent(D^v)$值越大表示提升越多。</p>
<p>每一次划分时，选择信息增益最大的属性用于划分。</p>
<h3 id="增益率"><a href="#增益率" class="headerlink" title="增益率"></a>增益率</h3><p>信息增益准则对于可选择值数目更多的属性有所偏好，使用增益率选择划分属性可减少其不利影响</p>
<p>$Gain_ratio(D,a) = \frac{Gain(D,a)}{IV(a)}$</p>
<p>其中定义了属性的固有值</p>
<p>$IV(a)=-\sum_{v=1}^{V}\frac{|D^v|}{|D|}log_2\frac{|D^v|}{|D|}$用来减少取值数目的影响，但是对取值数目较少的属性有所偏好</p>
<p>折衷：先选出信息增益高于平均水平的属性，再从中选择增益率最高的</p>
<h3 id="基尼指数"><a href="#基尼指数" class="headerlink" title="基尼指数"></a>基尼指数</h3><p>CART算法定义基尼指数用于度量数据集的纯度</p>
<p>$Gini(D) = 1-\sum_{k=1}^{[Y]} p_k^2$</p>
<p>定义属性的基尼指数</p>
<p>$Gini<em>index(D,a) = \sum</em>{v=1}^{V}\frac{|D^v|}{|D|}Gini(D^v)$</p>
<p>选择使划分后基尼指数最小的属性来划分</p>
<h2 id="剪枝处理"><a href="#剪枝处理" class="headerlink" title="剪枝处理"></a>剪枝处理</h2><p>用于处理过拟合的情况，即去掉一些分支降低过拟合风险。</p>
<p>预剪枝：划分之前判断该次划分能否提升性能。</p>
<p>后剪枝：对于生成的决策树自底向上判断非叶节点能不能换为叶子节点。</p>
<h3 id="预剪枝"><a href="#预剪枝" class="headerlink" title="预剪枝"></a>预剪枝</h3><p>每次划分之前，对于划分前后的泛化性能进行估计</p>
<p>使用正确率、假正率/假反率等在验证集上进行判断</p>
<p>精度不变、精度降低、内部样例已经为同一类时，不进行划分</p>
<h3 id="后剪枝"><a href="#后剪枝" class="headerlink" title="后剪枝"></a>后剪枝</h3><p>对于已经得到的一颗决策树，自底向上考虑每一个非叶子节点。</p>
<p>如果使用节点取代子树后精度提高或者不变，则进行剪枝。</p>
<p>后剪枝得到的决策树更小但是训练成本高。</p>
<h2 id="连续与缺失值"><a href="#连续与缺失值" class="headerlink" title="连续与缺失值"></a>连续与缺失值</h2><h3 id="连续属性的处理"><a href="#连续属性的处理" class="headerlink" title="连续属性的处理"></a>连续属性的处理</h3><p>使用连续属性离散化技术</p>
<p>对于数据集中取值连续的N个点，选择相邻两点的中位值作为作为候选划分点</p>
<p>通常可以将划分点选择为数据集中不高于中位点的最大值</p>
<p>注意使用连续值划分后，子树中仍然可使用同一连续属性</p>
<h3 id="缺失值的处理"><a href="#缺失值的处理" class="headerlink" title="缺失值的处理"></a>缺失值的处理</h3><p>由于存在不完整样本，某些样本的某些属性值缺失</p>
<p>（1）如何选择划分属性（2）对于不完整样本如何在该属性下划分</p>
<p>首先选择在属性a下没有缺失值的样本子集$\tilde{D}$从而得到取值为$a^v$的元素$\tilde{D^v}$</p>
<p>定义一系列常数</p>
<p>$\rho =\frac{\sum<em>{\tilde{D}}\omega_x}{\sum_D\omega_x}$、$\tilde{p^k} = \frac{\sum</em>{\tilde{D}<em>k}\omega_x}{\sum</em>{\tilde{D}}\omega<em>x}$、$\tilde{r_v} = \frac{\sum</em>{\tilde{D}^v}\omega<em>x}{\sum</em>{\tilde{D}}\omega_x}$</p>
<p>将原本的公式推广得到$Gain(D,a) = \rho \times Gain(\tilde{D},a) = \rho \times (Ent(\tilde{D}) - \sum{\tilde{r}_vEnt(\tilde{D^v})})$、$Ent(\tilde{D}) = - \sum \tilde{p}_k log_2 \tilde{p}_k$</p>
<p>相当于将属性缺失的样本按不同概率划分到不同的子类中</p>
<h2 id="多变量决策树"><a href="#多变量决策树" class="headerlink" title="多变量决策树"></a>多变量决策树</h2><p>每个属性看作一个坐标轴，d个属性得到d维空间的一个数据点</p>
<p>分类任务是则为寻找坐标空间的分类边界</p>
<p>决策树学习得到的分类边界由若干个平行轴的分段组成</p>
<p>使用多变量决策树时，每个非叶节点是针对所有属性加权平均实现的线性分类器</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Machine Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>使用verilog设计FSM并生成状态图</title>
    <url>/2019/11/24/verilog-fsm/</url>
    <content><![CDATA[<p>使用Verilo HDL设计FSM并在MacOS下仿真和生成状态图</p>
<a id="more"></a>
<p>Verilog课程大作业有一个题要求设计FSM并生成状态图。按要求是使用ModelSIM，但是一方面配置Win7并使用ModelSIM太烦了，另一方面由于设计中为了更好的可读性，没有使用ModelSIM要求的可综合的编程规范（我猜大概是这个原因），所以使用ModelSIM的FSM View并不能生成想要的状态图，于是在MacOS中探索了FSM仿真和状态图的生成。</p>
<h2 id="FSM编写"><a href="#FSM编写" class="headerlink" title="FSM编写"></a>FSM编写</h2><p>使用FSM监测0110和1101序列，其中时钟下降沿触发，同步复位。代码如下</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> seq_detect(<span class="keyword">output</span> <span class="keyword">reg</span> flag, <span class="keyword">input</span> din, clk, rst_n);</span><br><span class="line">    <span class="keyword">reg</span> [<span class="number">8</span>:<span class="number">0</span>] state;</span><br><span class="line">    <span class="keyword">parameter</span>   IDLE = <span class="number">9'b0_0000_0001</span>,</span><br><span class="line">                A0 = <span class="number">9'b0_0000_0010</span>, A1 = <span class="number">9'b0_0000_0100</span>,</span><br><span class="line">                A2 = <span class="number">9'b0_0000_1000</span>, A3 = <span class="number">9'b0_0001_0000</span>,</span><br><span class="line">                B0 = <span class="number">9'b0_0010_0000</span>, B1 = <span class="number">9'b0_0100_0000</span>,</span><br><span class="line">                B2 = <span class="number">9'b0_1000_0000</span>, B3 = <span class="number">9'b1_0000_0000</span>;</span><br><span class="line">    <span class="keyword">always</span> @(<span class="keyword">negedge</span> clk) <span class="keyword">begin</span></span><br><span class="line">        <span class="keyword">if</span> (!rst_n ) <span class="keyword">begin</span> state &lt;= IDLE;   flag &lt;= <span class="number">1'b0</span>; <span class="keyword">end</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">begin</span></span><br><span class="line">            <span class="keyword">case</span>(state)</span><br><span class="line">                IDLE:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  A0:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A1;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  A1:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A1;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= A2;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  A2:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A3;  flag &lt;= <span class="number">1'b1</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  A3:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= B2;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  B0:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= B1;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  B1:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= B2;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                  B2:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A1;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B3;  flag &lt;= <span class="number">1'b1</span>;   <span class="keyword">end</span></span><br><span class="line">                  B3:   <span class="keyword">if</span>(din) <span class="keyword">begin</span> state &lt;= A3;  flag &lt;= <span class="number">1'b1</span>;   <span class="keyword">end</span></span><br><span class="line">                        <span class="keyword">else</span>    <span class="keyword">begin</span> state &lt;= B0;  flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">                <span class="keyword">default</span>:        <span class="keyword">begin</span> state &lt;= IDLE;flag &lt;= <span class="number">1'b0</span>;   <span class="keyword">end</span></span><br><span class="line">            <span class="keyword">endcase</span></span><br><span class="line">        <span class="keyword">end</span></span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>
<h2 id="仿真"><a href="#仿真" class="headerlink" title="仿真"></a>仿真</h2><p>在macOS使用icarus-Verilog仿真器和Scansion查看波形。测试台模块如下：</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="meta">`<span class="meta-keyword">include</span> "seq_detect.v"</span></span><br><span class="line"><span class="meta">`<span class="meta-keyword">timescale</span> 10ns/ 1ns</span></span><br><span class="line"><span class="keyword">module</span> tb_seq_detect();</span><br><span class="line">    <span class="keyword">wire</span> p_flag;</span><br><span class="line">    <span class="keyword">reg</span> p_din, p_clk, p_rst_n;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">reg</span> [<span class="number">35</span>:<span class="number">0</span>] data = <span class="number">36'b0000_1100_0010_0110_1001_1011_0010_0001_1101</span>;</span><br><span class="line"></span><br><span class="line">    seq_detect dec(<span class="variable">.flag</span>(p_flag), <span class="variable">.clk</span>(p_clk), <span class="variable">.din</span>(p_din), <span class="variable">.rst_n</span>(p_rst_n));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">        p_clk = <span class="number">1'b1</span>;</span><br><span class="line">        <span class="keyword">forever</span> #<span class="number">5</span> p_clk = ~p_clk;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">integer</span> k;</span><br><span class="line">    <span class="keyword">initial</span></span><br><span class="line">    <span class="keyword">begin</span></span><br><span class="line">        p_rst_n = <span class="number">1'b0</span>;</span><br><span class="line">        p_din = <span class="number">1'bx</span>;</span><br><span class="line">        #<span class="number">7</span> p_rst_n = <span class="number">1'b1</span>;</span><br><span class="line">        <span class="keyword">for</span>(k = <span class="number">0</span>; k &lt; <span class="number">36</span>; k = k + <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">begin</span></span><br><span class="line">            #<span class="number">10</span>;</span><br><span class="line">            p_din = data[<span class="number">35</span>];</span><br><span class="line">            data = data &lt;&lt; <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">end</span></span><br><span class="line">        #<span class="number">20</span> <span class="built_in">$finish</span>;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">initial</span> <span class="keyword">begin</span></span><br><span class="line">        <span class="built_in">$monitor</span>(<span class="built_in">$time</span>, <span class="string">" rst = %b, din = %b, flag = %b"</span>,</span><br><span class="line">                        p_rst_n, p_din, p_flag);</span><br><span class="line">        <span class="built_in">$dumpfile</span>(<span class="string">"tb_seq_detect.vcd"</span>);</span><br><span class="line">        <span class="built_in">$dumpvars</span>(<span class="number">0</span>, tb_seq_detect);</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>
<p>仿真步骤如下，可以得到Monitor输出和vcd波形文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iverilog tb_seq_detect.v</span><br><span class="line">vvp a.out</span><br><span class="line">open -a Scansion tb_seq_detect.vcd</span><br></pre></td></tr></table></figure>
<h2 id="生成状态图"><a href="#生成状态图" class="headerlink" title="生成状态图"></a>生成状态图</h2><p>可以使用Graphviz工具根据dot文件生成状态图。</p>
<p>dot文件的基本格式如下，即定义图的边以及标签。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">digraph fsm &#123;</span><br><span class="line">    &quot;IDLE/flag=0&quot;  -&gt; &quot;A0=1/flag = 0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;IDLE/flag=0&quot;  -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A0=1/flag = 0&quot; -&gt; &quot;A1=11/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A0=1/flag = 0&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A1=11/flag=0&quot; -&gt; &quot;A1=11/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A1=11/flag=0&quot; -&gt; &quot;A2=110/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A2=110/flag=0&quot; -&gt; &quot;A3=1101/flag=1&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A2=110/flag=0&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A3=1101/flag=1&quot; -&gt; &quot;B2=011/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;A3=1101/flag=1&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B0=0/flag=0&quot; -&gt; &quot;B1=01/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B0=0/flag=0&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B1=01/flag=0&quot; -&gt; &quot;B2=011/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B1=01/flag=0&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B2=011/flag=0&quot; -&gt; &quot;A1=11/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B2=011/flag=0&quot; -&gt; &quot;B3=0110/flag=1&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B3=0110/flag=1&quot; -&gt; &quot;A3=1101/flag=1&quot; [ label = &quot;0&quot; ]</span><br><span class="line">    &quot;B3=0110/flag=1&quot; -&gt; &quot;B0=0/flag=0&quot; [ label = &quot;0&quot; ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用Graphviz生成文件具体命令如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dot -Tpng fsm.dot &gt; fsm.png</span><br><span class="line">circo -Tpdf fsm.dot &gt; fsm.pdf</span><br></pre></td></tr></table></figure>
<p>需要注意Graphviz的输出默认输出到stdcout，因此需要重定向写入到文件。其中-T可以指定输出文件的格式。而dot和circo分别为不同的状态图风格，具体的使用方法可以参见man dot。</p>
<p>部分内容如下(详细内容自行查看)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">NAME</span><br><span class="line">       dot - filter for drawing directed graphs</span><br><span class="line">       neato - filter for drawing undirected graphs</span><br><span class="line">       twopi - filter for radial layouts of graphs</span><br><span class="line">       circo - filter for circular layout of graphs</span><br><span class="line">       fdp - filter for drawing undirected graphs</span><br><span class="line">       sfdp - filter for drawing large undirected graphs</span><br><span class="line">       patchwork - filter for squarified tree maps</span><br><span class="line">       osage - filter for array-based layouts</span><br><span class="line">       ...</span><br><span class="line"></span><br><span class="line">Traditionally, Graphviz supports the following:</span><br><span class="line">       -Tdot (Dot format containing layout information),</span><br><span class="line">       -Txdot (Dot format containing complete layout information),</span><br><span class="line">       -Tps (PostScript),</span><br><span class="line">       -Tpdf (PDF),</span><br><span class="line">       -Tsvg -Tsvgz (Structured Vector Graphics),</span><br><span class="line">       -Tfig (XFIG graphics),</span><br><span class="line">       -Tpng (png bitmap graphics),</span><br><span class="line">       -Tgif (gif bitmap graphics),</span><br><span class="line">       -Tjpg -Tjpeg (jpeg bitmap graphics),</span><br><span class="line">       -Tjson (xdot information encoded in JSON),</span><br><span class="line">       ...</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Verilog HDL</tag>
        <tag>Graphviz</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第三章:线性模型</title>
    <url>/2019/11/16/melonC3/</url>
    <content><![CDATA[<p>西瓜书第三章</p>
<a id="more"></a>
<h2 id="基本形式"><a href="#基本形式" class="headerlink" title="基本形式"></a>基本形式</h2><p>线性模型试图学得一个通过属性的线性组合进行预测的函数，即</p>
<script type="math/tex; mode=display">f(\mathcal{x}) = \omega_{1}x_{1} +\omega_{2}x_{2} +\omega_{3}x_{3} +...+\omega_{d}x_{d} + b</script><p>也可以写作向量形式<script type="math/tex">f(\mathcal{x}) = \omega^{T}\mathcal{x} + b</script></p>
<p><strong>特点：</strong>非线性模型可以通过线性模型引入层级结构或者高维映射解决。同时线性模型具有良好的可解释性。</p>
<h2 id="线性回归"><a href="#线性回归" class="headerlink" title="线性回归"></a>线性回归</h2><p>回归任务：学得一个线性模型，尽可能准确的预测实值输出标记。</p>
<p><strong>属性的处理</strong>：对于有顺序关系的离散量，可以连续化为一个连续值。若不存在顺序关系，则变为k维布尔向量，k为取值的个数。</p>
<p><strong>关键</strong>：性能度量</p>
<h3 id="最小二乘法"><a href="#最小二乘法" class="headerlink" title="最小二乘法"></a>最小二乘法</h3><p>性能度量使用均方误差，也即欧式距离。</p>
<p>基于均方误差最小化进行求解的方法为最小二乘法。</p>
<h4 id="单属性情形"><a href="#单属性情形" class="headerlink" title="单属性情形"></a>单属性情形</h4><p>学习目标<script type="math/tex">(\omega^{*},b^{*}) = argmin_{(\omega, b)} \sum^{m}_{i=1}(y_{i} - \omega_{i}-b)^{2}</script></p>
<p>其中<script type="math/tex">E_{(\omega, b)} = \sum^{m}_{i=1}(y_{i} - \omega_{i}-b)^{2}</script> 为均方误差，是一个关于$\omega$和b 的凸函数，导数为0时得到最优解</p>
<p>可以解得最优解的闭式解</p>
<script type="math/tex; mode=display">\omega = \frac{\sum_{i=1}^{m}y_{i}(x_{i}-\bar{x})}{\sum_{i=1}^{m}x_{i}^{2} - \frac{1}{m}(\sum_{i=1}^{m}x_{i})^{2}}</script><script type="math/tex; mode=display">b = \frac{1}{m} \sum_{i=1}^{m}(y_{i}-\omega x_{i})</script><h4 id="多元线性回归"><a href="#多元线性回归" class="headerlink" title="多元线性回归"></a>多元线性回归</h4><p>更一般的情况下，自变量是一个多维向量（多属性值）</p>
<p>在分析中，将常数项b收入参量$\omega$中，得到$\hat{\omega} = (\omega, b)$作为最终的参数</p>
<p>而数据集可以表示为一个矩阵，行数为数据量，列数为属性量加一，最后一列为1，与参数中的常数b对应。</p>
<p>此时可以将均方误差表示为<script type="math/tex">E_{\hat{\omega}} = (y - X\hat{\omega})^{T}(y-X\hat{\omega})</script></p>
<p>学习目标为<script type="math/tex">\hat{\omega}^{*} = argmin_{\hat{\omega}}(y - X\hat{\omega})^{T}(y-X\hat{\omega})</script></p>
<p>最优解的闭式解为<script type="math/tex">\frac{\partial E _{\hat{\omega}}}{\partial \hat{\omega}} = 2 X^{T}(X \hat{\omega} - y)</script></p>
<p>求解该方程的过程比较复杂，仅当$X^{T}X$满秩或者正定的时候可解出$\hat{\omega}^{*} = (X^{T}X)^{-1}X^{T}y$</p>
<p><strong>注意</strong>：通常情况下$X^{T}X$不满秩，可得到多组解，此时输出结果取决于算法的归纳偏好，通常引入正则化。</p>
<h3 id="广义线性模型"><a href="#广义线性模型" class="headerlink" title="广义线性模型"></a>广义线性模型</h3><p>$y = g^{-1}(\omega^{T}x + b)$ 其中$g(x)$是联系函数</p>
<p>可以将非线性函数映射或转化到线性空间</p>
<p>例如$g(x) = ln(x)$时，为对数线性回归，将对数的实际预测模型与线性的预测值联系起来。</p>
<h2 id="对数几率回归"><a href="#对数几率回归" class="headerlink" title="对数几率回归"></a>对数几率回归</h2><p>使用线性模型实现二分类任务，输出标记为{0,1}</p>
<p>方法一：</p>
<p>使用阶跃函数</p>
<p>预测值大于0判为正例（1），小于零判为反例（0），预测值为0则随机判别（0.5）。</p>
<p>方法二：</p>
<p>使用对数几率回归（对数几率函数）</p>
<p>$y = \frac{1}{1+e^{-z}}$将线性回归模型的预测值与分类任务的标记（0和1）联系起来。</p>
<p>进一步可以化为$y = \frac{1}{1+e^{-(w^{T}x+b)}}$即$ln\frac{y}{1-y} = w^{T}+b$</p>
<p><strong>求解</strong>：将分类结果y看作后验概率估计，有$ln\frac{p(y=1|x)}{p(y=0|x)} = w^{T}+b$</p>
<p>从而解得<script type="math/tex">p(y = 1|x) = \frac{e^{w^{T}+b}}{1+e^{w^{T}+b}}</script>and<script type="math/tex">p(y = 0|x) = \frac{1}{1+e^{w^{T}+b}}</script></p>
<p>使用<strong>极大似然估计</strong>得到模型参数的具体值。</p>
<p>最终得到求似然项的极值问题（凸优化），使用梯度下降法、牛顿法等，都可以得到结果。</p>
<h2 id="线性判别分析"><a href="#线性判别分析" class="headerlink" title="线性判别分析"></a>线性判别分析</h2><p>简称LDA，将训练样例投影到一条直线上，使得同类的投影点尽量近，不同类的投影点尽量远</p>
<p>定义相关的符号，$i\in{0,1}$时</p>
<p>$\omega$为投影方向（单位向量），$X<em>{i}$表示样例的集合，$\mu</em>{i}$表示样例的平均值，$\Sigma_{i}$表示协方差矩阵</p>
<p>得到优化目标为同类样例的协方差尽可能小，异类样例中心的距离尽可能大。</p>
<p>定义类内散度矩阵<script type="math/tex">S_{\omega}= \Sigma_{0}+\Sigma_{1} = \sum_{x\in X_{0}}(x-\mu_{0})(x-\mu_{0})^{T}+\sum_{x\in X_{1}}(x-\mu_{1})(x-\mu_{1})^{T}</script></p>
<p>定义类间散度矩阵<script type="math/tex">S_{b} = (\mu_{0}-\mu_{1})(\mu_{0}-\mu_{1})^{T}</script></p>
<p>优化目标为广义瑞利熵<script type="math/tex">J = \frac{\omega^{T}S_{b}\omega}{\omega^{T}S_{\omega}\omega}</script></p>
<p>使用拉格朗日乘子法求解优化问题</p>
<h2 id="多分类学习"><a href="#多分类学习" class="headerlink" title="多分类学习"></a>多分类学习</h2><p>基本思路：将多分类问题拆分为若干个二分类任务。将其结果结合可得到多分类模型。</p>
<p>主要的拆分策略有三种：</p>
<p>一对一拆分：对于N个类别，生成$C_{N}^{2}$个学习任务。每个学习器区分两两配对的一组。投票产生结果。</p>
<p>一对多拆分：生成N个学习任务，区分一个类别和其余类别。使用置信度综合得到结果。</p>
<p>多对多拆分：每次将一部分作为正例，另一部分作为反例。正反类的构造需要特别设计。</p>
<p>纠错输出吗技术：ECOC</p>
<p>通过编码矩阵指定类别划分，对于二元码，划分为正反类。对于三元码划分为正负类和停用类。</p>
<p>对于测试用例的分类结果，通过与各类的编码比较，距离最小的为结果。</p>
<p>通常情况编码越长纠错能力越强。通常情况不同类之间的距离越远，分类效果越好。</p>
<h2 id="类别不平衡问题"><a href="#类别不平衡问题" class="headerlink" title="类别不平衡问题"></a>类别不平衡问题</h2><p>正反例的数量差别不大的时候，对学习结果的影响不大。</p>
<p>但是当正反例的数量差别较大时，学习的结果会收到非常严重的影响。</p>
<p>主要做法：</p>
<p>欠采样：去除一些多余的样本。</p>
<p>过采样：增加一些样本，但是不能使用原有样本的简单重复。一种方法是使用差值。</p>
<p>阈值移动：在训练好的预测时，不是将大于0.5的判别为1，而是将$\frac{y}{1-y} &gt; \frac{m^{+}}{m^{-}}$判别为1</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Machine Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog电路设计思路</title>
    <url>/2019/11/16/verilog-design/</url>
    <content><![CDATA[<p>使用Verilog设计电路的一些思路</p>
<a id="more"></a>
<p>注意事项:</p>
<p>时序逻辑电路使用非阻塞赋值</p>
<p>组合逻辑电路使用阻塞赋值</p>
<h2 id="组合逻辑电路"><a href="#组合逻辑电路" class="headerlink" title="组合逻辑电路"></a>组合逻辑电路</h2><ul>
<li>概念：输出仅由当前的输入决定</li>
<li>注意：不能蕴含触发器的逻辑，即==不具有记忆功能==（case语句的default子句、连续赋值语句、电平敏感的always语句）</li>
<li>常见的组合逻辑电路：复用器、译码器、编码器、三态缓冲器、比较器、加法器、乘法器。</li>
<li>实现方法：<ul>
<li>使用assign连续赋值语句（数据流级）和==电平敏感==的always行为语句（行为级）</li>
<li>使用always引导的语句块实现对于中间变量的逻辑运算</li>
<li>使用assign引导的连续赋值语句实现==简单变换和多路输出==（如果有的话）</li>
<li>注意：always的使用的中间变量为reg型，assign和子模块使用的中间变量为wire型</li>
</ul>
</li>
<li>设计思路：<ul>
<li>给定==电路原理图==：使用门原语和模块实例</li>
<li>给定布尔方程：使用连续赋值语句和==布尔运算==（真值表得到布尔方程）</li>
<li>给定模块功能/IO（==真值表==）：使用行为级描述（if/case配合真值表）</li>
</ul>
</li>
<li>可采用的设计模式：<ul>
<li>assign连续赋值语句 + 条件表达式（？：） + 布尔运算</li>
<li>always语句块 + 条件表达式（？：）</li>
<li>always语句块 + 条件语句（if/case）</li>
<li>assign连续赋值语句 + 函数（function [automatic]）可以在数据流语句中引入行为级语句逻辑</li>
<li>always语句块 + 函数（function [automatic]）</li>
<li>always语句块 + 任务（task [automatic]）</li>
<li>建议不直接修改输出变量，而是使用中间变量，在输出时使用assign实现（综合好像能得到一个buf）</li>
</ul>
</li>
</ul>
<h2 id="时序电路"><a href="#时序电路" class="headerlink" title="时序电路"></a>时序电路</h2><ul>
<li>概念：输出由当前输入和内部的状态决定</li>
<li>注意：具有记忆功能，内部包含==锁存器latch（电平敏感）和触发器flip-flop（边沿敏感）==等储存器件，case语句的default子句可以为空</li>
<li>常见时序电路：锁存器latch、触发器flip-flop</li>
<li>实现方法：<ul>
<li>使用always引导的语句块（电平敏感或边沿敏感）实现逻辑运算</li>
<li>使用assign引导的连续赋值语句实现简单变换和多路输出（例如assign qbar=～q）</li>
<li>==不能使用门原语==，门原语定义的时序电路不可综合（例如使用两个与非门实现的RS锁存器）</li>
</ul>
</li>
<li>时序逻辑：<ul>
<li>同步控制（复位/置位）：<ul>
<li>只有在时钟信号的有效跳变沿状态才能改变</li>
<li>always的事件控制列表没有复位/置位信号</li>
<li>语句块中先检查复位/置位信号，然后执行其他逻辑。使用嵌套的if-else可以控制复位/置位/输入逻辑的优先级（==复位&gt;置位&gt;其他逻辑==）</li>
</ul>
</li>
<li>异步控制（复位/置位）：<ul>
<li>复位/置位信号激活时立即响应</li>
<li>always的控制列表中包括时钟边沿和复位/置位信号边沿</li>
<li>语句块中先检查是时钟触发的还是复位/置位信号触发的，并执行相应的逻辑。使用嵌套的if-else指定优先级（==复位&gt;置位&gt;其他逻辑==）</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="可综合电路"><a href="#可综合电路" class="headerlink" title="可综合电路"></a>可综合电路</h2><ul>
<li>关键：使用Verilog HDL中的可综合子集</li>
<li>可综合的Verilog HDL结构<ul>
<li>端口：input, inout, output</li>
<li>参数：parameter</li>
<li>模块定义：module</li>
<li>信号和变量：wire, reg, tri 允许向量</li>
<li>实例调用：module instance primitive gate instance</li>
<li>函数任务：function, task 不考虑时序结构</li>
<li>过程：always, if, then, else, case ==不支持initial==</li>
<li>过程块：begin, end, named block, disable</li>
<li>数据流：assign ==不考虑#延迟信息==</li>
<li>循环：for, while, forever 需要包含@(posedge clock)或@(negedge clock)</li>
</ul>
</li>
<li>使用复位机制取代initial进行初始化</li>
<li>赋值<ul>
<li>组合逻辑电路使用阻塞赋值=</li>
<li>时序电路使用非阻塞赋值&lt;=</li>
<li>同时描述时序和组合逻辑使用非阻塞赋值&lt;=</li>
</ul>
</li>
<li>编码风格<ul>
<li>有意义的信号和变量名</li>
<li>==避免混合使用上升沿触发和下降沿触发==</li>
<li>使用圆括号而不是运算优先级</li>
<li>条件语句中说明所有的可能情况</li>
<li>不要多个always对同一变量赋值</li>
</ul>
</li>
</ul>
<h2 id="常见改错题思路"><a href="#常见改错题思路" class="headerlink" title="常见改错题思路"></a>常见改错题思路</h2><ul>
<li>变量类型错误<ul>
<li>注意input和output的端口类型检查</li>
</ul>
</li>
<li>变量位宽</li>
<li>赋值方式<ul>
<li>阻塞赋值和非阻塞赋值</li>
</ul>
</li>
<li>敏感量列表<ul>
<li>always的敏感量列表是电平触发还是边沿触发</li>
</ul>
</li>
<li>位运算/逻辑运算</li>
</ul>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Verilog HDL</tag>
      </tags>
  </entry>
  <entry>
    <title>verilog语法参考</title>
    <url>/2019/10/22/verilog-grammer/</url>
    <content><![CDATA[<p>简单记录一下verilog的语法，用于日后复习或者参考。</p>
<a id="more"></a>
<h2 id="模块框架"><a href="#模块框架" class="headerlink" title="模块框架"></a>模块框架</h2><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> 模块名 (端口列表);</span><br><span class="line">  <span class="comment">// 端口声明</span></span><br><span class="line">  <span class="keyword">input</span> input_port;</span><br><span class="line">  <span class="keyword">output</span> output_port;</span><br><span class="line">  <span class="comment">// 参数声明</span></span><br><span class="line">  <span class="keyword">parameter</span> param;</span><br><span class="line">  <span class="comment">// 数据流语句</span></span><br><span class="line">  <span class="keyword">assign</span> a = b &amp; c;</span><br><span class="line">  <span class="comment">// 模块实例和门原语</span></span><br><span class="line">  subModule Mod1(port1, port2);</span><br><span class="line">  subModule Mod2(<span class="variable">.port1</span>(PORT1),<span class="variable">.port2</span>(PORT2));</span><br><span class="line">  <span class="keyword">nand</span> (out, in1, in2);</span><br><span class="line">  <span class="comment">// 过程语句块</span></span><br><span class="line">  <span class="keyword">always</span> @(*)</span><br><span class="line">    过程语句</span><br><span class="line">  <span class="keyword">initial</span></span><br><span class="line">    过程语句</span><br><span class="line"></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>
<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>数字声明：\<size>‘\<base format>\<number></number></size></p>
<p>可以在’之后加s表示有符号数</p>
<p>建议使用_分割为四个二进制数一组</p>
<p>使用[begin:end]选择向量的部分位</p>
<p>使用{a,b,c[3:0]}表示变量拼接</p>
<p>位宽大的数向位宽小的数赋值：获得低位并截断</p>
<p>位宽小的数向位宽大的数赋值：高位补0。例如reg[4:0]a;a = 2’bx;得到00xx。</p>
<p>位扩展时根据最高位扩展x和z。例如4’bx0和4’bz1得到xxx0和zzz1。</p>
<p>常用系统函数：</p>
<p>$time    返回仿真时间</p>
<p>$display 显示输出</p>
<p>$monitor 监控值的变化，通常第一个参数为$time，格式类似printf</p>
<p>$finish   结束仿真</p>
<h2 id="结构域描述"><a href="#结构域描述" class="headerlink" title="结构域描述"></a>结构域描述</h2><h3 id="子模块连接"><a href="#子模块连接" class="headerlink" title="子模块连接"></a>子模块连接</h3><p>端口连接规则：</p>
<ol>
<li>输入：外界net或reg，内部net</li>
<li>输出：外界net，内部net或reg</li>
<li>双向：外界net，内部net</li>
<li>允许按端口列表顺序相连或者按名字相连</li>
</ol>
<p>模块也可以定义成实例数组</p>
<p>通常设计测试台包括四个部分：定义外部与端口相连的变量（通常输入用reg输出用wire），定义模块并相连，initial初始化并控制驱动信号，显示monitor信息等。</p>
<h3 id="门级建模"><a href="#门级建模" class="headerlink" title="门级建模"></a>门级建模</h3><p>门原语属于模块一级，相当于并发语句。</p>
<p>只能驱动wire型的变量。</p>
<h4 id="and-or门"><a href="#and-or门" class="headerlink" title="and/or门"></a>and/or门</h4><p>包括and,nand,or,nor.xor,xnor六种</p>
<p>一个输出端和多个输入端，第一个参数为输出端</p>
<p>真值表中有x/z时，输出通常为x</p>
<h4 id="buf-not门"><a href="#buf-not门" class="headerlink" title="buf/not门"></a>buf/not门</h4><p>包括buf,not两种</p>
<p>一个输入端和多个输出端，最后一个参数为输入端</p>
<h4 id="三态门"><a href="#三态门" class="headerlink" title="三态门"></a>三态门</h4><p>包括bufif1,bufif0,notif1,notif0四种</p>
<p>控制信号是最后一个参数，输入信号是倒数第二个参数</p>
<p><strong><em>要求：</em></strong>当控制信号关断时，输出信号必须为z。</p>
<p>控制信号为x/z时，或者输入信号为x/z时，输出通常为x。</p>
<h4 id="延迟指定"><a href="#延迟指定" class="headerlink" title="延迟指定"></a>延迟指定</h4><p>门原语后使用#(上升 下降 关断)制定</p>
<p>使用#(最小:典型:最大 最小:典型:最大 最小:典型:最大)指定延迟</p>
<p>上升延迟指变为1，下降延迟指变为0，关断延迟指变为z</p>
<h2 id="行为域描述"><a href="#行为域描述" class="headerlink" title="行为域描述"></a>行为域描述</h2><h3 id="数据流级"><a href="#数据流级" class="headerlink" title="数据流级"></a>数据流级</h3><h4 id="连续赋值"><a href="#连续赋值" class="headerlink" title="连续赋值"></a>连续赋值</h4><p>连续赋值语句assign，左值一定为net类型</p>
<p>总是激活，时刻监控，描述组合逻辑</p>
<h5 id="普通延迟"><a href="#普通延迟" class="headerlink" title="普通延迟"></a>普通延迟</h5><p>在assign之后使用#指定延迟</p>
<p><strong><em>惯性延迟</em></strong>右端信号保持的持续时间必须大于延时宽度才能使左值改变</p>
<h5 id="线网延迟"><a href="#线网延迟" class="headerlink" title="线网延迟"></a>线网延迟</h5><p>声明wire变量时使用#指定线网延迟</p>
<p>实际延迟为线网延迟和赋值语句延迟之和</p>
<p>信号保持的时间要大于两者的最大值</p>
<h4 id="操作符"><a href="#操作符" class="headerlink" title="操作符"></a>操作符</h4><p>向量参与逻辑运算时，首先进行缩减或操作得到逻辑值，使用逻辑值运算</p>
<p>逻辑等价(== !=)对于含有x/z的操作数结果为x。</p>
<p>case等价(\=\=\= !\=\=)对于含有x/z的操作数严格的逐位比较。</p>
<p>三目条件运算符（cond）？expr1:expr2；若cond为1或0，则分别执行expr1和expr2。若cond为x，则诸位比较expr1和expr2，相同的位得到该位的值，不同的位得到x。</p>
<h3 id="行为级"><a href="#行为级" class="headerlink" title="行为级"></a>行为级</h3><p><strong><em>过程语句块中绝对不能出现连续赋值语句</em></strong></p>
<h4 id="过程语句"><a href="#过程语句" class="headerlink" title="过程语句"></a>过程语句</h4><p>主要有两种过程语句initial和always</p>
<p>相互不能嵌套，都是模块级的语句</p>
<p>要求：initial用于仿真，always用于设计</p>
<p>不要在不同的语句块中对同一变量进行操作（过程语句的并发性）</p>
<ul>
<li>initial<ul>
<li>从时刻0开始执行，只执行一次</li>
<li>所有的initial语句块并发执行</li>
<li>可以使用initial foreveer实现always的功能</li>
</ul>
</li>
<li>always<ul>
<li>从时刻0开始执行，无限循环</li>
<li>可以定义局部变量，但是不能定义的同时赋初值</li>
<li>可以用always @(*)实现assign的功能</li>
</ul>
</li>
</ul>
<h4 id="语句块"><a href="#语句块" class="headerlink" title="语句块"></a>语句块</h4><p>不建议两种块混合使用</p>
<p>可以在begin/fork后用：为块命名</p>
<p>可以使用disable禁止块的运行</p>
<ul>
<li>顺序块<ul>
<li>begin-end</li>
<li>顺序执行，延迟是相对于上一语句结束时</li>
</ul>
</li>
<li>并行块<ul>
<li>fork-join</li>
<li>并发执行，延迟是相对于整个块</li>
</ul>
</li>
</ul>
<h4 id="时序控制"><a href="#时序控制" class="headerlink" title="时序控制"></a>时序控制</h4><h5 id="基于延迟"><a href="#基于延迟" class="headerlink" title="基于延迟"></a>基于延迟</h5><ul>
<li>常规延迟：<ul>
<li>#delay a = b;</li>
<li>遇到该语句时，等待delay时间后执行</li>
</ul>
</li>
<li>内嵌延迟<ul>
<li>a = #delay b+c</li>
<li>遇到该语句时计算右边，等待delay时间后赋值</li>
</ul>
</li>
<li>零延迟：<ul>
<li>#0</li>
<li>表示在当前时间步结束时执行</li>
</ul>
</li>
</ul>
<h5 id="基于事件"><a href="#基于事件" class="headerlink" title="基于事件"></a>基于事件</h5><ul>
<li>常规事件控制（边沿触发）<ul>
<li>@(posedge clock)、@(negedge clock)、@(clock)</li>
<li>表示正向跳变、反向跳变、值改变</li>
<li>数组中任意一个值变化</li>
</ul>
</li>
<li>OR事件控制<ul>
<li>@(cond1 or cond2 or cond3)三个条件满足其一</li>
<li>@(*)语句块中任意元素值改变</li>
</ul>
</li>
<li>命名事件<ul>
<li>定义一个事件event aEvent;</li>
<li>满足条件触发事件-&gt; aEvent;</li>
<li>事件触发执行always @(aEvent)</li>
</ul>
</li>
</ul>
<h5 id="电平敏感（电平触发）"><a href="#电平敏感（电平触发）" class="headerlink" title="电平敏感（电平触发）"></a>电平敏感（电平触发）</h5><p>等待条件为真时 wait(cond)</p>
<p>cond为数组时，使用逻辑值判断</p>
<h4 id="过程控制"><a href="#过程控制" class="headerlink" title="过程控制"></a>过程控制</h4><p>只能用在过程语句块中，不能用于模块一级</p>
<ul>
<li>if…else…<ul>
<li>多条语句使用begin-end组成一块</li>
</ul>
</li>
<li>case…cond1:…default…endcase<ul>
<li>建议必须添加default</li>
<li>逐位比较</li>
</ul>
</li>
<li>casex…cond1:..default…endcase<ul>
<li>候选式使用x和z表示无关值</li>
</ul>
</li>
<li>casez…cond1:..default…endcase<ul>
<li>候选式使用z表示无关值</li>
</ul>
</li>
<li>while(condition)</li>
<li>for(count = 0; count &lt; 128;count = count+1)</li>
<li>repeat(times)<ul>
<li>以上三种都要在外部声明reg型的循环变了</li>
</ul>
</li>
<li>forever<ul>
<li>遇到$finish停止</li>
<li>可以被disable关闭</li>
</ul>
</li>
</ul>
<h4 id="赋值语句"><a href="#赋值语句" class="headerlink" title="赋值语句"></a>赋值语句</h4><p>仅能实现对寄存器的操作</p>
<p>要求：时序逻辑电路使用非阻塞赋值，组合逻辑电路使用阻塞赋值</p>
<p>严禁：在always块中混合使用阻塞和非阻塞</p>
<p>顺序块中的非阻塞赋值和并行块中的阻塞赋值的效果类似，推荐前者。</p>
<ul>
<li>阻塞赋值<ul>
<li>串行块中，按序执行，有延迟时停止</li>
<li>并行块中，并发执行</li>
</ul>
</li>
<li>非阻塞赋值<ul>
<li>串行块中，并发执行，不影响后面的语句</li>
<li>内嵌赋值语句被延迟到在目标时间步结束时执行赋值</li>
<li>并行块中，并发执行</li>
</ul>
</li>
</ul>
<h3 id="任务和函数"><a href="#任务和函数" class="headerlink" title="任务和函数"></a>任务和函数</h3><p>定义在模块内部</p>
<p>内部不能出现always或initial，只有行为语句</p>
<ul>
<li>函数（相当于同名的reg变量 要声明位宽）<ul>
<li>可以调用函数，不可以调用任务，出现在assign、always、initial中</li>
<li>遇到即执行，一定不包含延迟和时序控制</li>
<li>返回一个值，不能有output</li>
</ul>
</li>
<li>任务<ul>
<li>可以调用函数和任务，出现在initial、always中</li>
<li>可以延迟或时序控制</li>
<li>不返回值，有多个output</li>
<li>行为级语句，处理reg型变量</li>
</ul>
</li>
</ul>
<h2 id="生成块"><a href="#生成块" class="headerlink" title="生成块"></a>生成块</h2><ul>
<li>在模块一级，使用if，case，while等过程控制来控制门原语、模块调用、数据流赋值等语句生成</li>
<li>使用临时变量genvar控制</li>
<li>使用generate和endgenerate表示开始和结束</li>
<li>循环生成时内部的begin-end块需要命名，以便使用层次命名访问</li>
</ul>
<h2 id="UDP用户定义原语"><a href="#UDP用户定义原语" class="headerlink" title="UDP用户定义原语"></a>UDP用户定义原语</h2><ul>
<li>格式</li>
</ul>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">primitive</span> upd_and(<span class="keyword">output</span> y, <span class="keyword">input</span> a);</span><br><span class="line">  <span class="keyword">table</span></span><br><span class="line">    <span class="comment">//  a b : y</span></span><br><span class="line">        <span class="number">0</span> <span class="number">0</span> : <span class="number">0</span>;</span><br><span class="line">        <span class="number">0</span> <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">        <span class="number">1</span> <span class="number">0</span> : <span class="number">0</span>;</span><br><span class="line">        <span class="number">1</span> <span class="number">1</span> : <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">endtable</span></span><br><span class="line"><span class="keyword">endprimitive</span></span><br></pre></td></tr></table></figure>
<ul>
<li>可以使用？表示无关匹配</li>
</ul>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Verilog HDL</tag>
      </tags>
  </entry>
  <entry>
    <title>iptables常用规则</title>
    <url>/2019/10/20/firewall-iptables-rules/</url>
    <content><![CDATA[<p>常见的服务的iptables规则设置</p>
<a id="more"></a>
<p><strong><em>注意：配置规则时注意协议的双向性</em></strong></p>
<p>在安全性更高的系统中可以为每一条命令具体指定双方的IP地址。</p>
<h2 id="基本设置"><a href="#基本设置" class="headerlink" title="基本设置"></a>基本设置</h2><p>使用脚本配置规则时要首先清空所有规则</p>
<p>同时建议将默认规则设置为DROP，即使用白名单</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># flush all rules</span></span><br><span class="line"></span><br><span class="line">iptables -F -t filter</span><br><span class="line">iptables -F -t mangle</span><br><span class="line">iptables -F -t nat</span><br><span class="line"></span><br><span class="line"><span class="comment"># set default rules</span></span><br><span class="line"></span><br><span class="line">iptables -P INPUT DROP</span><br><span class="line">iptables -P OUTPUT DROP</span><br><span class="line">iptables -P FORWARD DROP</span><br></pre></td></tr></table></figure>
<h2 id="HTTP服务"><a href="#HTTP服务" class="headerlink" title="HTTP服务"></a>HTTP服务</h2><p>http服务默认使用tcp协议的80端口</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># I.open http port:80</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -A INPUT -p tcp --dport 80 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -A OUTPUT -p tcp --sport 80 -j ACCEPT</span><br></pre></td></tr></table></figure>
<h2 id="ping命令"><a href="#ping命令" class="headerlink" title="ping命令"></a>ping命令</h2><p>ping命令使用icmp协议</p>
<p>发送方发送icmp-type=8，即icmp-request，<br>接收方发送icmp-type=0，即icmp-reply</p>
<p>注意方向性，可以只放行一个方向的ping命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># II.open ping</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. open outbound</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p icmp --icmp-type 0 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p icmp --icmp-type 8 -j ACCEPT</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. open inbound</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -I INPUT -p icmp --icmp-type 8 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -I OUTPUT -p icmp --icmp-type 0 -j ACCEPT</span><br></pre></td></tr></table></figure>
<h2 id="FTP服务"><a href="#FTP服务" class="headerlink" title="FTP服务"></a>FTP服务</h2><p>FTP可以按主动模式或者被动模式运行，客户端登录时指定运行模式</p>
<p>统一使用TCP协议，使用21端口发送控制命令。</p>
<p>抓包分析可以发现，FTP的连接和传输过程主要分为三部分：控制连接建立和命令传输，文件传输，控制端口释放。<br>因此在iptables规则的设计要根据FTP协议的工作方式确定。</p>
<h3 id="主动模式"><a href="#主动模式" class="headerlink" title="主动模式"></a>主动模式</h3><p>使用20端口传输数据。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># III.open ftp active</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. open control port:21</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -A INPUT -p tcp --dport 21 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -A OUTPUT -p tcp --sport 21 -j ACCEPT</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. open data port:20</span></span><br><span class="line"></span><br><span class="line">iptables -t filter -A INPUT -p tcp --dport 20 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -t filter -A OUTPUT  -p tcp --sport 20 -j ACCEPT</span><br></pre></td></tr></table></figure>
<h3 id="被动模式"><a href="#被动模式" class="headerlink" title="被动模式"></a>被动模式</h3><p>使用随机生成的端口发送数据，端口范围可以在/etc/vsftpd.conf指定。</p>
<p>因此要使用状态监测来放行相关的端口。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># IV.open ftp passive</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. open control port:21</span></span><br><span class="line"></span><br><span class="line">iptables -A INPUT -p tcp --dport 21 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -A OUTPUT -p tcp --sport 21 -j ACCEPT</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. open data port:random port&gt;=1024</span></span><br><span class="line"></span><br><span class="line">iptables -A INPUT -p tcp --sport 1024: --dport 1024: -m state --state NEW,RELATED,ESTABLISHED -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -A OUTPUT -p tcp --sport 1024: --dport 1024: -m state --state RELATED,ESTABLISHED -j ACCEPT</span><br></pre></td></tr></table></figure>
<h2 id="SSH连接"><a href="#SSH连接" class="headerlink" title="SSH连接"></a>SSH连接</h2><p>默认使用TCP协议的22端口。</p>
<p>同样可以只放行一个方向的数据。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># V. open ssh port:22</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. open ssh inbound</span></span><br><span class="line"></span><br><span class="line">iptables -A INPUT -p tcp --dport 22 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -A OUTPUT -p tcp --sport 22 -m state --state ESTABLISHED,RELATED -j ACCEPT</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. open ssh outbound</span></span><br><span class="line"></span><br><span class="line">iptables -A OUTPUT -p tcp --dport 22 -j ACCEPT</span><br><span class="line"></span><br><span class="line">iptables -A INPUT -p tcp --sport 22 -m state --state ESTABLISHED,RELATED -j ACCEPT</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>Orange Pi的使用</title>
    <url>/2019/10/15/orangepi/</url>
    <content><![CDATA[<p>使用较低配置的orange pi作为树莓派的替代品</p>
<p>可以在内网搭建web服务器，ftp服务器等用于学习</p>
<a id="more"></a>
<h2 id="基本配置"><a href="#基本配置" class="headerlink" title="基本配置"></a>基本配置</h2><h3 id="下载镜像"><a href="#下载镜像" class="headerlink" title="下载镜像"></a>下载镜像</h3><p>最开始使用ubuntu core for orange pie 但是ubuntu SSO账号设置有问题 总是密码错误无法登陆</p>
<p>后来下载armbian镜像 MacOS端使用软件Etcher写入内存卡中</p>
<h3 id="开机"><a href="#开机" class="headerlink" title="开机"></a>开机</h3><p>插卡 上电 使用网线与路由器连接 等灯常亮 查看mac地址和IP地址</p>
<h3 id="连接"><a href="#连接" class="headerlink" title="连接"></a>连接</h3><p>armbian默认的用户为root 密码为1234</p>
<p>使用ssh连接 根据引导更改密码 创建新的用户 设置密码</p>
<p>根据指示 使用armbian-config设置Wi-Fi</p>
<p>更改/etc/sudoers文件为自己的用户设置管理员权限（可选）</p>
<p>更新软件 apt update 和 apt upgrade</p>
<p>在主机设置 ssh-copy-id username@address 并输入密码设置ssh公钥免密登陆</p>
<h2 id="基本软件安装"><a href="#基本软件安装" class="headerlink" title="基本软件安装"></a>基本软件安装</h2><p>常用软件大多系统内置 例如vim，make，g++等</p>
<p>还需要安装一些常用的软件(自选)：</p>
<p>基本软件：ssh tree less  vnc git</p>
<p>web服务器：apache2/nignx</p>
<p>MySQL数据库：mysql-server mysql-client libmysqlclient-dev</p>
<p>PHP支持：php</p>
<p>项目编译：cmake make</p>
<p>JAVA环境：default-jre default-jdk</p>
<p>Python环境：python python3 python3-pip virtualenv</p>
<p>NodeJS环境：npm</p>
<p>…</p>
<p>mysql需要切换至root登陆 然后添加新用户并发放权限</p>
<blockquote>
<p>CREATE USER ‘username’@’localhost’ IDENTIFIED BY ‘passwd’</p>
<p>GRANT ALL ON *.* TO ‘username’@’localhost’</p>
<p>SET PASSWORD FOR ‘username’@’localhost’ = PASSWORD(“passwd”)</p>
<p>SET PASSWORD = PASSWORD(“passwd”)</p>
</blockquote>
<h2 id="路由器设置"><a href="#路由器设置" class="headerlink" title="路由器设置"></a>路由器设置</h2><p>将Wi-Fi动态DHCP设置为静态IP地址分发 确定一个合适的IP地址</p>
<p>设置端口转发 将路由器的22端口转发板子的22端口用于外网ssh连接</p>
<p>将路由器的80端口转发板子的80端口用于外网对web服务器的访问</p>
<p>或者直接使用路由器的DMZ功能</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Orange Pi</tag>
      </tags>
  </entry>
  <entry>
    <title>iptables使用</title>
    <url>/2019/10/13/firewall-iptables/</url>
    <content><![CDATA[<p>课上学的iptables的使用方法简单笔记。</p>
<a id="more"></a>
<h2 id="数据包的传递"><a href="#数据包的传递" class="headerlink" title="数据包的传递"></a>数据包的传递</h2><h3 id="发往本机"><a href="#发往本机" class="headerlink" title="发往本机"></a>发往本机</h3><ol>
<li>网卡接收</li>
<li>mangle的PREROUTING</li>
<li>nat的PREROUTING（做DNAT转换）</li>
<li>路由判断本机/转发</li>
<li>mangle的INPUT</li>
<li>filter的INPUT</li>
<li>到达应用程序</li>
</ol>
<h3 id="发往外部"><a href="#发往外部" class="headerlink" title="发往外部"></a>发往外部</h3><ol>
<li>生成数据，路由判断</li>
<li>mangle的OUTPUT</li>
<li>nat的OUTPUT</li>
<li>filter的OUTPUT</li>
<li>mangle的POSTROUTING</li>
<li>nat的POSTROUTING（SNAT转换）</li>
</ol>
<h3 id="转发"><a href="#转发" class="headerlink" title="转发"></a>转发</h3><ol>
<li>网卡接收</li>
<li>mangle的PREROUTING</li>
<li>nat的PREROUTING</li>
<li>路由判断</li>
<li>mangle的FORWARD</li>
<li>filter的FORWARD</li>
<li>mangle的POSTROUTING</li>
<li>nat的POSTROUTING（SNAT转换）</li>
</ol>
<h2 id="命令使用"><a href="#命令使用" class="headerlink" title="命令使用"></a>命令使用</h2><p>命令格式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iptables -t TABLE [-operation] CHAIN [-matches ] -j TARGET</span><br></pre></td></tr></table></figure>
<p>即主要内容五部分：表、操作、链、匹配、目标动作</p>
<p>TABLE包括：filter，nat，mangle</p>
<p>CHAIN包括：INPUT，OUTPUT，FORWARD，PREROUTING，POSTROUTING</p>
<p>TARGET包括：ACCEPT、DROP、SNAT、DNAT</p>
<p>operation包括：-A(ppend添加) -I(sert插入) -D(elete删除) -R(eplace替换) -L(ist显示所有) -P(默认策略) -X(删除链) -N(新建链) -F(清空规则) -Z(清空计数器)</p>
<p>match包括：-p(rotocol协议) -s/d(源/目标IP) —s/dport(源/目标端口) -i/o(interface进出网络接口)</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>iptables</tag>
      </tags>
  </entry>
  <entry>
    <title>编辑器vim的使用</title>
    <url>/2019/10/11/linux-vim/</url>
    <content><![CDATA[<p>命令行编辑器vim的使用方法。<br>vim应该是linux最常用的编辑器。基本的使用方法如下。</p>
<a id="more"></a>
<h2 id="普通模式"><a href="#普通模式" class="headerlink" title="普通模式"></a>普通模式</h2><p>打开文档后的默认模式</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>按键</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>h/j/k/l</td>
<td>光标移动（左下上右）</td>
</tr>
<tr>
<td>ctrl+F/B</td>
<td>上一页/下一页</td>
</tr>
<tr>
<td>G</td>
<td>最后一行</td>
</tr>
<tr>
<td>行号+G</td>
<td>指定行</td>
</tr>
<tr>
<td>gg</td>
<td>第一行</td>
</tr>
</tbody>
</table>
</div>
<h3 id="基本编辑操作"><a href="#基本编辑操作" class="headerlink" title="基本编辑操作"></a>基本编辑操作</h3><div class="table-container">
<table>
<thead>
<tr>
<th>按键</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>x</td>
<td>删除字符</td>
</tr>
<tr>
<td>dd</td>
<td>删除整行</td>
</tr>
<tr>
<td>dw</td>
<td>删除单词</td>
</tr>
<tr>
<td>d$</td>
<td>删除该行光标后的部分</td>
</tr>
<tr>
<td>J</td>
<td>删除换行符</td>
</tr>
<tr>
<td>u</td>
<td>撤销命令</td>
</tr>
<tr>
<td>a/i</td>
<td>插入（ESC退出）</td>
</tr>
<tr>
<td>A</td>
<td>在行尾插入</td>
</tr>
<tr>
<td>r</td>
<td>替换单个字符</td>
</tr>
<tr>
<td>R</td>
<td>使用新的字符替换/覆盖（ESC退出）</td>
</tr>
<tr>
<td>o</td>
<td>下一行插入空行并进入插入模式</td>
</tr>
<tr>
<td>O</td>
<td>前一行插入空行</td>
</tr>
<tr>
<td>%</td>
<td>在配对的括号件移动</td>
</tr>
<tr>
<td>m</td>
<td>添加书签（使用单个小写字符作为书签名）</td>
</tr>
<tr>
<td>’</td>
<td>跳转到目标书签</td>
</tr>
<tr>
<td>v</td>
<td>可视化选择</td>
</tr>
</tbody>
</table>
</div>
<p>大部分命令前加数字表示连续操作多次</p>
<h3 id="复制粘贴操作"><a href="#复制粘贴操作" class="headerlink" title="复制粘贴操作"></a>复制粘贴操作</h3><p>dd+p可以用来剪切（p表示取回删除的数据）<br>y+p 命令表示复制粘贴<br>可以用 v（可视模式）选择文本 按y复制 按p粘贴</p>
<h3 id="查找数据"><a href="#查找数据" class="headerlink" title="查找数据"></a>查找数据</h3><p>斜线后加所要查找的字符 例如 /name<br>光标移动到向下遍历的找到的第一个数据<br>找到多个匹配时 按 n 表示下一个（next）</p>
<h2 id="命令行模式"><a href="#命令行模式" class="headerlink" title="命令行模式"></a>命令行模式</h2><div class="table-container">
<table>
<thead>
<tr>
<th>命令</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>:q!</td>
<td>取消修改并退出</td>
</tr>
<tr>
<td>:wq</td>
<td>保存并退出</td>
</tr>
<tr>
<td>:w !sudo tee %</td>
<td>强制保存修改只读文件</td>
</tr>
<tr>
<td>:w</td>
<td>保存（加文件名为另存为）</td>
</tr>
<tr>
<td>:s/old/new/g</td>
<td>全文替换</td>
</tr>
<tr>
<td>:n,ms/old/new/g</td>
<td>替换n-m行</td>
</tr>
<tr>
<td>:s/old/new/gc</td>
<td>替换前询问</td>
</tr>
<tr>
<td>:%s/old/new/g</td>
<td>替换整个文件</td>
</tr>
</tbody>
</table>
</div>
<h2 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h2><ul>
<li><p>使用vim命令打开文件，如果文件名不存在则会创建空文件</p>
</li>
<li><p>使用a/s/i进入插入模式（insert）输入代码，使用ESC退出</p>
</li>
<li><p>使用行数+G直接到达debug信息指定的行号</p>
</li>
<li><p>编辑结束，使用:wq退出</p>
</li>
</ul>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog编程语言接口</title>
    <url>/2019/10/11/verilog-vpi/</url>
    <content><![CDATA[<p>在MacOS上使用Icarus Verilog时实现编程语言接口PLI</p>
<a id="more"></a>
<h2 id="定义系统函数模块"><a href="#定义系统函数模块" class="headerlink" title="定义系统函数模块"></a>定义系统函数模块</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Icarus Verilog使用的头文件</span></span><br><span class="line"><span class="meta"># <span class="meta-keyword">include</span>  <span class="meta-string">&lt;vpi_user.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译时函数</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">hello_compiletf</span><span class="params">(<span class="keyword">char</span>* user_data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 调用时函数</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">hello_calltf</span><span class="params">(<span class="keyword">char</span>* user_data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">      vpi_printf(<span class="string">"Hello, World!\n"</span>);</span><br><span class="line">      <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 注册函数，注册编译时函数和调用时函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">hello_register</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">      s_vpi_systf_data tf_data;</span><br><span class="line"></span><br><span class="line">      tf_data.type      = vpiSysTask;</span><br><span class="line">      tf_data.tfname    = <span class="string">"$hello"</span>;</span><br><span class="line">      tf_data.calltf    = hello_calltf;</span><br><span class="line">      tf_data.compiletf = hello_compiletf;</span><br><span class="line">      tf_data.sizetf    = <span class="number">0</span>;</span><br><span class="line">      tf_data.user_data = <span class="number">0</span>;</span><br><span class="line">      vpi_register_systf(&amp;tf_data);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 加载模块时运行函数</span></span><br><span class="line"><span class="keyword">void</span> (*vlog_startup_routines[])() = &#123;</span><br><span class="line">    hello_register,</span><br><span class="line">    <span class="number">0</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>仿真加载PLI模块时，首先检查函数指针数组startup，用0作为数据结束符。</p>
<p>接着运行startup中指定的函数，通常为register函数，注册对应的系统函数。</p>
<p>compiletf函数为vvp加载模块自动编译时执行，通常用于监测param参数状态。也可以留空。</p>
<p>calltf指函数为verilog中每次调用PLI执行的结果。</p>
<p><strong>注意</strong><br>vpi<em>子程序是存取子程序(acc</em>)和实用子程序(tf_)的拓展集合。</p>
<h2 id="编译PLI模块"><a href="#编译PLI模块" class="headerlink" title="编译PLI模块"></a>编译PLI模块</h2><h3 id="使用GCC编译"><a href="#使用GCC编译" class="headerlink" title="使用GCC编译"></a>使用GCC编译</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 编译目标文件.o</span></span><br><span class="line">gcc -c -fpic hello.c</span><br><span class="line"><span class="comment"># 编译得到模块文件.vpi</span></span><br><span class="line">gcc -shared -o hello.vpi hello.o -lvpi</span><br></pre></td></tr></table></figure>
<p>前提：安装有vpi_user.h和vpi的动态链接库</p>
<h3 id="使用Icarus编译"><a href="#使用Icarus编译" class="headerlink" title="使用Icarus编译"></a>使用Icarus编译</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 编译得到.o和.vpi文件</span></span><br><span class="line">iverilog-vpi hello.c</span><br></pre></td></tr></table></figure>
<h2 id="调用系统函数"><a href="#调用系统函数" class="headerlink" title="调用系统函数"></a>调用系统函数</h2><p>在verilog中调用自定义系统函数</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">module</span> main;</span><br><span class="line"><span class="keyword">initial</span> $hello;</span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure>
<p>运行系统仿真</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 编译verilog文件得到仿真文件.vvp</span></span><br><span class="line">iverilog -ohello.vvp hello.v</span><br><span class="line"><span class="comment"># 运行仿真</span></span><br><span class="line">vvp -M. -mhello hello.vvp</span><br></pre></td></tr></table></figure>
<p>参数-M指定搜索路径 在这里为当前文件夹</p>
<p>参数-m指定系统函数模块名 在这里为hello</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Verilog HDL</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX图片、表格、参考文献和代码</title>
    <url>/2019/10/10/latex-tfbc/</url>
    <content><![CDATA[<p>LaTeX图片、表格、参考文献和代码部分</p>
<a id="more"></a>
<h3 id="图片部分"><a href="#图片部分" class="headerlink" title="图片部分"></a>图片部分</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;graphicx&#125; (graph包)</span><br><span class="line">​\usepackage&#123;caption&#125; (标注包)</span><br><span class="line">\usepackage&#123;graphics&#125;</span><br><span class="line">\usepackage&#123;float&#125;</span><br></pre></td></tr></table></figure>
<p>添加图片</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">% 图片和标注环境</span><br><span class="line">\begin&#123;figure&#125;[htbp]</span><br><span class="line">    % 居中命令</span><br><span class="line">    \centering</span><br><span class="line">    % 添加图片</span><br><span class="line">    \includegraphics[参数]&#123;图片名&#125;</span><br><span class="line">    % 参数包括  宽width=3.00in,高height=4.00in</span><br><span class="line">​    % 图片必须和.tex文件位于同一路径</span><br><span class="line">    % 图片编号和标注</span><br><span class="line">    \caption&#123;Figure #:balabala&#125;</span><br><span class="line">    % 图片标签</span><br><span class="line">    \label&#123;&#125;</span><br><span class="line">\end&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<p>多图片环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">​\begin&#123;figure&#125;[htbp]</span><br><span class="line">    \begin&#123;minipage&#125;[t]&#123;0.5/textwidth&#125;</span><br><span class="line">        \includegraphics[参数]&#123;图片名&#125;</span><br><span class="line">        \caption&#123;Figure #:balabala&#125;</span><br><span class="line">    \end&#123;minipage&#125;</span><br><span class="line">\end&#123;figure&#125;</span><br></pre></td></tr></table></figure>
<h3 id="表格部分"><a href="#表格部分" class="headerlink" title="表格部分"></a>表格部分</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">% 表格环境</span><br><span class="line">\begin&#123;tabular&#125;</span><br><span class="line">\end&#123;tabular&#125;</span><br><span class="line">% 表格宽度在begin后加&#123;|c|p&#123;4.5in&#125;&lt;&#123;\centering&#125;|&#125;表示第二列宽4.5寸且居中</span><br><span class="line">% 竖表格线在begin后加 &#123;|c|c|&#125;   竖线表示表格线 c表示居中 l表示左对齐 r表示右对齐</span><br><span class="line">% 横表格线在任一行加 \hline 表示在前后两部分之间加横线</span><br><span class="line">% 居中环境(不标序号)</span><br><span class="line">\begin&#123;center&#125;</span><br><span class="line">\end&#123;center&#125;</span><br><span class="line">% 引用环境（标序号）</span><br><span class="line">\begin&#123;equation&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<p>表格每个单元格高度修改</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;array&#125;</span><br><span class="line">\renewcommand\arraystretch&#123;倍数&#125;  即高度变为默认的n倍</span><br></pre></td></tr></table></figure>
<p>复杂表格</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;multicol&#125; \usepackage&#123;multirow&#125;</span><br><span class="line">​\multicolumn&#123;列数&#125;&#123;|c|&#125;&#123;内容&#125;</span><br><span class="line">​\multirow&#123;行数&#125;&#123;内容&#125;</span><br><span class="line">多行多列单元格合并\multicolumn&#123;列数&#125;&#123;|c|&#125;&#123;\multirow&#123;行数&#125;&#123;*&#125;&#123;内容&#125;&#125;</span><br></pre></td></tr></table></figure>
<p>表格填充颜色</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;colortbl&#125;</span><br><span class="line">\usepackage&#123;color&#125;</span><br><span class="line">\usepackage&#123;array&#125;</span><br><span class="line">\cellcolor[gray]&#123;灰度值&#125;&#123;内容&#125;</span><br><span class="line">\cellcolor&#123;颜色(例如 bule)&#125;&#123;内容&#125;</span><br><span class="line">\columncolor</span><br><span class="line">\rowcolor</span><br></pre></td></tr></table></figure>
<h3 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h3><p>引用宏包</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;cite&#125;</span><br><span class="line"></span><br><span class="line">% 引用编号为上标用</span><br><span class="line">\usepackage[superscript]&#123;cite&#125;</span><br><span class="line">% 引用编号为上标加中括号用</span><br><span class="line">\usepackage[super,square]&#123;natbib&#125;</span><br></pre></td></tr></table></figure>
<p>设置参考文献</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;thebibliography&#125;&#123;最大标号值&#125;</span><br><span class="line">    % 参考文献条目</span><br><span class="line">    \bibitem&#123;标签&#125;</span><br><span class="line">        %强调期刊名称为斜体</span><br><span class="line">        \emph&#123;&#125;</span><br><span class="line">\end&#123;thebibliography&#125;</span><br><span class="line"></span><br><span class="line">% 使用参考文献时</span><br><span class="line">\cite&#123;标签&#125;</span><br></pre></td></tr></table></figure>
<h2 id="代码部分"><a href="#代码部分" class="headerlink" title="代码部分"></a>代码部分</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;listings&#125;宏包</span><br><span class="line"></span><br><span class="line">\begin&#123;lstlisting&#125;&#123;language = &#125;</span><br><span class="line">会保留代码的格式，更改代码字体，语法高亮</span><br><span class="line">保留缩进</span><br><span class="line">\end&#123;lstlisting&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX数学公式</title>
    <url>/2019/10/10/latex-math/</url>
    <content><![CDATA[<p>LaTeX数学公式的基本格式<br>与大多数数学公式编辑器基本相同</p>
<a id="more"></a>
<h2 id="基本部分"><a href="#基本部分" class="headerlink" title="基本部分"></a>基本部分</h2><p>引用数学公式包</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\usepackage&#123;amsmath&#125; (math包)</span><br><span class="line">​\usepackage&#123;amssymb&#125; (symble包)</span><br></pre></td></tr></table></figure>
<p>基本的数学符号的表示</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">% 数学公式(一行内容)</span><br><span class="line">\$内容\$</span><br><span class="line">%数学公式（多行内容）</span><br><span class="line">\[ 内容 可换行 \]</span><br><span class="line">% 希腊字母</span><br><span class="line">\读音 (如\alpha \beta)</span><br><span class="line">% 分式</span><br><span class="line">\frac&#123;分子&#125;&#123;分母&#125;</span><br><span class="line">% 斜分数</span><br><span class="line">\usepackage&#123;xfrac&#125;</span><br><span class="line">\sfrac&#123;1&#125;&#123;2&#125;</span><br><span class="line"></span><br><span class="line">% 根式</span><br><span class="line">\sqrt&#123;底数&#125;</span><br><span class="line">% 上标（幂）</span><br><span class="line">\$底数^指数\$</span><br><span class="line">%下标</span><br><span class="line">\$主体_下标\$</span><br><span class="line"></span><br><span class="line">%偏导符号</span><br><span class="line">\partial</span><br><span class="line">%向量头顶箭头</span><br><span class="line">\vec</span><br><span class="line">%加粗</span><br><span class="line">\mathbf</span><br><span class="line">%对时间求导（头顶圆点）</span><br><span class="line">\dot</span><br><span class="line"></span><br><span class="line">% 正负号</span><br><span class="line">\pm （plus+minus）</span><br><span class="line">% 乘（叉乘）</span><br><span class="line">\times</span><br><span class="line">%除</span><br><span class="line">\div (divide)</span><br><span class="line">%点乘</span><br><span class="line">\cdot</span><br><span class="line"></span><br><span class="line">%交集</span><br><span class="line">\cap</span><br><span class="line">%并集</span><br><span class="line">\cup</span><br><span class="line"></span><br><span class="line">%大等于</span><br><span class="line">\geq</span><br><span class="line">%小等于</span><br><span class="line">\leq</span><br><span class="line">%不等于</span><br><span class="line">\neq</span><br><span class="line">%约等于</span><br><span class="line">\approx</span><br><span class="line">%恒等于</span><br><span class="line">\equiv</span><br><span class="line"></span><br><span class="line">%属于</span><br><span class="line">\in</span><br><span class="line">%不属于</span><br><span class="line">\notin</span><br><span class="line"></span><br><span class="line">% 求和（∑）</span><br><span class="line">\sum</span><br><span class="line">%连乘（π）</span><br><span class="line">\prod</span><br><span class="line">%极限</span><br><span class="line">\lim</span><br><span class="line">%积分</span><br><span class="line">\int \iint \iiint \idotsint</span><br><span class="line">%体积较大的运算符可以用 \limit 压缩体积</span><br></pre></td></tr></table></figure>
<h2 id="分隔符"><a href="#分隔符" class="headerlink" title="分隔符"></a>分隔符</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">%括号</span><br><span class="line">()</span><br><span class="line">%方括号</span><br><span class="line">[]</span><br><span class="line">%花括号</span><br><span class="line">\&#123; \&#125;</span><br><span class="line">%尖括号&lt;&gt;</span><br><span class="line">\langle \rangle</span><br><span class="line">%绝对值(单竖线)</span><br><span class="line">\lvert \rvert</span><br><span class="line">%向量绝对值（双竖线）</span><br><span class="line">\lVert \rVert</span><br><span class="line">%大小调整符（由大到小） 放在符号前调整大小</span><br><span class="line">\Biggl\biggl\Bigl\bigl</span><br><span class="line">\Biggr\biggr\Bigr\bigr</span><br><span class="line">%空格</span><br><span class="line">\</span><br><span class="line">% 半个字符宽</span><br><span class="line">\quad</span><br><span class="line">%一个字符宽</span><br><span class="line">\qquad</span><br><span class="line"></span><br><span class="line">矩阵、行列式、方程联立 两侧符号</span><br><span class="line">\left符号 \right符号</span><br></pre></td></tr></table></figure>
<h2 id="常见环境"><a href="#常见环境" class="headerlink" title="常见环境"></a>常见环境</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">%矩阵环境</span><br><span class="line">\begin&#123;array&#125; &#123;lcr&#125;内容左对齐 &#123;right&#125;右对齐 &#123;center&#125;中对齐</span><br><span class="line">    不同行的中间加\&amp; 自动将每一行的\&amp;对齐在同一列</span><br><span class="line">\end&#123;array&#125;</span><br><span class="line">​</span><br><span class="line">%公式对齐环境(不需加\[ \])</span><br><span class="line">\begin&#123;align&#125;</span><br><span class="line">​    将每一行中加的\&amp;对齐在同一列</span><br><span class="line">\end&#123;align&#125;</span><br><span class="line"></span><br><span class="line">%方程联立环境(必须加\[ \])</span><br><span class="line">\begin&#123;aligned&#125;</span><br><span class="line">​    左侧大括号\left\&#123; 右侧对应有\right.（这里有句号）</span><br><span class="line">\end&#123;aligned&#125;</span><br><span class="line"></span><br><span class="line">特殊矩阵环境：</span><br><span class="line">pmatrix 括号</span><br><span class="line">bmatrix 方括号</span><br><span class="line">Bmatrix 尖括号</span><br><span class="line">vmatrix 单竖线</span><br><span class="line">Vmatrix 双竖线</span><br><span class="line">smallmatrix 小矩阵</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX基础部分</title>
    <url>/2019/10/10/latex-basic/</url>
    <content><![CDATA[<p>LaTeX基础部分的使用方法</p>
<a id="more"></a>
<h3 id="文章框架"><a href="#文章框架" class="headerlink" title="文章框架"></a>文章框架</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">% 指定文章类型</span><br><span class="line">\documentclass[a4paper]&#123;article&#125;</span><br><span class="line">% ctex为中文环境</span><br><span class="line">\documentclass[a4paper]&#123;ctexart&#125;</span><br><span class="line">% 如不支持则使用</span><br><span class="line">% \usepackage&#123;ctex&#125;</span><br><span class="line"></span><br><span class="line">% 指明作者</span><br><span class="line">\author&#123;&#125;</span><br><span class="line">% 文章标题</span><br><span class="line">\title&#123;&#125;</span><br><span class="line">% 指定时间</span><br><span class="line">\date&#123;&#125;  </span><br><span class="line">% 可用参数 \today 括号内什么都不写则标题里不会写时间</span><br><span class="line"></span><br><span class="line">% 文章开始</span><br><span class="line">\begin&#123;document&#125;</span><br><span class="line">% 文章结束</span><br><span class="line">\end&#123;document&#125;</span><br><span class="line"></span><br><span class="line">% 写标题</span><br><span class="line">\maketitle</span><br><span class="line">% 目录</span><br><span class="line">\tableofcontents</span><br><span class="line">% 章节1</span><br><span class="line">\section&#123;&#125;</span><br><span class="line">% 章节1.1</span><br><span class="line">\subsection&#123;&#125;</span><br><span class="line">% 章节1.1.1</span><br><span class="line">\subsubsection&#123;&#125;</span><br><span class="line">% 段落开头加粗名词介绍</span><br><span class="line">\paragraph&#123;名词&#125;</span><br><span class="line">% 此段落加粗名词介绍</span><br><span class="line">\subparagraph&#123;名词&#125;</span><br><span class="line">% 换行</span><br><span class="line">\\</span><br><span class="line">% 翻页</span><br><span class="line">\newpage</span><br><span class="line">% 加粗黑体</span><br><span class="line">\textbf&#123;&#125;</span><br><span class="line">% 垂直间距</span><br><span class="line">\vskip1.0cm</span><br></pre></td></tr></table></figure>
<h3 id="添加目录树"><a href="#添加目录树" class="headerlink" title="添加目录树"></a>添加目录树</h3><p>超链接包\usepackage{hyperref}</p>
<p>可以在pdf中生成目录树和超链接<br>点击目录和参考文献可以自动跳转</p>
<p>默认情况下超链接通过红色边框标明<br>Colorlinks命令可以使超链接通过颜色标明<br>后接linkcolor=red citecolor=green可以指明不同种类链接的颜色</p>
<p>如果不想有颜色可以指明颜色为black，超链接功能仍适用<br>该方法可以不显示红色边框</p>
<h3 id="列举条目"><a href="#列举条目" class="headerlink" title="列举条目"></a>列举条目</h3><p>列举包\usepackage{enumerate}</p>
<p>条目环境\begin{itemize} \end{itemize}<br>​每一条目用\item （必须加空格）</p>
<p>列举环境\begin{enumerate}[参数] \end{enumerate}<br>​也需要用\item (加空格)<br>​参数标明序号格式 例 [part 1][i][<1>]</1></p>
<h3 id="页面边框"><a href="#页面边框" class="headerlink" title="页面边框"></a>页面边框</h3><p>边框包\usepackage{geometry}</p>
<p>​指定边框\geometry{left = cm, right = cm, top = cm, bottom = cm}</p>
<p>局部字体大小<br>\Huge \huge \LARGE \large \normalsize \small \footnotsize \scriptsize \tiny</p>
<h3 id="单页竖排"><a href="#单页竖排" class="headerlink" title="单页竖排"></a>单页竖排</h3><p>使用landscape包 \usepackage{lscape}</p>
<p>\begin{landscape}<br>\end{landscape}</p>
<h3 id="居中环境"><a href="#居中环境" class="headerlink" title="居中环境"></a>居中环境</h3><p>\begin{center}<br>\end{center}</p>
<p>或者在环境内加入<br>\centering</p>
<h3 id="页码设置"><a href="#页码设置" class="headerlink" title="页码设置"></a>页码设置</h3><p>页码样式\pagestyle{type}</p>
<p>本页面样式\thispagestyle{type}<br>无页码empty 无页眉页脚为页码plain 无页脚页眉有章节和页码headings 无页脚页眉只有页码myheadings</p>
<p>手动设置页码\setcounter{page}{number}<br>页码风格\pagenumberingstyle{style}<br>数字arabic 小写罗马字roman 大写罗马字Roman 小写字符alph 大写字符Alph</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux软件安装</title>
    <url>/2019/10/10/linux-software/</url>
    <content><![CDATA[<p>通常可以使用系统的包管理程序安装<br>也可以使用源码安装</p>
<a id="more"></a>
<h2 id="便捷安装"><a href="#便捷安装" class="headerlink" title="便捷安装"></a>便捷安装</h2><p>使用PMS（package management system）包管理系统</p>
<h3 id="dpkg-基于Debian的包管理系统命令"><a href="#dpkg-基于Debian的包管理系统命令" class="headerlink" title="dpkg 基于Debian的包管理系统命令"></a>dpkg 基于Debian的包管理系统命令</h3><p>aptitude 最常用的基于dpkg命令行的包管理系统实例</p>
<p>包括ubuntu、armbian等debian系的系统</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> apt       <span class="comment">#aptitude命令</span></span></span><br><span class="line">            update          #更新apt</span><br><span class="line">            upgrade         #更新所有软件</span><br><span class="line">            show            #查看程序包信息</span><br><span class="line">            search          #通过关键词搜索软件包</span><br><span class="line">            install         #安装</span><br><span class="line">            safe-upgrade    #安全更新</span><br><span class="line">            remove          #删除软件包 保留数据</span><br><span class="line">            purge           #彻底删除软件包和数据</span><br></pre></td></tr></table></figure>
<p>文件/etc/apt/source.list是软件仓库</p>
<p>通常不需要更改，默认的软件仓库基本可以满足需求</p>
<p>有需要时在其他软件仓库官网上复制仓库信息并加入仓库配置文件中</p>
<h3 id="rpm-基于RedHat的包管理系统"><a href="#rpm-基于RedHat的包管理系统" class="headerlink" title="rpm 基于RedHat的包管理系统"></a>rpm 基于RedHat的包管理系统</h3><p>yum  最常用的基于rpm命令行的包管理系统</p>
<p>包括centOS、fedora等redhat系的系统</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> yum       <span class="comment">#yum命令</span></span></span><br><span class="line">            install         #安装</span><br><span class="line">            list installed  #查看已安装</span><br><span class="line">            provides        #查看文件对应的软件包</span><br><span class="line">            remove          #删除软件包 保留数据</span><br><span class="line">            erase           #删除软件包和数据</span><br><span class="line">            repolist        #可以显示软件来源仓库</span><br></pre></td></tr></table></figure>
<p>可以手动下载rpm文件并用yum的localinstall命令本地安装</p>
<p>有时存在软件包库依赖关系损坏的情况<br>一般先clean all后updae<br>或者用deplist显示库依赖后手动修改<br>最终也可以update - -skip-broken</p>
<h2 id="源码安装"><a href="#源码安装" class="headerlink" title="源码安装"></a>源码安装</h2><p>不用包管理系统 从源码安装软件</p>
<p>下载tar文件（一般从官网下载 .tar.gz文件）</p>
<p>用tar -zxvf解压后 <strong>阅读README文件</strong></p>
<p>一般根据README文件的指示 make或者make install命令可以将软件安装</p>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux基础操作</title>
    <url>/2019/10/08/linux-basic/</url>
    <content><![CDATA[<p>Linux基础操作</p>
<p><strong><em>学会查看帮助文档！</em></strong></p>
<p>使用man或help或info查看具体使用方法</p>
<a id="more"></a>
<h2 id="文件-目录操作"><a href="#文件-目录操作" class="headerlink" title="文件/目录操作"></a>文件/目录操作</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">pwd</span>           <span class="comment"># 显示当前目录</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ls            <span class="comment"># 查看当前目录内容</span></span></span><br><span class="line">                # 常用方法为加-alF或者-as或者-li</span><br><span class="line">                -i # 显示文件编号</span><br><span class="line">                -d # 显示dir本身</span><br><span class="line">                --color # 颜色显示</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> touch         <span class="comment"># 创建空文件,也能刷新修改时间</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> cp            <span class="comment"># 复制文件</span></span></span><br><span class="line">                -i # 询问是否覆盖</span><br><span class="line">                -r # 可递归复制目录所有内容</span><br><span class="line"><span class="meta">$</span><span class="bash"> ln            <span class="comment"># 创建硬链接</span></span></span><br><span class="line">                -s # 创建符号链接</span><br><span class="line"><span class="meta">$</span><span class="bash"> readlink      -f <span class="comment"># 查看链接文件的最末端</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> mv            <span class="comment"># 移动或者重命名(重命名后修改时间和文件编号都不变)</span></span></span><br><span class="line">                # -i询问是否覆盖</span><br><span class="line"><span class="meta">$</span><span class="bash"> rm            <span class="comment"># 删除文件</span></span></span><br><span class="line">                -i # 询问是否确认删除</span><br><span class="line">                -r # 向下递归所有子目录</span><br><span class="line">                -rf # 强制删除整个目录</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> mkdir         <span class="comment"># 创建目录</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> rmdir         <span class="comment"># 删除空目录</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> tree          <span class="comment"># 查看目录信息(一个工具 需要apt安装)</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> file          <span class="comment"># 查看文件类型</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> cat           <span class="comment"># 查看文件内容</span></span></span><br><span class="line">                -n # 内容全部加行号</span><br><span class="line">                -b # 有字的内容加行号</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> more          <span class="comment"># 分页显示文本内容</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> less          <span class="comment"># 分页显示文本内容（需要apt安装）</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> tail          <span class="comment"># 查看文件末尾内容</span></span></span><br><span class="line">                -n # 指明行数</span><br><span class="line">                -f # 自动刷新(实时监测log文件末尾)</span><br><span class="line"><span class="meta">$</span><span class="bash"> head          <span class="comment"># 查看文件头</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> cmp           <span class="comment"># 比较文件</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> diff          <span class="comment"># 逐行显示文件区别</span></span></span><br><span class="line">                # 分别表示为a添加c改变d删去</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> gzip          <span class="comment"># 压缩文件.gz</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> zcat          <span class="comment"># 查看压缩文件.gz</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> gunziq        <span class="comment"># 解压文件.gz</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> tar           <span class="comment"># 归档命令</span></span></span><br><span class="line">                -cvf # 创建 显示 输出文件 .tar</span><br><span class="line">                -tf # 列出内容但是不提取</span><br><span class="line">                -xvf # 提取全部文件 显示 .tar</span><br><span class="line">                -zxvf # 解压.tgz的文件[gzip+tar]</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> grep          <span class="comment"># 搜索数据</span></span></span><br><span class="line">                -v # 反向搜索（不含某字段的行）</span><br><span class="line">                -n # 显示行号</span><br><span class="line">                -c # 显示匹配到的数量</span><br><span class="line">                -e # 添加多个搜索的字段</span><br><span class="line">                # 匹配模式默认支持正则表达式</span><br></pre></td></tr></table></figure>
<h2 id="进程操作"><a href="#进程操作" class="headerlink" title="进程操作"></a>进程操作</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ps            <span class="comment"># 监测系统进程</span></span></span><br><span class="line">                -ef # 所有进程的详细信息</span><br><span class="line">                -l  # 扩展信息</span><br><span class="line">                -f  # 显示较详细信息</span><br><span class="line">                --forest # 用tree的方法表明父子进程</span><br><span class="line"><span class="meta">$</span><span class="bash"> top           <span class="comment"># 实时监测系统进程</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> basename      <span class="comment"># 剥离路径名 显示文件名</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> killall       <span class="comment"># 中断进程 与kill类似</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">kill</span>          <span class="comment"># 向进程发出信号 默认为TERM</span></span></span><br><span class="line">                # 后加进程代码和信号参数</span><br><span class="line">                -s # 强制发送信号</span><br><span class="line"><span class="meta">#</span><span class="bash"><span class="comment">#####################################</span></span></span><br><span class="line"><span class="meta">#</span><span class="bash"> <span class="built_in">kill</span>的信号参数：</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> KILL      无条件终止【SIGKILL信号】</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> HUP       挂起【SIGHUP信号】</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> INT       中断【SIGINT信号】</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> TERM      尽可能终止【SIGTERM信号】</span></span><br><span class="line"><span class="meta">#</span><span class="bash"><span class="comment">#####################################</span></span></span><br></pre></td></tr></table></figure>
<h2 id="系统操作"><a href="#系统操作" class="headerlink" title="系统操作"></a>系统操作</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> mount         <span class="comment"># 挂载媒体命令</span></span></span><br><span class="line">                # 默认输出已挂载设备列表</span><br><span class="line">                -t # 手动挂载时指定type 包括vfat，ntfs类型等</span><br><span class="line"><span class="meta">$</span><span class="bash"> umount        <span class="comment"># 卸载设备</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> df            <span class="comment">#查看磁盘空间</span></span></span><br><span class="line">                -h # 用M和G表示磁盘空间</span><br><span class="line"><span class="meta">$</span><span class="bash"> du            <span class="comment"># 显示目录下所有文件占用的空间</span></span></span><br><span class="line">                -c # 显示总大小</span><br><span class="line">                -h # 用M和G表示磁盘空间</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> date          <span class="comment">#显示时间</span></span></span><br><span class="line">                +%y%m%d # 按照两位数年月日显示</span><br><span class="line">                +%H%M%S # 按照时分秒显示</span><br><span class="line">                # 其他格式参见man文件</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> who           <span class="comment"># 显示登录信息</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> uptime        <span class="comment"># 显示系统登陆时间信息</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> tty           <span class="comment"># 显示登陆所用终端文件路径</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> cal           <span class="comment"># 显示日历</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> write         <span class="comment"># 用户间单方面传递信息</span></span></span><br><span class="line">                # 可以重定向文件</span><br><span class="line"><span class="meta">$</span><span class="bash"> talk          <span class="comment"># 用户间建立交互式信息传递</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> wall          <span class="comment"># 对所有用户传递信息</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> mesg          <span class="comment"># 通信设置（是否接受他人信息等）</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">$</span><span class="bash"> telnet        <span class="comment"># 链接网络上的远程主机</span></span></span><br><span class="line">                # 后接URL或者IP地址和端口</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Hexo搭建个人博客并部署到私人服务器</title>
    <url>/2019/10/07/hexo-blog/</url>
    <content><![CDATA[<p>使用hexo搭建个人博客，即本站的搭建过程。<br>希望能够帮助到有需要的人。</p>
<a id="more"></a>
<h2 id="本地环境"><a href="#本地环境" class="headerlink" title="本地环境"></a>本地环境</h2><h3 id="nodeJS环境"><a href="#nodeJS环境" class="headerlink" title="nodeJS环境"></a>nodeJS环境</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install node</span><br></pre></td></tr></table></figure>
<h3 id="初始化Hexo"><a href="#初始化Hexo" class="headerlink" title="初始化Hexo"></a>初始化Hexo</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装依赖环境</span></span><br><span class="line">$ npm install -g hexo-cli</span><br><span class="line">$ npm install hexo-deployer-git --save</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建博客目录</span></span><br><span class="line">$ mkdir Blog</span><br><span class="line">$ <span class="built_in">cd</span> Blog</span><br><span class="line"></span><br><span class="line"><span class="comment"># 博客初始化</span></span><br><span class="line">$ hexo init</span><br><span class="line">$ npm install</span><br></pre></td></tr></table></figure>
<h3 id="Hexo使用"><a href="#Hexo使用" class="headerlink" title="Hexo使用"></a>Hexo使用</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新建页面</span></span><br><span class="line">$ hexo new page categories</span><br><span class="line">$ hexo new page tags</span><br><span class="line"><span class="comment"># 新建并发布草稿</span></span><br><span class="line">$ hexo new draft my_draft</span><br><span class="line">$ hexo publish my_draft</span><br><span class="line"><span class="comment"># 新建文章</span></span><br><span class="line">$ hexo new post my_post</span><br><span class="line"><span class="comment"># 开启本地服务器</span></span><br><span class="line">$ hexo server (--debug)</span><br><span class="line"><span class="comment"># 生成静态文件</span></span><br><span class="line">$ hexo generate</span><br><span class="line">$ hexo g</span><br><span class="line"><span class="comment"># 部署到服务器</span></span><br><span class="line">$ hexo deploy</span><br><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>
<h3 id="密钥生成和提交"><a href="#密钥生成和提交" class="headerlink" title="密钥生成和提交"></a>密钥生成和提交</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 生成公钥和私钥</span></span><br><span class="line">$ <span class="built_in">cd</span> ~/.ssh</span><br><span class="line">$ ssh-keygen</span><br><span class="line"><span class="comment"># 将公钥复制到服务器</span></span><br><span class="line">$ ssh-copy-id username@address</span><br></pre></td></tr></table></figure>
<h2 id="服务器环境"><a href="#服务器环境" class="headerlink" title="服务器环境"></a>服务器环境</h2><h3 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 软件安装</span></span><br><span class="line">$ apt install nginx</span><br><span class="line">$ apt install nodejs</span><br><span class="line">$ apt install git</span><br><span class="line"><span class="comment"># 新建git用户</span></span><br><span class="line"><span class="comment"># 并添加到 /etc/sudoers</span></span><br><span class="line">$ adduser git</span><br><span class="line">$ vim /etc/passwd</span><br><span class="line"><span class="comment"># 修改git用户的默认shell为/ysr/bin/git-shell</span></span><br><span class="line"><span class="comment"># 禁止git用户用ssh登陆</span></span><br></pre></td></tr></table></figure>
<h3 id="仓库设置"><a href="#仓库设置" class="headerlink" title="仓库设置"></a>仓库设置</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 来到仓库位置</span></span><br><span class="line">$ <span class="built_in">cd</span> /home/git</span><br><span class="line"><span class="comment"># 创建空仓库</span></span><br><span class="line">$ git init --bare Blog.git</span><br><span class="line"><span class="comment"># 创建临时文件夹</span></span><br><span class="line">$ mkdir tmp/Bog</span><br><span class="line"><span class="comment"># 设置git钩子</span></span><br><span class="line">$ vim Blog.git/hooks/post-receive</span><br><span class="line"><span class="comment"># 设置权限</span></span><br><span class="line">$ chmod +x Blog.git/hooks/post-receive</span><br><span class="line">$ chown -R git:git Blog.git</span><br><span class="line">$ chmod 777 -R Blog.git</span><br></pre></td></tr></table></figure>
<p>使用的hooks脚本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/bin/bash -l</span></span><br><span class="line"></span><br><span class="line">GIT_REPO=/home/git/Blog.git</span><br><span class="line"></span><br><span class="line">TMP_GIT_CLONE=/home/git/tmp/Blog</span><br><span class="line"></span><br><span class="line">PUBLIC_WWW=/var/www/blog</span><br><span class="line"></span><br><span class="line">rm -rf <span class="variable">$&#123;TMP_GIT_CLONE&#125;</span></span><br><span class="line"></span><br><span class="line">git <span class="built_in">clone</span> <span class="variable">$GIT_REPO</span> <span class="variable">$TMP_GIT_CLONE</span></span><br><span class="line"></span><br><span class="line">rm -rf <span class="variable">$&#123;PUBLIC_WWW&#125;</span></span><br><span class="line"></span><br><span class="line">cp -rf <span class="variable">$&#123;TMP_GIT_CLONE&#125;</span> <span class="variable">$&#123;PUBLIC_WWW&#125;</span></span><br></pre></td></tr></table></figure>
<p>另一个可选的脚本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line">git --work-tree=/var/www/blog --git-dir=/home/git/Blog.git checkout -f</span><br></pre></td></tr></table></figure>
<h3 id="web服务器设置"><a href="#web服务器设置" class="headerlink" title="web服务器设置"></a>web服务器设置</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建静态文件目录</span></span><br><span class="line">$ rm -rf /var/www/html</span><br><span class="line">$ mkdir /var/www/blog</span><br><span class="line">$ chmod 777 /var/www</span><br><span class="line"><span class="comment"># 设置nginx</span></span><br><span class="line">$ vim /etc/nginx/site-available/default</span><br><span class="line">$ mkdir /home/git/nginxlog</span><br><span class="line">$ touch /home/git/nginxlog/blog_access.log</span><br><span class="line">$ touch /home/git/nginxlog/blog_error.log</span><br><span class="line"><span class="comment"># 重启web服务器</span></span><br><span class="line">$ service nginx restart</span><br></pre></td></tr></table></figure>
<p>nignx配置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen 80 default_server;</span><br><span class="line">    listen [::]:80 default_server;</span><br><span class="line"></span><br><span class="line">    server_name _;</span><br><span class="line"></span><br><span class="line">    access_log  /home/git/nginxlog/blog_access.log;</span><br><span class="line">    error_log  /home/git/nginxlog/blog_error.log;</span><br><span class="line"></span><br><span class="line">    root /var/www/blog;</span><br><span class="line">    index index.html</span><br><span class="line"></span><br><span class="line">    location / &#123;</span><br><span class="line">        try_files $uri $uri/ =404;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虚拟主机配置</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> /etc/nginx/sites-available</span><br><span class="line"><span class="comment"># 修改虚拟主机配置</span></span><br><span class="line"><span class="comment"># 单物理主机多虚拟主机</span></span><br><span class="line">$ vim new_server</span><br><span class="line">$ mkdir /var/www/new_server</span><br><span class="line">$ touch /home/git/nginxlog/new_server_access.log</span><br><span class="line">$ touch /home/git/nginxlog/new_server_error.log</span><br><span class="line"><span class="comment"># 链接激活</span></span><br><span class="line">$ ln /etc/nginx/sites-available /etc/nginx/sites-enable</span><br><span class="line"><span class="comment"># 检查web服务器</span></span><br><span class="line">$ service nginx status</span><br><span class="line">$ service nginx restart</span><br></pre></td></tr></table></figure>
<h2 id="git自动部署"><a href="#git自动部署" class="headerlink" title="git自动部署"></a>git自动部署</h2><h3 id="修改部署配置"><a href="#修改部署配置" class="headerlink" title="修改部署配置"></a>修改部署配置</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line"><span class="attr">  type:</span> <span class="string">git</span></span><br><span class="line"><span class="attr">  message:</span> <span class="string">post-receive</span></span><br><span class="line"><span class="attr">  repo:</span> <span class="string">git@address:/home/git/blog.git</span></span><br><span class="line"><span class="attr">  branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>
<p>接下来可以正常部署</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>
<h3 id="使用github-pages"><a href="#使用github-pages" class="headerlink" title="使用github pages"></a>使用github pages</h3><p>github提供了免费的github pages</p>
<p>可以创建一个仓库，仓库名为username.github.io</p>
<p>然后将静态文件上传即可使用</p>
<p>在deploy设置中更改对应的repo地址即可</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第一章:绪论</title>
    <url>/2019/10/06/melonC1/</url>
    <content><![CDATA[<blockquote>
<p>机器学习所研究的主要内容，是关于在计算机上从数据中产生“模型“的算法，即”学习算法“。</p>
</blockquote>
<p>模型泛指从数据中学得的结果。</p>
<a id="more"></a>
<h2 id="相关的术语"><a href="#相关的术语" class="headerlink" title="相关的术语"></a>相关的术语</h2><p>数据集，示例/样本，属性/特征，属性值，属性空间/样本空间/输入空间，样本是一个属性空间中的向量。</p>
<p>从数据中得到模型的过程叫训练/学习，训练样本，训练集，学得的模型为假设。</p>
<p>预测模型，标记，样例是有标记的示例，标记空间/输出空间。</p>
<p>预测离散值的为分类任务，预测连续值的为回归任务。</p>
<p>测试样本，测试集，测试。</p>
<p>聚类，簇。</p>
<p>监督学习/有标记/分类和回归等。无监督学习/无标记/聚类等。</p>
<p>泛化能力：训练集训练得到的模型适用于属性空间的新样本。</p>
<p>假设所有的样本满足独立同分布。</p>
<h2 id="假设空间"><a href="#假设空间" class="headerlink" title="假设空间"></a>假设空间</h2><p>从样例中学习是一个归纳的过程，也叫归纳学习。</p>
<p>学习过程可看作在所有假设组成的空间中搜索的过程。实际过程中假设空间往往极其巨大。</p>
<p>实际应用中往往得到满足训练集的假设集合，成为版本空间。</p>
<h2 id="归纳偏好"><a href="#归纳偏好" class="headerlink" title="归纳偏好"></a>归纳偏好</h2><p>具体的学习算法必定要从假设空间中选择一个模型/假设</p>
<p>通常的选择有：”尽可能特殊”/“使用情形尽可能少” 或者 “尽可能一般”/“适用情形尽可能多”</p>
<blockquote>
<p>学习算法在学习过程中对某种类型假设的偏好，成为归纳偏好，简称偏好。</p>
</blockquote>
<p>最常用的基本原则为奥卡姆剃刀原理，选择最简单的假设。</p>
<p>“没有免费的午餐”定理（No Free Lunch Theorem）不同的学习算法的误差的期望相同。</p>
<p>NFL的前提为所有问题出现的机会相同或所有问题的重要程度相同。</p>
<blockquote>
<p>脱离具体问题，空泛的谈论“什么学习算法更好”毫无意义，因为若考虑所有潜在的问题，则所有的学习算法一样好。要谈论算法的相对优劣，必须要针对具体的学习问题。</p>
</blockquote>
<h2 id="发展历程和现状"><a href="#发展历程和现状" class="headerlink" title="发展历程和现状"></a>发展历程和现状</h2><p>符号主义学习：决策树和基于逻辑的学习。决策树学习以信息论为基础，以信息熵最小化为目标。</p>
<p>连接主义学习：基于神经网络</p>
<p>统计学习：支持向量机和核方法</p>
<p>深度学习</p>
<p>数据挖掘：数据库的研究提供数据管理技术。机器学习和统计学研究提供数据分析技术。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Machine Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>西瓜书第二章:模型评估与选择</title>
    <url>/2019/10/06/melonC2/</url>
    <content><![CDATA[<blockquote>
<p>Machine Learning is that, for a specific task T, based on the experience E, improve the performance P.</p>
</blockquote>
<p>模型评估的依据就是这里的performance P。</p>
<a id="more"></a>
<h2 id="经验误差和过拟合"><a href="#经验误差和过拟合" class="headerlink" title="经验误差和过拟合"></a>经验误差和过拟合</h2><p>错误率  $E = \frac  { 错误样本a  }    { 样本总数m  }  $</p>
<p>精度  = 1 - 错误率</p>
<p>训练误差/经验误差：在训练集上的误差。泛化误差：在新样本上的误差。</p>
<p>过拟合：将训练样本的特点当作是一般性质，泛化性能下降。机器学习的关键障碍。</p>
<p>欠拟合：学习效果较差。</p>
<p>机器学习中的“模型选择”问题：选择合适的学习算法和参数配置。理论上选择泛化误差最小的。</p>
<p>实际上泛化误差难以获得，训练误差存在过拟合。</p>
<h2 id="模型评估方法"><a href="#模型评估方法" class="headerlink" title="模型评估方法"></a>模型评估方法</h2><p>使用测试误差来作为泛化误差的近似。测试集应该与训练集互斥。</p>
<p>方法：对数据集D进行分割得到训练集S和测试集T</p>
<p><strong><em>这里的测试集可以称为验证集，用于区分训练得到的模型实际使用中的数据</em></strong></p>
<h3 id="留出法"><a href="#留出法" class="headerlink" title="留出法"></a>留出法</h3><p>直接将数据集划分为两个集合。需要注意训练/测试集的划分要保持数据分布的一致性。即分层采样。</p>
<p>通常需要若干次随机划分、重复实验评估后取平均值。</p>
<p>常规选取2/3-4/5的数据用于训练。</p>
<h3 id="交叉验证法"><a href="#交叉验证法" class="headerlink" title="交叉验证法"></a>交叉验证法</h3><p>将数据集划分为k个子集。每次将k-1个子集的并集用做训练，剩下的自己用做测试。称为k折交叉验证。</p>
<p>通常需要进行若干次划分。例如十次十折交叉验证为进行100次训练和测试。</p>
<p><strong>特例：</strong>留一法，对于m个样本的数据集，分成m个子集，训练集与原始样本相比只少了一个样本。</p>
<p>留一法的缺陷为数据集很大时的训练成本。</p>
<h3 id="自助法"><a href="#自助法" class="headerlink" title="自助法"></a>自助法</h3><p>从原始数据集D中有放回的选择m个集合形成采样集D‘，D中有部分样本重复出现，有部分样本不出现。</p>
<p>最终数据集D中有$\frac  { 1  }    { e  }   = 0.368$的样本不出现。使用D‘作为训练集，D/D’作为测试集。</p>
<p>适用于数据集较小的情况。</p>
<h3 id="调参"><a href="#调参" class="headerlink" title="调参"></a>调参</h3><p>大多数学习算法需要调节参数，不同的参数配置有不同的训练结果。</p>
<p>模型评估和选择调参等任务完成后，要使用原始的完整的数据集D训练模型，得到最后交付的模型。</p>
<h2 id="性能度量"><a href="#性能度量" class="headerlink" title="性能度量"></a>性能度量</h2><p>评估模型/学习器的性能，要将预测结果与真实标记相比较。</p>
<p>回归任务常用的度量为均方误差。</p>
<h3 id="错误率和精度"><a href="#错误率和精度" class="headerlink" title="错误率和精度"></a>错误率和精度</h3><p>错误率 <script type="math/tex">E(f;D) = \frac  { 1  }    { m  }   \Sigma_{ i=1  }  ^  { m  }  \mathbb  { I  }  (f(x_  { i  }  )\neq y_  { i  }  )</script>  </p>
<p>精度 <script type="math/tex">acc(f;D) = 1 - E(f;D)</script>  </p>
<p>其中 <script type="math/tex">\mathbb  { I  }  (·)</script>  为示性函数，· 为真时值为1，假时值为0.</p>
<h3 id="查准率和查全率"><a href="#查准率和查全率" class="headerlink" title="查准率和查全率"></a>查准率和查全率</h3><p>分类结果矩阵</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>真实情况</th>
<th>预测结果（正例）</th>
<th>预测结果（反例）</th>
</tr>
</thead>
<tbody>
<tr>
<td>正例</td>
<td>TP 真正例</td>
<td>FN 假反例</td>
</tr>
<tr>
<td>反例</td>
<td>FP 假正例</td>
<td>TN 真反例</td>
</tr>
</tbody>
</table>
</div>
<p>查准率/准确率  <script type="math/tex">P = \frac  { TP  }    { TP+FP  }</script>   预测的<strong>正例中确实是正例</strong>的比例</p>
<p>查全率/召回率  <script type="math/tex">R = \frac  { TP  }    { TP+FN  }</script>  <strong>实际的正例中被找到</strong>的比例</p>
<p>通常使用P-R曲线进行分析：将学习器的预测结果按可能性和确切程度排序，依次把每个样本作为正例，计算并绘制P-R曲线，查准率为纵轴，查全率为横轴。如果一个学习器的P-R曲线可以将另一个完全包住，认为前者性能更好。</p>
<p>P-R曲线的评估：</p>
<ul>
<li>曲线下面积的大小，</li>
<li>平衡点BEP（P = R处的值）</li>
<li>F1度量（调和平均 <script type="math/tex">F1 = \frac  { 2 \times P \times R  }    { P + R  }   = \frac  { 2 \times TP  }    { M + TP - TN  }</script>  其中M为样本总数）</li>
<li>加权F1（对于P或R有倾向性，用$\beta$表示查全率的重要程度  <script type="math/tex">F_  { \beta  }   = \frac  { 1  }    { 1+\beta^  { 2  }    }  (\frac  { 1  }    { P  }  +\frac  { \beta^  { 2  }    }    { R  }  )</script> ）</li>
</ul>
<p>实际使用中可能有多个混淆矩阵，例如多次训练/测试等，可以得到两套参数</p>
<ul>
<li>宏查准率，宏查全率，宏F1：对每一个混淆矩阵单独计算P、R、F1，最后取平均值</li>
<li>微查准率，微查全率，微F1：对所有混淆矩阵的对应元素求平均值，再计算P、R、F1</li>
</ul>
<h3 id="ROC和AUG"><a href="#ROC和AUG" class="headerlink" title="ROC和AUG"></a>ROC和AUG</h3><p>ROC称为受试者工作特征曲线。真正例率TPR为纵轴，假正例率FPR为横轴。将预测结果排序，依次作为正例，计算并绘制曲线。其中有 <script type="math/tex">TPR = \frac  { TP  }    { TP+FN  }</script>   <script type="math/tex">FPR = \frac  { FP  }    { TN + FP  }</script>  。</p>
<p>AUG为ROC曲线下方区域的面积。</p>
<p>由于ROC是通过有限个测试样本生成，因此得不到光滑曲线，面积的计算通过求和完成。</p>
<p>每一条ROC对应于样本的一个排序序列。</p>
<p>定义排序损失 <script type="math/tex">l_{ rnak  }   = \frac  { 1  }    { m^  { +  }  m^  { -  }    }   \sum_  { x^  { +  }  \in D^  { +  }    }   \sum_  { x^  { -  }   \in D^  { -  }    }  \left( \mathbb  { I  }  (f(x^  { +  }  ) < f(x^  { -  }  )) + \frac  { 1  }    { 2  }  \mathbb  { I  }  (f(x^  { +  }  )= f(x^  { -  }  )) \right)</script></p>
<p>即考虑对任何一对正反例，序列中如果正例在反例之后则记为1，相同则记0.5，最后求平均损失。</p>
<p>有 <script type="math/tex">AUG = 1- l_  { rank  }</script>  </p>
<h3 id="代价敏感错误率与代价曲线"><a href="#代价敏感错误率与代价曲线" class="headerlink" title="代价敏感错误率与代价曲线"></a>代价敏感错误率与代价曲线</h3><p>代价矩阵</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>真实情况</th>
<th>预测情况（正例）</th>
<th>预测情况（反例）</th>
</tr>
</thead>
<tbody>
<tr>
<td>正例</td>
<td>0（预测正确）</td>
<td>$cost_  { 10  }  $</td>
</tr>
<tr>
<td>反例</td>
<td>$cost_  { 01  }  $</td>
<td>0（预测正确）</td>
</tr>
</tbody>
</table>
</div>
<p>预测过程中可能会出现两类错误，其代价不同。</p>
<p>绘制代价曲线表示学习器的期望总体代价。横轴为正例概率代价（预测为正例），纵轴为归一化代价。</p>
<p>将ROC上的每一个点转化为一个线段。最终取所有线段的下界围成的面积为该ROC的期望总体代价。</p>
<p>其中正例概率代价 <script type="math/tex">P(+)cost = \frac  { p \times cost_{ 01  }    }    { p \times cost_{ 01  }   + (1-p) \times cost_  { 10  }    }</script>  p为预测结果为正例的概率。</p>
<p>归一化代价 <script type="math/tex">cost_{ norm  }   = \frac  {   { FNR \times p \times cost_{ 01  }   + FPR \times (1-p) \times cost_{ 10  }    } +   }    { p \times cost_{ 01  }   + (1-p) \times cost_{ 10  }    }</script>  FPR为假正例率，FNR为假反例率。</p>
<h2 id="比较检验"><a href="#比较检验" class="headerlink" title="比较检验"></a>比较检验</h2><p>使用统计假设检验，判断给定置信度下假设结果的可靠性。</p>
<p>参见概率论和数理统计课本。</p>
<h3 id="假设检验"><a href="#假设检验" class="headerlink" title="假设检验"></a>假设检验</h3><p>二项检验</p>
<p>t-检验</p>
<h3 id="交叉验证t检验"><a href="#交叉验证t检验" class="headerlink" title="交叉验证t检验"></a>交叉验证t检验</h3><p>对于多个学习器成对检验。</p>
<h3 id="McNemar检验"><a href="#McNemar检验" class="headerlink" title="McNemar检验"></a>McNemar检验</h3><p>也即卡方分布</p>
<h3 id="Friedman检验和Nemenyi后续检验"><a href="#Friedman检验和Nemenyi后续检验" class="headerlink" title="Friedman检验和Nemenyi后续检验"></a>Friedman检验和Nemenyi后续检验</h3><p>省略</p>
<h2 id="偏差与方差"><a href="#偏差与方差" class="headerlink" title="偏差与方差"></a>偏差与方差</h2><p>使用偏差-方差分解，对学习算法的期望泛化错误率进行拆解。</p>
<p>如果样本为x，数据集中的标记$y_  { D  }  $，真实标记y，f(x;D)为预测输出，</p>
<p>期望 <script type="math/tex">\bar  { f  }  (x) = \mathbb  { E  }  _  { D  }  [f(x;D)]</script>  </p>
<p>方差 <script type="math/tex">var(x) = \mathbb  { E  }  _  { D  }  [(f(x;D) - \bar  { f  }  (x))^  { 2  }  ]</script>  </p>
<p>噪声 <script type="math/tex">\epsilon^  { 2  }   = \mathbb  { E  }  _  { D  }  [(y_  { D  }   - y)^  { 2  }  ]</script>  理解为输入样本和真实情况的误差（采样过程引入的噪声）</p>
<p>偏差 <script type="math/tex">bias^  { 2  }  (x) = (\bar  { f  }  (x) - y)^  { 2  }</script>  即系统输出与真实情况的差距</p>
<p>便于分析使得噪声期望为0，即  <script type="math/tex">\mathbb  { E  }  _  { D  }  [y_  { D  }   - y] = 0</script></p>
<p>最终得到如下泛化误差</p>
<script type="math/tex; mode=display">E(f;D) \\= \mathbb  {E}_{ D  }  [(f(x;D) - y _{ D  }  )^  { 2  }  ]\\= \mathbb{ E  }_{ D  }  [(f(x;D) - \bar  { f  }  (x))^  { 2  }  ] + (\bar  { f  }  (x) - y)^  { 2  }   + \mathbb  { E  }  _ { D  }  [(y_{ D  }   - y)^  { 2  }  ]\\=bias^  { 2  }  (x) + var(x) + \epsilon^  { 2  }</script><p>即泛化误差可以分解为偏差、方差和噪声。</p>
<blockquote>
<p>偏差度量了学习算法的期望预测与真实结果的偏离程度，即刻画了学习算法本身的拟合能力。</p>
<p>方差度量了同样大小的训练集的变动所导致的学习性能的变化，即刻画了数据扰动所造成的影响。</p>
<p>噪声则表达了在当前任务上任何学习算法所能达到的期望泛化误差的下界，即刻画了学习问题本身的难度。</p>
</blockquote>
<p><strong>偏差方差窘境</strong>：</p>
<p>整体上说，随着训练程度逐渐增大，偏差逐渐减小，方差逐渐增大，总的泛化误差成U型。</p>
<blockquote>
<p>给定学习任务，……能控制训练程度……训练不足时，……拟合能力不够强，训练数据的扰动不足以使学习器产生显著变化，偏差主导了泛化错误率；……训练程度加深……拟合能力逐渐增强，训练数据发生的扰动渐渐能被学习器学到，方差逐渐主导了泛化错误率；……训练程度充足……轻微扰动都会导致……显著变化，……自身的、非全局的特性被学习器学到了，则将发生过拟合。</p>
</blockquote>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
      <tags>
        <tag>Machine Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>CSV数据处理</title>
    <url>/2019/10/05/python-csv/</url>
    <content><![CDATA[<p>CSV是一种一行中使用逗号分隔的数据表格格式</p>
<a id="more"></a>
<h2 id="数据读取"><a href="#数据读取" class="headerlink" title="数据读取"></a>数据读取</h2><h3 id="直接读取"><a href="#直接读取" class="headerlink" title="直接读取"></a>直接读取</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打开文件并读取 得到字符串</span></span><br><span class="line">file = open(<span class="string">"name.csv"</span>,<span class="string">"r"</span>)</span><br><span class="line">data = file.read()</span><br><span class="line"><span class="comment"># 切分成行</span></span><br><span class="line">data_line = data.split(<span class="string">"\n"</span>)</span><br><span class="line">data_conp = []</span><br><span class="line"><span class="comment"># 每一行划分成list</span></span><br><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> data_line:</span><br><span class="line">    data_conp.append(line.split(<span class="string">","</span>))</span><br></pre></td></tr></table></figure>
<h3 id="使用csv包读取"><a href="#使用csv包读取" class="headerlink" title="使用csv包读取"></a>使用csv包读取</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入CSV库</span></span><br><span class="line"><span class="keyword">import</span> csv</span><br><span class="line">file = open(<span class="string">"name.csv"</span>,<span class="string">"r"</span>)</span><br><span class="line"><span class="comment"># 获得reader对象</span></span><br><span class="line">reader = csv.reader(file)</span><br><span class="line"><span class="comment"># 每次调用返回一行数据</span></span><br><span class="line">headline = next(reader)</span><br><span class="line"><span class="comment"># 返回所有数据&lt;list of list&gt;</span></span><br><span class="line">data = list(reader)</span><br></pre></td></tr></table></figure>
<p>通常情况下，数据表格中含有表头，需单独处理</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 直接读取数据时要去掉表头</span></span><br><span class="line">raw_data = list(reader)</span><br><span class="line">header = raw_data[<span class="number">0</span>]</span><br><span class="line">data = raw_data[<span class="number">1</span>:]</span><br><span class="line"><span class="keyword">for</span> index, item <span class="keyword">in</span> enumerate(header):</span><br><span class="line">  print(index.item)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Matplotlib绘图</title>
    <url>/2019/10/05/python-matplotlib/</url>
    <content><![CDATA[<p>使用方法类似于Matlab</p>
<p>数据基于 class ‘numpy.ndarray’</p>
<p>也可以基于 class ‘list’</p>
<a id="more"></a>
<h2 id="导入库"><a href="#导入库" class="headerlink" title="导入库"></a>导入库</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入库</span></span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy</span><br></pre></td></tr></table></figure>
<h2 id="数据的导入"><a href="#数据的导入" class="headerlink" title="数据的导入"></a>数据的导入</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基于ndarray的数据</span></span><br><span class="line">x = np.array([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>])</span><br><span class="line">y = np.array([<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">0</span>])</span><br><span class="line"><span class="comment"># 基于list的数据</span></span><br><span class="line">z = [<span class="number">1</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>]</span><br></pre></td></tr></table></figure>
<h2 id="绘图"><a href="#绘图" class="headerlink" title="绘图"></a>绘图</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 绘图函数的使用方法与Matlab相同</span></span><br><span class="line"><span class="comment"># 颜色/线型/标记的设置和Matlab相同</span></span><br><span class="line"><span class="comment"># 或者使用名值对指定绘图参数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 折线图</span></span><br><span class="line">plt.plot(x,y)</span><br><span class="line"><span class="comment"># 'r-x'            # 红色 单实线 ‘x’标记</span></span><br><span class="line"><span class="comment"># linewidth = 5    # 线粗为5</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 条形图</span></span><br><span class="line">plt.bar(x,y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 火柴图</span></span><br><span class="line">plt.stem(x,y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 散点图</span></span><br><span class="line">plt.scatter(x,y)</span><br><span class="line"><span class="comment"># s = 40                        # size</span></span><br><span class="line"><span class="comment"># c = 'red'                     # color</span></span><br><span class="line"><span class="comment"># c = (0,0,0.87)                # RGB color</span></span><br><span class="line"><span class="comment"># c = y,cmap = plt.cm.Blues     # 颜色映射 不同的值深度不同</span></span><br><span class="line"><span class="comment"># edgecolor='none'              # 数据点轮廓</span></span><br></pre></td></tr></table></figure>
<h2 id="图像设置"><a href="#图像设置" class="headerlink" title="图像设置"></a>图像设置</h2><p>通常情况下可以理解为状态机参数的调整</p>
<p>因此后面的语句会覆盖之前的代码</p>
<h3 id="坐标轴"><a href="#坐标轴" class="headerlink" title="坐标轴"></a>坐标轴</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置x,y轴的显示范围</span></span><br><span class="line">plt.xlim((<span class="number">-1</span>,<span class="number">1</span>))</span><br><span class="line">plt.ylim((<span class="number">0</span>,<span class="number">3</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 也可以用axis设置</span></span><br><span class="line">plt.axis([<span class="number">-1</span>,<span class="number">1</span>,<span class="number">0</span>,<span class="number">3</span>])</span><br></pre></td></tr></table></figure>
<h3 id="图表标题"><a href="#图表标题" class="headerlink" title="图表标题"></a>图表标题</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置图表标题</span></span><br><span class="line">pl.title(<span class="string">"title"</span>)</span><br></pre></td></tr></table></figure>
<h3 id="坐标轴标签"><a href="#坐标轴标签" class="headerlink" title="坐标轴标签"></a>坐标轴标签</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">plt.xlabel(<span class="string">'xname'</span>)</span><br><span class="line">plt.ylabel(<span class="string">'yname'</span>)</span><br><span class="line"><span class="comment"># 设置字体  fontproperties='SimHei'</span></span><br><span class="line"><span class="comment"># 设置字号  fontsize=14</span></span><br></pre></td></tr></table></figure>
<h3 id="坐标轴刻度"><a href="#坐标轴刻度" class="headerlink" title="坐标轴刻度"></a>坐标轴刻度</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置坐标轴的刻度</span></span><br><span class="line"><span class="comment"># 使用numpy，设置-1到1，均分成5份</span></span><br><span class="line"><span class="comment"># 当范围大于xlim指定的范围时会扩展</span></span><br><span class="line">plt.xticks(np.linespace(<span class="number">-1</span>,<span class="number">1</span>,<span class="number">5</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用字符串list命名</span></span><br><span class="line">plt.yticks([<span class="number">-5</span>,<span class="number">0</span>,<span class="number">5</span>], [<span class="string">"negative"</span>,<span class="string">"neutral"</span>,<span class="string">"positive"</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 可以设置标记刻度的样式</span></span><br><span class="line">plt.tick_params(axis=<span class="string">"both"</span>, labelsize=<span class="number">14</span>)</span><br></pre></td></tr></table></figure>
<h3 id="四边边框设置"><a href="#四边边框设置" class="headerlink" title="四边边框设置"></a>四边边框设置</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 获得四边边框对象</span></span><br><span class="line">ax = plt.gca()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置颜色，设置为none可以隐藏</span></span><br><span class="line">ax.spines[<span class="string">'top'</span>].set_color(<span class="string">'none'</span>)</span><br><span class="line"><span class="comment"># 设置坐标轴位置，可以设置横轴经过数据的0点</span></span><br><span class="line">ax.spines[<span class="string">'bottom'</span>].set_position((<span class="string">'data'</span>,<span class="number">0</span>))</span><br><span class="line"><span class="comment"># 设置坐标轴的位置，可以设置纵轴在最右端</span></span><br><span class="line">ax.spines[<span class="string">'left'</span>].set_position((<span class="string">'axes'</span>,<span class="number">1</span>))</span><br><span class="line"><span class="comment"># 设置坐标轴上标注的位置，可以设置在纵轴的右侧</span></span><br><span class="line">ax.spines[<span class="string">'left'</span>].set_ticks_position(<span class="string">'right'</span>)</span><br></pre></td></tr></table></figure>
<h3 id="图例设置"><a href="#图例设置" class="headerlink" title="图例设置"></a>图例设置</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方法一：在plot绘图时指定label的内容</span></span><br><span class="line">lab_sin = <span class="string">"this is a sinesoidal wave"</span></span><br><span class="line">plt.plot(x,y,label = lab_sin)</span><br><span class="line">plt.legend()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法二：在plot绘图时获得句柄，在legend中指定内容</span></span><br><span class="line">han1,=plt.plot(x,y)</span><br><span class="line">han2,=plt.plot(x,z)</span><br><span class="line">plt.legend(handles=[han1,han2], labels=[<span class="string">"y_values"</span>,<span class="string">"z_values"</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># loc='best'                 # 指定图例的位置,‘best’自动分配最佳位置</span></span><br></pre></td></tr></table></figure>
<h3 id="图像输出"><a href="#图像输出" class="headerlink" title="图像输出"></a>图像输出</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 显示图像</span></span><br><span class="line"><span class="comment"># 在plt.show()之前的部分会画在一张图里 没有plt.show()不画图</span></span><br><span class="line">plt.show()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改图像的输出</span></span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>,<span class="number">6</span>))</span><br><span class="line"><span class="comment"># dpi = 128                    # 屏幕分辨率</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存图像</span></span><br><span class="line">plt.savefig(<span class="string">"file_name.png"</span>)</span><br><span class="line"><span class="comment"># bbox_inches='tight'          # 将图像边缘白边裁掉</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Matplotlib</tag>
      </tags>
  </entry>
  <entry>
    <title>Verilog on MacOSX</title>
    <url>/2019/10/04/verilog-macos/</url>
    <content><![CDATA[<p>这学期的Verilog课程要求使用ModelSim仿真软件。<br>实在不想再开一个win7的虚拟机了。<br>于是探索了一下如何在MacOS中进行Verilog的仿真和波形监测。</p>
<a id="more"></a>
<h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><h3 id="仿真工具"><a href="#仿真工具" class="headerlink" title="仿真工具"></a>仿真工具</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用icarus-verilog进行仿真</span></span><br><span class="line">brew install icarus-verilog</span><br></pre></td></tr></table></figure>
<h3 id="波形工具"><a href="#波形工具" class="headerlink" title="波形工具"></a>波形工具</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用Scansion查看波形</span></span><br><span class="line">brew cask install scansion</span><br></pre></td></tr></table></figure>
<h2 id="仿真过程"><a href="#仿真过程" class="headerlink" title="仿真过程"></a>仿真过程</h2><h3 id="仿真"><a href="#仿真" class="headerlink" title="仿真"></a>仿真</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 生成vvp仿真程序</span></span><br><span class="line"><span class="comment"># 默认生成a.out</span></span><br><span class="line">iverilog stimulus.v -o stimulus.vvp</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行仿真</span></span><br><span class="line">./stimulus.vvp</span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">vvp stimulus.vvp</span><br></pre></td></tr></table></figure>
<h3 id="波形观察"><a href="#波形观察" class="headerlink" title="波形观察"></a>波形观察</h3><h4 id="生成波形文件"><a href="#生成波形文件" class="headerlink" title="生成波形文件"></a>生成波形文件</h4><p>需要在verilog文件中生成波形</p>
<figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">initial</span></span><br><span class="line">  <span class="keyword">begin</span></span><br><span class="line">    <span class="comment">// 指明文件名</span></span><br><span class="line">    <span class="built_in">$dumpfile</span>(<span class="string">"stimulus.vcd"</span>)</span><br><span class="line">    <span class="comment">// 指明监控的module，这里表示stimulus及下面的所有module</span></span><br><span class="line">    $dumpvar(<span class="number">0</span>,stimulus)</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 如果stimulus只调用了一个counter模块</span></span><br><span class="line"><span class="comment">     * $dumpvar(1, stimulus)</span></span><br><span class="line"><span class="comment">     * $dumpvar(0, stimulus.counter)</span></span><br><span class="line"><span class="comment">     * 两者等价</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">  <span class="keyword">end</span></span><br></pre></td></tr></table></figure>
<h4 id="查看波形"><a href="#查看波形" class="headerlink" title="查看波形"></a>查看波形</h4><p>使用命令行查看</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用xCode的cmd-tools 在terminal中打开Scansion软件</span></span><br><span class="line">open -a Scansion stimulus.vcd</span><br></pre></td></tr></table></figure>
<p>或者直接在Scansion中打开vcd文件</p>
<p>或者直接双击vcd文件</p>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Verilog HDL</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式的使用</title>
    <url>/2019/10/04/reg-ex/</url>
    <content><![CDATA[<p>简单了解了正则表达式在python和C++中的使用方法</p>
<a id="more"></a>
<h2 id="基本元素和规则"><a href="#基本元素和规则" class="headerlink" title="基本元素和规则"></a>基本元素和规则</h2><div class="table-container">
<table>
<thead>
<tr>
<th>符号</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>^</td>
<td>句首</td>
</tr>
<tr>
<td>$</td>
<td>句尾</td>
</tr>
<tr>
<td>.</td>
<td>匹配单个字符</td>
</tr>
<tr>
<td>*</td>
<td>多次出现（$\geq0$）</td>
</tr>
<tr>
<td>+</td>
<td>多次出现（$\geq1$)</td>
</tr>
<tr>
<td>?</td>
<td>零次或一次出现</td>
</tr>
<tr>
<td>{n,m}</td>
<td>表示匹配n-m次</td>
</tr>
<tr>
<td>\</td>
<td>匹配转义字符</td>
</tr>
<tr>
<td>()</td>
<td>匹配子串并获取</td>
</tr>
<tr>
<td>&#124;</td>
<td>匹配两项之间的一个</td>
</tr>
<tr>
<td>/ /</td>
<td>表示pattern匹配</td>
</tr>
<tr>
<td>[]</td>
<td>表示多个可选项 其中的^表示非</td>
</tr>
</tbody>
</table>
</div>
<h3 id="非打印字符的匹配"><a href="#非打印字符的匹配" class="headerlink" title="非打印字符的匹配"></a>非打印字符的匹配</h3><div class="table-container">
<table>
<thead>
<tr>
<th>字符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>\cx</td>
<td>控制字符control+x，这里的x必须是A-Z</td>
</tr>
<tr>
<td>\f</td>
<td>换页符号</td>
</tr>
<tr>
<td>\n</td>
<td>换行符</td>
</tr>
<tr>
<td>\r</td>
<td>回车键</td>
</tr>
<tr>
<td>\s</td>
<td>空白字符 类似[\f\n\r\t\v]</td>
</tr>
<tr>
<td>\S</td>
<td>非空白字符</td>
</tr>
<tr>
<td>\t</td>
<td>制表符</td>
</tr>
<tr>
<td>\v</td>
<td>垂直制表符</td>
</tr>
</tbody>
</table>
</div>
<h3 id="定位符"><a href="#定位符" class="headerlink" title="定位符"></a>定位符</h3><div class="table-container">
<table>
<thead>
<tr>
<th>符号</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>\b</td>
<td>单词边界（单词和空格交界处）</td>
</tr>
<tr>
<td>\B</td>
<td>非单词边界</td>
</tr>
</tbody>
</table>
</div>
<h3 id="常用的匹配"><a href="#常用的匹配" class="headerlink" title="常用的匹配"></a>常用的匹配</h3><div class="table-container">
<table>
<thead>
<tr>
<th>匹配</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>\d</td>
<td>匹配一个数字 相当于[0-9]</td>
</tr>
<tr>
<td>\w</td>
<td>匹配一个字符 相当于[A-Za-z0-9_]</td>
</tr>
</tbody>
</table>
</div>
<h3 id="预查"><a href="#预查" class="headerlink" title="预查"></a>预查</h3><div class="table-container">
<table>
<thead>
<tr>
<th>符号</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>(?:pattern)</td>
<td>匹配但是不获取（缓存）</td>
</tr>
<tr>
<td>a(?=pattern)</td>
<td>匹配pattern之前的a</td>
</tr>
<tr>
<td>a(?!pattern)</td>
<td>匹配后面没有pattern的a</td>
</tr>
<tr>
<td>(?&lt;=pattern)b</td>
<td>匹配前面有pattern的b</td>
</tr>
<tr>
<td>(?&lt;!pattern)b</td>
<td>匹配前面没有pattern的b</td>
</tr>
</tbody>
</table>
</div>
<h2 id="在各种环境中使用正则表达式"><a href="#在各种环境中使用正则表达式" class="headerlink" title="在各种环境中使用正则表达式"></a>在各种环境中使用正则表达式</h2><h3 id="C-中使用正则表达式REGEX"><a href="#C-中使用正则表达式REGEX" class="headerlink" title="C++中使用正则表达式REGEX"></a>C++中使用正则表达式REGEX</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 引入头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;regex&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个正则表达式</span></span><br><span class="line"><span class="built_in">std</span>::<span class="function">regex <span class="title">pattern</span><span class="params">(<span class="string">"([0-9]+)"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明匹配结果变量,结果变量要和字符串形式相对应</span></span><br><span class="line"><span class="built_in">std</span>::match_results&lt;<span class="keyword">const</span> <span class="keyword">char</span>*&gt; cResult;</span><br><span class="line"><span class="comment">// 简化定义std::cmatch</span></span><br><span class="line"><span class="built_in">std</span>::match_results&lt;<span class="built_in">std</span>::<span class="built_in">string</span>::const_iterator&gt; sResult;</span><br><span class="line"><span class="comment">// 简化定义std::smatch</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义待匹配的字符串</span></span><br><span class="line"><span class="keyword">char</span> cStr[] = <span class="string">"hello"</span>;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> sStr = <span class="string">"123abc"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 进行匹配</span></span><br><span class="line"><span class="comment">// regex_match(string, result, pattern)</span></span><br><span class="line"><span class="comment">// 其中string可以是迭代器指明的区间</span></span><br><span class="line"><span class="comment">// result可以省略</span></span><br><span class="line"><span class="keyword">bool</span> cValid = regex_match(cStr, cResult, pattern);</span><br><span class="line"><span class="keyword">bool</span> sValid = regex_match(sStr, sResult, pattern);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回匹配结果</span></span><br><span class="line">cResult.size();    <span class="comment">//返回匹配的子串个数</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; cResult.size();i++)&#123;</span><br><span class="line">  cResult[i];</span><br><span class="line">  cResult.str(i);  <span class="comment">//显示匹配的部分子串</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * 同样支持</span></span><br><span class="line"><span class="comment"> * regex_search(string, result, pattern)</span></span><br><span class="line"><span class="comment"> * regex_replace(string, pattern, substr)</span></span><br><span class="line"><span class="comment"> * 可以用$1 $2 %3指括号匹配到的子串</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure>
<h3 id="Java中正则表达式的类库"><a href="#Java中正则表达式的类库" class="headerlink" title="Java中正则表达式的类库"></a>Java中正则表达式的类库</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.regex.*</span><br><span class="line"><span class="comment">// 实际上包含了Pattern、Matcher、PatternSyntaxException三个类</span></span><br></pre></td></tr></table></figure>
<h3 id="javascript中使用正则表达式"><a href="#javascript中使用正则表达式" class="headerlink" title="javascript中使用正则表达式"></a>javascript中使用正则表达式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> re = <span class="keyword">new</span> <span class="built_in">RegExp</span>(pattern, modifiers);</span><br></pre></td></tr></table></figure>
<h3 id="python中使用正则表达式"><a href="#python中使用正则表达式" class="headerlink" title="python中使用正则表达式"></a>python中使用正则表达式</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 引用正则表达式包</span></span><br><span class="line"><span class="keyword">import</span> re</span><br></pre></td></tr></table></figure>
<p>正则表达式标志位</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>标志</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>re.I</td>
<td>忽略大小写</td>
</tr>
<tr>
<td>re.M</td>
<td>多行模式</td>
</tr>
<tr>
<td>re.L</td>
<td>特殊字符集依赖于当前环境</td>
</tr>
<tr>
<td>re.S</td>
<td>任意字符</td>
</tr>
<tr>
<td>re.U</td>
<td>特殊字符依赖于Unicode</td>
</tr>
<tr>
<td>re.X</td>
<td>忽略空格和注释</td>
</tr>
</tbody>
</table>
</div>
<h4 id="匹配函数：match"><a href="#匹配函数：match" class="headerlink" title="匹配函数：match"></a>匹配函数：match</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 匹配函数</span></span><br><span class="line">obj = re.match(pattern, string, flags=<span class="number">0</span>)</span><br><span class="line"><span class="comment"># 返回一个re匹配对象</span></span><br><span class="line"><span class="comment"># 匹配失败返回None</span></span><br><span class="line"></span><br><span class="line">obj.span()        <span class="comment"># 显示匹配到的子串的位置</span></span><br><span class="line">obj.groups()      <span class="comment"># 显示括号匹配到的子串</span></span><br><span class="line">obj.group(num=<span class="number">0</span>)  <span class="comment"># 显示匹配到的整个字符串</span></span><br><span class="line">obj.start()       <span class="comment"># 显示匹配到的子串的起始位置</span></span><br><span class="line">obj.end()         <span class="comment"># 显示匹配到的子串的结束位置</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 可以使用?P&lt;name&gt;为子串命名并用group取出</span></span><br><span class="line">pattern=<span class="string">":(?P&lt;port&gt;[0-9]+)"</span></span><br><span class="line">obj.group(<span class="string">"port"</span>)</span><br></pre></td></tr></table></figure>
<h4 id="查找函数：research"><a href="#查找函数：research" class="headerlink" title="查找函数：research"></a>查找函数：research</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">re.search(pattern, string, flags=<span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p><strong>区别：</strong></p>
<p>match()从头开始匹配，不满足返回None</p>
<p>search()匹配整个字符串直到找到一个匹配</p>
<p>findall()匹配所有</p>
<p>finditer()返回迭代器（list？）</p>
<p>split()按子串分割原字符串</p>
<h4 id="替换函数：sub"><a href="#替换函数：sub" class="headerlink" title="替换函数：sub"></a>替换函数：sub</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找并替换</span></span><br><span class="line">re.sub(pattern, repl, string, count=<span class="number">0</span>, flags=<span class="number">0</span>)</span><br><span class="line"><span class="comment"># 将满足pattern的部分换成repl，count指明替换的次数</span></span><br></pre></td></tr></table></figure>
<p><strong>注意：</strong></p>
<p>这里的repl可以是一个函数，函数参数为matched</p>
<p>matched匹配对象，可以使用group取出匹配到的子串并做相应的处理</p>
<h4 id="编译函数：compile"><a href="#编译函数：compile" class="headerlink" title="编译函数：compile"></a>编译函数：compile</h4><p>将pattern编译为一个对象，具有match和search的成员函数</p>
<h3 id="其他环境"><a href="#其他环境" class="headerlink" title="其他环境"></a>其他环境</h3><p>C#中正则表达式的类库</p>
<figure class="highlight c#"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> System.Text.RegularExpressions;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>实用工具</category>
      </categories>
      <tags>
        <tag>Regular Expression</tag>
      </tags>
  </entry>
  <entry>
    <title>C++中的STL数据结构</title>
    <url>/2019/10/04/cpp-stl/</url>
    <content><![CDATA[<p>C++已经实现了常见的各种数据结构</p>
<p>每种数据结构的实现被称为容器，定义迭代器作为容器中对象的指针</p>
<p>迭代器被定义为公有内嵌类，类名为iterator或const_iterator</p>
<p>借助容器储存数据的容器称为容器适配器(栈和队列)</p>
<p>查找表容器通常称为关联容器</p>
<p><strong><em>建议：直接查看头文件中的函数声明，使用关键字检索</em></strong></p>
<a id="more"></a>
<h2 id="线性表容器"><a href="#线性表容器" class="headerlink" title="线性表容器"></a>线性表容器</h2><p>vector：用动态数组实现的线性表#include\<vector\></vector\></p>
<p>list：用双链表实现的线性表#include\<list\></list\></p>
<p>deque：经过优化的线性表，兼具两者特点，用于实现栈和队列</p>
<h3 id="线性表变量声明"><a href="#线性表变量声明" class="headerlink" title="线性表变量声明"></a>线性表变量声明</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; vc;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator itr;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt; ls;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt;::iterator its;</span><br></pre></td></tr></table></figure>
<h3 id="两者的共有操作"><a href="#两者的共有操作" class="headerlink" title="两者的共有操作"></a>两者的共有操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> <span class="keyword">const</span></span>;    <span class="comment">//元素个数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;        <span class="comment">//清空元素</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span></span>;        <span class="comment">//判断是否为空</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push_back</span><span class="params">(<span class="keyword">const</span> object &amp;x)</span></span>;    <span class="comment">//添加到表尾</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pop_back</span><span class="params">()</span></span>;                    <span class="comment">//删除表尾元素</span></span><br><span class="line"><span class="function"><span class="keyword">const</span> object &amp; <span class="title">front</span> <span class="params">()</span> <span class="keyword">const</span></span>;      <span class="comment">//第一个元素</span></span><br><span class="line"><span class="function"><span class="keyword">const</span> object &amp; <span class="title">back</span><span class="params">()</span> <span class="keyword">const</span></span>;        <span class="comment">//最后一个元素</span></span><br></pre></td></tr></table></figure>
<h3 id="特有操作"><a href="#特有操作" class="headerlink" title="特有操作"></a>特有操作</h3><p>list可以在表头操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push_front</span><span class="params">(<span class="keyword">const</span> object &amp;x)</span></span>;   <span class="comment">//表头添加元素</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pop_front</span><span class="params">()</span></span>;                   <span class="comment">//表头删除元素</span></span><br></pre></td></tr></table></figure>
<p>vector可以实现类似数据的特征</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">object &amp;opeator[](<span class="keyword">int</span> idx);      <span class="comment">//下标运算符重载，无检查</span></span><br><span class="line"><span class="function">object &amp;<span class="title">at</span><span class="params">(<span class="keyword">int</span> idx)</span></span>;             <span class="comment">//返回指定位置元素，有下标检查</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">capacity</span><span class="params">()</span></span>;                  <span class="comment">//数组容量</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">reseave</span><span class="params">(<span class="keyword">int</span> newCapacity)</span></span>;   <span class="comment">//指定数组容量</span></span><br></pre></td></tr></table></figure>
<h3 id="迭代器相关操作"><a href="#迭代器相关操作" class="headerlink" title="迭代器相关操作"></a>迭代器相关操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">iterator <span class="title">begin</span><span class="params">()</span></span>;        <span class="comment">//表头位置</span></span><br><span class="line"><span class="function">const_iterator <span class="title">begin</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">iterator <span class="title">end</span><span class="params">()</span></span>;          <span class="comment">//表尾位置</span></span><br><span class="line"><span class="function">const_iterator <span class="title">end</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">iterator <span class="title">insert</span><span class="params">(iterator pos,<span class="keyword">const</span> object &amp;x)</span></span>;  <span class="comment">//插入元素</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span><span class="params">(iterator pos)</span></span>;                   <span class="comment">//删除元素</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span><span class="params">(iterator start,iterator end)</span></span>;    <span class="comment">//删除区间[start,end-1)</span></span><br></pre></td></tr></table></figure>
<h3 id="迭代器操作"><a href="#迭代器操作" class="headerlink" title="迭代器操作"></a>迭代器操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">itr ++;     <span class="comment">//下一位置</span></span><br><span class="line">* itr;      <span class="comment">//取出元素</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//STL中定义了distance函数，可以确定两个iterator之间的距离</span></span><br><span class="line"><span class="built_in">std</span>::distance(<span class="built_in">exp</span>.begin(),itr);<span class="comment">//可以得到itr指向的元素的下标</span></span><br></pre></td></tr></table></figure>
<h3 id="其他操作"><a href="#其他操作" class="headerlink" title="其他操作"></a>其他操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">max_size</span><span class="params">()</span></span>;            <span class="comment">//vector的最大范围</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">resize</span><span class="params">(size)</span></span>;         <span class="comment">//显式指定数组空间</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">resize</span><span class="params">(size,fill)</span></span>;    <span class="comment">//显示指定扩大数组空间，并填充</span></span><br><span class="line"><span class="function">iterator <span class="title">rbegin</span><span class="params">()</span> <span class="keyword">const</span></span>;   <span class="comment">//反转后的表头（实际的表尾）</span></span><br><span class="line"><span class="function">iterator <span class="title">rend</span><span class="params">()</span>  <span class="keyword">const</span></span>;    <span class="comment">//反转后的表尾</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="built_in">vector</span> &amp;obj)</span></span>;    <span class="comment">//与另一个vector交换数据</span></span><br></pre></td></tr></table></figure>
<p>vector减少空间的方法被称为“收缩到合适（shrink to fit）”</p>
<p>使capacity收缩到size（capacity默认为2的指数）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;(vc).swap(vc);</span><br><span class="line"><span class="comment">//使用vector的拷贝构造函数，复制已有的数据，释放多余的空间</span></span><br></pre></td></tr></table></figure>
<h2 id="栈容器"><a href="#栈容器" class="headerlink" title="栈容器"></a>栈容器</h2><p>stack栈容器，#include\<stack\></stack\></p>
<h3 id="栈变量声明"><a href="#栈变量声明" class="headerlink" title="栈变量声明"></a>栈变量声明</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">stack</span>&lt;<span class="keyword">int</span>,<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; stack1;<span class="comment">//借助vector储存数据</span></span><br><span class="line"><span class="built_in">stack</span>&lt;<span class="keyword">int</span>,<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt;&gt; stack2;<span class="comment">//借助list储存数据</span></span><br></pre></td></tr></table></figure>
<h3 id="栈基本操作"><a href="#栈基本操作" class="headerlink" title="栈基本操作"></a>栈基本操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span></span>;              <span class="comment">//检查栈是否为空</span></span><br><span class="line"><span class="function">reference <span class="title">top</span><span class="params">()</span></span>;           <span class="comment">//返回栈顶元素</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">()</span></span>;                <span class="comment">//出栈</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(type &amp;x)</span></span>;        <span class="comment">//压栈</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">const</span> type &amp;x)</span></span>;  <span class="comment">//压栈</span></span><br></pre></td></tr></table></figure>
<h2 id="队列容器"><a href="#队列容器" class="headerlink" title="队列容器"></a>队列容器</h2><p>queue队列容器，#include\<queue\></queue\></p>
<h3 id="队列变量声明"><a href="#队列变量声明" class="headerlink" title="队列变量声明"></a>队列变量声明</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">queue</span>&lt;<span class="keyword">int</span>&gt; queue1;             <span class="comment">//使用deque实现的队列（默认）</span></span><br><span class="line"><span class="built_in">queue</span>&lt;<span class="keyword">int</span>,<span class="built_in">deque</span>&lt;<span class="keyword">int</span>&gt;&gt; queue2;  <span class="comment">//使用deque实现的队列（默认）</span></span><br><span class="line"><span class="built_in">queue</span>&lt;<span class="keyword">int</span>,<span class="built_in">list</span>&lt;<span class="keyword">int</span>&gt;&gt; queue3;  <span class="comment">//使用list实现的队列</span></span><br></pre></td></tr></table></figure>
<h3 id="队列基本操作"><a href="#队列基本操作" class="headerlink" title="队列基本操作"></a>队列基本操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(type &amp;x)</span></span>;        <span class="comment">//入队</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">const</span> type &amp;x)</span></span>;  <span class="comment">//入队</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">()</span></span>;                <span class="comment">//出队</span></span><br><span class="line"><span class="function">reference <span class="title">front</span><span class="params">()</span></span>;         <span class="comment">//返回队头（下一出队元素）</span></span><br><span class="line"><span class="function">reference <span class="title">back</span><span class="params">()</span></span>;          <span class="comment">//返回队尾（上一入队元素）</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span> </span>;       <span class="comment">//是否为空</span></span><br><span class="line"><span class="function">size_type <span class="title">size</span><span class="params">()</span> <span class="keyword">const</span></span>;    <span class="comment">//队列规模</span></span><br></pre></td></tr></table></figure>
<h2 id="优先级队列容器"><a href="#优先级队列容器" class="headerlink" title="优先级队列容器"></a>优先级队列容器</h2><p>priority_queue优先级队列容器，#include\<queue\></queue\></p>
<p>（内部使用二叉树实现）</p>
<h3 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">priority_queue&lt;<span class="keyword">int</span>,<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;,less&lt;<span class="keyword">int</span>&gt;&gt; pq1;</span><br><span class="line"><span class="comment">//默认使用vector实现，内部降序排列，最大值出队</span></span><br><span class="line">priority_queue&lt;<span class="keyword">int</span>,<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;,greater&lt;<span class="keyword">int</span>&gt;&gt; pq2;<span class="comment">//vector实现，升序排列，最小值出队</span></span><br><span class="line">priority_queue&lt;<span class="keyword">int</span>,<span class="built_in">deque</span>&lt;<span class="keyword">int</span>&gt;,less&lt;<span class="keyword">int</span>&gt;&gt; pq3;<span class="comment">//使用deque实现</span></span><br></pre></td></tr></table></figure>
<p>第三个参数为仿函数，即使用一个类，实际上完成一个函数的作用</p>
<h3 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">const</span> type &amp;x)</span></span>;    <span class="comment">//入队</span></span><br><span class="line"><span class="function"><span class="keyword">const</span> type &amp; <span class="title">top</span><span class="params">()</span> <span class="keyword">const</span></span>;    <span class="comment">//返回队首值</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pop</span><span class="params">()</span></span>;                  <span class="comment">//出队</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span></span>;                <span class="comment">//检查是否为空</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;                <span class="comment">//清空队列</span></span><br></pre></td></tr></table></figure>
<h2 id="查找表容器"><a href="#查找表容器" class="headerlink" title="查找表容器"></a>查找表容器</h2><p>set“集合”：数据只有一个字段键的查找表#include\<set\></set\></p>
<p>map“映射”：数据为键值对的查找表#include\<map\></map\></p>
<p>pair类型：键值对类型，map的元素#include\<utility\></utility\></p>
<p>注：set和map中不允许key出现重复，需要出现重复的key时可以使用multiset和multimap</p>
<h3 id="查找表变量声明"><a href="#查找表变量声明" class="headerlink" title="查找表变量声明"></a>查找表变量声明</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set</span>&lt;<span class="keyword">int</span>&gt; s;</span><br><span class="line"><span class="built_in">map</span>&lt;<span class="built_in">string</span>,<span class="built_in">string</span>&gt; m;</span><br><span class="line">pair&lt;<span class="built_in">string</span>,<span class="built_in">string</span>&gt; p;</span><br></pre></td></tr></table></figure>
<h3 id="查找表基本操作"><a href="#查找表基本操作" class="headerlink" title="查找表基本操作"></a>查找表基本操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">pair <span class="title">makr_pair</span><span class="params">(class1 obj1,class2 obj2)</span></span>;  <span class="comment">//生成pair变量，元素类型自动推断</span></span><br><span class="line">p.first;  p.second;                         <span class="comment">//pair变量的内容</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span></span>;       <span class="comment">//判断是否为空</span></span><br><span class="line"><span class="function">size_type <span class="title">size</span><span class="params">()</span></span>;   <span class="comment">//容器规模</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;       <span class="comment">//清空容器</span></span><br><span class="line"><span class="function">size_type <span class="title">count</span><span class="params">(<span class="keyword">const</span> key_type&amp; key)</span> <span class="keyword">const</span></span>;  <span class="comment">//计数键出现的次数（判断键是否存在）</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span><span class="params">(iterator position)</span></span>;           <span class="comment">//删除某位置元素</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span><span class="params">(iterator begin,iterator end)</span></span>; <span class="comment">//删除一段元素</span></span><br><span class="line"><span class="function">size_type <span class="title">erase</span><span class="params">(<span class="keyword">const</span> key_type&amp; key)</span></span>;        <span class="comment">//删除键为某值的元素</span></span><br><span class="line"><span class="function">iterator <span class="title">find</span><span class="params">(<span class="keyword">const</span> key_type&amp; key)</span></span>;          <span class="comment">//查询键的位置（找不到时返回end）</span></span><br><span class="line"><span class="function">iterator <span class="title">insert</span><span class="params">(value_type&amp; value)</span></span>;          <span class="comment">//插入（map中value_type为pair）</span></span><br><span class="line"></span><br><span class="line"><span class="function">mapped_type <span class="title">at</span><span class="params">(<span class="keyword">const</span> key_type&amp; key)</span></span>;         <span class="comment">//查找key对应的值</span></span><br><span class="line"><span class="comment">//map中可以直接使用下标运算，效果和at相同。但是如果目标不存在时会被创建并初始化</span></span><br></pre></td></tr></table></figure>
<h3 id="查找表迭代器相关操作"><a href="#查找表迭代器相关操作" class="headerlink" title="查找表迭代器相关操作"></a>查找表迭代器相关操作</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">iterator <span class="title">begin</span><span class="params">()</span></span>;    <span class="comment">//返回头部</span></span><br><span class="line"><span class="function">iterator <span class="title">end</span><span class="params">()</span></span>;      <span class="comment">//返回尾部</span></span><br><span class="line"></span><br><span class="line"><span class="function">iterator <span class="title">lower_bound</span><span class="params">(key_value&amp; key)</span></span>;  <span class="comment">//返回值不小于key的第一个迭代器</span></span><br><span class="line"><span class="function">iterator <span class="title">upper_bound</span><span class="params">(key_value&amp; key)</span></span>;  <span class="comment">//返回大于key的第一个迭代器</span></span><br><span class="line">pair&lt;iterator,iterator&gt; equal_range(key_value&amp; key);  <span class="comment">//返回等于key的范围</span></span><br><span class="line"><span class="comment">//找不到的时候返回end</span></span><br></pre></td></tr></table></figure>
<p>特点：</p>
<ol>
<li><p>map中的key是不可以修改的，但是每个key对应的value可以通过迭代器修改。</p>
<p>set中只有key，因此不可以通过迭代器修改key的值</p>
</li>
<li><p>map中的元素默认按key升序排列</p>
</li>
</ol>
]]></content>
      <categories>
        <category>代码学习</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Make的简单使用</title>
    <url>/2019/10/03/makefile_base/</url>
    <content><![CDATA[<p>简单了解make的用法。</p>
<a id="more"></a>
<h3 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h3><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">target(生成文件名): source(依赖文件名)</span></span><br><span class="line">    command(指令)</span><br></pre></td></tr></table></figure>
<h3 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h3><p>步骤一：所有的目标文件和静态库文件连接成可执行文件</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">main : file1.o file2.o ... lib1.a lib2.a ...</span><br><span class="line">    g++ file1.o file2.o...lib1.a lib2.a... -o main</span><br></pre></td></tr></table></figure>
<p>步骤二：指定的目标文件打包为静态库文件</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">lib.a : libfile1.o libfile2.o ...</span><br><span class="line">    ar libfile1.o libfile2.o... -r lib.a</span><br></pre></td></tr></table></figure>
<p>步骤三：所有的源码编译为目标文件</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">file.o : file.cpp</span><br><span class="line">    g++ -c file.cpp</span><br></pre></td></tr></table></figure>
<p>步骤四：指定clean内容</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">clean:</span></span><br><span class="line">    rm main file1.o ... libfile1.a ...</span><br></pre></td></tr></table></figure>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>make指令：依据makefile的要求进行编译</p>
<p>make clean指令：依据makefile的clean指令删除指定的文件</p>
<p><strong><em>[注意]</em></strong></p>
<p>不需要指定头文件(可写可不写)</p>
<p>不过建议加在对应的依赖文件处 如main.o : main.cpp lib1.hpp</p>
<p>编译预处理时#include “file.hpp”的含义为在本文件夹中寻找头文件并链接到该位置</p>
<p>编译预处理时#include &lt; iostream &gt; 的含义为在C++标准库中寻找头文件并链接到该位置</p>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
        <tag>Make</tag>
      </tags>
  </entry>
  <entry>
    <title>C++库的编译</title>
    <url>/2019/09/30/library-build/</url>
    <content><![CDATA[<p>简单介绍C/C+静态库和动态库的编译方法。</p>
<a id="more"></a>
<h2 id="静态库"><a href="#静态库" class="headerlink" title="静态库"></a>静态库</h2><p>静态库的命名格式：libname.a</p>
<p>即以lib为前缀，.a作为后缀</p>
<h3 id="生成"><a href="#生成" class="headerlink" title="生成"></a>生成</h3><p>打包为库的工具为ar</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ar -crv libtest.a test.o</span><br><span class="line"><span class="meta">#</span><span class="bash">或者</span></span><br><span class="line">ar -cr libtest.a test.o</span><br></pre></td></tr></table></figure>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>g++编译时需要指明路径和名字</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">g++ main.cpp -L . -l test -o main</span><br><span class="line"><span class="meta">#</span><span class="bash">静态库名不需要加前缀和后缀</span></span><br></pre></td></tr></table></figure>
<h2 id="动态库"><a href="#动态库" class="headerlink" title="动态库"></a>动态库</h2><p>linux的动态库的命名格式：libname.so</p>
<p>即以lib为前缀，.so为后缀</p>
<h3 id="动态库生成"><a href="#动态库生成" class="headerlink" title="动态库生成"></a>动态库生成</h3><p>使用编译器创建动态库</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">g++ -f PIC -c test.cpp#为了生成多程序共享的动态库</span><br><span class="line">g++ -shared -o libtest.so test.o#创建链接</span><br><span class="line"><span class="meta">#</span><span class="bash">或者合并为一句</span></span><br><span class="line">g++ -f PIC -shared -o libtest.so test.cpp</span><br></pre></td></tr></table></figure>
<h3 id="动态库使用"><a href="#动态库使用" class="headerlink" title="动态库使用"></a>动态库使用</h3><p>具体使用方法与静态库一样</p>
<p>但是需要注意 必须显式指明动态连接库的位置</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">查看库的位置</span></span><br><span class="line">pwd</span><br><span class="line"><span class="meta">#</span><span class="bash">编辑ld配置文件</span></span><br><span class="line">sudo vim /etc/ld.so.conf#在最后一行加入库目录</span><br><span class="line">sudo ldconfig#重建库的位置文件</span><br><span class="line"><span class="meta">#</span><span class="bash">使用动态连接库</span></span><br><span class="line">g++ main.cpp -L . -l test -o main</span><br><span class="line">./main</span><br><span class="line"><span class="meta">#</span><span class="bash">如果没有指明库的路径 编译时没有问题但是运行可执行文件会报错</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>基础知识</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Welcome back</title>
    <url>/2019/09/29/test-page/</url>
    <content><![CDATA[<p>今天再一次开通了hexo博客，并配置到了自己的服务器上。<br><a id="more"></a><br>经历过一次硬盘爆炸&amp;数据丢失以后。<br>因为觉得太麻烦，所以一直都没有再次开通我的博客。<br>最近刚好比较不忙，于是用了两天的时间再一次配置完成了hexo。<br>希望这个学期可以坚持写博客坚持分享。</p>
<p>尽管配置过一次hexo，但是当时是在github pages上开的。<br>这次配置过程中遇到的各种问题主要是发生在将静态文件部署到服务器上的过程中。<br>还好最后一切顺利，完成了配置。</p>
<p>希望这次能够坚持下去，坚持分享自己的心得和收获。<br>（有了备份硬盘这下不怕数据会没有啦hhh）<br>希望大家能喜欢</p>
]]></content>
      <categories>
        <category>开发经历</category>
      </categories>
  </entry>
</search>
